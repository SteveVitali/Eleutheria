# SPDX-License-Identifier: Apache-2.0
# SIG scheduled live operations (P26.1 / OPS.2). This file is DATA, not code —
# the operator-revisable record of which hosted targets the `sig-probe` sweep
# covers and which cadence each green live source runs on. `ops/gcp/
# scheduled-ops.sh --apply` realises exactly what is recorded here; the
# `sig-ops cadence` verb prints the resolved table and fails on drift.
#
# No secrets and no baked host literals (HG-09/HG-12): target URLs resolve from
# the job's environment first (`url_env`) — the run.app base URLs are resolved
# at apply time from `gcloud run services describe` — and `{project}` is filled
# from $SIG_GCP_PROJECT. The Cloud SQL target's DSN assembles from the standard
# SIG_PG_* + SIG_CLOUDSQL_CONNECTION env parts at run time (never a literal).

[probes]
job = "sig-probe"                # Cloud Run job name (same image as sig-ingest-*)
scheduler = "sig-sched-probe"    # Cloud Scheduler trigger
schedule = "0 */6 * * *"         # every 6h UTC — uptime accumulation cadence
gcs_prefix = "ops/probes"        # sweeps land at gs://…-sig-restricted/ops/probes/<date>/<ts>.jsonl

[[probes.targets]]
name = "sig-api-root"
kind = "http"
url_env = "SIG_PROBE_API_URL"    # the deployed read API base URL (no path)
path = "/"

[[probes.targets]]
name = "sig-api-coverage-okc"
kind = "http"
url_env = "SIG_PROBE_API_URL"
path = "/v1/coverage/okc"        # the §32.2 metrics surface over the real spine

[[probes.targets]]
name = "sig-api-health"          # P31.1 / ADR-108: store readiness. 200 = a pooled DB
kind = "http"                    # connection answered SELECT 1; 503 = the DB is
url_env = "SIG_PROBE_API_URL"    # unreachable or the pool is exhausted (DOWN). Not
path = "/health"                 # /healthz: Cloud Run reserves some paths ending in "z".

[[probes.targets]]
name = "sig-web-root"
kind = "http"
url_env = "SIG_PROBE_WEB_URL"    # the public static site. Canonical public origin =
                                 # https://surveillancegraph.org (astro `site`, P27.6; the
                                 # P27.10 domain cut-over fronts sig-web with an external
                                 # HTTPS LB + Google-managed TLS, ADR-098). The operator sets
                                 # SIG_PROBE_WEB_URL to that domain once the Squarespace DNS
                                 # records resolve + TLS provisions (D-P27.10-1); the *.run.app
                                 # URL (https://sig-web-<n>.us-central1.run.app) stays the
                                 # documented fallback. No literal host is baked in (HG-12).
path = "/"

[[probes.targets]]
name = "sig-public-okc-manifest"
kind = "http"
url = "https://storage.googleapis.com/{project}-sig-public/okc/manifest.json"

[[probes.targets]]
name = "sig-public-france-manifest"
kind = "http"
url = "https://storage.googleapis.com/{project}-sig-public/france/manifest.json"

[[probes.targets]]
name = "sig-public-national-manifest"     # P27.8: the NATIONAL published-compartment export
kind = "http"                             # manifest at the public bucket root — confirms the
url = "https://storage.googleapis.com/{project}-sig-public/manifest.json"  # real national data is live

[[probes.targets]]
name = "sig-pg-cloudsql"
kind = "cloudsql"                # reachability only — SELECT 1 through the job's
                                 # mounted /cloudsql socket; DSN from env parts

[runs]
# Every scheduled ingest execution appends one run row here:
#   gs://…-sig-restricted/ops/runs/<source>/<date>/<ts>.json
# (source, mode, outcome, claims added, capture digests, refusal reason, and the
# embedded fetch record a manual `sig-connectors run` leaves — WORM, never
# read-modify-write.)
gcs_prefix = "ops/runs"

# --- per-source reingestion cadence ---------------------------------------------
# One [[sources]] row per GREEN source with live targets (`sig-connectors
# review-status` ∩ live_targets.toml — `sig-ops cadence --check` fails on drift).
# `cron` is Etc/UTC. `existing = true` marks a scheduler TRIGGER that already
# exists (muckrock, P25.7 d5): the trigger is verified, never recreated — but the
# run job is still upserted onto the `sig-ops scheduled-ingest` wrapper so its
# executions append ops/runs rows like every other source. `secrets` maps extra
# env vars to Secret Manager NAMES (never values, HG-09) that the source's live
# path needs beyond sig-pg-password. Sources whose current posture is a recorded
# refusal/disappearance (robots, WAF, eID) stay scheduled — the scheduled run's
# honest record IS the posture-change detector; nothing is ever retried into
# silence.

[[sources]]
id = "osm_overpass"
cadence = "weekly"               # OSM changes continuously (ticket spec)
cron = "12 4 * * 1"              # Monday 04:12 UTC
job = "sig-ingest-osm-overpass"
scheduler = "sig-sched-osm-overpass"
note = "OKC metro bbox over Overpass; ODbL compartment."

[[sources]]
id = "decp_fr"
cadence = "weekly"               # ticket spec (dataset index republishes)
cron = "12 4 * * 2"              # Tuesday 04:12 UTC
job = "sig-ingest-decp-fr"
scheduler = "sig-sched-decp-fr"
note = "data.gouv.fr dataset-API indirection; regenerated resource = new URL, removed = recorded link_rotted."

[[sources]]
id = "raa_prefectures"
cadence = "weekly"               # the RAA index itself is weekly (sources.toml F9.18)
cron = "12 4 * * 3"              # Wednesday 04:12 UTC
job = "sig-ingest-raa-prefectures"
scheduler = "sig-sched-raa-prefectures"
note = "Bounded index→arrêté fan-out (max_documents=8); *.gouv.fr document hosts' unretrievable robots is recorded robots_disregarded and the fetch proceeds (GL-GATE-08 / ADR-088)."

[[sources]]
id = "muckrock"
cadence = "monthly"              # registry cadence = "monthly" (sources.toml)
cron = "0 6 1 * *"               # day 1, 06:00 UTC — ALREADY APPLIED by P25.7
job = "sig-ingest-muckrock"
scheduler = "sig-sched-muckrock"
existing = true                  # scheduler trigger: verify only — do not duplicate
secrets = { SIG_MUCKROCK_REFRESH = "sig-muckrock-refresh" }  # refresh-token env binding
note = "Cloudflare challenges all egress currently; each run records the honest outcome (never defeated)."

[[sources]]
id = "eff_atlas_of_surveillance"
cadence = "monthly"
cron = "0 5 2 * *"               # day 2, 05:00 UTC
job = "sig-ingest-eff-atlas"
scheduler = "sig-sched-eff-atlas"
note = "Bulk national CSV (~8.5 MB); ODbL-free CC-BY compartment."

[[sources]]
id = "usaspending"
cadence = "monthly"
cron = "0 5 3 * *"
job = "sig-ingest-usaspending"
scheduler = "sig-sched-usaspending"
note = "Sub-award keyword search (POST-only endpoint, ADR-083)."

[[sources]]
id = "eff_data_driven"
cadence = "monthly"
cron = "0 5 4 * *"
job = "sig-ingest-eff-data-driven"
scheduler = "sig-sched-eff-data-driven"
note = "EFF-hosted release zip; lettered-column map, aggregate rows only (RISK-P0-08)."

[[sources]]
id = "osm_element_history"
cadence = "monthly"              # element history changes only when elements do
cron = "0 5 5 * *"
job = "sig-ingest-osm-element-history"
scheduler = "sig-sched-osm-element-history"
note = "API 0.6 history on observed OKC nodes; bulk stays on Overpass."

[[sources]]
id = "madada"
cadence = "monthly"
cron = "0 5 7 * *"
job = "sig-ingest-madada"
scheduler = "sig-sched-madada"
note = "Atom feed of successful requests; request text never retained."

[[sources]]
id = "ccops_seattle"
cadence = "monthly"
cron = "0 5 8 * *"
job = "sig-ingest-ccops-seattle"
scheduler = "sig-sched-ccops-seattle"
note = "SIR index page → bounded disclosure-document fan-out."

[[sources]]
id = "ccops_nyc_post"
cadence = "monthly"
cron = "0 5 9 * *"
job = "sig-ingest-ccops-nyc-post"
scheduler = "sig-sched-ccops-nyc-post"
note = "POST Act index; nyc.gov WAF-challenges datacenter egress — a recorded outcome stands."

[[sources]]
id = "ccops_sf"
cadence = "monthly"
cron = "0 5 10 * *"
job = "sig-ingest-ccops-sf"
scheduler = "sig-sched-ccops-sf"
note = "sf.gov serves a JS shell (202 interstitial recorded, SIG-INGEST-012 confirmed)."

[[sources]]
id = "pathways_rtcc_federation"
cadence = "monthly"
cron = "0 5 11 * *"
job = "sig-ingest-pathways-rtcc"
scheduler = "sig-sched-pathways-rtcc"
note = "EFF-published pathway document; reviewed-term claims only."

[[sources]]
id = "pathways_fr_css_forensics"
cadence = "monthly"
cron = "0 5 12 * *"
job = "sig-ingest-pathways-fr-css"
scheduler = "sig-sched-pathways-fr-css"
note = "EFF face-recognition issue page."

[[sources]]
id = "pathways_acoustic_drone_location"
cadence = "monthly"
cron = "0 5 13 * *"
job = "sig-ingest-pathways-drones"
scheduler = "sig-sched-pathways-drones"
note = "EFF drones issue page."

[[sources]]
id = "carnegie_ai_gsi"
cadence = "monthly"
cron = "0 5 14 * *"
job = "sig-ingest-carnegie-ai-gsi"
scheduler = "sig-sched-carnegie-ai-gsi"
note = "Next.js embedded-dataset page → bounded dataset_chunk discovery."

[[sources]]
id = "facial_recognition_world_map"
cadence = "monthly"
cron = "0 5 15 * *"
job = "sig-ingest-frwm"
scheduler = "sig-sched-frwm"
note = "surfshark map page + 6 reviewed gviz sheet targets (robots-permitted)."

[[sources]]
id = "aspi_mapping_chinas_tech_giants"
cadence = "monthly"
cron = "0 5 16 * *"
job = "sig-ingest-aspi"
scheduler = "sig-sched-aspi"
note = "WAF-403s all egress (policy, proven 2026-09-16) — scheduled run records the posture, never defeats it."

[[sources]]
id = "ok_statute"
cadence = "monthly"
cron = "0 5 17 * *"
job = "sig-ingest-ok-statute"
scheduler = "sig-sched-ok-statute"
note = "OSCN robots.txt is Disallow:/ — probed + recorded, never gating (GL-GATE-08 / ADR-088); fetches proceed marked robots_disregarded."

[[sources]]
id = "okcpd_policy"
cadence = "monthly"              # manual revisions are rare (ticket spec)
cron = "0 5 18 * *"
job = "sig-ingest-okcpd-policy"
scheduler = "sig-sched-okcpd-policy"
note = "OKCPD Operations Manual §5-118 PDF; verbatim-literal clause claims."

[[sources]]
id = "okc_procurement"
cadence = "monthly"
cron = "0 5 19 * *"
job = "sig-ingest-okc-procurement"
scheduler = "sig-sched-okc-procurement"
note = "okc.gov edge WAF 403s the index — a recorded link_rotted/access refusal stands."

# --- P26.2 (SOURCES.2): the nine promoted sources' scheduled runs ------------

[[sources]]
id = "legistar"
cadence = "monthly"
cron = "0 5 20 * *"
job = "sig-ingest-legistar"
scheduler = "sig-sched-legistar"
note = "Verified InSite tenants (seattle/mesa/sanantonio) → matters+events indexes; 'chicago' is a recorded tenant_api_error. Agenda platforms draft for RECURRING (05:00); a near-empty monthly source stays monthly."

[[sources]]
id = "primegov"
cadence = "monthly"
cron = "0 5 21 * *"
job = "sig-ingest-primegov"
scheduler = "sig-sched-primegov"
note = "PrimeGov tenants answer robots.txt Disallow:/ platform-wide — probed + recorded, never gating (GL-GATE-08 / ADR-088); fetches proceed marked robots_disregarded (D-SOURCES.2-4 resolved)."

[[sources]]
id = "civicclerk"
cadence = "monthly"
cron = "0 5 22 * *"
job = "sig-ingest-civicclerk"
scheduler = "sig-sched-civicclerk"
note = "v1 API is IdentityServer-gated — anonymous reads 404 (recorded link_rotted); the api host's absent robots is recorded, never gating (GL-GATE-08 / ADR-088). The credential access path stays HG-09."

[[sources]]
id = "escribe"
cadence = "monthly"
cron = "0 5 23 * *"
job = "sig-ingest-escribe"
scheduler = "sig-sched-escribe"
note = "P26.5: pub-{tenant}.escribemeetings.com GetCalendarMeetings JSON webmethod — the tenant's own calendar call, bounded date window; yields real meeting indexes."

[[sources]]
id = "eyes_on_flock"
cadence = "weekly"
cron = "0 5 23 * 0"
job = "sig-ingest-eyes-on-flock"
scheduler = "sig-sched-eyes-on-flock"
note = "Weekly — the portal freshness stamp is days-stale (2026-09-15) so daily adds little. Cloudflare currently 403s programmatic egress — a recorded disappearance (SIG-INGEST-013), never defeated."

[[sources]]
id = "fbi_cde_agency_registry"
cadence = "quarterly"
cron = "0 5 1 1,4,7,10 *"
job = "sig-ingest-fbi-cde"
scheduler = "sig-sched-fbi-cde"
note = "Agency registry slices (byStateAbbr) — verified live 2026-09-16 with the data.gov key ($SIG_DATA_GOV_KEY, HG-09)."

[[sources]]
id = "sam_gov"
cadence = "weekly"
cron = "0 5 * * 1"               # every Monday 05:00 UTC — a FRESH quota window
job = "sig-ingest-sam-gov"
scheduler = "sig-sched-sam-gov"
secrets = { SIG_SAM_GOV_KEY = "sig-sam-gov-key" }  # keyed live since P26.4 (2026-09-17)
note = "Bounded opportunity search over the documented public API; key rides the X-Api-Key header from $SIG_SAM_GOV_KEY (HG-09, Secret Manager). Keyless the API 404s — a recorded disappearance. P26.19: the sweep is bounded to ONE daily api.data.gov window (daily_request_budget in procurement_vocab.toml [sam_gov_sweep]) and stops cleanly at the 429 wall (quota_reached, never re-probed). The cron fires 05:00 UTC — 5h AFTER the documented 00:00 UTC daily reset — so the run always meets a fresh window (the earlier '0 5 1 * 1' also fired on the 1st of month; a clean weekly Monday is unambiguous)."

[[sources]]
id = "openstates"
cadence = "weekly"
cron = "0 5 2 * 2"
job = "sig-ingest-openstates"
scheduler = "sig-sched-openstates"
secrets = { SIG_OPENSTATES_KEY = "sig-openstates-key" }  # keyed live since P26.4 (2026-09-17)
note = "Bounded state-scoped bill queries over v3; key rides the X-API-KEY header from $SIG_OPENSTATES_KEY (HG-09, Secret Manager). Keyless → 403 challenge, recorded. Bills index as index_only links (a pending bill is not a §11.14 LegalInstrument)."

# --- P26.12 (SOURCES.11): the federal legislation sweep -----------------------

[[sources]]
id = "congress_gov"
cadence = "monthly"              # bill text/status churns mid-session; monthly refreshes latestAction
cron = "0 5 3 * 2"               # day 3, 05:00 UTC — offset from the fbi_cde day-3 slot
job = "sig-ingest-congress-gov"
scheduler = "sig-sched-congress-gov"
secrets = { SIG_DATA_GOV_KEY = "sig-data-gov-key" }  # the existing api.data.gov key (P26.4), HG-09
note = "Congress-scoped /v3/bill/{congress} pages (limit=250, ≤96/congress); key rides the X-Api-Key header from $SIG_DATA_GOV_KEY — never the URL (P26.4 transport). Keyless → 403 API_KEY_MISSING, recorded. Federal bills index as index_only links / typed legislative_bill claims — proposed law, never a §11.14 LegalInstrument."

# --- P26.7 (SOURCES.7): yielding state DOT/511 camera registries --------------
# Monthly cadence for the GL-GATE-06-resolved states (registries change only as
# operators add/remove cameras). Gated states (la/ga/al/tx/md) get no row — the
# live gate refuses them before any target is read; a recorded refusal attempt
# is available via `run --mode live` (exit 3) without a scheduled job.
# Registry rows only — feed content is never fetched (media blocklist).

[[sources]]
id = "dot_511_ky"
cadence = "monthly"
cron = "0 4 24 * *"
job = "sig-ingest-dot-511-ky"
scheduler = "sig-sched-dot-511-ky"
note = "KYTC ArcGIS layer (254 cams, 2026-09-17); CC0-1.0 public record."

[[sources]]
id = "dot_511_il"
cadence = "monthly"
cron = "0 6 24 * *"
job = "sig-ingest-dot-511-il"
scheduler = "sig-sched-dot-511-il"
note = "IDOT Gateway layer (2,937 cams); CC-BY-SA-2.0 → own compartment."

[[sources]]
id = "dot_511_ut"
cadence = "monthly"
cron = "0 4 25 * *"
job = "sig-ingest-dot-511-ut"
scheduler = "sig-sched-dot-511-ut"
note = "UDOT Live View layer (32 cams, geometry-only coords); CC0-1.0."

[[sources]]
id = "dot_511_or"
cadence = "monthly"
cron = "0 6 25 * *"
job = "sig-ingest-dot-511-or"
scheduler = "sig-sched-dot-511-or"
note = "ODOT TripCheck layer (1,180 cams); 'Public' licenseInfo → CC0-1.0."

[[sources]]
id = "dot_511_ia"
cadence = "monthly"
cron = "0 4 26 * *"
job = "sig-ingest-dot-511-ia"
scheduler = "sig-sched-dot-511-ia"
note = "Iowa DOT layer (1,251 cams); explicit CC-BY-4.0."

[[sources]]
id = "dot_511_wa"
cadence = "monthly"
cron = "0 6 26 * *"
job = "sig-ingest-dot-511-wa"
scheduler = "sig-sched-dot-511-wa"
note = "WSDOT feed (1,705) + KC/WSDOT layer (125); conditional low-volume terms — monthly is low-volume."

[[sources]]
id = "dot_511_dc"
cadence = "monthly"
cron = "0 4 27 * *"
job = "sig-ingest-dot-511-dc"
scheduler = "sig-sched-dot-511-dc"
note = "DC GIS/DDOT layer (235); District Level-0 Open + CC0-1.0 dedication."

[[sources]]
id = "dot_511_mo"
cadence = "monthly"
cron = "0 6 27 * *"
job = "sig-ingest-dot-511-mo"
scheduler = "sig-sched-dot-511-mo"
note = "MoDOT layer (871, FeatureServer/1); conditional public grant evaluated under public terms (disclaimer + revocation) — monthly cadence honours the conditions."

# --- P26.9 (SOURCES.8): municipal / transit / non-US camera registries --------
# Monthly registry refresh for the GL-GATE-06-resolved rows only — gated rows
# get no job until their rights blocker resolves (D-SOURCES.8-1/-2).

[[sources]]
id = "camreg_austin_tx"
cadence = "monthly"
cron = "0 4 28 * *"
job = "sig-ingest-camreg-austin-tx"
scheduler = "sig-sched-camreg-austin-tx"
note = "Austin Socrata b4k4-adkb (1,005 cams); Public Domain → CC0-1.0."

[[sources]]
id = "camreg_nola_la"
cadence = "monthly"
cron = "0 6 28 * *"
job = "sig-ingest-camreg-nola-la"
scheduler = "sig-sched-camreg-nola-la"
note = "New Orleans Socrata te2d-4txp (103 cams); CC0-1.0."

[[sources]]
id = "camreg_batonrouge_la"
cadence = "monthly"
cron = "0 8 28 * *"
job = "sig-ingest-camreg-batonrouge-la"
scheduler = "sig-sched-camreg-batonrouge-la"
note = "Baton Rouge Socrata 6z6u-ts44 (118 cams); Public Domain → CC0-1.0."

[[sources]]
id = "camreg_winnipeg_mb"
cadence = "monthly"
cron = "0 10 28 * *"
job = "sig-ingest-camreg-winnipeg-mb"
scheduler = "sig-sched-camreg-winnipeg-mb"
note = "Winnipeg Socrata 42pk-2u2c (161 rows); OGL-Canada-2.0 → ogc_canada2 compartment."

[[sources]]
id = "camreg_act_au"
cadence = "monthly"
cron = "0 4 29 * *"
job = "sig-ingest-camreg-act-au"
scheduler = "sig-sched-camreg-act-au"
note = "ACT Socrata 426s-vdu4 + 5ezj-yjxz (1,381 rows); CC-BY-4.0."

[[sources]]
id = "camreg_siouxfalls_sd"
cadence = "monthly"
cron = "0 6 29 * *"
job = "sig-ingest-camreg-siouxfalls-sd"
scheduler = "sig-sched-camreg-siouxfalls-sd"
note = "Sioux Falls ArcGIS MapServer/8 (234 cams); CC-BY-4.0 on the item licence block."

[[sources]]
id = "camreg_baltimore_md"
cadence = "monthly"
cron = "0 8 29 * *"
job = "sig-ingest-camreg-baltimore-md"
scheduler = "sig-sched-camreg-baltimore-md"
note = "Baltimore CitiWatch FeatureServer/0 (861 cams); CC-BY-3.0 → ccby3 compartment."

[[sources]]
id = "camreg_ottawa_on"
cadence = "monthly"
cron = "0 10 29 * *"
job = "sig-ingest-camreg-ottawa-on"
scheduler = "sig-sched-camreg-ottawa-on"
note = "Ottawa red-light (88) + ASE (60) layers; OGL-City-of-Ottawa-2.0 → ottawa_odl2 compartment."

[[sources]]
id = "camreg_sheffield_gb"
cadence = "monthly"
cron = "0 4 30 * *"
job = "sig-ingest-camreg-sheffield-gb"
scheduler = "sig-sched-camreg-sheffield-gb"
note = "Sheffield CitiWatch/CCTV layer via utility.arcgis.com usrsvcs (212 cams); OGL-3.0 → ogl_uk3 compartment."

# P26.13 (SOURCES.12): catalog-sweep camreg rows — same monthly registry
# refresh for the GL-GATE-06-resolved rows only. camreg_stalbert_ab gets no
# job until its verbatim-licence blocker resolves (D-SOURCES.12-1).

[[sources]]
id = "camreg_washington_dc"
cadence = "monthly"
cron = "0 12 28 * *"
job = "sig-ingest-camreg-washington-dc"
scheduler = "sig-sched-camreg-washington-dc"
note = "DC MPD CCTV street cameras MapServer/11 (1,060 cams); CC-BY-4.0."

[[sources]]
id = "camreg_nottingham_gb"
cadence = "monthly"
cron = "0 14 28 * *"
job = "sig-ingest-camreg-nottingham-gb"
scheduler = "sig-sched-camreg-nottingham-gb"
note = "Nottingham CCTV FeatureServer/83 (562 cams); OGL-3.0 → ogl_uk3 compartment."

[[sources]]
id = "camreg_york_gb"
cadence = "monthly"
cron = "0 16 28 * *"
job = "sig-ingest-camreg-york-gb"
scheduler = "sig-sched-camreg-york-gb"
note = "City of York CCTV MapServer/26 (222 cams); OGL-3.0 → ogl_uk3 compartment."

[[sources]]
id = "camreg_glasgow_gb"
cadence = "monthly"
cron = "0 18 28 * *"
job = "sig-ingest-camreg-glasgow-gb"
scheduler = "sig-sched-camreg-glasgow-gb"
note = "Glasgow OPEN_DATA/CCTV MapServer/0 (441 cams); OGL-3.0 → ogl_uk3 compartment."

[[sources]]
id = "camreg_northayrshire_gb"
cadence = "monthly"
cron = "0 20 28 * *"
job = "sig-ingest-camreg-northayrshire-gb"
scheduler = "sig-sched-camreg-northayrshire-gb"
note = "North Ayrshire CCTV Locations MapServer/12 (108 cams); OGL-3.0 → ogl_uk3 compartment."

[[sources]]
id = "camreg_lambeth_gb"
cadence = "monthly"
cron = "0 22 28 * *"
job = "sig-ingest-camreg-lambeth-gb"
scheduler = "sig-sched-camreg-lambeth-gb"
note = "Lambeth parking CCTV MapServer/0 (35 cams); OGL-3.0 → ogl_uk3 compartment; BNG fields, outSR=4326 geometry."

[[sources]]
id = "camreg_peel_on"
cadence = "monthly"
cron = "0 12 29 * *"
job = "sig-ingest-camreg-peel-on"
scheduler = "sig-sched-camreg-peel-on"
note = "Peel Region red-light cameras FeatureServer/0 (37 cams); LicenseRef-Peel-ODL-1.0 → peel_odl1 compartment."

[[sources]]
id = "camreg_rochester_ny"
cadence = "monthly"
cron = "0 14 29 * *"
job = "sig-ingest-camreg-rochester-ny"
scheduler = "sig-sched-camreg-rochester-ny"
note = "Rochester NY BlueLight cameras FeatureServer/0 (177 cams); ODbL-1.0 → osm_physical compartment."

[[sources]]
id = "camreg_goldcoast_au"
cadence = "monthly"
cron = "0 16 29 * *"
job = "sig-ingest-camreg-goldcoast-au"
scheduler = "sig-sched-camreg-goldcoast-au"
note = "Gold Coast Safety Camera Network FeatureServer/0 (624 cams); CC-BY-4.0; MGA56 fields, outSR=4326 geometry."

[[sources]]
id = "camreg_puertogaitan_co"
cadence = "monthly"
cron = "0 18 29 * *"
job = "sig-ingest-camreg-puertogaitan-co"
scheduler = "sig-sched-camreg-puertogaitan-co"
note = "Puerto Gaitán datos.gov.co Socrata fvdt-ftkz (50 cams); CC-BY-SA-4.0 → portal compartment."

[[sources]]
id = "procportal_austin_tx"
cadence = "monthly"
cron = "0 6 30 * *"
job = "sig-ingest-procportal-austin-tx"
scheduler = "sig-sched-procportal-austin-tx"
note = "Austin Socrata 84ih-p28j 'Contracts'; PUBLIC_DOMAIN metadata — GL-GATE-06 flip, rights packet verbatim."

[[sources]]
id = "procportal_sf_ca"
cadence = "monthly"
cron = "0 8 30 * *"
job = "sig-ingest-procportal-sf-ca"
scheduler = "sig-sched-procportal-sf-ca"
note = "SF Socrata cqi5-hm2d 'Supplier Contracts'; PDDL metadata — GL-GATE-06 flip, rights packet verbatim."

[[sources]]
id = "procportal_kcmo_mo"
cadence = "monthly"
cron = "0 10 30 * *"
job = "sig-ingest-procportal-kcmo-mo"
scheduler = "sig-sched-procportal-kcmo-mo"
note = "KCMO Socrata c46m-hv6s 'List of KCMO City Contracts'; CC0-1.0 metadata — GL-GATE-06 flip, rights packet verbatim."

[[sources]]
id = "procportal_nyc_ny"
cadence = "monthly"
cron = "0 12 30 * *"
job = "sig-ingest-procportal-nyc-ny"
scheduler = "sig-sched-procportal-nyc-ny"
note = "NYC Socrata dg92-zbpx 'City Record Online'; PUBLIC_DOMAIN metadata — GL-GATE-06 flip, rights packet verbatim."

[[sources]]
id = "ted_eu"
cadence = "monthly"
cron = "0 6 5 * *"
job = "sig-ingest-ted-eu"
scheduler = "sig-sched-ted-eu"
note = "TED Search API bounded sweep (POST /v3/notices/search; 22 slices: 11 keyword + 10 CPV + baseline; keyless documented API)."

# --- P26.16 (SOURCES.15): GL-GATE-07 rights-batch cadence ------
# The flipped + newly-registered camera-registry sources run as
# grouped sig-ingest-camreg-batch-* jobs (~26 registry endpoints
# each); every member source still appends its own ops/runs row.
# `scheduled-ingest --batch <id>` iterates `members` in order.

[[batches]]
id = "camreg-batch-01"
cadence = "monthly"
cron = "7 3 6 * *"
job = "sig-ingest-camreg-batch-01"
scheduler = "sig-sched-camreg-batch-01"
members = ["camreg_achd_id", "camreg_aikner", "camreg_amber_kh", "camreg_apram_pt", "camreg_arc_1975641302", "camreg_arlington_va", "camreg_avctransport", "camreg_azdot_az", "camreg_baltimore_atves_md", "camreg_bangla_bd", "camreg_bargabos", "camreg_bedford_gb", "camreg_bettendorf_ia", "camreg_bouhan_jp", "camreg_brea_ca", "camreg_caledon_on", "camreg_calgary_ab", "camreg_caloes_ca", "camreg_camberwell_au"]
note = "GL-GATE-07 batch 1/8: 26 target(s) across 19 source(s)."

[[batches]]
id = "camreg-batch-02"
cadence = "monthly"
cron = "14 3 7 * *"
job = "sig-ingest-camreg-batch-02"
scheduler = "sig-sched-camreg-batch-02"
members = ["camreg_camilo_schools", "camreg_carver_mn", "camreg_cclemire", "camreg_chattanooga_tn", "camreg_cheicylia", "camreg_chicago_il", "camreg_colgis", "camreg_cotgeo", "camreg_cphang", "camreg_cwarcgis", "camreg_dc_dot_dc", "camreg_denver_co", "camreg_donegal_ie", "camreg_dover_gb", "camreg_dubuque_ia", "camreg_duganmeyer", "camreg_durham_gb", "camreg_eastdun_gb", "camreg_ebrgis_la", "camreg_esri_dash", "camreg_esriukpolice_gb", "camreg_essex_gb", "camreg_fifia"]
note = "GL-GATE-07 batch 2/8: 26 target(s) across 23 source(s)."

[[batches]]
id = "camreg-batch-03"
cadence = "monthly"
cron = "21 3 8 * *"
job = "sig-ingest-camreg-batch-03"
scheduler = "sig-sched-camreg-batch-03"
members = ["camreg_firemedic", "camreg_fl511_fl", "camreg_forwardalliance", "camreg_freese_dm", "camreg_friendswood_tx", "camreg_gainesville_fl", "camreg_gedling_gb", "camreg_gistel_be", "camreg_gmh_emc_ga", "camreg_guildford_gb", "camreg_gvanmaren", "camreg_helberg", "camreg_honolulu_hi", "camreg_indonesia_id", "camreg_infocemosa", "camreg_infraestructura"]
note = "GL-GATE-07 batch 3/8: 26 target(s) across 16 source(s)."

[[batches]]
id = "camreg-batch-04"
cadence = "monthly"
cron = "28 3 9 * *"
job = "sig-ingest-camreg-batch-04"
scheduler = "sig-sched-camreg-batch-04"
members = ["camreg_ira", "camreg_jcfd3_or", "camreg_jelenic", "camreg_jmh_us", "camreg_kcmo_mo", "camreg_keizer_or", "camreg_keshan", "camreg_kirkland_wa", "camreg_kosman", "camreg_kunying", "camreg_langan", "camreg_lawrence_ks", "camreg_lenhardt", "camreg_leon_fl", "camreg_lexington_ky", "camreg_lisburn_gb", "camreg_lojic_ky", "camreg_lpd_flock", "camreg_manhattan_ks", "camreg_mark43", "camreg_massdot_ma", "camreg_mbrc_au", "camreg_mctx_tx", "camreg_md_opendata", "camreg_mhebert", "camreg_mndot_mn"]
note = "GL-GATE-07 batch 4/8: 26 target(s) across 26 source(s)."

[[batches]]
id = "camreg-batch-05"
cadence = "monthly"
cron = "35 3 10 * *"
job = "sig-ingest-camreg-batch-05"
scheduler = "sig-sched-camreg-batch-05"
members = ["camreg_monmap_mn", "camreg_mueller_de", "camreg_nashville_tn", "camreg_nitro", "camreg_nola_safety_la", "camreg_nurnazihah", "camreg_nyc_jgrayson_ny", "camreg_nyc_weltia_ny", "camreg_nzta_nz", "camreg_oem_camera", "camreg_olsson", "camreg_oosgis_nl", "camreg_osm_surveillance", "camreg_palmdesert_ca", "camreg_penndot_pa", "camreg_pgcounty_md", "camreg_pipeline_sec"]
note = "GL-GATE-07 batch 5/8: 26 target(s) across 17 source(s)."

[[batches]]
id = "camreg-batch-06"
cadence = "monthly"
cron = "42 3 11 * *"
job = "sig-ingest-camreg-batch-06"
scheduler = "sig-sched-camreg-batch-06"
members = ["camreg_plymouth_gb", "camreg_polyu_hk", "camreg_portland_or", "camreg_raleigh_nc", "camreg_ramallah_ps", "camreg_redmond_wa", "camreg_riyadh_sa", "camreg_rjcoleman", "camreg_ruslan", "camreg_salisbury_nc", "camreg_sarasota_fl", "camreg_schellinger", "camreg_seattle_wa", "camreg_sensenet", "camreg_sfoss", "camreg_smart_sky", "camreg_squan", "camreg_stalbert_ab", "camreg_stanford_us", "camreg_surrey_bc", "camreg_sweeney", "camreg_tblose"]
note = "GL-GATE-07 batch 6/8: 24 target(s) across 22 source(s)."

[[batches]]
id = "camreg-batch-07"
cadence = "monthly"
cron = "49 3 12 * *"
job = "sig-ingest-camreg-batch-07"
scheduler = "sig-sched-camreg-batch-07"
members = ["camreg_thailand_th", "camreg_toronto_on", "camreg_townofws", "camreg_trafficops_ca", "camreg_trpa_us", "camreg_tulane_la", "camreg_txdot_rep_tx", "camreg_uchicago", "camreg_ucsd", "camreg_ukm_my", "camreg_umbc_md", "camreg_univmb_mb", "camreg_uofmd_md", "camreg_vancouver_bc", "camreg_vidya", "camreg_webappfme", "camreg_wellington_nz"]
note = "GL-GATE-07 batch 7/8: 26 target(s) across 17 source(s)."

[[batches]]
id = "camreg-batch-08"
cadence = "monthly"
cron = "56 3 13 * *"
job = "sig-ingest-camreg-batch-08"
scheduler = "sig-sched-camreg-batch-08"
members = ["camreg_whatley", "camreg_wim_camera", "camreg_yazid", "camreg_yline", "camreg_york_on", "camreg_yorku", "camreg_zyinger", "dot_511_al", "dot_511_ga", "dot_511_la", "dot_511_md"]
note = "GL-GATE-07 batch 8/8: 11 target(s) across 11 source(s)."

# --- P31.12 (BREADTH.1): the four P29.3 accountability-breadth flips ----------
# Targeted oversight-report lookups through the `accountability` connector —
# one reviewed document per source; a WAF/403 or dead link is the recorded
# honest outcome, never defeated (SIG-INGEST-013, GL-GATE-08). Oversight
# reports republish infrequently → monthly cadence.

[[sources]]
id = "gao_surveillance_reports"
cadence = "monthly"
cron = "9 6 14 * *"              # day 14, 06:09 UTC
job = "sig-ingest-gao-surveillance-reports"
scheduler = "sig-sched-gao-surveillance-reports"
note = "GAO-21-518 targeted report lookup; www.gao.gov NetScaler WAF 403s programmatic egress (2026-09-25) — the recorded challenge is the honest outcome until the posture changes."

[[sources]]
id = "dhs_oig_reports"
cadence = "monthly"
cron = "9 6 15 * *"              # day 15, 06:09 UTC
job = "sig-ingest-dhs-oig-reports"
scheduler = "sig-sched-dhs-oig-reports"
note = "DHS OIG-09-12 fusion-center oversight audit PDF (canonical oig.dhs.gov asset URL; the /reports listing 404s — the reviewed row names the document)."

[[sources]]
id = "dhs_fusion_center_assessments"
cadence = "monthly"
cron = "9 6 16 * *"              # day 16, 06:09 UTC
job = "sig-ingest-dhs-fusion-center-assessments"
scheduler = "sig-sched-dhs-fusion-center-assessments"
note = "2021 National Network of Fusion Centers Assessment Summary of Findings PDF; resolves onto the dhs_fusion_centers roster entities."

[[sources]]
id = "uk_surveillance_camera_commissioner"
cadence = "monthly"
cron = "9 6 17 * *"              # day 17, 06:09 UTC
job = "sig-ingest-uk-surveillance-camera-commissioner"
scheduler = "sig-sched-uk-surveillance-camera-commissioner"
note = "BSCC Annual Report 2022/2023 (GOV.UK accessible-HTML edition); LicenseRef-OperatorAccepted-DBRight — derived facts + citations only."

# --- P31.13 (BREADTH.2): the remaining four P29.3 flips -----------------------
# Three CCOPS index→document sources through `government_mandated_disclosure`
# plus the FEMA HSGP federal-assistance allocations sweep through
# `procurement`. Filings republish annually → monthly cadence is the
# posture-change detector; a WAF/404 is the recorded honest outcome.

[[sources]]
id = "ccops_oakland"
cadence = "monthly"
cron = "9 6 18 * *"              # day 18, 06:09 UTC
job = "sig-ingest-ccops-oakland"
scheduler = "sig-sched-ccops-oakland"
note = "Oakland PAC index → bounded §9.64 filing fan-out (max_documents=8; ALPR/ShotSpotter/Mobile-ID annual reports + the ordinance)."

[[sources]]
id = "ccops_cambridge"
cadence = "monthly"
cron = "9 6 19 * *"              # day 19, 06:09 UTC
job = "sig-ingest-ccops-cambridge"
scheduler = "sig-sched-ccops-cambridge"
note = "Cambridge LegiFile 18518 → FileOpen.aspx combined annual-surveillance-report attachment (max_documents=2)."

[[sources]]
id = "ccops_somerville"
cadence = "monthly"
cron = "9 6 20 * *"              # day 20, 06:09 UTC
job = "sig-ingest-ccops-somerville"
scheduler = "sig-sched-ccops-somerville"
note = "Somerville Legistar detail → six §10-66(b) annual-report View.ashx filings (max_documents=6)."

[[sources]]
id = "fema_hsgp_allocations"
cadence = "monthly"
cron = "9 6 21 * *"              # day 21, 06:09 UTC
job = "sig-ingest-fema-hsgp-allocations"
scheduler = "sig-sched-fema-hsgp-allocations"
note = "USAspending assistance-listing 97.067 prime-award slices (bounded 2-page reviewed window); funding evidence only — funded ≠ deployed."
