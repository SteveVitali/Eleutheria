[project]
name = "sig-connectors"
version = "0.0.0"
description = "SIG connectors package (skeleton; see docs/2_canonical_design_spec.md §47)."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
sig-connectors = "connectors.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/connectors"]
