# SPDX-License-Identifier: Apache-2.0
# The versioned `procurement` connector vocabulary (§23.6, §11.11, §11.12, P07.3).
# This table is DATA, not code (SIG-ENG-001): the Contract / FundingInstrument
# predicate surface, the acquisition-channel and funding-instrument vocabularies,
# the cooperative-vehicle source set, the agenda-platform set, and the USAspending
# sub-award facts are data rows reviewed on their own.
# Changes after P07.3 are VERSIONED MIGRATIONS (§20), not edits — bump
# `vocab_version` and add the change, never silently rewrite a row, so a
# re-extraction under a new vocabulary is a new interpretation, never a
# destructive overwrite (SIG-INGEST-017).
#
# The acquisition-channel and funding-instrument value sets mirror the frozen
# ontology enums AcquisitionChannel / FundingInstrumentType and the ProcurementState
# lifecycle track (ontology/schema/common.yaml, §11.11/§11.12/§13.4); they are
# duplicated here as the connector's runtime allowlist and a test asserts they stay
# in lock-step with the ontology (drift is a failed test).
#
# NOTE ON TOML ORDERING: every top-level key MUST appear ABOVE the first [table]
# header — otherwise TOML nests it under that table. The [sources] and per-source
# tables therefore come last.

# The connector's own vocabulary version, stamped onto every run (§20).
# 2026.09.16 (P26.2 / SOURCES.2): +procurement_notice surface (SAM.gov federal
# opportunities), +agenda_item index rows + the per-platform index endpoints /
# reviewed matter-type patterns the agenda-platform tenant path now resolves.
# 2026.09.17 (P26.5 / SOURCES.5): +escribe platform (eScribe Meetings — the
# ASP.NET `MeetingsCalendarView.aspx/GetCalendarMeetings` JSON webmethod,
# `items_key = "d"`) joins the agenda-platform set.
# 2026.09.17 (P26.6 / SOURCES.6): +content_term predicate (a captured agenda/
# minutes/matter document contains a reviewed agenda_content_vocab term —
# verbatim literal + document locator); +per-platform document endpoints
# (doc_url_template / doc_link_keys / doc_id_keys / doc_genre /
# doc_required_fields) and the reviewed bounded-window bounds
# (doc_per_tenant / doc_run_cap) each platform's index captures fan out under;
# escribe matter_link_template corrected to the verified Meeting.aspx page
# (the MeetingsCalendarView.aspx/Meeting link 404s — probed 2026-09-17).
# 2026.09.18 (P26.10 / SOURCES.9): +portal_index predicate (the per-tenant
# portal index outcome row), +procurement_portals source set (BidNet agency
# storefronts, Bonfire/OpenGov portals, city Socrata contract datasets), and
# the [platform_endpoints.bidnet]/[platform_endpoints.socrata] reviewed
# endpoint facts — bounded keyword-window searches and SoQL slices, verbatim
# agenda_content_vocab scans deciding every emitted claim.
# 2026.09.18 (P26.14 / FEDERAL.1): +matched_keyword +description predicates
# (the typed federal-award notice surface), +[usaspending_sweep] bounded
# prime/sub-award keyword-and-agency sweep plan and +[sam_gov_sweep] the
# widened SAM.gov keyword set — federal awards emit `procurement_notice`
# claims only (procured ≠ deployed: a purchase signal, never deployment
# evidence — SIG-INGEST-033 boundary preserved).
vocab_version = "2026.09.18-p26.14"

# The predicate allowlist enforced as a hard schema gate (SIG-INGEST-033): the
# `procurement` connector may write ONLY the Contract (§11.11) and FundingInstrument
# (§11.12) predicate surfaces plus the dated lifecycle transition. Writing anything
# else — a device count, a deployment, a records request, a parsed-document claim —
# is a schema error, the D6 admissibility filter enforced at ingest (§10.5).
# `contract` and `funding_instrument` are the entity assertions themselves; the
# rest are their predicates.
predicate_allowlist = [
  "contract",
  "buyer",
  "seller",
  "amount",
  "currency",
  "signed_date",
  "start_date",
  "end_date",
  "renewal_options",
  "products",
  "quantities",
  "document",
  "acquisition_channel",
  "parent_cooperative_contract",
  "amends_contract",
  "lifecycle_transition",
  "external_id",
  "funding_instrument",
  "funder",
  "recipient",
  "instrument_type",
  "program_name",
  "award_date",
  "period",
  "conditions",
  "federal_award_id",
  # P26.2 (SOURCES.2): the SAM.gov federal-opportunity surface — a notice is a
  # dated procurement-lifecycle event (solicitation/award notice), NOT a
  # contract; `procurement_notice` is the entity assertion, the rest are its
  # predicates. And `agenda_item` is the agenda-platform index row marker.
  "procurement_notice",
  "notice_type",
  "posted_date",
  "response_deadline",
  "agenda_item",
  # P26.6 (SOURCES.6): a captured agenda/matter document contains a reviewed
  # agenda_content_vocab term — the verbatim literal stays raw_value, the term
  # id is the typed value, and the claim carries the document locator.
  "content_term",
  # P26.10 (SOURCES.9): the per-tenant procurement-portal index outcome row —
  # one row per captured tenant index recording items/matches/bounds (the
  # portal analogue of the agenda_index marker).
  "portal_index",
  # P26.14 (FEDERAL.1): the typed federal-award notice surface. `description`
  # is the award's own description text verbatim; `matched_keyword` asserts
  # only that the record's text CONTAINS a reviewed surveillance-relevance
  # literal (raw_value = the verbatim slice) — never that a system was
  # deployed (procured ≠ deployed; the claim names a text match, period).
  "description",
  "matched_keyword",
]

# Predicate GENRES the procurement connector MUST NOT write (scope-creep guard,
# §23.6 / spec "Out of scope"): the device/deployment/config write-set owned by
# other tickets, the records-request surface (P07.2), and parsed-document claims
# (the parser is P07.1's; this connector CALLS it to classify a captured contract
# document, it does not run a layer engine). Named here so the refusal is data, not
# a magic string — the allowlist above is authoritative; this documents the
# out-of-scope write-set explicitly.
forbidden_predicate_genres = [
  "deployment_exists",
  "device_count",
  "camera_count",
  "records_request",
  "response_status",
  "parsed_document_claim",
  "configuration_state",
]

# The Contract acquisition-channel vocabulary (§11.11, AcquisitionChannel). A
# REQUIRED model element, not a convenience (SIG-ONTO-032).
acquisition_channels = [
  "direct_award",
  "competitive_rfp",
  "sole_source",
  "cooperative_piggyback",
  "bundle_inclusion",
  "free_trial",
  "donation",
  "grant_funded",
]

# The acquisition channel a piggyback contract carries. When a contract is on this
# channel it MUST set parent_cooperative_contract (the ridden master award) —
# SIG-ONTO-032: an agency riding a master award often files NO local RFP, and a
# model assuming a local competition would wrongly conclude no procurement evidence
# exists. Enforced in code (Contract.__post_init__), named here as data.
cooperative_channel = "cooperative_piggyback"

# The FundingInstrument instrument-type vocabulary (§11.12, FundingInstrumentType).
# Funder != recipient != purchaser: a BID/HOA/foundation/federal program can fund
# surveillance an agency operates, a pattern CCOPS ordinances (which regulate agency
# acquisition) miss (SIG-ONTO-033).
funding_instrument_types = [
  "federal_grant",
  "state_grant",
  "private_donation",
  "bid_assessment",
  "hoa_assessment",
  "foundation_grant",
  "asset_forfeiture",
  "vendor_provided_free",
]

# The procurement lifecycle-track states (§13.4, ProcurementState). A dated
# lifecycle transition is a (state, date) pair the connector may write.
procurement_states = [
  "unknown",
  "proposed",
  "rfp_issued",
  "awarded",
  "contracted",
  "cooperative_piggyback",
  "bundle_included",
  "free_trial",
  "donated",
  "third_party_funded",
  "grant_funded_pending",
  "renewed",
  "nonrenewed",
  "canceled",
  "rejected",
]

# Federal grant PROGRAM names that name local surveillance purchases in USAspending
# sub-award data (§11.12 program_name, SIG-ONTO-033). A program_name is a literal,
# recorded verbatim; this list documents the ones R4 verified route to LPR/ALPR and
# camera purchases by sheriffs and cities. NOT a closed vocabulary — a program not
# listed is still recorded from its raw value.
federal_programs = [
  "Byrne JAG",
  "Edward Byrne Memorial Justice Assistance Grant",
  "UASI",
  "Urban Area Security Initiative",
  "COPS",
  "Operation Stonegarden",
  "HIDTA",
  "Homeland Security Grant Program",
  "State Homeland Security Program",
]

# The cooperative purchasing vehicles (§22.3, §11.11). A contract sourced from one
# of these is a piggyback: acquisition_channel = cooperative_piggyback and it MUST
# link the master award via parent_cooperative_contract (SIG-ONTO-032). Values are
# registry source ids (data/sources.toml).
cooperative_vehicles = [
  "sourcewell",
  "omnia_partners",
  "naspo_valuepoint",
  "buyboard",
  "tips_usa",
  "hgacbuy",
  "equalis_group",
  "gsa",
]

# The agenda-platform registry source ids (§22.3). Agenda platforms are per-tenant
# APIs; the municipality->platform directory is the published tenant registry in
# data/agenda_tenants.toml, which the connector reads its targets from.
agenda_platforms = [
  "legistar",
  "primegov",
  "civicclerk",
  "civicplus",
  "escribe",
]

# The procurement-portal registry source ids (P26.10 / SOURCES.9, §22.3-class).
# Procurement portals are multi-tenant exactly like the agenda platforms; the
# per-tenant registry is data/procurement_portal_tenants.toml and each source
# below expands to its tenants' bounded index targets. Socrata cities are
# per-publisher source rows (rights granularity — the P26.9 precedent).
procurement_portals = [
  "bidnet_direct",
  "bonfire",
  "opengov_procurement",
  "procportal_austin_tx",
  "procportal_sf_ca",
  "procportal_kcmo_mo",
  "procportal_nyc_ny",
  "procportal_chicago_il",
]

# The artifact_type genres this connector may stamp on a captured procurement
# document (§10.3.2, SIG-INGEST-047). Mirrors the frozen ArtifactType ontology enum;
# a test asserts these are members of it.
artifact_types = [
  "contract",
  "grant_award",
  "budget",
  "state_auditor_survey",
  "warrant",
  "procurement_aggregator_record",
]

# The registry source ids the `procurement` connector runs against, by connector
# role key (§22.6). Each is a row already seeded in data/sources.toml.
[sources]
usaspending    = "usaspending"
sam_gov        = "sam_gov"
govspend       = "govspend"
sourcewell     = "sourcewell"
omnia_partners = "omnia_partners"
gsa            = "gsa"
legistar       = "legistar"
primegov       = "primegov"
civicclerk     = "civicclerk"
escribe        = "escribe"

# Procurement-portal registry sources (P26.10 / SOURCES.9): each maps to its
# published tenant rows in data/procurement_portal_tenants.toml. The Socrata
# cities are per-publisher source rows so rights posture stays per-dataset.
bidnet_direct          = "bidnet_direct"
bonfire                = "bonfire"
opengov_procurement    = "opengov_procurement"
procportal_austin_tx   = "procportal_austin_tx"
procportal_sf_ca       = "procportal_sf_ca"
procportal_kcmo_mo     = "procportal_kcmo_mo"
procportal_nyc_ny      = "procportal_nyc_ny"
procportal_chicago_il  = "procportal_chicago_il"

# --- USAspending: sub-awards, not only prime awards (§23.6, SIG-ONTO-033) ------
# USAspending exposes prime awards AND sub-awards. Sub-awards are the traceable
# link from a federal grant program (Byrne JAG, UASI) to the LOCAL purchase that
# appears in no local procurement record. The connector MUST pull sub-awards
# (`subaward_type = "sub-grant"`/`"sub-contract"`), not only prime awards.
[usaspending]
api_base           = "https://api.usaspending.gov/api/v2"
subawards_endpoint = "/subawards/"                  # POST spending_by_award w/ subaward=true
prime_endpoint     = "/search/spending_by_award/"
# The award-id field on a sub-award that is the traceable federal_award_id link.
federal_award_id_field = "prime_award_generated_internal_id"
subaward_id_field      = "internal_id"
# Sub-awards MUST be pulled: this is the flag the connector asserts on every
# USAspending target (assert_pulls_subawards).
subaward = true

# --- Agenda-platform index endpoints (P26.2 / SOURCES.2) ----------------------
# Each agenda-platform tenant row names the per-tenant api_base; the platform's
# INDEX endpoint (the public meetings/matters listing the portal itself reads)
# is appended here — a bounded fetch (top/N or a reviewed POST body), never a
# crawl. Probed live 2026-09-16: webapi.legistar.com/v1/<client>/matters returns
# the client's public matter index; lacity.primegov.com PublicPortal /search
# returns the meeting index; {tenant}.api.civicclerk.com/v1/Events is the
# portal's own endpoint (the tenant key comes from the portal's embedded
# config — anonymous requests currently 404, recorded honestly, never faked).
[platform_endpoints.legistar]
# InSite API: the tenant matter index is `{api_base}/matters` (OData; $top bounds
# the pull, $orderby keeps the freshest records first). The bare api_base 404s.
index_path  = "/matters"
index_query = "$top=100&$orderby=MatterLastModifiedUtc%20desc"
# The human-readable legislation detail page for a matter (link, not bytes).
matter_link_template = "https://{tenant}.legistar.com/LegislationDetail.aspx?ID={matter_id}&GUID={matter_guid}"
# A matter becomes a Contract claim ONLY when its MatterTypeName matches one of
# these reviewed type-name substrings (case-insensitive). A municipality's
# contract-approval instruments carry contract-family type names (Mesa:
# 'Contract', 'Agreement'; San Antonio: 'Contract', 'Agreement', 'Purchase',
# 'Lease', 'Interlocal', 'Settlement', 'Professional Services'). An ordinance /
# resolution / appointment is legislative business, not a contract — it lands as
# an agenda_index row only, never a Contract (§3.1 — no synthetic certainty).
contract_matter_patterns = [
  "contract",
  "agreement",
  "purchase",
  "lease",
  "interlocal",
  "professional services",
  "settlement",
]
# P26.6 — the matter DOCUMENT endpoint + bounded window. `{api_base}/matters/
# {MatterId}` is the documented InSite matter detail (probed 2026-09-17:
# webapi.legistar.com/v1/seattle/matters/11400 → the matter JSON, API-mode
# allow-listed). A 200 JSON object missing every `doc_required_fields` key — or
# a non-JSON body — is genre drift (ContentDrift), not a document.
doc_url_template    = "{api_base}/matters/{item_id}"
doc_id_keys         = ["MatterId"]
doc_genre           = "json"
doc_required_fields = ["MatterId"]
# The reviewed window: at most 20 most-relevant matters per tenant per run
# (vocab-/contract-matching index items first, then index order = recency),
# and at most 350 document fetches per platform run so the monthly job stays
# inside its task window. Both bounds ride each generated target row as data.
doc_per_tenant = 20
doc_run_cap    = 350
# Canonical selection order — most-recent first by the item's own date fields
# (item DATA, never the server's return order), id as the deterministic
# tie-break. A re-run over the same index item set selects the same window.
doc_order_fields = ["MatterLastModifiedUtc", "MatterIntroDate"]

[platform_endpoints.primegov]
# PrimeGov PublicPortal: POST {api_base}/search returns the tenant meeting index
# (probed 2026-09-16 — LA City; a bounded body bounds the window).
index_path = "/search"
index_method = "post"
index_body = {}
# Meeting objects carry no contract-typing — index rows only.
# P26.6: the meeting's own `itemsSearchResultsDefaultLinkOnMeetingPortal` field
# names the human portal document — the document URL comes from the captured
# payload, never constructed. doc_genre "any" sniffs PDF/JSON/HTML from the
# captured bytes. NOTE: every PrimeGov host's robots.txt answers `Disallow: /`
# (platform-wide, P26.5 probe 2026-09-17) — these document targets resolve only
# if an index capture exists, and each fetch is a recorded robots refusal while
# the wall stands. The bound is reviewed anyway so the row is complete data.
doc_link_keys  = ["itemsSearchResultsDefaultLinkOnMeetingPortal"]
doc_genre      = "any"
doc_per_tenant = 20
doc_run_cap    = 100
doc_order_fields = ["meetingDate"]

[platform_endpoints.civicclerk]
# CivicClerk v1: {api_base} already names the Events index (the portal's own
# endpoint; api_base template is https://{tenant}.api.civicclerk.com/v1/Events).
# The endpoint speaks OData — $top bounds the pull (P26.3: verified 2026-09-17,
# the index is otherwise the tenant's full event list).
index_path  = ""
index_query = "$top=100"
# P26.6 — the meeting DOCUMENT endpoint (probed 2026-09-17):
# /v1/Meetings/{agendaId} answers the meeting JSON — `items[]` agenda objects
# (agendaObjectItemName/Description/HtmlContent) + `publishedFiles[]` (agenda
# PDF links) — for an event whose `agendaId` is non-zero. An event with no
# agenda (agendaId 0/absent) resolves no document. A non-JSON body is drift.
doc_url_template    = "https://{tenant}.api.civicclerk.com/v1/Meetings/{item_id}"
doc_id_keys         = ["agendaId"]
doc_genre           = "json"
doc_required_fields = ["id"]
doc_per_tenant      = 20
doc_run_cap         = 350
doc_order_fields    = ["startDateTime"]

[platform_endpoints.escribe]
# eScribe Meetings (P26.5): the tenant's public Meetings calendar page calls its
# own ASP.NET JSON webmethod — POST {api_base}/MeetingsCalendarView.aspx/
# GetCalendarMeetings answers {"d": [ …meeting records… ]} (verified live
# 2026-09-17 across the pub-* tenant surface; api_base template is
# https://pub-{tenant}.escribemeetings.com). The reviewed index_body bounds the
# calendar window — a dated slice, never a full-history crawl.
index_path   = "/MeetingsCalendarView.aspx/GetCalendarMeetings"
index_method = "post"
index_body   = { calendarStartDate = "2025-01-01T00:00:00", calendarEndDate = "2026-12-31T23:59:59" }
items_key    = "d"
# The human-readable meeting page (link, not bytes) — {tenant} is the pub-*
# slug, {matter_id} the meeting's ID guid. P26.6 probe (2026-09-17): the
# MeetingsCalendarView.aspx/Meeting path 404s; the classic Meeting.aspx page is
# the tenant's real public agenda/minutes document — corrected.
matter_link_template = "https://pub-{tenant}.escribemeetings.com/Meeting.aspx?Id={matter_id}&Agenda=Agenda&lang=English"
# P26.6 — the meeting DOCUMENT is that same server-rendered Meeting.aspx page
# (probed: 211 KB HTML agenda with item text, genre html). A non-HTML capture
# here is genre drift; a page with no text is an honest `empty` outcome.
doc_url_template = "{api_base}/Meeting.aspx?Id={item_id}&Agenda=Agenda&lang=English"
doc_id_keys      = ["ID", "Id"]
doc_genre        = "html"
doc_per_tenant   = 20
doc_run_cap      = 300
# GetCalendarMeetings returns the same meeting set in a DIFFERENT order per
# call (verified 2026-09-17 — 465/474 positions shuffled between two immediate
# fetches). Canonical order = the item's own StartDate (desc), ID tie-break —
# the bounded window is a function of the item SET, not server ordering.
doc_order_fields = ["StartDate"]

# --- Procurement-portal index endpoints (P26.10 / SOURCES.9) ------------------
# Each portal tenant row names its platform family + surface facts; the
# platform's INDEX shape is reviewed here — bounded targeted lookups (the
# portal's own search field / the documented SoQL rows API), never a crawl.
[platform_endpoints.bidnet]
# BidNet Direct agency storefronts: `{storefront}/solicitations/{kind}-bids`
# serves the tenant's server-rendered index (`mets-table-row` items; verified
# markup 2026-09-18 — `div.sol-num`, `a.solicitation-link`,
# `span.sol-publication-date`/`span.sol-closing-date` > `span.date-value`).
# `?keywords=<kw>` is the portal's OWN public search field — a bounded
# targeted lookup, never a crawl; the reviewed recall set below covers the
# agenda_content_vocab families (precision comes from the verbatim scan,
# never the keyword). Page-1 of each index is the reviewed window (25 items —
# deeper pages are out of scope for a sweep; `?page=` is robots-disallowed
# anyway). The storefront's open-bids index is additionally fetched
# unfiltered (the current window regardless of keywords).
index_kinds = ["open", "closed", "awarded"]
index_keywords = [
  "surveillance", "camera", "cctv", "license", "drone", "gunshot",
  "facial", "stingray", "unmanned", "predictive",
]
index_unfiltered_kinds = ["open"]
host = "www.bidnetdirect.com"
# Item markup facts (fail-closed): an index page that carries neither a
# `solicitation-link` item nor the `id="solicitationList"` container (the list
# region, present even on a zero-result page) is ContentDrift — the markup
# contract changed, never a silent empty.
items_marker = "solicitation-link"
empty_marker = "solicitationList"
# The detail DOCUMENT is the item's own public detail page — the captured
# `a.solicitation-link` href (never constructed): `<group>/<agency>/
# solicitations/<slug>/<id>?purchasingGroupId=<gid>&origin=2`. It serves the
# verbatim description text to anonymous viewers (verified 2026-09-18). A
# non-HTML body is genre drift; a page with no item text is `empty`.
doc_genre = "html"
doc_per_tenant = 12
doc_run_cap = 100
doc_order_fields = ["published", "closing"]
# index_kind -> procurement lifecycle state (§13.4): an open solicitation is
# rfp_issued; an awarded bid is awarded. A closed-not-awarded bid asserts no
# transition (closure alone is not a vocabulary state — §3.1).
[platform_endpoints.bidnet.notice_map]
open = "rfp_issued"
awarded = "awarded"

[platform_endpoints.socrata]
# City Socrata contract/award datasets: the documented SoQL rows API
# `https://{host}/resource/{dataset}.json` (API-mode allow-listed hosts). The
# per-tenant registry row carries the reviewed field aliases + window bound;
# the connector builds one bounded query per tenant — `$limit` rows ordered
# by the reviewed date field DESC (recent window, deterministic across
# re-runs) — and scans each record's reviewed text fields verbatim. A
# non-array payload is ContentDrift (Socrata error envelopes answer 200 JSON
# objects, not lists — fail closed).
index_path_template = "https://{host}/resource/{dataset}.json"
doc_per_tenant = 0
doc_run_cap = 0
# A dataset row is a published award/contract record — `contracted` is the
# lifecycle state the register itself asserts (an award the city recorded).
# Rows carrying their own notice-type field (e.g. NYC CROL's
# type_of_notice_description) map through the tenant's reviewed notice_map.
[platform_endpoints.socrata.notice_map]
default = "contracted"
solicitation = "rfp_issued"
award = "awarded"
award_notice = "awarded"
contract_award = "contracted"
contract_award_notice = "contracted"

# --- SAM.gov federal opportunities (P26.2 / SOURCES.2) -------------------------
# The documented public opportunities search (api.sam.gov, open.gsa.gov): a
# bounded keyword+window query, api_key from $SIG_SAM_GOV_KEY (HG-09, never in a
# file). A notice is a dated lifecycle event on the procurement track, NOT a
# contract — `notice_type` → lifecycle state is the reviewed data map below; an
# unlisted type emits index facts only (never a guessed lifecycle, §3.1).
[sam_gov]
api_base = "https://api.sam.gov/prod/opportunities/v2"
search_endpoint = "/search"
api_key_env = "SIG_SAM_GOV_KEY"
# The api.data.gov-gateway credential header (P26.4): the key rides the request
# header, never the URL — a keyed query would land in the recorded source_uri /
# run records (HG-09). Verified live 2026-09-17: X-Api-Key → 200, keyless → 404.
api_key_header = "X-Api-Key"

# Reviewed SAM notice type → procurement lifecycle state (§13.4) +
# acquisition_channel hint. `channel` is emitted only where the notice type ITSELF
# declares it (a Justification & Approval notice is a sole-source intent).
[sam_gov.notice_map]
Solicitation                        = { state = "rfp_issued" }
"Combined Synopsis/Solicitation"    = { state = "rfp_issued" }
Presolicitation                     = { state = "proposed" }
"Sources Sought"                    = { state = "proposed" }
"Special Notice"                    = { state = "proposed" }
"Intent to Bundle Requirements"     = { state = "proposed" }
"Fair Opportunity / Limited Sources" = { state = "proposed" }
"Justification"                     = { state = "proposed", channel = "sole_source" }
"Award Notice"                      = { state = "awarded" }

# --- P26.14 (FEDERAL.1): bounded federal sweep plans ---------------------------
# The widened surveillance keyword space shared by USAspending and SAM.gov.
# Keywords are API SEARCH terms (precision comes from the verbatim
# agenda_content_vocab scan at normalize, never the keyword); bounds are
# reviewed data — every generated target carries its page/size caps so a
# sweep stays finite and each slice is one counted request (a refused
# request is recorded and NEVER re-probed — SIG-INGEST-012/013).
[usaspending_sweep]
keywords = [
  "license plate reader",
  "automated license plate",
  "automatic license plate",
  "alpr",
  "facial recognition",
  "drone",
  "unmanned aerial",
  "cell-site simulator",
  "stingray",
  "shotspotter",
  "acoustic gunshot",
  "surveillance camera",
  "predictive policing",
  "geofence",
  "data broker",
  "gunshot detection",
  "real time crime center",
  "body camera",
  "video surveillance",
]
# Awarding agencies whose pass-through grant programs buy local surveillance
# (the high-signal funder set: Byrne JAG/UASI/HSGP live under DOJ + DHS; DoD
# is included for direct federal surveillance purchases).
agencies = [
  { name = "Department of Justice" },
  { name = "Department of Homeland Security" },
  { name = "Department of Defense" },
]
[usaspending_sweep.bounds]
page_size        = 100
sub_max_pages    = 2      # sub-award keyword slices: the federal→local link
prime_max_pages  = 1      # prime award keyword slices: bounded to the top window
agency_max_pages = 1      # agency slices search the whole keyword set at once
time_period = [ { start_date = "2018-01-01", end_date = "2026-12-31" } ]
# Assistance award types (grants/coops/direct payments — the pass-through set).
sub_award_type_codes   = [ "02", "03", "04", "05", "06", "07", "08", "09", "10", "11" ]
# All award types for prime slices (contracts A/B/C/D + assistance above).
prime_award_type_codes = [ "A", "B", "C", "D", "02", "03", "04", "05", "06", "07", "08", "09", "10", "11" ]
sub_fields = [ "Sub-Award ID", "Sub-Awardee Name", "Sub-Award Date", "Sub-Award Amount", "Sub-Award Description", "Awarding Agency", "prime_award_generated_internal_id" ]
prime_fields = [ "Award ID", "Recipient Name", "Award Amount", "Start Date", "End Date", "Awarding Agency", "Description" ]

[sam_gov_sweep]
# The same reviewed keyword space over the documented opportunities search —
# one bounded title query per keyword (the API's own `title` field; `limit` +
# posted window bound each slice). The key rides X-Api-Key, never the URL.
keywords = [
  "license plate reader",
  "automated license plate",
  "automatic license plate",
  "alpr",
  "facial recognition",
  "drone",
  "unmanned aerial",
  "cell-site simulator",
  "stingray",
  "shotspotter",
  "acoustic gunshot",
  "surveillance camera",
  "predictive policing",
  "geofence",
  "data broker",
  "gunshot detection",
  "real time crime center",
  "body camera",
  "video surveillance",
]
limit       = 25
posted_from = "01/01/2026"
posted_to   = "12/31/2026"
