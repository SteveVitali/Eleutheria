# web/ — SIG public web surface (skeleton)

TypeScript is confined to this package (SIG-ENG-010). This is an empty-but-real
placeholder; the public site is built in Phase 15. No dependencies are installed
yet and the Python CI does not build it.
