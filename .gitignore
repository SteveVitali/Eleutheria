# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Stray research fetch artifacts (Cloudflare challenge pages, partial downloads)
icpsr.html
*.tmp
*.partial

# Scratch
scratch/
tmp/

# Secrets — tokens/keys are env-only, never committed (RISK-P21-05, HG-09).
# SIG_MUCKROCK_TOKEN / SIG_DATA_GOV_KEY / SIG_OVERPASS_ENDPOINT / SIG_CIVICCLERK_BASE
# are read from the environment; a local .env* file is developer-only and ignored.
.env
.env.*
*.env

# Local live-run capture roots (OCFL captures land here off the committed tree).
.sig/

# Python / uv
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Generated SBOM (produced per release via `make sbom`)
sbom.cdx.json

# Node (web/)
node_modules/
web/dist/
web/.astro/
web/test-results/
web/playwright-report/
web/.lighthouseci/
exports/out/
