# Architecture Decision Records (Appendix F)

Every architectural decision is recorded here under a consistent template
(context, decision, status, consequences, alternatives considered, and a
**revisit trigger**). An ADR with no revisit trigger is incomplete
(SIG-STORE-007); `tests/unit/test_policy_adrs.py` enforces this mechanically.

Decisions are immutable: a change is a **new ADR** (SIG-ENG-003), never an edit
of a landed one.

## Index

| ADR | Decision |
|---|---|
| ADR-001 | PostgreSQL 18 + PostGIS as the canonical store; everything else a projection |
| ADR-002 | Append-only claim table; entity tables hold identity only |
| ADR-003 | Two interval time dimensions + one ordering scalar |
| ADR-004 | EDTF for uncertain dates |
| ADR-005 | Resolution as a stored decision record, not a view |
| ADR-006 | OCFL 1.1 evidence store on object storage with governance-mode Object Lock |
| ADR-007 | LinkML as the single ontology source of truth |
| ADR-008 | SKOS for published controlled vocabularies |
| ADR-009 | SPDX expressions for per-source licensing, with a build-time compatibility gate |
| ADR-010 | DuckDB/Parquet analytics boundary; no raw audit rows anywhere |
| ADR-011 | The ODbL posture (§42.3) — OSM-derived assets in a separate compartment |
| ADR-012 | Sensitivity tiers enforced by RLS, applied at the view layer |
| ADR-013 | uv as the workspace and lockfile tool |
| ADR-014 | Astro for the public web surface |
| ADR-015 | AWS S3 evidence store + CloudFront for bulk-export delivery (reconciling §38.5 egress) |
| ADR-016 | Dagster OSS for orchestration, kept reversible |
| ADR-017 | FastAPI for the read API |
| ADR-018 | MapLibre GL for the web map |
| ADR-019 | pytest + Hypothesis for testing |
| ADR-020 | GitHub Actions for CI |
| ADR-021 | Source registry as seeded data, with a runtime ingestion gate in `connectors` (P00.4) |
| ADR-022 | Defer physical partitioning of `claim` to preserve the `claim_id` FK contract (P02.1) |
| ADR-023 | An `evidence/` package + content-addressed blob dedup for the capture row (P02.2) |
| ADR-024 | A pinned, deterministic, in-repo EDTF envelope derivation (P02.3) |
| ADR-025 | Temporal invariants as pipeline data-quality checks; as-of as SQL functions (P02.3) |
| ADR-026 | The eight-stage connector framework: fetch-only egress + socket-level network-isolated replay (P04.1) |
| ADR-027 | The `osm` connector: versioned tag vocabulary, `(type,id,version)` keying, ODbL landing (P04.2) |
| ADR-028 | The `atlas` connector: family-level `deployment_exists`, agency-id keying, category-retirement-not-a-world-change (P04.3) |
| ADR-029 | Splink 4 on DuckDB for the probabilistic ER tiers 4–5, as a fully-specified deterministic model (P05.1) |
| ADR-030 | Review queue + LLM-extraction scaffolding as a library plus CLI (P05.2) |
| ADR-031 | A minimal count-reconciliation seed + the three missing count predicates, for the vertical slice (P06.1) |
| ADR-032 | A minimal slice dossier renderer with a print-CSS PDF path, ahead of the production surface (P06.1) |
| ADR-033 | The layered document-parsing stack as the parser interface every connector extracts through (P07.1) |
| ADR-034 | The `records` connector: targeted-lookup posture, MuckRock api_v2 + short-lived JWT, and the `no_responsive_records` → coverage bridge (P07.2) |
| ADR-035 | The `procurement` connector: cooperative-piggyback → parent award, USAspending sub-award tracing, the `FundingInstrument` runtime shape, the published agenda-platform tenant registry, and `artifact_type` as an ontology vocabulary (P07.3) |
| ADR-036 | The reconciliation workflows layered on the resolver (P08.2) |
| ADR-037 | The materialized `Contradiction` entity, its lifecycle, and the byte-identical L3 rebuild (P08.3) |
| ADR-038 | The coverage-metrics layer in `inference`, reuse of `db.absence`/currency, and the executable capture–recapture prohibition (P09.1) |
| ADR-039 | The research-task engine: detector/closing-condition as callables, the `resolved_no_evidence_exists`→`CoverageRecord` bridge, and executable anti-abuse (P10.1) |
| ADR-040 | The §33.2 detector catalog (34 registered task types) and the §31 contradiction→task map, cross-checked against `CONTRADICTION_TYPES` (P10.2) |
| ADR-041 | The records-request generator: the 51-jurisdiction records-law table + templates as versioned data, `not_researched` (not `searched_not_found`) for the residency barrier, local-filer routing, measured template success rates, and the consent gate (P10.3) |
| ADR-042 | The `flock_portal` connector: portal claims in their own CC-BY-SA-4.0 compartment (export merge with the CC-BY graph fails the build), change detection + back-fill keyed on the upstream `data_last_updated` snapshot field (never fetch time), a challenge honoured as a refusal, the §29.3 sharing-edge + §29.7 snapshot-diff reconcilers invoked (not forked) with only deterministic edges in the claim stream, and the SIG-INGEST-031 fallbacks retained as named routes (P11.1) |
| ADR-043 | The `audit_structural` connector: agency Flock audit CSVs parsed into **structural aggregates only** (per-search rows read transiently, aggregated, dropped — `assert_no_per_row_output` as the §18.1 schema gate), the audit `Camera Count` as an independent `active_device_count` claim reconciled by P08.2's §29.1 count reconciler (never merged), `SharedNetworks.csv` as directional configured-access edges via P08.2's §29.3 reconciler (blanks negative, `valid_from_kind='unknown'`), `***` redaction as a distinct recorded state (`classify_cell`), the four audit source types as a closed non-interchangeable set, and a new `agency_audit_export` source (not the derived HIBF export, SIG-INGEST-046a) with the schema as versioned data (P11.2) |
| ADR-044 | The usage-analytics boundary (§18): the `db.analytics` substrate over DuckDB-written **Hive-partitioned Parquet** (SIG-STORE-027, no columnar Postgres extension, no `pyarrow`), the bright line as a columnar schema property (`ANALYTICS_COLUMNS` carries UUIDs + period + facts + lineage, **no plate-capable and no name column**, SIG-STORE-025/026), the join enforced structurally to `{searching_org_id, source_org_id, period}` with `ingest_run_id`+`agg_ruleset_version` lineage (a name key is a hard `JoinKeyError`, SIG-STORE-028), partition-as-evidence (each partition content-addressed with the interop multihash and cited by a *summary* claim, SIG-STORE-029), and rationale-driven small-cell suppression in `db.suppression` (institutional-conduct publishes even when small; individual small cells suppress to `null`+`suppressed_flag`+`k_threshold`, never zero; ambiguous → suppress + review task; complementary suppression for non-invertibility; one-month floor; k=5 as SIG's own policy with the stricter partner threshold winning, SIG-STORE-030/031/032/033) (P12.1) |
| ADR-045 | Access-path closure (§30.2) as the `inference.access_paths` value-object module: only `configured_access` and `federates_search_to` compose (`observed_use`/`declared_policy`/query-direction `distributes_list_to` never do, and no hop's §12.2 kind is ever merged, SIG-ONTO-042/SIG-RECON-049), edges normalized into accessor→provider terms, scope may not broaden along a chain and a path's scope is its narrowest hop, every hop must be valid at the as-of time (an expired hop taints the path `historical`; single-snapshot edges always valid, SIG-ONTO-044), confidence is the **path minimum** never the average, a hard `MAX_PATH_HOPS` enumeration cap and a published `SPECULATIVE_HOP_THRESHOLD` beyond which a path is **speculative** and excluded from headline figures (SIG-RECON-050), every hop carries evidence and the full hop list survives into the L4 `Inference` (SIG-RECON-047/048/049); plus tightening `AccessRelationship.automaticity` to **required** (SIG-ONTO-049) (P12.2) |
| ADR-046 | Policy/legal instruments (§§11.13–11.14): the `Policy` / `LegalInstrument` predicate surfaces and the §29.6 divergence reconciler are consumed from P01.1 / P08.2 (no schema change, generation gate untouched), so P13.2 lands as the ticket's acceptance tests (`Policy`/`LegalInstrument` predicate surfaces; `Policy` ≠ `ConfigurationState` never merged at schema **and** resolution layer, SIG-ONTO-034/SIG-RECON-044) plus `connectors.curated_index` — the **general form** of SIG-EPIS-030: a `CuratedSourceIndex` held as an index whose `index_records()` are `index_only` and whose `as_claims()` raises, with the P13.1 `accountability` Abuse Library path refactored to build a `CuratedIndexEntry` (byte-identical row shape, additive) (P13.2) |
| ADR-047 | The public read API (§37): a hand-written, versioned FastAPI contract (OpenAPI generated, never a schema reflection, SIG-API-001) built over a `ReadStore` seam with an in-memory implementation (no in-Python DB fetch layer exists yet). Every material fact leaves only inside a `ResolutionEnvelope` (SIG-API-002) with a coverage statement (SIG-API-003) and — for collections/entities — a computed licence + upstream attribution reusing `policy.licensing` (SIG-API-004); every read echoes the two-axis as-of pair via `db.temporal.AsOf` (SIG-API-005) and cacheability follows belief-pinning (`belief_pinned = not belief_defaulted`; explicit belief → immutable, defaulted → `no-store`, SIG-API-006); belief-time filtering makes a belief-pinned citation reproducible after a correction. `/id/{type}/{uuid}` content-negotiates HTML/JSON-LD/RDF (SIG-API-008); `/changes` reuses `reconcile.snapshot_diff.diff_series` (SIG-API-009). The prohibited-endpoint bar is fail-closed at app construction plus a per-person entity-type guard (SIG-API-012); sealed/restricted captures are served only as their SIG-EVID-009/010 public representation (bytes never), coordinates reduced by `policy.sensitivity.apply_tier`; `assert_public_visibility` refuses `restricted`/`sealed` records to every tier (SIG-API-011); `/export` is index-only (P14.2 owns bulk); GraphQL (SIG-API-010) is deliberately not the only surface (P14.1) |
| ADR-048 | Bulk exports (§38, §42.4): a release is a pure function of a `BuildSpec` (two-axis as-of + ruleset + resolver versions → deterministic `release_id`/`concept_id`), each table's compartment computed from its rights record (an incompatible mix fails the build, ODbL/CC-BY physically separate), silently-travelling share-alike fails closed, per-row rights provenance via the same `downstream_obligations` the API uses, seven deterministic format writers + checksummed manifest, and Zenodo concept/version DOIs; `exports` must not import `api` (P14.2) |
| ADR-050 | The production local-dossier surface in `web/` (§39.2): the twelve-section contract + validator, the three action blocks and the derived `next_decision_date` (a stable interface P15.4 keys on), "what we don't know" single-sourced across the HTML summary / print export / static JSON API, and a **print-to-PDF path** (a paginated print route with an as-of + permalink footer on every page — no server-side PDF renderer, verified via headless `page.pdf()`); the P06.1 `exports/dossier.py` slice renderer is superseded but retained (closes out ADR-032) |
| ADR-049 | The epistemic visual language + the no-JS archivable web shell (§§39.1/39.9/40): the four independent fields / four-step glyph / single absence hatch / persistent contested marker / contradiction value-range as pure colour-free logic P15.2–P15.5 consume, "green never for epistemic state" enforced by a stylesheet-parsing test, zero-JS-by-default static build (0 client JS, no `client:` directive) with a tabular equivalent per map and a list per graph verified with JS disabled, committed fixtures that are a faithful subset of the read-API envelope (extras on a `FactView` view-model), a base64url pre-generated no-JS absence→research-task route, belief-pinned permalink + citation on every page, and the CI gates for WCAG 2.2 AA / OSI-licences (with a documented CC0+BlueOak waiver) / performance budgets (P15.1) |
| ADR-051 | The infrastructure map (§39.3) + network explorer (§39.4) honest-rendering rules as pure logic (`web/src/lib/map.ts`, `network.ts`) — the coverage↔point single bound control (SIG-UI-017), low-coverage≠low-density desaturation (SIG-UI-018), per-tier point honesty + national binning (SIG-UI-019), no-coordinate jurisdiction indicators with a conservation law (SIG-UI-020), ego-default sharing/network (SIG-UI-021/022), the structurally-required inline ER-quality disclosure on every centrality statistic (SIG-UI-023), the three unmerged §12.2 access edge types (SIG-UI-024), and access-path closure with per-hop evidence + speculative-beyond-threshold (SIG-UI-025) mirroring `inference.access_paths`; plus the **static-PMTiles serving contract** at `/map/style.json` (self-hosted PMTiles v3 + OSM attribution + no third-party CDN, SIG-UI-038/SIG-GEO-012/013) landed WITHOUT bundling the maplibre-gl runtime, a recorded deviation from ADR-018 that keeps the ADR-049 zero-JS + perf gates green (P15.3) |
| ADR-053 | The research **queue** (§39.7), the public **corrections log** (§39.8 — the required eighth surface), the one-click **dispute/correction** path (§45), the **methodology** / **data-freshness** (§32.4) / **coverage-metrics** (§32.5) pages, and the **editorial-standards conformance gate** (§41), as pure logic + committed fixtures + static pages in `web/` (SIG-ENG-010): the queue's disposition-by-assignee vocabulary carrying "searched, found nothing" (SIG-TASK-009) and claiming-as-priority-not-exclusivity with expiry (SIG-TASK-010/011, no leaderboard SIG-TASK-012); the coverage page's **executable "never a total"** — a NAMED denominator required and a population total unrepresentable (SIG-METRIC-008/009/010); a base-layout **"How we know this"** six-component module (SIG-UI-044) and one-click **dispute link** (SIG-UI-033) on every page, which is also how methodology/freshness/coverage are linked from every dossier (SIG-UI-034); and the **editorial gate** — a register-rule denylist applied to generated rationale as well as hand-written copy (SIG-UI-043/046), the three example cases as the spec's exact copy (SIG-UI-045), and the hostile-reader review whose undispositioned finding fails the build (SIG-UI-042). Seven surfaces + corrections asserted to exist across the built site (P15.5) |
| ADR-054 | The contributor system (§34) as pure, tested `tasks`-package logic ahead of persistence: the **five write tiers** with cumulative scope + review requirement and the `Person`-creation gate at Curator (SIG-CONTRIB-001), submissions that **enter at L0** with no claim-without-provenance and device observations routed to OSM/DeFlock (SIG-CONTRIB-002/004), **structural pseudonymity + data minimisation** (no real-name/device-id field, an operational-log purge window, SIG-CONTRIB-005/006), the re-runnable **moderated onboarding usability study** gate (≥5 ontology-naïve, median ≤10 min, SIG-CONTRIB-003), **revert-as-a-unit** recorded as a new append-only assertion that deletes nothing — modelled in `tasks.revert` and proven on the live claim spine via `retraction_of` (SIG-CONTRIB-009, §16.6) — and **poisoning resistance** that assumes bad faith: an anomaly router with no `reject` outcome that guards false absence equally (SIG-CONTRIB-010/011), the vendor operating-territory check that holds an unsupported claim at lowest confidence + raises a verification task (SIG-CONTRIB-011a), a hard refusal of SIG-applied mass reverts (SIG-CONTRIB-011b), and unverified-community-below-records-derived visual weight (SIG-CONTRIB-011c) (P16.1) |
| ADR-052 | The renewal **watch** (§39.5), evidence **recommender** (§39.5a), and evidence **viewer** (§39.6) as pure logic (`web/src/lib/watch.ts`, `recommender.ts`, `evidence-viewer.ts`): per-jurisdiction iCal (RFC 5545, folded) + RSS subscriptions emitted as **static files** keyed on `next_decision_date` reused verbatim from P15.2 (SIG-UI-027/014b); the recommender's **structural neutrality guarantee** — a six-axis ordinal ranking with a denylist-enforced ban on persuasiveness/sentiment/vote signals (SIG-UI-027a/b) — plus an exportable citation list (SIG-UI-027c); and the evidence viewer's span-highlight at the locator, field-by-field capture diff (§29.7), and sealed metadata-only representation (§17.5) with the SIG-UI-028 completeness contract enforced at build. Contested values carry the persistent marker at every appearance (SIG-UI-008). All zero-JS/archivable; a11y + perf gates hold (P15.4) |
| ADR-055 | **No direct automated OSM writes** — the human-mediated suggestion workflow + the OSM compliance analysis (records the spec's Appendix-F "ADR-017", §35.2). Contribution back in `tasks.contribution` as pure, tested logic: `write_to_osm` exists only to refuse and an `AppliedEdit` cannot name SIG as the applier (SIG-CONTRIB-014); a MapRoulette **cooperative** (`cooperativeType=tags`) `TagSuggestion` a human mapper applies in their own account (SIG-CONTRIB-015/015a, account holder + located API docs disclosed 015b); the Automated-Edits-Code **scope test** recorded — operator attribution is in scope, the exceptions do not apply, individual human review is what keeps SIG outside the Code, and a genuine bulk edit needs the full proposal/decision path (SIG-CONTRIB-016/016a/016b/016c); the declared changeset hashtag `#sig_operator_attribution` required on every SIG edit and wired to the §7 leverage metric via `LeverageLedger` reading accepted hashtag-bearing changesets (SIG-CONTRIB-016e); the published + registered **Organised Editing activity page** disclosing tools + data sources with usage conditions and metrics-as-task-outcomes (SIG-CONTRIB-016d/016g); the **contribution-path licence gate** `policy.licensing.assert_contribution_permitted` (derivative + relicensable-to-ODbL, distinct from the export gate, SIG-CONTRIB-016f / §42.3a / SIG-LIC-007a–c) applied before a suggestion is rendered; per-project correction channels (SIG-CONTRIB-018); and structural per-claim upstream attribution on the API `/claim` response (SIG-CONTRIB-020) (P16.2) |
| ADR-056 | The jurisdiction adapter framework — country-namespaced vocabularies, BCP-47 labels, and coarse international ingestion; `canonical_name` as a spec-faithful scalar (P18.1) |
| ADR-057 | The France/Belgium (Technopolice) connectors — prefectural orders → `LegalInstrument`, DECP → `Contract`, and the OSM import study (P18.2) |
| ADR-058 | **Tickets and build memory are committed; agent scratch is unified** — `docs/tickets/` leaves `.gitignore` and is committed as the build's contract record; the non-regenerable planning artifacts are promoted to `docs/build/`; `.agents/scratch/` becomes the single gitignored scratch root with a fixed layout; an `AGENTS.md` hierarchy is bootstrapped (SIG-ENG-001/003/012/031); the LD-X09/X10/X11 manifest/template defects are fixed (P19.1) |
| ADR-059 | **Capstone spine wiring: `PgClaimSink`, `PgReadStore`, and the compute-on-read seam** — connector output persists to the PG spine via a `db`-owned `PgClaimSink` selected through a `connectors` factory (append-only, idempotent on a new `content_digest`); the read API is served over PG by `PgReadStore` (publication at the store boundary, RLS enabled, as-of belief); graph annotations `_compute_on_read()` until P21.2 materialises them; `reconcile resolve --dsn` prints the envelope with contradictions visible; ER-over-PG (LD-F04) is deferred to P19.5 per the size guard (P19.4) |
| ADR-060 | **The resolver (P08.1) — retro-fitted record** — the §28 deterministic `RESOLVE` core, documented after the fact (LD-X05): no random tie-break with a total order (`claim_id` last, SIG-RECON-007), contradictions kept visible (`U5` load-bearing, §3.1), the ruleset as versioned/attributable data with `resolver_version`≠`ruleset_version` (SIG-RECON-021/SIG-STORE-017), and template-generated quotable rationales with an LLM never resolving (§25.2). A record of what P08.1 shipped, changing no code; companion to the new §53 Phase-8 resolver risks RISK-P8-00a/00b (P19.5 for P08.1) |
| ADR-061 | **Capstone gap closure: the derivative export gate, jurisdiction-conditional web render, and ER over PostgreSQL** — `policy.licensing` fails the export gate closed on `derivative_permitted=false` (reason string `derivative_permitted=false`) with a non-raising `partition_exportable` companion (SIG-INGEST-048b, §0.7 reduce-only); a TypeScript `publication.ts` mirror runs `adapter_publication_permitted` in the web build-time data path so FR-GDPR/BE-GDPR withhold a public-employee name (localised, zero-JS preserved, SIG-PUB-017); ER over PG reuses ADR-059's names — `sig-resolution match/review decide --dsn`, the additive `review_queue` sqitch (`review_item` + append-only `review_decision`), `PgReviewQueue` with JSONL still the default — flipping the composed `LD-F04` xfail (P19.5) |
| ADR-062 | **Spec reconciliation after the 46-ticket build** — the seven ticked HG-13 amendments (A1–A7) applied at `spec_src`, three fold-back ids (SIG-UI-047, SIG-EVID-020, SIG-ENG-039), Appendix F rebuilt to repository ADR numbering (single source of truth `docs/adr/README.md`, SIG-ENG-039), Appendix G.5, and `check_spec_src.py`; no package code or schema changed (P20.2) |
| ADR-063 | **Registry review metadata and the flip rule** — additive optional row fields `rights_reviewed_by` (reviewer role, never a name — Part VIII §0.7), `rights_reviewed_on`, `review_packet`; the flip rule (`ingestion_permitted=true` ⇒ resolved rights block + `rights_reviewed_by` + `rights_reviewed_on` + `last_verified`, else `sig-connectors validate` fails naming the id, `connectors.review`); `sig-connectors review-status [--source ID]`; the 6 OKC registry rows (115 total); the 27 rights-review packets + `_TEMPLATE.md` (facts separate from judgement, §3.1); `RIGHTS_REVIEW_INDEX.md`; the 19-project `STAGE0_OUTREACH_RECORD.md` + outreach letter template. `ingestion_permitted` still defaults false; nothing flipped (HG-03/HG-04 skipped ⇒ `loadable now: 0`, `flip-ready: 18`) (P21.1) |
| ADR-065 | **Live connector wiring** — the `HttpxTransport` over `httpx` (promoted to a `connectors` dependency), the `OcflCaptureStore` adapter, and the gated `sig-connectors run --mode live\|replay\|shadow` CLI that refuses a live fetch (exit 3) for any non-green source and owns the content-free fetch-record format. No source is green (HG-03 pending), so no live fetch runs; the transport/backoff/etiquette path is proven over stubs (P21.3) |
| ADR-067 | **Zenodo deposits, object store/CDN, tile generation, mirrors, and degraded mode** — `exports.zenodo.ZenodoHttpTransport` over `httpx` + `sig-exports deposit` (dry-run default; `--sandbox` a real sandbox deposit gated on `SIG_ZENODO_SANDBOX_TOKEN`, exit 4 `gate pending: HG-07` absent; DEPOSITS.md marks env, RISK-P21-08); bulk store reconciled to **Cloudflare R2** (zero-egress) with **S3/CloudFront (ADR-015) the documented alternative** + the evidence Object-Lock store; `sig-exports push` content-hash keys + immutable `Cache-Control` (tested vs a fake S3 — no live store), `sig-ops egress-report` over `ops/config.toml` budget (RISK-P21-09), `.torrent` pure-Python bencode peer mirror; `sig-exports tiles` renders real PMTiles (tippecanoe else pure-Python MVT/PMTiles v3, ODbL metadata preserved) closing LD-F07/LD-H08, consumed by the web build in `export` mode; `ops/mirrors.toml` + `docs/build/SUCCESSION.md` + `sig-ops swh-save` (untriggered); `sig-ops degraded` + monthly `keepalive.yml` (fails loud, $0 beyond the static host). **A1 ticked — no MapLibre island** (P21.5) |
| ADR-066 | **Runtime composition (`sig-ops`) and the export-backed web data layer** — `ops/docker-compose.yml` (PG18+PostGIS + sqitch deploy on start) with `sig-ops up\|status\|down\|seed --jurisdiction okc` (API + static as host processes; down leaves no containers; seed loads the OKC slice incl. the 299-vs-190 contradiction, append-only via `PgClaimSink`); `web/src/lib/data.ts` chooses `SIG_DATA_SOURCE=fixtures\|export` (fixtures the CI default, build-time only, zero-JS unchanged); `sig-exports build --jurisdiction okc` emits `web/dossiers.json` (the `/v1` dossier contract) alongside the separate ODbL/CC-BY compartments; the OKC dossier renders the contradiction from the export bytes, crossing LD-V08. `docs/build/tools/run_okc.sh` drives the whole staging path in shadow mode (no green sources — not live). RISK-P21-06/07 (P21.4) |
| ADR-068 | **The authenticated curation service and the `/curate/**` web surface** — `api/src/api/curation.py` (`create_curation_app`/`sig-api serve-curation`) mounted **only** when `SIG_CURATION_ENABLED=1` (routes structurally absent otherwise, RISK-P21-10), a second loopback-bound `api-curation` process/`docker-compose` service **never on the public API** (Part VIII §0.7), `sig-ops up\|status` manage/report it separately; bearer-token auth bound to the P16.1 pseudonymous contributor tiers with per-route tier checks (401/403); every write one append-only row with a **human actor id** via the JSONL curation log + `PgReviewQueue`/`review_decision` for ER decisions, contradiction/task dispositions **compute-on-read** (P21.2 shrunk → no new persistence); machine suggestions labelled, never auto-applied (**amends ADR-030**); `web/src/pages/curate/**` progressively-enhanced no-JS forms under WCAG 2.2 AA (not the public zero-JS budget) + `/curate/**` lighthouse block; closes LD-F05/LD-H04; RISK-P21-10/11 (P21.6) |
| ADR-069 | **Contribution-back live** (amends **ADR-055**) — the MapRoulette client (`tasks/src/tasks/maproulette.py`): `cooperativeType=tags` payloads matching API v2, `checkinComment` carrying the changeset hashtag **+** `#sig-<jurisdiction>`, **dry-run** without `SIG_MAPROULETTE_API_KEY`; **sensitive tiers never pushed** (`geo_tier>=3` → C3/C4/C5 excluded before the payload exists, RISK-P21-12); the **registration gate** `ops/config.toml [tasks.contribution] registered` read by `tasks.contribution.contribution_registered()` (**fails closed**) → `sig-tasks maproulette push` **REFUSES exit 3** while `false` (RISK-P16-14); the OSM **changeset feed** (`tasks/src/tasks/osm_feed.py`) reading id + `comment` **only** (never `user`/`uid`, Part VIII §0.7), attributing hashtag-bearing changesets to `LeverageLedger` (append-only, idempotent, re-run adds 0), **fixtures-replay default** (no live poll, HG-08); the §7 metric page (`web/src/pages/contribution-back.astro`) via `data.ts` `getLeverageMetric()` (fixtures/export, fails loud); opt-in **aggregate-only** onboarding timing (`tasks.onboarding.OnboardingTimingAggregate`, count+median, **no per-user rows**) on the L0 form + `GET /v1/curation/onboarding-timing`; moderated study protocol landed but **not yet run** (HG-10). Closes LD-F11 (gate-pending HG-08), LD-P03/LD-F10-usability (gate-pending HG-10) (P21.7) |
| ADR-070 | **The Data Driven release model and the coarse-international capture path** — the first-class `data_driven` connector (`connectors/src/connectors/data_driven.py`) for EFF/MuckRock's Data Driven releases (§23.9): the **release is the unit of ingestion**, pinned by a content digest per version + retrieval date (`ReleaseManifest`, SIG-INGEST-043b), so a **versioned re-ingest = new dated claims, never overwrite** (SIG-INGEST-043d/017); **per-agency AGGREGATE rows ONLY** enforced at the ingest boundary (`assert_aggregate_only` refuses any per-search/per-person/per-edge column, `PerSearchColumnError`, RISK-P0-08/RISK-P21-15) plus a predicate allowlist; **sharing DEGREE only, never the edge list** (SIG-INGEST-043c); **per-column retention windows preserved in incommensurable units** (SIG-INGEST-043d); **agency crosswalk through the P03.2 cascade** tier-labelled (§14.6); **records-request linkage** to `connectors.records.RecordsRequest` (SIG-INGEST-044); every claim historical (`observed_at`, §28.3). Registered in `list-connectors`; two release fixtures (v1,v2) prove versioning; live REFUSES exit 3 (HG-03 pending). The **coarse-international REFERENCE-capture path** (`connectors.coarse_international.CoarseInternationalConnector`) routes every row through `coarse_claim` (anti-disaggregation guard, SIG-INGEST-042), keeps LINK posture, runs replay/shadow over fixtures, live refused until a packet+flip. `eff_data_driven` → MIRROR candidate, rights UNDETERMINED; packets produced for it + the coarse trio; RISK-P21-14/15; closes BL-042 (gate-pending HG-03) (P21.8) |
| ADR-071 | **The `pathways` connector — one connector + three extractors, the ADR-033-deferred parser layers, and the procured≠deployed epistemic guard** — one `pathways` connector (`connectors/src/connectors/pathways.py`) with three per-family extractors (`rtcc_federation`/`fr_css_forensics`/`acoustic_drone_location`), chosen over three connectors because all three reuse the parser layers + claim-shaping + epistemic guard (realises the ADR-033-deferred concrete engines, LD-F17). Two engines land in `parsing/`: `parsing.tables` (**layer-4 `pdf_table`** — procurement table → cell-located claims) and `parsing.clauses` (**layer-3 `pdf_text`** — policy document → byte-range clause locators); `parsing.genre` adds **document-genre classification** (`sig-parsing classify` reports it). The **procured≠deployed** rule (RISK-P21-16, §46): a `deployment` claim requires a **deployment-genre** document — a genre-level guard (`DeploymentInferenceError`) + a predicate-level guard, with the genre **re-derived from the document text** (never a fixture label). Every claim typed/evidenced/dated; predicate allowlist (SIG-INGEST-033). Coverage is a test (`tests/connectors/test_pathway_coverage.py`: every P17 conformance pathway ≥1 connector claim → `docs/build/STAGE5_CONNECTORS.md`), the mechanical retirement of RISK-P17-03. Three LINK-posture sources, rights UNDETERMINED, `ingestion_permitted=false`; live REFUSES exit 3; replay/shadow 0 diffs; packets for each; closes BL-043 (gate-pending HG-03) (P21.9) |
| ADR-072 | **Documentation freshness gates** — two vendored detectors under `scripts/docs/` (P22.1's `check-repo-docs-freshness.sh` for human docs + `check-agent-docs-freshness.sh` for the `AGENTS.md` hierarchy), a `make docs-check` target running both, and a PR-triggered `docs` job in `.github/workflows/ci.yml` — all structural-only and LLM-free, so doc drift (broken references, coverage gaps, key-file/line drift) fails a PR check instead of rotting silently. Kept **out of `make check`** on the measured ~30s cost (over the ~10s inner-loop budget); LLM-driven generation/`--deep` verification stay human-triggered. Refresh cadence per `INTEGRATION_PLAN.md` §(d) = every release; revisit if the detector false-positive rate exceeds 1 per PR (RISK-P22-03/04) (P22.2) |

ADR-001…012 are the §15.5 decision set; ADR-013…020 are the stack ADRs. The
egress question of §38.5 is resolved for the whole project in ADR-015. ADR-021 is
the P00.4 source-registry decision; ADR-022 is the P02.1 claim-spine partitioning
decision; ADR-023 is the P02.2 evidence-store package + blob-dedup decision;
ADR-024 and ADR-025 are the P02.3 temporal-semantics decisions (the EDTF envelope
function, and the invariant/as-of enforcement surfaces). ADR-026 is the P04.1
connector-framework decision (the eight-stage contract, fetch-only egress, and
socket-level network-isolated replay). ADR-027 is the P04.2 `osm`-connector
decision (the versioned tag vocabulary, `(type,id,version)` keying,
history-derived `first_observed`, snapshot-diff deletion, and ODbL landing).
ADR-028 is the P04.3 `atlas`-connector decision (family-level `deployment_exists`,
agency-id keying with a surrogate fallback, and category-retirement-as-vocabulary
event). ADR-029 is the P05.1 probabilistic-ER decision (Splink 4 on DuckDB as a
fully-specified, deterministic, versioned-as-data model for cascade tiers 4–5, with
sized blocking, the gold set + frozen holdout, and the §14.7 quality gates).
ADR-030 is the P05.2 review-queue + LLM-extraction decision (the boundary as a
library plus CLI). ADR-031 and ADR-032 are the P06.1 vertical-slice decisions: a
minimal count-reconciliation seed plus the three missing §29.1 count predicates
(ahead of the Phase-8 engine), and a minimal §39.2 dossier renderer with a
print-CSS PDF path (ahead of the P15.2 production surface). ADR-033 is the P07.1
layered-parsing-stack decision (the §24 parser interface every connector extracts
through: the seven-layer cheapest-sufficient enum, byte/zip-manifest classification
with per-member archive handling, the six-kind locator schema, the `raw_value`
contract, the versioned reversible reason-code mapping, and the fixtures + canary
parser-drift defences — no new heavy dependency and no DDL). ADR-034 is the P07.2
`records`-connector decision (MuckRock/NextRequest/DocumentCloud as a targeted-lookup
client that cannot be configured into a crawler; MuckRock api_v2 with a five-minute
JWT that refreshes early and on a 401 and rides an additive per-request `headers`
seam on the shared fetcher; the `no_responsive_records` → `NO_EVIDENCE_FOUND`
coverage bridge reusing `db.absence`; the predicate allowlist and candidate-only
party keying; released documents captured as `EvidenceArtifact` rows and classified
through the P07.1 parser, with the extraction engines still deferred). ADR-035 is the
P07.3 `procurement`-connector decision (cooperative-piggyback contracts that cannot be
recorded without their ridden master award, SIG-ONTO-032; USAspending **sub-awards**
pulled — not only prime awards — and traced to a local deployment via `federal_award_id`,
SIG-ONTO-033; the `FundingInstrument` runtime shape making funder ≠ recipient ≠ purchaser
enforced; the published `data/agenda_tenants.toml` municipality→platform tenant registry
the connector reads its targets from, with discovery negatives retained as `db.absence`
coverage records ahead of P09.1, SIG-METRIC-002a; and `artifact_type` promoted to a
controlled `ArtifactType` ontology enum carrying the SIG-INGEST-047 additions).
