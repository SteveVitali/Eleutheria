# P21.7 CONTRIB.1 (GL-CONTRIB-01) — Lane-B re-run of contribution-back live (prepare-only)

## Summary
Lane-B **re-run** of the already-landed contract
`docs/tickets/P21.7__contribution-back-live.md` (chain row 77, GL-CONTRIB-01) — **not a new
contract**. Re-verifies the contribution-back edge on the current chain tip
(`devin/p21-5-infra-rerun`). **No product code changed** — every stub/fixture/protocol path was
already green on the tip; this run re-proves them for real and advances build memory.

Run line (verbatim):
`implement-spec spec=docs/tickets/P21.7__contribution-back-live.md live_verification=true`.

**Gate state** (GATE DECISIONS 2026-09-09 + P23.4/ACCT.1): **HG-08** (MapRoulette API key +
registered OSM Organised-Editing page) `provided: no`; **HG-10** (≥5 ontology-naïve participants)
not scheduled. Both stay gate-pending → RETURN PASS. No creds, no participants, no network egress,
**no automated OSM edit ever**.

## What changed
- `docs/tickets/DEFERRALS.md` — `D-P21.7-1` (HG-08) and `D-P21.7-2` (HG-10) proxy-now advanced to the
  re-verified stub/fixture paths; **both stay OPEN**.
- `docs/build/runs/P21.7.md` — appended a dated `## 2026-09-10 re-run (CONTRIB.1, prepare-only)`
  section (append-only; original run ledger untouched).
- `docs/build/BUILD_INDEX.md` — row 77.
- `docs/build/LEDGER.md` — CURRENT STATE advanced (`lastCompleted: P21.7`, `nextTicket: P21.8`,
  `chainTip: devin/p21-7-contrib-rerun`); PHASE LOG "done" entry appended; P21.7 kept in RETURN PASS.

## Paths that ran FOR REAL (no network)
- **Dry-run MapRoulette payload + registration gate:** `sig-tasks maproulette push --challenge 1
  --jurisdiction okc --dry-run` → **REFUSED exit 3** with the OE-registration reason
  (`ops/config.toml [tasks.contribution] registered=false`, kept that way; SIG-CONTRIB-016d,
  RISK-P16-14). Dry-run challenge JSON build, MapRoulette API v2 cooperative payload shape,
  `#sig-<jurisdiction>` hashtag, dry-run-when-registered-but-keyless, and sensitive-tier (C3/C4/C5)
  **exclusion** re-proven.
- **OSM changeset feed → LeverageLedger over committed fixtures:** `sig-tasks osm-feed pull` over
  `osm_changesets_okc_page1/2.xml` → 4 hashtag-bearing changesets attributed to the §7
  `LeverageLedger`, 3 accepted, `web/leverage.json` export-mode metric written, **idempotent**
  (0 new on re-run), **no OSM display names stored** (Part VIII §0.7).
- **Usability protocol + opt-in aggregate-only instrumentation:** protocol + `USABILITY_STUDY.md`
  carry the explicit **"not yet run — gate pending HG-10"** line; opt-in aggregate-only L0 timing
  (count + median, no per-user rows, no personal data) re-proven.

## Gate-pending (deferred, not fabricated)
- Real MapRoulette challenge push + real OSM `pull` → **HG-08** (`D-P21.7-1` OPEN).
- Usability study results (n, median ≤10 min for ≥5 participants) → **HG-10** (`D-P21.7-2` OPEN).

## Verification
- `SIG_REQUIRE_DB_TESTS=1 make check` (Docker up) → **2767 passed, 2 skipped, 0 failed, 0 xfailed**;
  ruff/format/mypy clean; `verify-gen` byte-clean.
- `make docs-check` → **exit 0** (1 pre-existing BM-ADR-04 warning).
- `bash scripts/docs/check-build-memory.sh .` → **exit 0**.
- **No token literal** — `SIG_MAPROULETTE_API_KEY`/`SIG_OSM_OE_PAGE` never assigned; `registered =
  false` retained.

## Acceptance-criteria → evidence
| AC (P21.7) | verdict | evidence |
|---|---|---|
| `pytest tests/tasks -k "maproulette or osm_feed"` green; v2 payload shapes; sensitive-tier excluded | met | `tests/tasks/test_tasks_maproulette.py` 11 passed, `test_tasks_osm_feed.py` 9 passed; `test_sensitive_tier_task_is_excluded_from_push_payload` |
| `maproulette push --dry-run` prints challenge JSON; exits 3 w/o `registered=true` | met | CLI → REFUSED exit 3; `test_push_dry_run_when_registered_but_keyless`, `test_push_refused_while_unregistered` |
| `osm-feed pull` adds N rows, 0 on re-run; no OSM display names | met | CLI → 4 attributed, idempotent; `test_no_osm_user_names_are_stored_anywhere` |
| real challenge + real pull (conditional on key + registration) | gate-pending | HG-08 `provided: no` → `D-P21.7-1` OPEN; **not fabricated** |
| `USABILITY_STUDY.md` protocol + aggregate results or gate-pending line; no participant-identifying data | met | "not yet run — gate pending HG-10"; aggregate-only timing tests pass |
| study median ≤10 min for ≥5 (conditional on HG-10) | gate-pending | HG-10 not scheduled → `D-P21.7-2` OPEN; **not fabricated** |
| Phase-gate: `make check` green | met | 2767 passed / 2 skipped / 0 failed |

## Requirement ids
SIG-CONTRIB-* (contribution-back, routed P21.7), SIG-UI-001, the P16.1 usability ids (AC7),
SIG-INGEST-037 (conduct toward OSM APIs).

## Spec
`docs/tickets/P21.7__contribution-back-live.md`. Stacked on `devin/p21-5-infra-rerun`. No merge/tag/
push to `main`.
