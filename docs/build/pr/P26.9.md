# P26.9 — camera-registry expansion: municipal, transit, non-US (SOURCES.8)

The `dot_511` registry-only pattern extends beyond state DOT: **23 municipal /
transit / non-US camera registries** enumerated as targets, **9 flipped under
GL-GATE-06 and run into the hosted spine** — Socrata city portals, ArcGIS city
layers, and GB/CA/AU/NZ national layers. Registries only; feed content is never
fetched.

## What landed

- **`camera_registry_targets.toml`** (new) — 23 verified targets across three
  tiers + recorded non-target outcomes (`keyed_api`, `unresolved_rights`,
  `access_restricted`, `not_found`, `unreachable`).
- **`dot_511` connector extended** — merged target registry; new `socrata_rows`
  platform (paged SoQL `$select=*,:id` for deterministic pages; a full last
  page is `ContentDrift`, never silent truncation); per-target
  `jurisdiction_scheme` (`us.state_abbr` / `iso.3166_2` / `iso.3166_1_alpha2`)
  so non-US rows never carry a fabricated US state; vocab `2026.09.17` →
  `2026.09.18` with the observed Socrata/ArcGIS aliases. Media fields
  (`screenshot_address`, `image_view`, `camera_url`, `iframe`, …) drop to
  `excluded_fields` — names only.
- **Rights** — 9 flips with packets (`docs/build/reports/rights/camreg_*.md`):
  Austin/NOLA/Baton Rouge (CC0), Sioux Falls + ACT (CC-BY-4.0), Baltimore
  (CC-BY-3.0), Winnipeg (OGL-Canada-2.0), Ottawa (LicenseRef-Ottawa-ODL-2.0),
  Sheffield (OGL-3.0) — four new licence rows, each in its own compartment.
  14 sources stay `UNDETERMINED`/gated with verbatim blockers
  (`D-SOURCES.8-1`), plus keyed QLD/NSW APIs (`D-SOURCES.8-2`).
- **Wiring** — `sources.toml` 157→180 registered, 52→61 loadable;
  `live_targets.toml` +23; `runner.py` maps all `camreg_*` to `dot_511`;
  `api_allowlist.toml` +10 host prefixes (no wildcard); `ops/cadence.toml` +9
  monthly rows.
- **Hosted** — image rebuilt linux/amd64 (`sha256:bdb70ed7…`, connector
  verified in-image); 9 `sig-ingest-camreg-*` jobs + 9 `sig-sched-*` triggers
  deployed (P26.8-identical per-job upserts — host ADC still corrupt, user
  account authed); **all 9 executed into the hosted spine**.

## Verification

- `make check` — **3,290 passed / 3 skipped** (lint/format/mypy 231 files/verify-gen clean)
- `sig-connectors validate` — green (180 registered / 61 loadable)
- `tests/connectors/test_dot_511.py` — 48 passed incl. 15 P26.9 tests (Socrata
  paging + drift, media exclusion, committed Austin/NOLA fixtures, ISO
  jurisdiction on the claim, per-source live gate, replay/shadow determinism)
- **Local live:** 9 sources → 38,873 records / 11 captures, zero refusals
  (`live_runs/2026-09-18_camreg_*_p269.json`)
- **Hosted spine: 183,752 → 218,402** (+34,650 camreg claim rows; 4,223
  `traffic_camera` entity records — exact per-source match to local counts)
- **Idempotency:** NOLA re-run → identical capture digest, **+0 new claims**

## Records

`docs/tickets/DEFERRALS.md` gains `D-SOURCES.8-1` (14 gated sources, HG-03) and
`D-SOURCES.8-2` (keyed QLD/NSW APIs, HG-09) — named operator blockers.
Run ledger: `docs/build/runs/P26.9.md`.

Generated with [Devin](https://devin.ai)
