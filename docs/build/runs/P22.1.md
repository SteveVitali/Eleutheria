# implement-spec ledger — P22.1 repo-docs-refresh

- spec: docs/tickets/P22.1__repo-docs-refresh.md
- base branch: devin/p21-9-stage5-pathway-connectors
- base commit: 8d8deb4
- branch: devin/p22-1-repo-docs-refresh
- audit mode: full; apply=true; live_verification=false

## Deliverable 0 — VERIFIED (skip re-commit)
- P22.1/P22.2 contracts + manifest rows 64-65 committed in 28e4680 (PR #57). Tracked. OK.

## Phase 0 — detector run
- `bash <skill>/scripts/check-repo-docs-freshness.sh .` → EXIT 1
- docsScanned=613, criticalCount=147, staleSuspect=0
- BUT 143 of 147 criticals are inside `.venv/` (third-party linkml docs) — NOT repo docs.
- Only ONE repo doc flagged: docs/research/R1_osm_physical_layer_and_odbl.md (4 refs) —
  false positives: Overpass query fragments/bbox coords in code blocks, e.g. `33.74,-84.42,...`,
  `tile`, `area.a`. Report-only (frozen research). → excluded-scope finding, owner: accepted (false positive).
- In-scope critical count from detector: 0.

## Findings ledger (Phase 1 — full audit of in-scope set)

### README.md (reference/how-to) — GAPS
- gap 3(a): no per-package one-line description for the 14 members (names listed only).
- gap 3(a): no "three ways to run SIG" (make check; sig-ops up + run_okc.sh; sig-ops degraded).
  sig-ops/run_okc/degraded NOT mentioned at all (grep NONE).
- OK: 14 members all named; build memory location; release cutting (INTEGRATION_PLAN §(d));
  licence posture sentence; "not a running service / no source fetched live" (matches OPERATIONAL_READINESS).
- verified: spec_src BUILD.sh path exists; make targets exist.

### CONTRIBUTING.md (how-to) — OK
- commands verified against Makefile; stacked-PR workflow accurate; spec-amendment rule accurate;
  licence headers accurate. No edits needed beyond command-execution evidence.

### CHANGELOG.md (reference) — GAP (backfill)
- Covers Phases 0-20 + a single P21.4 blockquote. Chain now runs P21.1-P21.9.
- Known-limitations lists items since resolved (P21.5 tiles, P21.6 curation, P21.2 persistence path, P21.4 LD-V08).
- 0.1.0 stays unreleased. Backfill P21 operationalization block, evidence-backed from git log / PR#56-65 / ADR-063,065-071.

### docs/README.md — MISSING (create; deliverable 3b)
- classify every top-level docs/ entry: generated/frozen/append-only/historical/living.

### docs/governance/README.md (reference index) — GAP (deliverable 3e)
- table has 7 policy rows, missing stage0-outreach-letter.md; no status column.
- 8 governance docs on disk. Add status: adopted / protocol-adopted-not-run / published / template.

### db/README.md, evidence/README.md, ops/README.md, web/README.md — OK (verify commands)
- all detailed and current; verified against code. Commands to be executed for evidence.

### docs/adr/README.md (index only) — CURRENT
- index complete through ADR-071 (no ADR-064 file; index consistent). No edit needed.

### docs/build/README.md (index only) — CURRENT (P21 reports listed generically). No edit.

### docs/research/README.md (index only) — report-only body; index fine. No edit.

### docs/studies/osm-sous-surveillance-import.md — no broken md refs. OK.
### docs/slice/*.md — no broken md refs. Historical; no fix. OK.

## Code suspects (→ BACKLOG.csv, type=docs-drift)
- pyproject.toml `description` still says "skeleton" for db, parsing, reconcile, orchestration, policy —
  packages are fully built. (code metadata, not a scope doc) → BACKLOG rows.

## Excluded-scope findings (report-only set)
- docs/research/R1_...md: 4 detector "broken refs" are Overpass code fragments; false positive; owner: accepted.
- risk_register.md: P21.7 risks (RISK-P21-12/13) filed under the P21.6 header, no "(P21.7)" header —
  append-only register; owner: next register-maintaining ticket / accepted. Report-only.
