# P21.5 — infra, deposit, tiles — run ledger
- spec: docs/tickets/P21.5__infra-deposit-and-tiles.md
- base commit: 6a0c188 (devin/p21-4-...)
- branch: devin/p21-5-infra-deposit-and-tiles
- GATE: HG-07 accounts NO -> dry-run/sandbox-stub. HG-12 zero-cost. A1 TICKED -> no MapLibre island (deliverable 4 SKIPPED).

## Deliverables
1. Zenodo deposit: ZenodoHttpTransport(httpx) + sig-exports deposit --sandbox (dry-run default) + FakeZenodo real shapes + DEPOSITS.md + CITATION.cff DOI
2. push --store (boto3 stub) + egress-report + CDN template + .torrent (pure bencode)
3. tiles CLI -> valid PMTiles >=1 layer ODbL metadata; web export mode consumes
4. MapLibre island -> SKIPPED (A1 ticked)
5. SUCCESSION.md + mirrors.toml + swh-save (untriggered)
6. degraded mode + keepalive.yml + fails-loud test
7. ADR-067 + RISK-P21-08/09 + INFRA_RUNBOOK + BACKLOG + Appendix F + coverage

## 2026-09-10 re-run (INFRA.1, prepare-only)

- spec: docs/tickets/P21.5__infra-deposit-and-tiles.md (Lane-B re-run of the already-landed contract; manifest row 76, semantic INFRA.1 / GL-INFRA-01)
- base commit: 3259ca8 (devin/p23-6-go-public-marker) — current chain tip
- branch: devin/p21-5-infra-rerun
- Run line (verbatim): `implement-spec spec=docs/tickets/P21.5__infra-deposit-and-tiles.md live_verification=true`
- GATE: HG-07 credentials **provided: no** (P23.4 / ACCT.1, GATE DECISIONS 2026-09-10) → real Zenodo (sandbox) deposit + object-store push + SWH save CANNOT run → **PREPARE-ONLY**. HG-12 = zero-cost (SIG-STORE-003). A1 TICKED → no MapLibre island (deliverable 4 stays SKIPPED). **No credentialed green fabricated.**
- Environment clock read 2026-09-13; chain date for this re-run = 2026-09-10.

### Why prepare-only (honest constraint)
The credentialed integrations (real `sandbox.zenodo.org` deposit → `10.5072/…` concept DOI; S3-compatible object-store push; `egress-report` against a live usage API; Software Heritage save) all need HG-07 secrets that are `provided: no` and this isolated context has no network egress. So they stay exactly as the original run — dry-run / FakeZenodo / boto3-stub / untriggered. This re-run re-verifies the **free/dry-run paths for real over the current export**, re-confirms the deterministic ACs, and records the credentialed steps as still gate-pending. It changes **NO product code**.

### What this re-run did (verification, not modification)
1. **Built a real export** over the OKC slice: `sig-exports build --jurisdiction okc --out …` → 13 artifacts, `licenses: ["CC-BY-4.0","ODbL-1.0"]` (ODbL compartment separate), release `sig-2026-08-20-95c7a19a`; the build itself renders PMTiles via the pure-Python renderer.
2. **Tiles (LD-F07/H08) — REAL.** `sig-exports tiles --in <export>/osm_physical/devices.geojson --out okc.pmtiles --layer devices --license ODbL-1.0` → `renderer: pure-python` (tippecanoe not on PATH). Opened with the existing reader (struct+gzip): valid PMTiles v3, `vector_layers` len ≥ 1, `sig:license == ODbL-1.0`, attribution `© OpenStreetMap contributors (ODbL)`. Same for the export-embedded `web/tiles/sig-infrastructure.pmtiles`.
3. **`.torrent` mirror — REAL.** `sig-exports torrent --in <export>/sig_graph/claims.jsonl` → pure-Python bencode, `infohash=8d1d2b600258052c0de7ad1429310d2696700c66`; bdecode sanity check (`piece length` + `pieces` present) passes.
4. **Dry-run deposit — REAL; sandbox gate-pending.** `sig-exports deposit --dry-run` → exit 0, records a `dry-run`-marked DOI in the deposit ledger (offline `FakeZenodoTransport`, no network). `sig-exports deposit --sandbox` **without** a token → **exit 4** `gate pending: HG-07 … Wrote nothing.` (no fabricated real DOI).
5. **Object-store push — stub-tested only** (`tests/exports/test_push.py` 6 passed, fake S3 client); no real push (HG-07).
6. **Egress budget — gate-pending + computes.** `sig-ops egress-report` (no usage) → `gate-pending` level, `budget_gb 100.0`, `provider cloudflare-r2`, `usage_gb null` (no live usage API — HG-07). With a simulated `--usage-gb 5` → `level ok`, `ratio 0.05` (the alarm logic works).
7. **Software Heritage save — request built for real, untriggered.** `sig-ops swh-save --repo-url …` (no `--now`) → prints the exact `POST …/api/1/origin/save/git/url/…` request, `authenticated:false`; **not submitted** (no `--now`, no network) — matches the original untriggered posture. Real save stays gate-pending HG-07.
8. **Degraded-but-alive keepalive — REAL.** `sig-ops degraded --data-source fixtures` → built `web/dist` with **no API dependency**, "cost = $0 beyond the static host". `keepalive.yml` is valid YAML (job `degraded-rebuild`, `on: [schedule, workflow_dispatch]`) and runs exactly `uv run sig-ops degraded --data-source fixtures` — the same command.
9. **A1 stays ticked** — no MapLibre island built (deliverable 4 SKIPPED); `/map/` stays zero-JS static.
10. **No incremental product code needed** — all free paths are green on the current tip.

### Verify (full green over the free/dry-run path)
- P21.5 deterministic suites: `uv run pytest tests/exports/test_tiles.py tests/exports/test_torrent.py tests/exports/test_zenodo.py tests/exports/test_push.py tests/ops/test_degraded.py tests/ops/test_egress.py tests/ops/test_swh.py -q` → **43 passed**.
- `SIG_REQUIRE_DB_TESTS=1 make check` (Docker 29.x up) → **2767 passed, 2 skipped, 0 failed, 0 xfailed** (2 skips = env-gated live-api + unarmed GCP leak check, benign); ruff/format/mypy clean; verify-gen byte-clean.
- `make docs-check` exit 0; `bash scripts/docs/check-build-memory.sh .` exit 0 (1 pre-existing BM-ADR-04 warning, not a violation).
- **No-token-literal:** no assignment of `SIG_ZENODO_SANDBOX_TOKEN` / `SIG_OBJECT_STORE_KEY|SECRET` / `SIG_SWH_TOKEN` to a literal anywhere; `.gitignore` has `.env` / `.env.*` / `*.env`.

### Deferrals
- `D-P21.5-1` (HG-07 — real Zenodo/object-store/egress/SWH) stays **OPEN**; proxy-now advanced to the re-verified free paths on the current tip (tiles/.torrent/degraded/keepalive real; dry-run deposit + boto3-push-stub + egress + SWH-request tested; sandbox-no-token exits 4). NOT marked DONE — no real deposit/push/save happened.
