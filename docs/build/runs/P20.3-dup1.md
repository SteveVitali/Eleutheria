# implement-spec ledger — P20.3 integration & release (no merging)

- spec: docs/tickets/P20.3__integration-release.md
- base_branch: devin/p20-2-spec-reconciliation (chain tip)
- branch: devin/p20-3-integration-release (already created; HEAD == p20-2 tip b43a524, empty diff)
- base commit at branch: b43a524132c3acb57443b5dbf184277e1a3e128e

## Baseline facts (Phase 1.2)
- `make check` at base: **2418 passed, 1 xfailed** (green). Matches operator baseline.
- git tag -l: EMPTY. Must stay empty.
- worktree list: only main tree. Must stay clean after merge_dryrun.
- main=2f4e9e57c49067e297f97f070e24da31f15bb749  origin/main=5435e5db0bb889a1a9d95128f44a64540cdc380e (record; must be unchanged after dry-run)
- Docker: UP (for tests/db + tests/e2e verification).

## ANCHOR DRIFT (record for report)
- Ticket assumes open PRs #20–#53. Reality 2026: operator already merged #20–#26 into main
  (origin/main tip = "Merge pull request #26"). Open PRs are **#27–#53**; my PR will be **#54**.
- merged PRs with number>=20 currently = **7** (#20–#26), pre-existing baseline. AC literal "= 0"
  cannot hold given operator already merged; the invariant I enforce: I merge NOTHING (count stays 7,
  tags stay empty). Documented as deviation.

## Version bump targets
- 14 members carry `version`: ontology already 0.1.0; other 13 = 0.0.0 -> bump to 0.1.0.
  (api, connectors, db, evidence, exports, inference, ops, orchestration, parsing, policy,
   reconcile, resolution, tasks) + ontology already done.
- root pyproject.toml: virtual workspace root, NO version field (leave).
- web/package.json + web/package-lock.json: version 0.0.0 -> 0.1.0 (keep lock in sync for npm ci).

## Carried follow-ups from P20.2
1. root AGENTS.md: add SIG-ENG-039 line to "Amending the specification" section.
2. ticket Load/AC citation touch-ups (cite not copy) for tickets amended by P20.2:
   P00.1(A7 evidence/), P01.1(SIG-ENG-039), P02.1(A4), P02.2(SIG-EVID-020+A7), P05.2(A6),
   P08.3(A5), P09.1(A5), P10.1(A5), P10.3(A2), P15.3(SIG-UI-047+A1), P16.1(A5/A6), P18.1(A3).
   BL-051 already tracks residual spec-vs-code drift.

## Deliverables checklist
1. merge_dryrun.sh (read-only) + table
2. INTEGRATION_PLAN.md (a)-(f)
3. CI_STATUS.md (needs branch CI run)
4. version bump + RELEASE_NOTES_v0.1.0.md + CITATION.cff
5. README/CHANGELOG.md/CONTRIBUTING.md (refresh-repo-docs)
6. manifest phase-gates line
7. traceability + risk register RISK-P20-03/04/05
