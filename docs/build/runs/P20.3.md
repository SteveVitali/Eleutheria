# implement-spec ledger — P20.3 Integration plan & release readiness (no merging)

- **Spec:** docs/tickets/P20.3__integration-release.md
- **Base branch:** devin/p20-2-spec-reconciliation @ b43a524132c3acb57443b5dbf184277e1a3e128e
- **Feature branch:** devin/p20-3-integration-release
- **Baseline make check (before changes):** 2418 passed, 1 xfailed (verify-gen clean). MUST stay identical.
- **CRITICAL CONSTRAINT:** merges NOTHING, tags NOTHING, no push to main, no branch-protection change, no branch delete, no force-push.

## Key baseline facts (verified 2026-09-XX this run)
- Open PRs: #27–#53 (27 open). #27 base=main; #28..#53 stack on prev head. (NOTE: manifest/spec text says #20..#54 & stacks on p18-2; reality: #20–#26 ALREADY MERGED to main by operator on 2026-09-09.)
- **FINDING:** `gh pr list --state merged --jq 'map(select(.number>=20))|length'` = **7** at run START (#20–#26 merged 2026-09-09, before this ticket). The AC literal "=0" cannot hold given pre-existing operator merges; the invariant this ticket enforces = adds ZERO new merges/tags. Record baseline 7, prove unchanged (still 7) at end; 0 tags at start and end.
- origin/main = 5435e5d (has #26 = p10-3). main (local) = 2f4e9e5 (has #19). git tag -l empty.
- 14 Python workspace members + root pyproject = 15 pyproject.toml files with version fields (to verify). web/ has package.json.
- CI: one workflow ci.yml (python, web jobs). tests/db in CI via SIG_REQUIRE_DB_TESTS=1.

## Requirements checklist (deliverables 1–7)
1. merge_dryrun.sh (read-only, idempotent, exits nonzero on conflict) — run live
2. INTEGRATION_PLAN.md (a) PR graph (b) dry-run table (c) 3 strategies (d) operator procedure incl tag+release (e) rollback (f) post-merge verify
3. CI_STATUS.md — this branch CI run, job×step, test counts incl tests/db + tests/e2e, not-covered, zero-cost posture, branch-protection rec
4. version bump all members → 0.1.0; RELEASE_NOTES_v0.1.0.md; CITATION.cff if absent; make check green; LD-X02 clean
5. refresh-repo-docs: README (14 members + web/ docs/build/ docs/tickets/, test cmds, release cutting); CHANGELOG.md; CONTRIBUTING.md; verify every command
6. Manifest "Phase gates" integration line
7. Traceability + risk register RISK-P20-03/04/05 + manifest update

## Carried-over P20.2 follow-ups (parent instruction)
- A) AGENTS.md: add SIG-ENG-039 one-liner ("new ADR → Appendix F spec_src/99a_appF_adr.md same PR"). AGENTS.md currently lacks it. (agent-doc; done as ticket work, noted.)
- B) Load/AC citation touch-ups for ~8 HG-13-amended tickets (cite SIG-UI-047, SIG-EVID-020, SIG-ENG-039, A1–A7 §§) — align to TICKET_VS_SPEC.md. If out of scope → BL-row in BACKLOG.csv.
  Amended tickets: P01.1(ENG-039), P02.1(A4), P02.2(EVID-020,A7), P05.2(A6), P08.3(A5), P09.1(A5), P10.1(A5), P10.3(A2), P15.3(A1,UI-047), P16.1(A5/A6), P18.1(A3).

## Requirement IDs to stamp: SIG-ENG-011/015/016/031, SIG-LIC-005, SIG-GOV-021.

## Gap table (Phase 4) — TBD
## Evidence log (Phase 5) — TBD
