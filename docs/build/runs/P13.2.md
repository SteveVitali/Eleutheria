# P13.2 — Policy, legal instruments, policy/config divergence — run ledger

- spec: docs/tickets/P13.2__policy-legal.md
- base commit: 388e851 (branch devin/p13-1-accountability -> new branch devin/p13-2-policy-legal)
- NOTE: pre-existing uncommitted changes on base (db/analytics.py, db/suppression.py + their tests) are P12 analytics territory, UNRELATED to P13.2. Left untouched, will NOT be staged.

## Key finding
Dependencies front-loaded most infrastructure:
- P01.1: ontology entities Policy/LegalInstrument/ConfigurationState (entities.yaml, generated artifacts).
- P08.2: reconcile.policy_config divergence finding (SIG-RECON-044) + tests + traceability + RISK-P8-08.
- P13.1: accountability connector consumes a curated index (abuse library) inline (index_only evidence links).

## Requirements / ACs (from spec)
- Deliverable 1: Policy identity-only + §11.13 predicate surface (SIG-ONTO-034). Entity EXISTS. Gap: no predicate-surface test.
- Deliverable 2: LegalInstrument [NEW] §11.14 predicate surface + edges. Entity EXISTS. Gap: no predicate-surface test.
- Deliverable 3: Policy/config divergence rendered finding (SIG-RECON-044) + Policy MUST NOT merge ConfigurationState (SIG-ONTO-034). Reconciler EXISTS+tested. Gap: no explicit "never merged" schema/resolution test (AC2).
- Deliverable 4: SIG-EPIS-030 curated index held WITHOUT normalization — GENERAL capability. GAP: only connector-inline instance. Real new work.
- AC1: divergence rendered finding test — MET (test_policy_config.py).
- AC2: Policy & ConfigurationState never merged — schema/resolution test — GAP.
- AC3: curated index held without normalization test — GAP.
- AC4: phase-gate — CI green, tests, ADR if deviation, traceability, risk-register.

## Plan
1. connectors/curated_index.py: CuratedIndexEntry + CuratedSourceIndex (held-as-index, as_claims() guard raises). SIG-EPIS-030 general form.
2. accountability connector: rely on CuratedIndexEntry in abuse-library path (output shape unchanged).
3. tests/connectors/test_curated_index.py (AC3).
4. tests/ontology/test_schema_structure.py: Policy predicate surface; LegalInstrument predicate surface; Policy≠ConfigurationState never merged (SIG-ONTO-034, AC2).
5. tests/reconcile/test_policy_config.py: distinct-types-never-merged (AC2 resolution layer).
6. traceability.md P13.2 section; risk_register.md Phase 13 P13.2 section.

## Test matrix / gap table / evidence — filled during run.
