# implement-spec ledger — P21.8 data_driven + coarse-international

- spec: docs/tickets/P21.8__data-driven-and-coarse-international.md
- base commit: 2981ce8 (devin/p21-7-contribution-back-live)
- branch: devin/p21-8-data-driven-and-coarse-international
- GATE HG-03/HG-04: SKIP — no source flipped. Fixtures-only. Live must REFUSE exit 3.
- baseline: 2632 passed, 1 skipped, 0 xfailed (per operator)

## Requirements (spec §23.9 / ticket)
- R1 registry+packets: eff_data_driven custody MIRROR candidate (rights UNDETERMINED); packets for eff_data_driven + coarse trio; RIGHTS_REVIEW_INDEX updated
- R2 data_driven connector: release versioning (digest+retrieval date, 043b); per-agency AGGREGATE rows only (043, RISK-P0-08/P21-15); agency crosswalk via P03.2 cascade (043c); versioned re-ingest = new dated claims never overwrite (043d); records-request linkage into tasks.records_request (044); 2 fixtures v1/v2; registered in list-connectors; runs replay/shadow; live refuses exit 3
- R2b: 043c sharing DEGREE only, never edge list; 043d per-column retention window preserved
- R3 coarse-international REFERENCE-capture path for trio; keep LINK; fixture tests
- R4 run what permitted: ECOSYSTEM_CONNECTORS.md
- R5 ADR-070; RISK-P21-14 (per-release parser pinned by digest); RISK-P21-15 (aggregate-only schema test rejects per-search col); COVERAGE_MATRIX INGEST-043*/044 MET

## AC (test matrix)
- list-connectors includes data_driven; validate passes
- pytest tests/connectors -k "data_driven or coarse": v1 retained + v2 added, 0 overwrites; aggregate-only rejects per-search col; crosswalk resolves >=1 agency
- shadow 0 diffs
- COVERAGE_MATRIX MET; check_coverage_matrix exits 0
- make check green; ADR-070; risk register; BACKLOG closed/gate-pending

## 2026-09-10 re-run (SOURCES.1, prepare-only; covers P21.8 + P21.9)

Lane-B SOURCES.1 (GL-SOURCES-01) re-run of manifest row 78 — a single chain row that
re-verifies BOTH already-landed contracts (P21.8 + P21.9) on the current tip. **PREPARE-ONLY**
per the operator resume prompt (rule 5): the owed D-P21.8-1 obligation is a **live fetch** gated
on per-source HG-03/HG-04 flips + HG-09 tokens + network egress — **none available in this
isolated context**, so the real fetch CANNOT run and is NOT run. No speculative source flips.

- branch: `devin/p21-8-sources-rerun`, base `devin/p21-7-contrib-rerun` (stacked; no
  merge/tag/push-main/branch-delete/force-push; no network egress).
- Verbatim Run line (P21.8): `implement-spec spec=docs/tickets/P21.8__data-driven-and-coarse-international.md live_verification=true`.
- **Product code changed: NONE.** Pure re-verify + append-only reporting.

### Re-verified on the current tip (data_driven connector + coarse-international path)
- `uv run pytest tests/connectors/test_data_driven.py tests/connectors/test_coarse_international.py`
  → all green (data_driven 23 passed, coarse 22 passed): two release snapshots v1/v2 yield
  versioned claims (v1 retained + v2 added, 0 overwrites); the aggregate-only schema test rejects
  a per-search-column fixture (RISK-P21-15, RISK-P0-08); agency crosswalk resolves ≥1 agency to an
  existing SIG identity; agency-level disaggregation refused.
- **Shadow diff = 0** re-confirmed: `test_data_driven.py::test_shadow_over_fixture_has_zero_diffs`
  and `test_coarse_international.py::test_shadow_replay_over_coarse_fixture_has_zero_diffs` both pass.
- **Gate refusal exit 3, no socket** re-confirmed: `sig-connectors run --source eff_data_driven
  --mode live` → **exit 3** (`ingestion_permitted=false` / UNDETERMINED / not-flipped reasons,
  refused before any fetch); same for `carnegie_ai_gsi` and the coarse trio. The sources stay
  rights UNDETERMINED / `ingestion_permitted=false` — **NOT flipped** (D-P21.8-1 sources:
  eff_data_driven, aspi, carnegie, facial_recognition_world_map).

### Verify (full green over the fixture/shadow path)
- `SIG_REQUIRE_DB_TESTS=1 make check` (Docker up) → **2767 passed, 2 skipped, 0 failed, 0 xfailed**;
  ruff/format/mypy clean; verify-gen byte-clean.
- `make docs-check` exit 0; `bash scripts/docs/check-build-memory.sh .` exit 0 (1 pre-existing
  BM-ADR-04 warning). **No token literal** anywhere (HG-09 `SIG_*` tokens never assigned).

### Gate posture (unchanged; no green fabricated)
- D-P21.8-1 stays **OPEN**: connector code + coarse path re-verified over fixtures (shadow diff 0),
  but the live fetch is gate-pending on HG-03/HG-04 per-source flips + HG-09 tokens + network egress.
  No flip performed; no live green fabricated. NEWS sources stay LINK-only.
