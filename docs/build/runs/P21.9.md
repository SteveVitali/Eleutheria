# implement-spec ledger — P21.9 Stage-5 pathway connectors

- spec: docs/tickets/P21.9__stage5-pathway-connectors.md
- base commit: d2ec536 (devin/p21-8-...)
- branch: devin/p21-9-stage5-pathway-connectors
- live_verification=true (fixtures/shadow only; NO live fetch)
- GATE: HG-03/HG-04 per source = SKIP (no source flipped). Ship over committed fixtures.
  New sources stay rights UNDETERMINED / ingestion_permitted=false; live REFUSES exit 3.

## Requirements (from spec)
1. ADR-071 coupling decision — one `pathways` connector vs three. DECISION: ONE `pathways`
   connector w/ 3 per-pathway extractors (shared parser layers + shared epistemic guard).
2. Parser layers (LD-F17, ADR-033-deferred) in parsing/ + unit tests; `sig-parsing classify`
   recognises new document genres.
3. Pathway connectors emit P17 claims (vendor/product, procurement, policy, deployment ONLY
   when evidence says so) w/ procured≠deployed rule; registry rows + packets per new source.
4. tests/connectors/test_pathway_coverage.py: every P17 pathway ≥1 connector claim; rendered
   into docs/build/STAGE5_CONNECTORS.md.
5. Run: fixture replay; live refuses exit 3 for unflipped.
6. ADR-071, RISK-P21-16, RISK-P21-17, traceability, COVERAGE_MATRIX P17 ids → MET,
   RISK-P17-03 → retired by P21.9 suffix.

## P17 conformance pathways (9) — coverage target
federation: private_camera_federation, rtcc_hub, broker_chain
forensics:  face_recognition, css_and_forensics, federal_authorization
acoustic:   gunshot_detection, drones, commercial_location

## Epistemic rule (§46 RISK-P21-16)
procured NEVER implies deployed. deployment claim requires deployment-genre document.
Genres: procurement_record, policy_document, deployment_report, vendor_disclosure, agenda_minutes.

## Gap table — TBD Phase 4
## Evidence log — TBD Phase 5

## 2026-09-10 re-run (SOURCES.1, prepare-only; covers P21.8 + P21.9)

Lane-B SOURCES.1 (GL-SOURCES-01) re-run of manifest row 78 — this single chain row re-verifies
BOTH already-landed contracts (P21.8 + P21.9) on the current tip. **PREPARE-ONLY** per the operator
resume prompt (rule 5): the owed D-P21.9-1 obligation is a **live fetch** of the Stage-5 pathway
sources, gated on per-source HG-03/HG-04 flips + HG-09 tokens + network egress — **none available
in this isolated context**. The real fetch CANNOT run and is NOT run. No speculative flips; the
D-P21.9-1 sources stay LINK-posture / UNDETERMINED — NOT flipped.

- branch: `devin/p21-8-sources-rerun`, base `devin/p21-7-contrib-rerun` (stacked; no
  merge/tag/push-main/branch-delete/force-push; no network egress).
- Verbatim Run line (P21.9): `implement-spec spec=docs/tickets/P21.9__stage5-pathway-connectors.md live_verification=true`.
- **Product code changed: NONE.** Pure re-verify + append-only reporting.

### Re-verified on the current tip (pathways connector + 3 extractors + parser layers)
- `uv run pytest tests/connectors/test_pathways.py tests/connectors/test_pathway_coverage.py
  tests/parsing` → all green (pathways 17 passed, coverage 4 passed, parsing suite green): every
  P17 pathway (federation/RTCC, FR/CSS/forensics, acoustic/drone/location) has ≥1 connector-produced
  claim; a `deployed` claim from a procurement-genre fixture is refused (procured ≠ deployed,
  RISK-P21-16, Part VIII binding); the ADR-033-deferred parser layers pass their unit tests.
- **Shadow diff = 0** re-confirmed for all three pathway families:
  `test_pathways.py::test_shadow_replay_over_fixture_has_zero_diffs[federation|forensics|acoustic]`
  all pass.
- **Gate refusal exit 3, no socket** re-confirmed: `sig-connectors run --source
  pathways_rtcc_federation --mode live` → **exit 3** (ingestion_permitted=false / LINK-only /
  UNDETERMINED / not_contacted reasons, refused before any fetch); same for
  `pathways_fr_css_forensics` and `pathways_acoustic_drone_location`. FR/CSS/forensics stay
  design-gated `ingestion_permitted=false`.

### Verify (full green over the fixture/shadow path)
- `SIG_REQUIRE_DB_TESTS=1 make check` (Docker up) → **2767 passed, 2 skipped, 0 failed, 0 xfailed**;
  ruff/format/mypy clean; verify-gen byte-clean.
- `make docs-check` exit 0; `bash scripts/docs/check-build-memory.sh .` exit 0 (1 pre-existing
  BM-ADR-04 warning). **No token literal** anywhere (HG-09 `SIG_*` tokens never assigned).

### Gate posture (unchanged; no green fabricated)
- D-P21.9-1 stays **OPEN**: pathways connector + 3 extractors + parser layers re-verified over
  fixtures (shadow diff 0), but the live fetch is gate-pending on HG-03/HG-04 per-source flips +
  HG-09 tokens + network egress. No flip performed; no live green fabricated.
