# implement-spec ledger — P21.2 (SHRUNK CASE)

- spec: docs/tickets/P21.2__persist-annotation-layer.md
- base branch: devin/p22-0-plan-extension @ 28e46800e9632b0ecc46ba891d9b27eaab417445
- feature branch: devin/p21-2-persist-annotation-layer
- live_verification=true (real Docker PG)

## GATE: SHRUNK confirmed
- HG-14: operator signed 76-row ACCEPTED list in CAPSTONE_CLOSURE.md §(b) incl. ADR-037/038/039/054 (compute-on-read).
- A5: ticked/applied by operator (HG-13, folded by P20.2).
- => Deliverables 1 (alignment tests) + 5 (ADR trigger notes) ONLY. 2/3/4 OUT.

## Deliverables (shrunk)
1. tests/db/test_annotation_alignment.py — introspect information_schema.columns vs dataclass fields; assert names/types/nullability agree. Fix mismatch by additive nullable sqitch col OR dataclass alias; never drop a column.
   Pairs (tables that exist):
   - contradiction ↔ reconcile.model.Contradiction
   - coverage_record ↔ inference.coverage.CoverageRecord
   - research_task ↔ reconcile.model.ResearchTask
   - inference.derived_fact ↔ reconcile.model.Inference  (value→value_json = dataclass alias)
   - NO contributor table exists in db/deploy → no contributor pair.
5. Append "evaluated at P21.2, NOT fired" note under existing ## Revisit trigger of ADR-037/038/039/054. Append-only. NO ADR-064.

## e2e xfails
- Only e2e xfail is LD-V08 → P21.4 (tests/e2e/test_composed_stack.py:771). NONE tagged P21.2/LD-V05/LD-D07. => change nothing there; state explicitly.

## Cross-cutting / build memory
- RISK-P21-03: history/annotation tables grow unbounded → partition/retention; ref ADR-022 revisit trigger + BL-003/BL-004.
- BACKLOG.csv rows landing P21.2: BL-004, BL-005, BL-006, BL-027. Close annotation compute-on-read family (BL-004, BL-005, BL-027) as closed-via-accepted; BL-006 is identity-registry (resolution layer, not annotation/compute-on-read) — judgment: leave, report.
- traceability.md: add P21.2 section.
- verify-gen must stay clean (no generation runs expected).

## Findings
- All 4 ADRs already have ## Revisit trigger — append sub-note.
- 0 additive sqitch columns needed; 1 dataclass alias (Inference.value→value_json). note/claim_values/evidence/rationale/alternatives/layer/pushable_to_osm = compute_only (deferred persistence).

## Status: DONE — verifying/PR
- alignment test: 4 pairs PASS live PG, 0 mismatches, 0 additive columns, 1 alias (value→value_json). Negative-tested (removing alias → 2 mismatches).
- 4 ADR notes appended (evaluated, not fired).
- RISK-P21-03 added; BACKLOG BL-004/005/027 closed (accepted); BL-006 left open (identity layer). traceability P21.2 section added.
- e2e: none tagged P21.2; only LD-V08→P21.4. Unchanged.
- make check GREEN: 2454 passed, 1 xfailed; verify-gen clean; check_spec_src OK.
