# Run ledger — P22.3 (build-memory v2 migration / EL.1)

**Spec:** `~/agent-skills/.agents/design/build-memory-v2-spec.md#EL.1` (relocated 2026-09-09 from `~/MetaHarness/`) (Part 0 + Part I contracts; Part II EL.1) · ticket `docs/tickets/P22.3__build-memory-v2-migration.md`
**Base:** `devin/sig-postbuild-capstone` (PR #68, commit `625d802`) — the current chain tip (the round-1 capstone landed on top of P22.2/PR#67; the spec's EL.1 header naming P22.2 was stale).
**Branch:** `devin/p22-3-build-memory-v2-migration`
**Config:** `implement-spec live_verification=false autonomous=true` · committed mode (marker in `docs/build/README.md`) · `worktree=~/Eleutheria`

## Deferrals read
No `docs/tickets/DEFERRALS.md` existed before this run (it is created by deliverable 4). The obligations owed at run time are the RETURN PASS rows in the (migrated) `docs/build/LEDGER.md`: P21.1 (HG-03/04), P21.3 (HG-03/09), P21.4 (HG-01/11/Go-public), P21.5 (HG-07), P21.7 (HG-08/10), P21.8 (HG-03/04), P21.9 (HG-03/04). This ticket does not unblock any of them (they need operator action); it **records** them as the 12 `D-P21.n-*` OPEN rows.

## Requirements (from EL.1)
BM-MIGE-01 migrate scratch · BM-MIGE-02 ledger state · BM-MIGE-03 BUILD_INDEX 47–66 · BM-MIGE-04 DEFERRALS · BM-MIGE-05 manifest + tail · BM-MIGE-06 ADR-073 · BM-MIGE-07 docs · registers (RISK-P22-05/06, traceability, vendor validator in `make docs-check`). BM-COMPAT-04/05. Project ids SIG-ENG-001/003/031/039.

## Acceptance criteria (from EL.1)
See `docs/tickets/P22.3__build-memory-v2-migration.md § Acceptance criteria`; verified in the Evidence report below.

## Plan
1. `build-memory migrate from=.agents/scratch apply=true`; handle leftovers; relocate pre-v2 `docs/build/` root reports → `reports/` (validator allowlist); byte-identity via `sha256`; rewrite `docs/build/README.md` with the marker + mapping + sha256; `.gitignore += docs/build/logs/`; remove `.agents/`.
2. Convert `LEDGER.md` to the v2 key set (round 2; legacy plan wrapped); carry PHASE LOG verbatim.
3. `BUILD_INDEX.md` §A.7 rows 47–66 (BM-INDEX-01 columns).
4. `DEFERRALS.md` from RETURN PASS (12 rows, `BL-` cross-refs).
5. Manifest banners + row 66 + Plan extensions + Round-2 tail `P23.1..P23.7`.
6. ADR-073 + Appendix F row + `BUILD.sh` + `check_spec_src.py` (72 ADRs) + `adr-index`.
7. `AGENTS.md`, `docs/README.md`, `docs/build/README.md`.
8. `docs/risk_register.md` (RISK-P22-05/06), `docs/traceability.md`, vendor `check-build-memory.sh` (+`adr-index.sh`) to `scripts/docs/` and add `docs-check-build-memory` to `make docs-check`.

## Test matrix
No product code changes → no new product tests. ADR-073 adds exactly one parametrized case to `tests/unit/test_policy_adrs.py` (the "every ADR file has a `## Revisit trigger`" gate applies to the new file) → `make check` count +1. Deterministic ACs are verified by scripts: `check-build-memory.sh` (layout/ledger/deferrals/ADR-index/secret/size), `check_spec_src.py` (72 ADRs), `sha256`/`cmp` (byte-identity), `grep -c '^| '` (BUILD_INDEX growth), `make check`, `make docs-check`.

## Progress
- Migrate applied; 189 scratch files moved byte-identical; 63 `docs/build/` report files relocated byte-identical; `.agents/` removed; runs count 62 (61 migrated + this run ledger).
- LEDGER converted (BM-LEDGER-02 order; legacy plan wrapped; round 2).
- BUILD_INDEX §A.7 added (22 rows now; P22.3 row at close → 23).
- DEFERRALS seeded (12 rows). Manifest + tail tickets written. ADR-073 + Appendix F + adr-index done (72 ADRs). Docs + registers + vendored validator done.

## Gap table
| item (spec §) | status | evidence |
|---|---|---|
| BM-MIGE-01 migrate + byte-identity | met | `docs/build/README.md` mapping + sha256; `test ! -d .agents`; `git ls-files docs/build/runs \| wc -l`=62 |
| BM-MIGE-02 ledger v2 | met | `docs/build/LEDGER.md` CURRENT STATE (19 keys in order), round 2, `## LEGACY PLAN`; PHASE LOG verbatim + P22.3 |
| BM-MIGE-03 BUILD_INDEX 47–66 | met | `docs/build/BUILD_INDEX.md §A.7`; `^\| ` grew by 23 |
| BM-MIGE-04 DEFERRALS | met | `docs/tickets/DEFERRALS.md` 12 rows, `BL-` cross-refs |
| BM-MIGE-05 manifest + tail | met | `00_MANIFEST.md` banners/row 66/Plan extensions; `P23.1..P23.7`; `nextTicket: P23.1` |
| BM-MIGE-06 ADR-073 | met | `ADR-073-*.md`; `99a_appF_adr.md`; `check_spec_src.py` 72 ADRs; `adr-index --check` clean |
| BM-MIGE-07 docs | met | `AGENTS.md`, `docs/README.md`, `docs/build/README.md` |
| registers + vendored validator | met | `risk_register.md` RISK-P22-05/06; `traceability.md`; `scripts/docs/check-build-memory.sh` in `make docs-check` |
| no code change; `make check` green | met | see Evidence report (make check counts) |
| Part VIII personal-data scan | met | scan clean (public officials/agencies/sources/gate ids only); recorded in PR body |

## Evidence log
- `bash scripts/docs/check-build-memory.sh .` → exit 0 (1 benign BM-ADR-04 warning: canonicalSpec memo uses logical ADR numbers).
- `python3 docs/build/tools/check_spec_src.py` → OK, 72 ADRs, byte-identical repro, 671 ids.
- `bash scripts/docs/adr-index.sh --check docs/adr | diff - docs/adr/README.md` → clean.
- `sha256` set comparison: 0 differences (189 scratch + 63 report files).
- personal-data scan: no emails (only `@pytest` decorators), no SSN/phone/plate, no secret tokens.
- `make check` / `make docs-check`: see Evidence report.

## Evidence report
The submitted PR body / evidence report is `docs/build/pr/P22.3.md` (acceptance-criteria → evidence table,
design decisions/reconciliations, the Part VIII personal-data scan, and the not-done list). Headline results:
`scripts/docs/check-build-memory.sh .` exit 0; `test ! -d .agents`; `git ls-files docs/build/runs`=62; 0
`*.log` under `docs/build`; byte-identity 0 differences (189+63 files); `BUILD_INDEX` +23 rows;
`DEFERRALS.md` 12 rows; `check_spec_src.py` exit 0 (72 ADRs); `make check` 2719 passed, 1 skipped, 0 failed,
0 xfailed; `make docs-check` exit 0. One deviation from "no code change": two test path constants repointed
to the moved `reports/` fixtures (behaviour-identical; no product `src` change).
