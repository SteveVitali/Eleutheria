# implement-spec ledger — P07.1 layered document-parsing stack
Base: b1b9965  Branch: devin/p07-1-parsing-stack  Package: parsing/
Baseline: parsing tests 29 pass; mypy parsing+connectors clean; ruff clean.

REQS (SIG-PARSE-001..008): layered/cheapest+method; classify-before-parse+per-member ZIP;
mandatory locator reject-if-missing; raw_value preserved incl unparseable; reason-code
versioned/inspectable/reversible mapping-as-data + mapping_version stamped + no history rewrite
(SIG-STORE-038); free_text vs constrained_dropdown distinguished; committed fixtures per parser;
nightly canary alerts-not-drops on structural drift (R11).

DB already ready (no DDL): claim.raw_value NOT NULL, normalization_id/version, claim_evidence.locator
jsonb, extraction.method/extractor_name/version. This ticket = the Python parser interface.

Modules to add: layers.py, classification.py, locator.py, claim.py, reason_codes.py + data/reason_codes.toml,
drift.py; update cli.py/__init__.py; tests+fixtures; ADR-033; traceability; risk R11.

STATUS: Phase 1 -> 2.

## Test matrix (1.4)
- layers: ordering=cost, method strings (llm_assisted==DB), cheapest_sufficient, empty->ValueError, from_method.
- classification: sniff all formats; scanned image; encrypted PDF; scanned PDF; xlsx single/multi/merged; recommended_layer per verdict; classify_archive per-member on committed mixed ZIP (AC3); to_row recorded.
- locator: 6 kinds valid+invalid; unknown field rejected; to_row tagged; LocatorRequired vs InvalidLocator.
- claim: raw_value preserved incl unparseable round-trip (AC2); ParsedClaim rejects missing locator (AC1); method recorded; reason travels.
- reason_codes: free_text vs dropdown distinguished + signal (AC:006); unmapped retains raw; reversible round-trip every code (AC4); mapping_version stamped; two-version independence == no history rewrite (AC4); ambiguity guard; vocabulary_migration const.
- drift: assert_no_drift raises on mismatch (AC5 fixtures); structural_findings + canary alert-not-drop; EACH; clean no findings.
- cli: classify (incl per-member zip), layers, reason (+reverse).
STATUS: impl done; writing tests+fixtures.

## Phase 4 gap table (re-read §24 + ticket ACs) — all MET
- SIG-PARSE-001 cheapest+method: layers.py ExtractionLayer/cheapest_sufficient/.method; claim.extraction_method. test_layers, test_claim. MET
- SIG-PARSE-002 classify-before + per-member: classification.classify/classify_archive; committed mixed_response.zip. test_classification. MET
- SIG-PARSE-003 mandatory locator + reject: locator.Locator + claim.ParsedClaim.__post_init__ LocatorRequired. test_claim/test_locator. MET
- SIG-PARSE-004 raw_value incl unparseable: claim.ParsedValue.unparseable/typed. test_claim round-trip. MET
- SIG-PARSE-005 versioned/reversible/stamped/no-rewrite: reason_codes + data toml; VOCABULARY_MIGRATION_METHOD. test_reason_codes. MET
- SIG-PARSE-006 free_text vs dropdown + signal: ReasonKind/SignalStrength. test_reason_codes. MET
- SIG-PARSE-007 committed fixtures: drift.assert_no_drift + FixtureCase + mixed_response.zip. test_drift. MET
- SIG-PARSE-008 canary alerts-not-drops: drift.run_canary/structural_findings/CanaryReport.alerted + canary fixtures. test_drift. MET
- AC6 phase-gate: make check green (lint/fmt/type/1171 tests/verify-gen); ADR-033; traceability P07.1; risk Phase7 R11 (RISK-P7-01). MET
- Layer-6 wiring: enum LLM_LAYER + method "llm_assisted" matches DB CHECK; §25 extraction.py IS the layer-6 impl (symmetric w/ layers 1-5 deferred to connectors per ADR-033). In-scope boundary respected.
- Cross-cutting: raw_value preserved (append-only/P2); no person/plate storage; additive (extraction.py untouched, new modules, CLI+docstring additive); no DDL; pylock/ontology unchanged (verify-gen clean).
- Out-of-scope respected: no P07.2/3 connectors; no §25 policy beyond layer-6 wiring; no field-level snapshot-diff.
STATUS: verification complete; committing + PR.
