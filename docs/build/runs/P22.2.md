# implement-spec ledger — P22.2 agent-docs refresh

- spec: docs/tickets/P22.2__agent-docs-refresh.md
- base branch: devin/p22-1-repo-docs-refresh @ a1bba93ce37f6d3c96ffb6e59d79fb09bfe6e578
- branch: devin/p22-2-agent-docs-refresh
- mode: refresh deep=true (NOT clean-slate; keep P19.1 AGENTS.md)
- live_verification=false

## Gate status (operator answer copied)
REFRESH (default) — NOT clean-slate. mode=refresh deep=true. KEEP existing AGENTS.md;
bootstrap uncovered packages, regenerate severely-drifted docs, preserve hand-written gotchas
(verify-gen / ontology canonicalisation). Do NOT delete AGENTS.md/CLAUDE.md. Clean-slate AC N/A.

## Detector baseline (Phase 0)
docsScanned=4, critical=5, moderate=0, minor=0, coverage gaps=0.
5 critical = root AGENTS.md refs docs/build/*.md files that the detector CANNOT SEE because its
EXCLUDES drops any */build/* path (false positives). Fix: reference docs/build/ as a directory
(no file extension → not matched by the detector's `\`path.ext\`` regex).

## Requirements checklist
1. Detector + mode → refresh deep=true. [done-recon]
2. Converged hierarchy: root AGENTS.md ≤250 + package docs (web/db/connectors exist; add api/exports/ops/tests).
3. CLAUDE.md bridge (@AGENTS.md first non-empty line) — already exists; verify.
4. Consistency sweep + quality checklist.
5. Vendor detector → scripts/docs/check-agent-docs-freshness.sh (MIT header); make docs-check = both detectors; CI docs step; make check gains docs-check only if <10s.
6. ADR-072 + docs/adr/README.md index. (Revisit trigger REQUIRED for make check test_policy_adrs.)
7. Traceability + risk (RISK-P22-03/04) appended to docs/risk_register.md + docs/traceability.md.

## Key facts (verified)
- make check = lint,format-check,typecheck,test,verify-gen (Makefile:46)
- make test-db = SIG_REQUIRE_DB_TESTS=1 pytest tests/db (Docker, postgis/postgis:18-3.6 conftest:30/48)
- e2e: SIG_REQUIRE_DB_TESTS=1 uv run pytest tests/e2e (Docker-gated)
- npm --prefix web run check = typecheck→test:unit→build→check:licenses→test:e2e (web/package.json)
- ingestion_permitted gate: connectors/src/connectors/loader.py assert_ingestion_permitted / assert_loadable (fail-closed)
- pyproject 14 members + evidence (ADR-023)
- zero-JS: lighthouserc.json script size maxNumericValue 0, total 153600
- e2e xfail convention: reason begins ^LD-[A-Z]+[0-9]+: (test_composed_stack.py:13-15)
- BUILD.sh: docs/research/_meta/spec_src/BUILD.sh (spec amendment)
- HG-01..14 human gates (DECISION_MEMO §4)
- check_spec_src.py NOT in make check/CI → AppF row for ADR-072 deferred (out of scope, touches generated spec)

## DEVIATIONS
- ADR-072 Appendix F row + spec rebuild deferred (spec_src/generated spec out of P22.2 scope). Noted in PR body.
