# implement-spec ledger — P20.4 (fix CI python-job red on composed e2e web-build seam)

- spec: inline (P20.4 fix-ticket)
- base_branch: devin/p20-3-integration-release
- base commit: 036b7db4773b4c234ee033f3240592e5c25b889e
- branch: devin/p20-4-fix-ci-e2e-webbuild
- autonomous: true

## Requirements
- R1: Gate S8 `web_build` session fixture (tests/e2e/test_composed_stack.py ~349-360):
  if `shutil.which("npm") is None` OR `web/node_modules` missing → `pytest.skip(reason)`
  (clean skip, mirroring Docker `_require_or_skip` pattern). Where env present, S8 runs
  EXACTLY as before → ends in existing `pytest.xfail("LD-V08: ...")`.
- R2: Do NOT change S8 assertions. Keep `^LD-...:` xfail convention intact.
- R3: Update docs/build/CI_STATUS.md — CI-RED-01 now FIXED (python job skips S8 cleanly,
  web job still covers it, local runs→xfails).
- R4: Update RISK-P20-05 note in docs/risk_register.md — reflect CI-RED-01 fixed.
- R5: BACKLOG.csv row status=closed closed-by:P20.4 IF a row tracks this.
- R6: ADR only if warranted; else code comment + docs suffice.

## Out of scope
- No ci.yml job-structure change (no Node in python job). No production code. No merges/tags/etc.

## Drift found (Phase 0.3)
- DRIFT-1: RISK-P20-05 in docs/risk_register.md is actually "The dry-run is stale by the
  time the operator merges" — NOT the CI-red. CI_STATUS.md line 64 cites RISK-P20-05 as the
  tracking id for the CI-red follow-up. Append-only invariant: do NOT rewrite the historical
  RISK-P20-05 row. Adapt: add a P20.4 annotation in the Phase-20 risk section documenting
  CI-RED-01 fixed, and update the CI_STATUS.md note (append-annotate).
- DRIFT-2: No BACKLOG.csv row tracks the CI-red. Closest is BL-007 (LD-V08 web wiring, P21.4)
  = the S8 xfail follow-up, not the test-gating. Also check_backlog.py LANDING_RE does not
  permit `closed-by:P20.4`. => R5 is a no-op (nothing to close); record in report.

## Test matrix
- T1 (node present): make check → 2418 passed, 1 xfailed (S8 still xfails LD-V08).
- T2 (node present): SIG_REQUIRE_DB_TESTS=1 uv run pytest tests/e2e -ra → S8 xfail present.
- T3 (node absent sim): env PATH=/usr/bin:/bin uv run pytest tests/e2e/test_composed_stack.py -k s8 -ra
  → S8 SKIPPED, 0 failed/errored. git status clean afterward.

## Status: Phase 2 (implement)
