# implement-spec ledger — P07.3 procurement connectors
base: dc7c73e; branch: devin/p07-3-procurement-connectors; baseline green (184 passed)

Requirements:
1 procurement connector -> Contract, FundingInstrument, acquisition_channel, quantities, renewal, dated lifecycle (§23.6)
2 cooperative piggyback MUST set parent_cooperative_contract; acquisition_channel required (SIG-ONTO-032)
3 FundingInstrument funder!=recipient!=purchaser; instrument_type, program_name, federal_award_id (§11.12)
4 USAspending sub-awards (not only prime) -> federal_award_id -> deployment (SIG-ONTO-033)
5 agenda-platform tenant registry built+published; connector reads it; negatives->coverage (§22.3, SIG-METRIC-002a)
6 artifact_type additions state_auditor_survey/warrant/procurement_aggregator_record; aggregator LINK (SIG-INGEST-047) [govspend LINK already present]

Design:
- ontology: Contract/FundingInstrument/AcquisitionChannel/FundingInstrumentType already exist. ADD ArtifactType enum (§10.3.2 + 3 additions) on EvidenceArtifact.artifact_type; regenerate.
- procurement.py connector; procurement_vocab.toml; agenda_tenants.toml (published registry); register in __init__.
- tests/connectors/test_procurement.py; test_cli listing; ADR-035; traceability; risk register.
Status: implementing
