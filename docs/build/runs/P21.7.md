# implement-spec ledger — P21.7 contribution-back live

- **spec:** docs/tickets/P21.7__contribution-back-live.md
- **base branch:** devin/p21-6-curation-web-ui @ 0f90fa0
- **branch:** devin/p21-7-contribution-back-live
- **baseline:** `make check` GREEN — 2585 passed, 1 skipped (env-gated live-api). Keep 0 failed/0 xfailed.
- **gate:** HG-08 accounts NOT provided → dry-run/stub, registered=false in ops/config.toml, OSM feed over fixtures. HG-10 study SKIP → protocol+instrumentation only, results "not yet run". Both → RETURN PASS (gate-pending).

## Requirements / deliverables
1. tasks.maproulette — challenge push/pull, geo-only, hashtag, HttpxTransport, dry-run without key, sensitive-tier excluded (RISK-P21-12).
2. tasks.osm_feed — poll changeset API by hashtag, attribute to LeverageLedger (append-only), NO OSM usernames, fixtures replay.
3. OE record — organised-editing-activity.md registration section; `registered` flag from ops/config.toml; push REFUSED while registered=false.
4. §7 metric web page reads LeverageLedger via data.ts export mode.
5. Usability — protocol finalize + opt-in AGGREGATE-ONLY timing (count+median, no per-user rows); USABILITY_STUDY.md "not yet run — gate pending HG-10".
6. ADR-069 + Appendix F + spec rebuild; risk register RISK-P21-12/13; traceability COVERAGE_MATRIX; CONTRIBUTION_BACK_LIVE.md; BACKLOG rows.

## Key facts discovered
- CLI tasks/cli.py is a SKELETON — must add subparsers (mirror connectors/cli.py; exit 3 = refusal).
- CHANGESET_HASHTAG = "#sig_operator_attribution" (contribution.py). Challenge also carries #sig-<jurisdiction>.
- LeverageLedger + UpstreamChangeset(id, comment, accepted) already in contribution.py — id+comment only, NO usernames structurally.
- SensitivityClass C1..C5, geo_tier_for(); geo_tier==3 (C3/C4/C5) = no geometry published → EXCLUDE from push.
- ContributorRepo (ticket anchor) does NOT exist; contributor persistence is in-memory (LD-F10). Aggregate timing implemented in tasks.onboarding (histogram → count+median, no rows) + api L0 hook (app.state, NOT in append-only row).
- L0 form = POST /v1/curation/submission (api/src/api/curation.py) + web/src/pages/curate/submit.astro (zero-JS).
- No contribution-back web page exists yet — create web/src/pages/contribution-back.astro + data.ts getLeverageMetric().
- ops/config.toml uses tomllib; repo root = Path(__file__).parents[3] from a pkg module.

## Status: PHASE 2 implement

---

## 2026-09-10 re-run (CONTRIB.1, prepare-only)

**Lane-B RE-RUN of the already-landed contract** (GL-CONTRIB-01 / chain row 77) — re-verify on the
current tip, not a new contract. Verbatim Run line:
`implement-spec spec=docs/tickets/P21.7__contribution-back-live.md live_verification=true`.

- **branch:** `devin/p21-7-contrib-rerun` · **base:** `devin/p21-5-infra-rerun` (chain tip) @ `e91b8f6`
- **gate state (GATE DECISIONS 2026-09-09 + P23.4/ACCT.1):** HG-08 (MapRoulette API key + registered
  OSM Organised-Editing page) `provided: no`; HG-10 (≥5 ontology-naïve participants) not scheduled.
  Both stay gate-pending → RETURN PASS. No creds, no participants, no network egress.
- **product code changed:** NONE — pure re-verify; every stub/fixture path already green on the tip.

### Paths that ran FOR REAL (no network)
- **Dry-run MapRoulette payload + registration gate:** `sig-tasks maproulette push --challenge 1
  --jurisdiction okc --dry-run` → **REFUSED exit 3** with the OE-registration reason
  (`ops/config.toml [tasks.contribution] registered=false`; SIG-CONTRIB-016d, RISK-P16-14). The
  dry-run challenge JSON build, the MapRoulette API v2 cooperative-challenge payload shape, the
  `#sig-<jurisdiction>` hashtag, the dry-run-when-registered-but-keyless path, and the sensitive-tier
  (C3/C4/C5, `geo_tier==3`) **exclusion** from the push payload are re-proven by
  `tests/tasks/test_tasks_maproulette.py` — **11 passed**.
- **OSM changeset feed → LeverageLedger over committed fixtures:** `sig-tasks osm-feed pull --out …`
  over `tests/tasks/fixtures/osm_changesets_okc_page1.xml` + `…page2.xml` → replayed 2 fixture files
  (no live poll — HG-08), **4 hashtag-bearing changesets attributed** to the §7 `LeverageLedger`
  (`140000001/002/004/005`), 3 §7 accepted operator-attributions, wrote the `web/leverage.json`
  data.ts export-mode metric; **idempotent** (a second `pull` adds 0 new ledger rows); **no OSM
  display names stored** beyond the public changeset id (Part VIII §0.7). Re-proven by
  `tests/tasks/test_tasks_osm_feed.py` — **9 passed** (incl. `test_no_osm_user_names_are_stored_anywhere`,
  `test_pull_attributes_and_is_idempotent_on_rerun`, `test_leverage_metric_json_is_public_and_username_free`).
- **Usability protocol + opt-in aggregate-only instrumentation:** the runnable protocol
  (`docs/governance/contributor-onboarding-usability-study.md`) and `docs/build/USABILITY_STUDY.md`
  carry the explicit **"not yet run — gate pending HG-10"** results line; the opt-in **aggregate-only**
  L0-form timing (count + median, no per-user rows, no personal data) re-proven by
  `tests/tasks/test_tasks_onboarding_timing.py` + `tests/api/test_curation_onboarding_timing.py`
  (**11 passed** combined).

### Gate-pending (deferred, NOT fabricated)
- **Real MapRoulette challenge push + real OSM `pull`** — blocked on HG-08 (`provided: no`); the gate
  refuses push exit 3 while `registered=false` (kept that way). No MapRoulette push, no OSM edit ever.
  → `D-P21.7-1` stays OPEN.
- **Usability study results (n, median ≤10 min for ≥5 participants)** — blocked on HG-10 (no
  participants); results stay "not yet run — gate pending HG-10". → `D-P21.7-2` stays OPEN.

### Verification
- `SIG_REQUIRE_DB_TESTS=1 make check` (Docker up) → **2767 passed, 2 skipped, 0 failed, 0 xfailed**
  (skips = env-gated live-api + one secrets stub); ruff/format/mypy clean; `verify-gen` byte-clean.
- `make docs-check` → **exit 0** (1 pre-existing BM-ADR-04 warning, not a violation).
- `bash scripts/docs/check-build-memory.sh .` → **exit 0** (same pre-existing warning).
- **No token literal** — no `SIG_MAPROULETTE_API_KEY`/`SIG_OSM_OE_PAGE` value assigned in any file;
  `registered = false` retained in `ops/config.toml`.

### Deferrals / ledger
- `D-P21.7-1` (HG-08 real push/pull) — **stays OPEN**, proxy-now advanced to the re-verified
  dry-run/fixture paths.
- `D-P21.7-2` (HG-10 usability study) — **stays OPEN**, proxy-now advanced to the re-verified
  protocol + aggregate-only instrumentation.
- LEDGER CURRENT STATE: `lastCompleted: P21.7`, `nextTicket: P21.8`, `chainTip:
  devin/p21-7-contrib-rerun`; P21.7 kept in RETURN PASS / `returnPass:`.
