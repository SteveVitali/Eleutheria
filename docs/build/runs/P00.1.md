# implement-spec ledger — P00.1 repo skeleton
- Spec: docs/tickets/P00.1__repo-skeleton.md
- Cited: §47 (SIG-ENG-010..014), §48 (015/016), §0.4/§0.6, Part I §3, §51.3
- Base: main @ a33177c ; Branch: p00-1 ; uv 0.12.6

## Requirements
- R1 SIG-ENG-011: uv workspace + committed lockfile + standards lock export + SBOM target
- R2 SIG-ENG-012: exact §47 layout as empty-but-real packages
- R3 SIG-ENG-013: every stage a plain CLI; orchestrator import confined to orchestration/ (lint/test)
- R4 SIG-ENG-014: policy/ real tested pkg (no logic)
- R5 SIG-ENG-015/016: CI lint+typecheck+test + generated-artifacts-match gate stub
- R6: licence headers + LICENSE posture placeholder
- SIG-ENG-010: Python primary; TS confined to web/

## AC
- AC1 workspace resolves; lockfile committed; CI installs from lockfile
- AC2 §47 packages exist+import; test asserts set matches §47
- AC3 test: no pkg outside orchestration/ imports orchestrator
- AC4 CI lint+typecheck+test green on empty skeleton
- AC5 §51.3 gate: CI green; req IDs in PR; R-11 noted

## Out of scope: no policy logic/registry/ontology/schema/connector; no ADRs
## Test matrix / Gap table / Evidence: below (filled per phase)

## PHASE 2 DONE — implemented
- Virtual uv workspace root pyproject.toml; 13 member pkgs (src layout, sig-<name>).
- CLI convention: cli.main + __main__ + [project.scripts] sig-<pkg>.
- orchestration/pipeline.py holds ORCHESTRATOR_MODULES (single source of truth).
- policy/ real typed pkg. web/ TS placeholder. LICENSE placeholder. Makefile. CI yml.
- Licence SPDX headers on all .py/.ts. .gitignore updated. uv.lock + pylock.toml committed-to-be.

## TEST MATRIX (1.4) -> tests/unit/
- AC2 package layout: test_package_layout.py (dirs exist; members==§47 py; import+__version__)
- AC3 boundary: test_import_boundary.py (AST scan; no forbidden imports outside orchestration)
- SIG-ENG-013 CLI: test_cli_convention.py (main([])==0; python -m <pkg> --version)
- SIG-ENG-014 policy: test_policy_package.py
- R6 headers: test_license_headers.py

## PHASE 5 EVIDENCE (local, uv 0.12.6, py3.12.14)
- make sync: 25 pkgs installed from --frozen lock. OK
- make lint: All checks passed
- make format-check: 46 files already formatted
- make typecheck: mypy Success, 40 source files
- make test: 104 passed
- make verify-gen: pylock regenerated, git diff clean, exit 0
- make check (full CI mirror): exit 0

## PHASE 4 GAP TABLE — all MET (evidence)
- AC1 workspace/lock/CI-install: uv.lock committed; make sync --frozen (25 pkgs). MET
- AC2 §47 exist+import+test: test_package_layout.py (3 tests). MET
- AC3 orchestrator boundary test: test_import_boundary.py. MET
- AC4 CI lint+typecheck+test green: ci.yml + make check exit 0. MET
- AC5 §51.3 gate: stamp IDs + R-11 in PR body. DONE-AT-PR
- R1 SIG-ENG-011 workspace+lock+pylock export+sbom target (verified 25 comp). MET
- R2 SIG-ENG-012 layout. MET  | R3 SIG-ENG-013 CLI+boundary. MET
- R4 SIG-ENG-014 policy pkg. MET | R5 015/016 CI+gen gate. MET
- R6 licence headers+LICENSE. MET | SIG-ENG-010 py/TS-in-web. MET
- Out-of-scope confirmed NOT done: no policy logic/registry/ontology/schema/connector/ADR.
- Pass2 fix: sbom target now targets .venv (was ephemeral uvx env).
- NOTE: docs/2_canonical_design_spec.md + spec_src/96_*.md are PRE-EXISTING edits -> EXCLUDE from commit.
