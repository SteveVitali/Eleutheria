# implement-spec ledger — P21.4 First jurisdiction ingest & publish

- spec: docs/tickets/P21.4__first-jurisdiction-ingest-and-publish.md
- base commit: 305f94d (devin/p21-3-live-connector-wiring)
- branch: devin/p21-4-first-jurisdiction-ingest-and-publish
- Mode: staging-only, fixture/shadow (NOT live). No green sources (HG-03 skipped).
- Gate answers: HG-12 local staging TICK; HG-01 SKIP; HG-11 SKIP; HG-02 TICKED (ODbL included in exports, separate compartment); Go-public NO.

## Deliverables (In scope)
1. ops/ runtime composition: docker-compose.yml + sig-ops up|down|status|seed --jurisdiction okc + ops/README.md
2. web/src/lib/data.ts export-backed data layer (SIG_DATA_SOURCE=fixtures|export)
3. docs/build/tools/run_okc.sh (shadow mode all sources) -> acceptance_<date>.json J-1 + Q1-13
4. Staging deploy + hostile-reader review
5. docs/build/PUBLICATION_CHECKLIST.md
6. docs/build/FIRST_JURISDICTION_REPORT.md (8 sections)
7. Go public — SKIP (N/A)
8. ADR-066; RISK-P21-06/07; traceability

## Baseline: 2492 passed, 1 xfailed (S8/LD-V08 to flip)

## Gap table: (fill in Phase 4)
## Evidence log: (fill in Phase 5)

---

## 2026-09-10 re-run (LIVE.2, prepare-only)

- **spec:** `docs/tickets/P21.4__first-jurisdiction-ingest-and-publish.md` (Lane-B RE-RUN of the existing landed contract — NOT a new contract)
- **Run line (verbatim):** `implement-spec spec=docs/tickets/P21.4__first-jurisdiction-ingest-and-publish.md live_verification=true`
- **branch:** `devin/p21-4-live2-rerun`  **base:** `devin/p21-3-live-wiring-rerun` (base commit `159b5ae`)
- **semantic:** chain row 74 / LIVE.2 / GL-LIVE-02 (`D-P21.4-1/2/3`)
- **mode:** local composed staging **run for real** over Docker 29.1.3; gated parts prepare-only. Environment clock read 2026-09-13 (evidence files carry that stamp); the chain date for this row is 2026-09-10.
- **product code changed:** NONE (re-verification + dated docs only).

### What ran
1. `sh docs/build/tools/run_okc.sh` end-to-end (8 steps): `sig-ops up --seed` (5 claims / 1 entity) → shadow connectors `eff_atlas_of_surveillance` + `osm_overpass` (OK, recorded, NOT live) → `sig-resolution match` (1 PROPOSED enqueued) → `reconcile resolve` (**`claimed_device_count` UNRESOLVED/CONTESTED — 299 vs 190, both retained**) → annotations rebuild SKIPPED (P21.2 shrunk, ADR-059) → `sig-exports build` (13 artifacts, CC-BY-4.0 + ODbL-1.0 separate compartment) → `SIG_DATA_SOURCE=export` web build (50 pages) → acceptance J-1 + Q-1…Q-13.
2. Deterministic ACs re-verified (see Evidence log below).

### Gap table (re-run)
| item (spec §) | status | evidence |
|---|---|---|
| AC1 `sig-ops up/status/down` clean on a Docker machine | met | status: PG/API/curation healthy; `down` → "no SIG containers or host processes remain"; `docker ps` empty |
| AC2 `SIG_DATA_SOURCE=export` build + `test:e2e` both modes + `check:perf` | met | e2e 192 passed fixtures AND 192 passed export; check:perf lhci exit 0 (budgets hold) |
| AC3 `run_okc.sh` completes; `acceptance_<date>.json` J-1 + Q-1…13 vs API | met | `docs/build/reports/okc/acceptance_2026-09-13.json`: J-1 pass, 2 pass / 11 blocked / 0 failed, fixture-subset all pass |
| AC4 live dossier shows 299-vs-190 contradiction (export-mode e2e) | met | `web/dist/dossier/oklahoma-city/index.html` has 299/190/contested/unresolved; `dossier.spec.ts:71` (SIG-UI-014) passes in export mode |
| AC5 checklist ticked-or-`gate pending`; report 8 sections | met | `PUBLICATION_CHECKLIST.md` + `FIRST_JURISDICTION_REPORT.md` dated re-run sections appended |
| AC6 Go-public (conditional) | n/a — gated | reserved to the human (GL-GATE-05 / P23.6); NOT done |
| AC7 hostile reader finds no uncited claim | met | `docs/build/reports/okc/hostile_reader_review.md`; contradiction shown with both sources + dates |
| Phase-gate: `make check` green + ADR-066 + traceability | met | `SIG_REQUIRE_DB_TESTS=1 make check` 2767 passed / 2 skipped / 0 failed |
| Out-of-scope guard (no source flip, no public cutover, no 0.2.0, no GCP) | met | no source flipped; no cutover; no version bump; no GCP apply |

### Evidence log (re-run)
- `sig-ops up --seed`: 5 claims inserted, 1 entity; `sig-ops status`: PG/API/curation healthy; `sig-ops down`: no containers remain.
- `run_okc.sh`: acceptance `2 pass, 11 blocked, 0 failed` (fixture subset all pass: True; J-1: pass, 12 hops, 5 source families).
- reconcile: `claimed_device_count` → `resolution_status=UNRESOLVED, agreement=CONTESTED, claimed_values=[299,190]`, both retained (§3.1).
- export: `artifact_count=13, licenses=["CC-BY-4.0","ODbL-1.0"]`, tiles pure-python PMTiles.
- e2e: `192 passed` (fixtures) and `192 passed` (export); dossier reconciliation SIG-UI-014 passes in export mode; a11y WCAG 2.2 AA clean; zero-JS held.
- `check:perf`: lhci autorun exit 0, 5 URLs asserted, budgets hold.
- `SIG_REQUIRE_DB_TESTS=1 make check`: `2767 passed, 2 skipped, 0 failed` (skips = env-gated live-api [covered by run_okc.sh] + GCP leak check [SIG_GCP_PROJECT unset, correct for prepare-only]); composed-stack e2e ran (10 passed).
- `make docs-check`: exit 0. `check-build-memory.sh .`: no violations (1 pre-existing BM-ADR-04 warning, unrelated).

### Deferrals
- `D-P21.4-1` (HG-01 real legal home) — **stays OPEN**; proxy-now advanced (interim posture only, not a real home).
- `D-P21.4-2` (HG-11 governance roles/concurrence/takedown) — **stays OPEN**; proxy-now advanced (mechanism served, roles owed).
- `D-P21.4-3` (Go-public) — **stays OPEN**; proxy-now advanced (local staging re-verified; Go-public reserved to the human).
- No deferral closed; no live/public green fabricated.

### Verdict
Local composed staging **complete + re-verified for real**; public publish **genuinely gated** (HG-01 real home + HG-11 + Go-public). → **RETURN PASS**. next chain row → **P23.6** (Go-public GATE-G2 marker; orchestrator/operator handles it).
