# implement-spec ledger — P21.3 live-connector-wiring

- spec: docs/tickets/P21.3__live-connector-wiring.md
- base commit: 1256adc (devin/p21-2-persist-annotation-layer)
- branch: devin/p21-3-live-connector-wiring
- GATE: HG-03 SKIP (no source flipped, NO live fetch). HG-09 no tokens. All deliverables land over stub+fixture. RETURN PASS.
- baseline: 2454 passed, 1 xfailed (S8/LD-V08).

## Deliverables (in scope)
1. HttpxTransport (connectors/transports/httpx_transport.py) — Transport over httpx; robots; retry/backoff 429/503 Retry-After; Overpass 429/504 semantics; ETag/conditional GET; UA from policy; no circumvention. Tests via httpx.MockTransport.
2. OcflCaptureStore (connectors/capture_ocfl.py) — CaptureStore over evidence.OcflStore; raw bytes+headers+retrieval time; content-addressed. Tests.
3. Runner CLI: sig-connectors run --source ID --mode live|replay|shadow --sink pg|memory --dsn --capture-dir. live refuses (exit 3 + gate reasons) unless review-status fully green (LD-X08 guard). fetch record docs/build/live_runs/<date>_<source>.json.
4. Connector wiring / shadow over fixtures diff=0.
5. Isolation with HttpxTransport present (LD-X06).
6. none green -> shadow + record blocker.
7. LIVE_WIRING_REPORT.md, ADR-065, RISK-P21-04/05, traceability, BACKLOG.csv.

## ACs
- AC1 pytest -k "transport or capture_ocfl or runner" green no network; 429 Retry-After:2 -> one retry >=2s; robots-disallowed never requested.
- AC2 run --source osm_overpass --mode live --sink memory -> exit 3 + gate reasons; no socket.
- AC3 shadow over fixtures 0 diffs.
- AC4 no SIG_MUCKROCK_TOKEN=/api_key= literal; .env* in .gitignore.
- AC5/6 conditional: 0 green -> report each source gate pending HG-03 + command.
- AC7 crawler-conduct evidence in fetch record.
- AC8 make check green; ADR-065; traceability+risk; BACKLOG rows.

## Progress
- (in progress) httpx promotion.
