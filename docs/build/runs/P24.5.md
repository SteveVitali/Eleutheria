<!-- Run ledger for P24.5 (META.1 / GL-META-01, Lane-C new code). build-memory v2, BM-INDEX-02. -->
# Run ledger — P24.5 (META.1 / GL-META-01) — housekeeping backlog

- **Spec:** `docs/tickets/P24.5__housekeeping-backlog.md`
- **Base:** `devin/p24-4-ci-hardening` @ `dc8688b` · **Branch:** `devin/p24-5-housekeeping-backlog`
- **Config:** implement-spec autonomous=true `live_verification=false`; tests/gap/self-review/evidence on.
- **Gate:** none. **Live stage:** offline-only.

## Deferrals read
Read `docs/tickets/DEFERRALS.md` first (2026-09-13). OPEN rows: D-P21.1-2 (HG-04),
D-P21.3-1 PARTIAL / D-P21.3-2 (HG-09), D-P21.4-1/2/3 (HG-01/HG-11/Go-public),
D-P21.5-1 (HG-07), D-P21.7-1/2 (HG-08/HG-10), D-P21.8-1, D-P21.9-1,
D-LEGAL.1-1 (HG-02), D-ACCT.1-1 (HG-12), D-DEPLOY.1-1 (HG-12), D-LIVE.1a-1 (HG-09),
D-OBS.1-1 (HG-12). P24.5 unblocks none — all are operator/credential/host/live-data
prerequisites, and this ticket is offline housekeeping. It **opens** D-META.1-1 /
D-META.1-2 (two BACKLOG.csv `P22+` items promoted to tracked obligations).

## Requirements to satisfy and stamp
- **GL-META-01** — `pyproject.toml` descriptions for the packages still marked
  "skeleton" (BL-052); remaining docs-drift; triage the `P22+` `BACKLOG.csv` rows
  into DEFERRALS/backlog with real landings. Closes BL-052.

## Acceptance criteria
- [ ] no package description says "skeleton" *(deterministic)*
- [ ] `check_backlog.py` green *(deterministic)*
- [ ] BL-052 closed in `BACKLOG.csv` *(deterministic)*
- [ ] universal phase-gate (tests for every new behaviour; ids stamped; BUILD_INDEX + LEDGER)

## Plan
- Re-confirm skeleton descriptions at build time: **4 remain** (`db`, `parsing`,
  `reconcile`, `policy`); `orchestration` was already re-described by P24.2 (SCHED.1).
  BL-052's title names five because it predates that fix — closing it is still
  honest because the AC is "no package description says skeleton".
- `docs/build/tools/check_backlog.py`: repoint `LD` + `THEMES` to the paths P22.3
  moved them to (`docs/build/reports/`); extend `LANDING_RE` for the triage
  landings (`closed-by:P24.5`, `P25+`); same one-line drift fixed in
  `p20_extract.py` (same moved file).
- `docs/build/tools/build_backlog_md.py` (new, stdlib): renders `docs/build/BACKLOG.md`
  from `BACKLOG.csv` — the committed mirror has drifted (stale titles/statuses since
  P21.x closures); regeneration IS the docs-drift fix.
- `BACKLOG.csv` triage: BL-052 → `closed-by:P24.5` + `closed`; the remaining `P22+`
  rows get real landings (`P25+` = post-manifest planning round) — two conditioned
  on post-live state are promoted to DEFERRALS rows (D-META.1-1 BL-045 calibration;
  D-META.1-2 BL-028 records-request backend).
- Lane-B re-run ticket Load-path drift: the 7 live re-run contracts
  (P21.1/P21.3/P21.4/P21.5/P21.7/P21.8/P21.9) still point `docs/build/<file>.md`
  refs at paths P22.3 moved to `docs/build/reports/` — update those pointer refs
  (content unchanged). Frozen historical tickets keep verbatim history.
- `00_MANIFEST.md` living-doc refs (`PLANNING_LEDGER`, `DECISION_MEMO`) → `reports/`.
- LEDGER OPEN FINDINGS: annotate MATRIX-INT-01 closed (verified: checker exits 0).
- `tests/unit/test_backlog_housekeeping.py` — pins every new behaviour.

## Test matrix
| AC / behaviour | test |
|---|---|
| no skeleton descriptions | `test_no_package_description_says_skeleton` (all workspace pyprojects) |
| check_backlog green + moved paths | `test_check_backlog_exits_zero`, `test_check_backlog_sources_resolve_under_reports` |
| landing enum | `test_landing_enum_accepts_real_landings` (+ rejects junk) |
| P22+ triaged | `test_no_p22_plus_landings_remain` |
| BL-052 closed | `test_bl052_closed` |
| BACKLOG.md mirror | `test_backlog_md_matches_csv_render` (drift fails) |
| live-ticket pointer refs resolve | `test_live_rerun_ticket_refs_resolve` |

## Progress
- **Skeleton descriptions (BL-052).** Re-confirmed at build time: 4 packages still
  read "skeleton" — `db`, `parsing`, `reconcile`, `policy` (`orchestration` was
  already re-described by P24.2/SCHED.1; BL-052's title names five because it
  predates that fix). All four re-described in the established style; zero
  "skeleton" descriptions remain. `uv` rebuilt the four dist-info installs cleanly.
- **`check_backlog.py` path drift (the flagged known issue).** `LD` and `THEMES`
  repointed to `docs/build/reports/` (P22.3's move). First run after the path fix
  surfaced **6 unmapped ADR revisit triggers** (ADR-073..078, added since the last
  reconcile) — each homed in exactly one `sources` cell per the defining standard:
  ADR-073 → BL-002 (monitor-style process ADR); ADR-074 → BL-023 (doc-connector
  live smoke; D-LIVE.1a-1 already cites BL-023); ADR-076 → BL-023 (cadence observed
  on real fetches post-HG-03); ADR-075 + ADR-077 → BL-037 (infra accounts/host —
  nearest home; no HG-12 BL row exists); ADR-078 → BL-048 (CI-jobs row).
  Checker now exits 0: `102/102` risk rows, `77/77` ADR triggers, `90/90` LD rows,
  0 duplicate sources — **and the themes check runs again** (it was silently
  skipped while `BACKLOG_THEMES.md` 404'd at the old path).
- **P22+ triage (9 rows, real landings).** BL-052 → `closed-by:P24.5` + `closed`.
  BL-028 + BL-045 verified unlanded in the tree (`tasks.records_request` is
  template-only; reconcile runs on build-time defaults) and conditioned on real
  post-go-live state → promoted to DEFERRALS rows `D-META.1-1`/`D-META.1-2`
  (kind F, citing the BL ids). BL-003/BL-031/BL-041/BL-047/BL-048/BL-049 verified
  unlanded, owned by no remaining manifest ticket (rows 84–87 = JURIS.2/CCOPS.1/
  REC.1/GATE-ACCEPT) → honest `P25+` landing (post-Round-4 planning round;
  `LANDING_RE` generalised to `closed-by:P<n>.<m>` / `P<n>.<m>` / `P<n>+`).
- **BACKLOG.md mirror regenerated.** New stdlib renderer
  `docs/build/tools/build_backlog_md.py` makes the committed mirror a pure
  function of the CSV (`--check` mode for tests). The old mirror was stale since
  the P21.x closures (wrong statuses/titles, missing groupings).
- **Docs-drift sweep (post-P22.3 moved paths).** Same drift class as
  `check_backlog.py` fixed wherever a *live* pointer existed: vendored tools
  (`p20_extract.py`, `classify.py`, `gen_rights_index.py`, `gen_rights_packets.py`,
  `gen_stage0_record.py`, `run_okc.sh` — which would have recreated a top-level
  `docs/build/okc/` on the next live run), the 7 Lane-B re-run contracts
  (P21.1/P21.3/P21.4/P21.5/P21.7/P21.8/P21.9 — re-run verbatim, so their Load
  pointers must resolve), `00_MANIFEST.md`, `docs/3_sig_golive_spec.md`,
  `INTEGRATION_PLAN.md`, `readouts/HUMAN-H1.md`, `reports/rights/_TEMPLATE.md`,
  `governance/stage0-outreach-letter.md`. **Deliberately left verbatim:** dated
  historical records — `reports/*.md` narratives, `LEDGER.md` PHASE LOG entries,
  `DEFERRALS.md` row cells, `pr/*`, `runs/*`, append-only `risk_register.md` /
  `traceability.md`, frozen P19/P20/P22/P23 tickets, and README's intentional
  old→new migration table. Rewriting those would falsify history; a reverted
  example: PLANNING_LEDGER §4.7 records P19.1 renaming the ledger to
  `docs/build/PLANNING_LEDGER.md` — true when written.
- **LEDGER OPEN FINDINGS.** MATRIX-INT-01 annotated CLOSED (verified
  `check_coverage_matrix.py` exits 0, 671 rows — the two enum cells were
  normalised per the finding's own prescription).
- **Tests.** `tests/unit/test_backlog_housekeeping.py` — 10 tests pinning every
  new behaviour + deterministic AC (fail-if-removed).

## Gap table
| item (spec §) | status | evidence |
|---|---|---|
| AC: no package description says "skeleton" | met | `db/parsing/reconcile/policy` pyproject line 4; `test_no_package_description_says_skeleton`; `grep -rn "skeleton" */pyproject.toml` = 0 |
| AC: `check_backlog.py` green | met | exits 0 (102/102, 77/77, 90/90, 0 dupes); `test_check_backlog_exits_zero` + path tests |
| AC: BL-052 closed in BACKLOG.csv | met | `status=closed`, `landing=closed-by:P24.5`; `test_bl052_closed` |
| Deliverable: 5 skeleton descriptions | met-differently | 4 remained (orchestration fixed by P24.2); AC is the invariant — zero skeletons remain |
| Deliverable: remaining docs-drift | met | moved-path sweep over tools + live docs (see Progress); frozen/historical docs deliberately untouched |
| Deliverable: P22+ triage w/ real landings | met | BL-052 closed; 2 → DEFERRALS (D-META.1-1/2); 6 → `P25+`; `test_no_p22_plus_landings_remain` |
| Universal phase-gate (tests/id-stamp/build-memory) | met | 10 new tests; GL-META-01 stamped in PR; BUILD_INDEX row 83 + LEDGER advanced |
| Out of scope: no new features/connectors; no DEPLOY.1/SCHED.1/OBS.1/CI.1-owned work | met | diff is docs/tools metadata only — no product code, no workflow changes |

## Evidence log
- `python3 docs/build/tools/check_backlog.py` → **exit 0** (was FileNotFoundError
  on `docs/build/LEDGER_DEFERRALS.md`; after the path fix it surfaced the 6
  unmapped ADRs, now homed).
- `python3 docs/build/tools/build_backlog_md.py --check` → exit 0.
- `uv run pytest tests/unit/test_backlog_housekeeping.py` → **10 passed**.
- `bash scripts/docs/check-build-memory.sh .` → exit 0 (1 pre-existing BM-ADR-04
  warning, same as base).
- `python3 docs/build/tools/check_spec_src.py` → exit 0 (77 ADRs, byte-identical).
- `make docs-check` → exit 0 (0 broken refs, 0 stale suspects, 0 agent-doc issues).
- `SIG_REQUIRE_DB_TESTS=1 make check` → (see final entry).
- No ADR: pure housekeeping/bookkeeping — the landing-enum extension is documented
  in `check_backlog.py`'s comment + BACKLOG.md's header (precedent: earlier enum
  extensions shipped without ADRs); no requirement-id or contract change.
- No RISK entry: no new risk introduced (RISK-P20-01 already covers backlog drift;
  the regenerated mirror + new tests are additional compensating controls).

## Evidence report
(filled at Phase 6)
