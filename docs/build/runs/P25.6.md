# Run ledger — P25.6 (LIVE-OPS.6 / unmapped-source triage)

## Spec / Base / Branch / Config

- **Spec:** `docs/tickets/P25.6__unmapped-source-triage.md` (semantic `LIVE-OPS.6`, P25+ backlog)
- **Branch:** `devin/deploy-gcp-live` — **landed interactively, not via an `implement-spec`
  dispatch**; retroactive record written 2026-09-15.
- **Commit:** `e2d5246` (dispositions + CI completeness check).

## What landed

1. **All 98 unmapped registry rows dispositioned** in `data/live_dispositions.toml`:
   38 link_only / 43 reference / 8 mirror / **9 promote** — each promote names its class
   ticket (e.g. `civicclerk`/`legistar`/`primegov` agenda-platform sources → the P25.2
   procurement tenants).
2. **CI completeness check** — `check_backlog.py` enforces `mapped ∪ dispositions == 121`;
   a future undispositioned source fails the build.
3. `SOURCE_LIVE_OPS_MATRIX.md §B` updated with the resolved dispositions.

## Acceptance evidence

- `check_backlog` → 98 dispositions + 23 mapped = 121, 0 duplicates; build-memory clean.
- The completeness check is enforced in-tree (a new undispositioned source fails CI).

## Owed

- None for the triage itself. Promotions execute under their named class ticket.
