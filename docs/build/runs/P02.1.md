# implement-spec ledger — P02.1 The bitemporal claim spine

Spec: docs/tickets/P02.1__claim-spine.md (authoritative: §16 + Appendix C; §20.4/§47/§48/§51.3)
Base commit: 551f602 | Branch: devin/p02-1-claim-spine | Config: full

## Locked decisions
- Harness: testcontainers-python -> postgis/postgis:18-3.6
- Migrations: real sqitch (sqitch/sqitch docker on shared network; CI apt-get sqitch). plan+deploy+revert+verify.
- Driver: psycopg v3
- Validated: PG18.6 + PostGIS3.6.4 + btree_gist + uuidv7() + GiST EXCLUDE ok; sqitch ok.

## Deviations (ADR + report)
1. claim NOT physically partitioned: §16.2 #6 PARTITION BY observed_at conflicts with claim_id single-col PK
   required by FK targets (claim_evidence/resolution/person/self). observed_at nullable -> cannot be in PK.
   No AC depends on partitioning. Keep FK contract, defer partitioning. -> ADR-013.
2. SIG-STORE-045 (DDL generated from LinkML) vs physical enforcement (triggers/RLS/partition/exclusion) not
   LinkML-expressible. Physical spine authored here as sqitch (ticket "owns physical schema"). Note only.

## Requirements
- R1 L0-L3 schema per §16 (+FK targets) uuidv7
- R2 append-only (trigger + role revoke + generated col-list guard + CI test) SIG-STORE-011/012/013/020
- R3 entity identity-only SIG-STORE-008/009
- R4 resolution + GiST exclusion (DB not app) SIG-STORE-014/016
- R5 RLS restrictive by tier; public no BYPASSRLS; export row_security=off fails loud SIG-STORE-023 ADR-012
- R6 no plate column - schema test SIG-STORE-026/047

## ACs
- AC1 UPDATE/DELETE claim rejected except close sys_period
- AC2 entity no attribute columns
- AC3 correction as_of_belief returns old value
- AC4 resolution overlap prevented by exclusion constraint
- AC5 RLS every role x tier (vis + non-vis)
- AC6 no plate column
- AC7 phase gate: CI green, req IDs in PR, retires Risk3(part)

## Status
Phase 0 DONE. Phase 1 in progress.

## Test matrix / Gap table / Evidence — TBD

## Phase 4 gap table (all MET)
- AC1 append-only: MET — test_append_only.py (update/delete blocked, sys_period close ok, lower immutable, already-closed) + SIG-STORE-012 privilege test
- AC2 entity identity-only: MET — test_schema_integrity.test_entity_tables_hold_no_duplicate_predicate_columns (allowlist=cached/typing per SIG-STORE-046)
- AC3 correction as_of: MET — test_corrections (prior belief=25, current=225, both rows retained)
- AC4 resolution exclusion: MET — test_resolution_exclusion (overlap rejected, contype 'x', superseded frees window)
- AC5 RLS role x tier: MET — test_rls (3 roles x tiers vis+non-vis, no BYPASSRLS, export fails loud, forced RLS)
- AC6 no plate: MET — test_schema_integrity.test_no_plate_capable_column_anywhere (token-based)
- AC7 phase gate: MET — make check green (lint/format/typecheck/test/verify-gen); req IDs stamped in PR; Risk 3 (part) retired in risk_register
- Deviation #3: ticket in-scope wording "native PERIOD/WITHOUT OVERLAPS" -> implemented per NORMATIVE §16 DDL + AC4 = tstzrange + GiST EXCLUDE (PG18 ships no temporal PKs). Documented.
- Out-of-scope confirmed NOT done: OCFL bytes/capture pipeline (P02.2); EDTF envelope fn / as-of fns / PROV-O (P02.3, columns+hooks only here); resolver write logic (P08.1, table+constraints only here).

## Evidence: 25/25 db tests pass; full `make check` green. Live PG18.6+PostGIS3.6.4 via testcontainers+sqitch.
