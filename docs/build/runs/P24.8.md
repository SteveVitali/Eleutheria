# Run ledger — P24.8 (REC.1 / go-live readiness delta, reconcile)

## Spec / Base / Branch / Config

- **Spec:** `docs/tickets/P24.8__golive-readiness-delta.md` (semantic id `REC.1`;
  tail=minimal per `docs/3_sig_golive_spec.md` D6 + Appendix B item 7 — a delta over
  the PR #68 capstone, NOT a re-verification, NOT CAP.1–CAP.3)
- **Base:** `devin/p24-7-ccops-disclosure` @ `919bde0` (chain tip)
- **Branch:** `devin/p24-8-readiness-delta`
- **Config:** `live_verification=false`; kind=reconcile; no gate; no live stage.
  Invokes `reconcile-build mode=backlog` (delta scope only). Docs/tooling only —
  no product code.
- **PR:** https://github.com/SteveVitali/Eleutheria/pull/86 (stacked on
  `devin/p24-7-ccops-disclosure`; pushed 2026-09-13; OPEN — not merged)

## Deferrals read

`docs/tickets/DEFERRALS.md` read first (per BM-DEFER-01). Enumerated from the file
(not the ticket prompt): **21 OPEN + 1 PARTIAL** rows —
`D-P21.1-2`, `D-P21.3-1` (PARTIAL), `D-P21.3-2`, `D-P21.4-1/-2/-3`, `D-P21.5-1`,
`D-P21.7-1/-2`, `D-P21.8-1`, `D-P21.9-1`, `D-LEGAL.1-1`, `D-ACCT.1-1`, `D-DEPLOY.1-1`,
`D-LIVE.1a-1`, `D-OBS.1-1`, `D-META.1-1/-2`, `D-JURIS.2-1/-2`, `D-CCOPS.1-1/-2`.
DONE (not owed): `D-P21.1-1`, `D-CI.1-1`. None is unblocked by this ticket — this
ticket reconciles them into the backlog/readiness picture; it opens no new deferral.

## Requirements

1. Readiness delta: refresh `docs/build/OPERATIONAL_READINESS.md` + `docs/build/BACKLOG.csv`
   against current DEFERRALS + open findings introduced/closed by Rounds 3–4 (no
   COVERAGE_MATRIX re-run).
2. Append any NEW accepted deviation from Rounds 3–4 to `docs/build/CAPSTONE_CLOSURE.md`
   (append-only); if none changed, state so explicitly.
3. `docs/build/tools/check_backlog.py` runs green.

## Acceptance criteria

- [ ] every OPEN/PARTIAL go-live deferral, OPEN FINDING and new ADR revisit trigger
      appears in exactly one BACKLOG source cell (deterministic)
- [ ] `OPERATIONAL_READINESS.md` reflects the Round 3–4 delta with `ticket:` +
      `proof:` on every new step and no TBD (deterministic)
- [ ] the CAPSTONE_CLOSURE ACCEPTED list is current — new deviations appended, or
      "no change" stated (deterministic)
- [ ] universal phase-gate: verification green; requirement ids stamped in the PR;
      DEFERRALS rows for anything not automatically verifiable; ADRs for owned
      decisions; BUILD_INDEX row + LEDGER advanced (agentic)

## Plan

- Enumerate OPEN/PARTIAL deferrals → confirm each names exactly one BL home;
  annotate the nine rows missing a `cites BL-*` pointer (append to status cell —
  the established "re-confirmed" convention, never a rewrite).
- BACKLOG.csv flips only (append-only discipline): BL-010 → accepted (A1 applied,
  SIG-UI-047 signed); BL-011 + BL-012 → closed-by:P20.2 (Appendix F reconciled —
  `check_spec_src` 79 ADRs == file set; `not_researched` in spec §32.1 per A2);
  stale landings → honest buckets (BL-006/BL-025/BL-026/BL-051 → P25+; parser-layer
  half of BL-025 landed P21.9/P23.5); BL-037 title names the HG-12 host/GCP-apply/
  OBS.1-live family it now homes; BL-023 title notes the connector-code half landed.
  Regenerate `BACKLOG.md` via `build_backlog_md.py`.
- OPERATIONAL_READINESS.md: full delta refresh — rights counts 109→121 sources /
  0→6 permitted+loadable; new connector rows (okc doc connectors, data_driven,
  coarse_international, pathways, government_mandated_disclosure, France);
  new capability rows (DEPLOY.1 IaC, SCHED.1, OBS.1, CI.1, JURIS.2, CCOPS.1);
  critical path re-verified with `ticket:` + `proof:` on every step; gate checklist
  + cost posture current.
- CAPSTONE_CLOSURE.md: append-only dated addendum — matrix MET-DIFFERENTLY count
  verified still 77; no new accepted deviation introduced by Rounds 3–4 → explicit
  "no change" for the operator's GATE-ACCEPT (P24.9) signature.
- OPEN FINDINGS: all four CLOSED/RETIRED (CI-RED-01 + DOCKER-DOWN by P24.4,
  MATRIX-INT-01 by P24.5, P17-FLIP-01 by P24.7) — zero to place.
- No ADR (pure reconcile; no owned decision).

## Test matrix

| AC | check | result |
|---|---|---|
| deferrals/findings/ADR-triggers in exactly one source cell | `check_backlog.py` → `deferral homes: 22/22` (new deterministic check); 0 open findings; 79/79 ADR | ✅ |
| readiness delta, ticket:+proof:, no TBD | re-read §(c) — 13 steps each with `ticket:`+`proof:`; `grep -ci TBD` = 0 | ✅ |
| CAPSTONE_CLOSURE current | append-only addendum 2; `MET-DIFFERENTLY` count re-verified = 77 → explicit "no change" | ✅ |
| backlog mirror consistent | `build_backlog_md.py --check` → matches | ✅ |
| new behaviour pinned by a failing test | `test_deferral_homes_parses_owed_rows` + `test_every_owed_deferral_cites_a_bl_home` | ✅ |
| verification green | `SIG_REQUIRE_DB_TESTS=1 make check` 2977/2/0; `docs-check` 0; `check-build-memory` 0; `check_spec_src` 0 | ✅ |

## Progress

- [x] Phase 0: branch `devin/p24-8-readiness-delta` off `919bde0`; context loaded
- [x] Phase 2: DEFERRALS cites + BACKLOG flips + readiness + capstone addendum
- [x] Phase 3: self-review (two hedge cells replaced with computed counts; date/env-clock noted; scheduling row reworded — BL-023 stays open)
- [x] Phase 4: gap analysis
- [x] Phase 5: verification
- [ ] Phase 6: finalize (commit ×2, PR, ledger close)

## Gap table

| requirement | status | evidence |
|---|---|---|
| refresh OPERATIONAL_READINESS.md + BACKLOG.csv vs current DEFERRALS + open findings | MET | doc rewritten for the Round 3–4 delta (121 sources / 6 permitted+loadable; 6 new connector rows; 4 new capability rows; 13-step critical path with `ticket:`+`proof:`; updated HG checklist + cost posture); `D-*`→`BL-*` map in the doc; BACKLOG.csv flips (BL-010 accepted; BL-011/012 closed-by:P20.2; BL-006/025/026/051 → P25+; BL-023/037 titles annotated); `BACKLOG.md` regenerated |
| every OPEN/PARTIAL deferral in exactly one BACKLOG source cell | MET (deterministic) | 9 rows annotated `(cites BL-nnn)`; `check_backlog.py` extended — `deferral homes: 22/22`, enforced in `make check` via `test_check_backlog_exits_zero` + 2 new tests |
| every OPEN FINDING in exactly one BACKLOG source cell | MET (vacuous) | 0 open findings — CI-RED-01 + DOCKER-DOWN closed P24.4, MATRIX-INT-01 closed P24.5, P17-FLIP-01 retired P24.7 |
| every new ADR revisit trigger in exactly one source cell | MET | 79/79 — ADR-079→BL-053, ADR-080→BL-054 (homed by their own tickets) |
| append new accepted deviation or explicit no-change | MET | CAPSTONE_CLOSURE addendum 2 — no new deviation; 77 unchanged; GATE-ACCEPT signs 77 rows (76 + addendum row) |
| `check_backlog.py` green | MET | exit 0: 102/102, 79/79, 90/90, 22/22 homes, 0 dup |
| ticket:+proof: on every new step; no TBD | MET | §(c) steps 1–13; `grep -ci TBD` = 0 |
| requirement ids stamped in PR | MET | `docs/build/pr/P24.8.md` — GL-META-00/01 direct + all GL-* delta areas |
| DEFERRALS rows for non-automatically-verifiable items | MET (none needed) | every deliverable is deterministically checked (checker + tests); no new obligation opened |
| ADRs for owned decisions | MET (none needed) | pure reconcile; no owned decision |
| BUILD_INDEX + LEDGER advanced | MET | row 86; `lastCompleted: P24.8`, `nextTicket: P24.9`, `chainTip: devin/p24-8-readiness-delta` (commit 2) |

## Evidence log

- `python3 docs/build/tools/check_backlog.py` (base) → exit 0 (102/102 risk,
  79/79 ADR, 90/90 LD, 0 dup)
- `python3 docs/build/tools/check_spec_src.py` → exit 0 (79 ADRs == docs/adr/
  file set; 671 ids; byte-identical) — proves BL-011's obligation closed
- `grep not_researched docs/2_canonical_design_spec.md` → §32.1 vocabulary row +
  §32.2 prose (A2 applied by P20.2) — proves BL-012's obligation closed
- `uv run sig-connectors validate` → 121 sources / 95 UNDETERMINED /
  6 permitted / 6 loadable (osm_overpass, deflock_repo, okc_procurement,
  okc_council, okcpd_policy, ok_statute)
- `grep -c MET-DIFFERENTLY docs/build/COVERAGE_MATRIX.csv` → 77 (unchanged since
  the capstone §(b) addendum; no new accepted deviation from Rounds 3–4)
- `python3 docs/build/tools/check_backlog.py` (final) → exit 0 — risk 102/102,
  ADR 79/79, LD 90/90, **deferral homes 22/22**, dup 0 (the new fifth count line;
  `deferral_homes()` parses DEFERRALS rows — OPEN/PARTIAL must name ≥1 BL home)
- `uv run pytest tests/unit/test_backlog_housekeeping.py` → **12 passed**
  (2 new: `test_deferral_homes_parses_owed_rows`,
  `test_every_owed_deferral_cites_a_bl_home` — cited homes must be real BL rows)
- `uv run ruff check/format --check` on both touched Python files → clean
- `SIG_REQUIRE_DB_TESTS=1 make check` → **2977 passed / 2 skipped / 0 failed**
  (skips = env-gated live-api + unarmed GCP leak check — both pre-existing)
- `make docs-check` → exit 0 (0 broken refs; 1 pre-existing stale suspect:
  README.md vs run_okc.sh — not touched by this ticket)
- `bash scripts/docs/check-build-memory.sh .` → exit 0 (0 violations; 1
  pre-existing BM-ADR-04 warning — same as base)
- `python3 docs/build/tools/check_spec_src.py` → exit 0 (79 ADRs == file set;
  671 ids; byte-identical — spec_src untouched)
- `python3 docs/build/tools/build_backlog_md.py --check` → BACKLOG.md matches
- `python3 docs/build/tools/check_coverage_matrix.py` → `671 rows OK`
- `grep -ci TBD docs/build/OPERATIONAL_READINESS.md` → 0

## Evidence report

See `docs/build/pr/P24.8.md` for the stamped requirement ids + acceptance table.
Environment clock reads 2026-09-14; chain date for the row = 2026-09-13 (the
prompt-anchored manifest convention; the France acceptance artifact committed
here is named `acceptance_2026-09-14.json` — it is the P24.6 run's evidence left
untracked, now committed as the §(c) step-11 proof).
