# Run ledger — P31.10 (DEEPEN.6) camera-site review surface over PG

- **Spec:** `docs/tickets/P31.10__camera-site-review-surface.md`
- **Base:** `devin/p31-9-negative-space-peer-classes` (P31.9 head `3357f8e`)
- **Branch:** `devin/p31-10-camera-site-review-surface`
- **Config:** implement-spec full rigor; `live_verification=true`.
- **Gate:** engineering (Wave B seeded 2026-09-25). **Re-scoped at the seed:**
  P31.17 dropped; this ticket is standalone **tooling** for the Round-10 human
  review — it runs **no** review campaign and needs **no** decisions.
  **D-P30.2b-1** stays OPEN (engineering intake half only);
  **D-R6.1-EVAL** stays OPEN; the public PROVISIONAL disclosure stays.
- **Hosted evidence:** `docs/build/reports/p31.10-hosted/` — VERIFICATION below.

## Deferrals read

- **D-P30.2b-1** — this ticket owns the engineering *intake* half only: a
  PG-backed review surface over the ~11.6k camera-site proposals, the stratified
  sampler, and the append-only decision write path. Closure still needs real
  Round-10 human decisions feeding clustering (P31.11) — row stays **OPEN**
  with a note appended.
- **D-R6.1-EVAL** — OPEN, untouched (no eval closure claimed or attempted).

## Requirements (from the spec)

1. **PG-backed queue.** With a DSN, `/review-queue` reads PG `review_item`
   (filterable by the `er_match:camera_site:` / `er_match:camera_site_disputed:`
   prefixes, tier, and bucket; `er_match:identity_duplicate:*` excluded from the
   camera-site queue); `decide` writes through `PgReviewQueue.decide` with the
   pseudonymous curator id; `accept`/`reject` only reach PG — "unsure"/defer
   writes **no** `review_decision`. Demo seed stays the no-DSN default.
2. **Evidence view.** Both observations per pair (source, coordinates at the
   curator's §19.4 tier, external refs, operator, description), the distance,
   the matcher tier and score, and any soft conflict. No raw person data.
3. **Campaign sampler.** `sig-resolution review sample --dsn … --strata …
   --n …` — stratified, seeded, reproducible; spans 1g/3g/4g/5g + soft-conflict
   + disputed; auto-write tiers included on purpose; campaign-tagged
   (append-only); default ≈400 pairs (§6 Q11) as the Round-10 design.
4. **Offline path.** `review export` / `review import` — JSONL labelling
   sessions writing the same append-only decisions.
5. **Tests.** PG decide → `review_decision` row w/ curator id; demo path
   unchanged w/o DSN; sampler reproducible per seed; zero-JS public pages
   unaffected; `/curate/**` within ADR-068.

## Acceptance criteria

- [x] decisions via the curation app land as PG `review_decision` rows with the
      pseudonymous curator id (real PG — `tests/db/test_camera_site_review.py`)
- [x] the stratified sampler is reproducible and campaign-tagged (append-only;
      hosted re-draw `+0`, digest-pinned)
- [x] hosted: sampler runs read-only (strata counts recorded); a default-size
      campaign materialized append-only, labelled prepared-for-Round-10;
      loopback app opened against the hosted DSN (walkthrough below).
      **No decisions made** — camera-site `review_decision` count stayed 0
- [x] `npm --prefix web run check` + `make check` + `make test-db` green;
      D-P30.2b-1 note appended (stays OPEN); D-R6.1-EVAL untouched (OPEN)

## Design decisions (recorded)

- **Campaign tag = new append-only tables** `review_campaign` +
  `review_campaign_item` (sqitch change `review_campaign`): `review_item`
  payloads are immutable (insert-only), so campaign membership needs its own
  append-only relation. Same shape as the other materialize artifacts:
  deterministic ids, `ON CONFLICT DO NOTHING` (+0 re-run), no UPDATE/DELETE
  grants.
- **Strata are predicates, not a partition.** `disputed` = the
  `camera_site_disputed:` prefix OR `active_learning:adjudicator_disagreement`;
  `soft-conflict` = `reason LIKE 'soft_conflict:%'`; tier strata = payload
  `tier` — a soft-conflicted 3g item belongs to *both* `3g` and `soft-conflict`
  (tier-3 proposals are exactly the soft-conflicted ones, and the audit intent
  is "humans see every stratum"). The campaign assigns each drawn item to the
  first stratum (spec order) that drew it.
- **Curator tier → geo tier (§19.4):** anonymous/registered → tier 2 (1 km
  grid), trusted_reviewer → tier 1 (2 dp truncation), curator/maintainer →
  tier 0 (full precision — the claims are sensitivity_tier 0, published C1 =
  "exact", and these tiers hold SENSITIVITY_CLASSIFICATION). The view records
  the applied tier so the reduction is auditable.

## Verification

### Local gates

- `make check` — **4260 passed, 3 skipped, 1 warning** (ruff + format + mypy +
  full pytest + `verify-gen` clean). Skips pre-existing/environment-dependent
  (live-API staging URL unset, `SIG_GCP_PROJECT` unset, no `SIG_*` creds).
- `make test-db` — **270 passed** (incl. `tests/db/test_camera_site_review.py`
  8/8: real-PG decide → `review_decision` row with the pseudonymous curator
  handle; defer/unsure write nothing; stratum/bucket filters; campaign
  append-only + idempotent re-draw; read-role INSERT refused).
- `tests/api` — **149 passed** (`test_curation_pg.py`: PG-mode queue filters,
  evidence view with per-tier coordinate reduction, demo path unchanged
  without a DSN, route absence without `SIG_CURATION_ENABLED`).
- `tests/resolution/test_review_sampler.py` — **5 passed** (strata mapping,
  seeded reproducibility, JSONL round trip).
- `npm --prefix web run check` — **green** (typecheck 0 errors; unit; build;
  licenses; **246 e2e passed** incl. `curate.nojs.spec.ts` — public zero-JS
  and `/curate/**` unchanged).

### Hosted (project `zeta-medley-508121-u7`, Cloud SQL proxy → `sig-pg`)

Pre-deploy read-only snapshot (existing state):

| metric | value |
|---|---|
| `er_match:camera_site:` pending | 11,591 |
| `er_match:camera_site_disputed:` pending | 34 |
| `er_match:identity_duplicate:` (P31.3 family, excluded) | 3 |
| pending strata | 1g=1, 4g=1,552, 5g=9,970, soft-conflict=25, disputed=77 (total 11,625) |
| camera-site `review_decision` rows | 0 |
| `review_campaign` | did not exist yet |

- `sqitch deploy --verify` (Docker `sqitch/sqitch`, `sig` owner, proxy
  `:5433`): `+ review_campaign .. ok` — the only pending change.
- Sampler dry-run, `SET ROLE sig_materialize`, seed `round10-v1`, `n=400`:
  drawn **1g=1, 3g=0, 4g=242, 5g=66, soft-conflict=25, disputed=66 → 400
  unique**; design digest `b090bc07…fda`.
- Campaign materialized append-only under the least-privilege role:
  `camsite-r10-seed-round10-v1` (purpose "prepared for Round 10 (P31.10
  tooling; the human review campaign is Round 10)", created_by
  `sig-resolution review sample`). Re-run: `items_inserted=0,
  items_skipped_existing=400` — idempotent.
- SQL verification: 400 campaign items, per-stratum counts match the draw;
  **0** `identity_duplicate` items in the campaign; **0** camera-site
  decisions; `sig_read_public` can SELECT both campaign tables but INSERT is
  refused (`InsufficientPrivilege`).
- **Loopback app walkthrough** (`sig-api serve-curation --dsn … --role
  sig_materialize`, 127.0.0.1, demo token store): unauthenticated → **401**;
  `GET /v1/curation/review-queue?bucket=soft-conflict` → pending rows with
  stratum labels; `GET …/{item_id}` → the two-observation evidence view (OR
  DOT vs WA DOT cameras 0.0 m apart — the `soft_conflict:jurisdiction` case),
  `geo_tier` per observation; trusted-reviewer token gets §19.4 tier-1
  coordinates (`45.53, -122.59` — 2 dp) vs curator tier-0 full precision;
  HTML pages render (queue filtered by `?campaign=…`, item evidence table);
  `POST decide` with `unsure` → `{"deferred": true, "review_decision": null}`
  and bogus value → 400. **Post-check: camera-site decisions still 0, total
  `review_decision` still 3 (the P31.3 rows).**

### What this ticket deliberately does NOT do

- Runs no review campaign; creates no human decisions.
- Does not wire decisions into `camera_site_match` clustering (P31.11).
- Does not close **D-P30.2b-1** (engineering intake only) or **D-R6.1-EVAL**;
  the public PROVISIONAL disclosure is unchanged; `/curate/**` stays the only
  interactive island, authenticated + loopback-only.
