# Rights-review packet — `madada` (Ma Dada — registre des demandes CADA)

> Facts (quoted terms + retrieval date) are separated from judgement (the Decision
> line). This packet asserts no legal conclusion (defining standard §3.1). Reading the
> terms page is permitted research, not ingestion; no source *content* was fetched.
> Authored by P24.6 (JURIS.2) as the second jurisdiction's rights artifact — the
> packet is the preparatory step; the flip is the HG-03 operator gate.

- **Source id:** `madada`
- **Homepage:** https://madada.fr/
- **Terms URL(s):** https://madada.fr/help/api (not fetched verbatim this pass — recorded for the reviewer)
- **robots.txt:** honor.

## Terms (verbatim)

> UNDETERMINED — no resolved rights block in the registry (SIG-LIC-004; fails the
> export gate closed). Ma Dada is a civic request-for-access (CADA) register run
> as a public-interest project; its content licence and API terms were not fetched
> verbatim this pass. The registry additionally records
> `compact_status = not_contacted` — the loader gate refuses even a fixture/shadow
> run on that basis, which P24.6 records as honest gate evidence (the source was
> never contacted). The reviewer should confirm terms *and* compact posture
> before any flip.

## SPDX candidate

`UNDETERMINED` — the site's licence terms are unresolved; the reviewer assigns an
accepted expression after fetching the terms page verbatim.

## redistributable analysis

Not asserted (UNDETERMINED). A request register may carry requester-authored
text; republication needs a real rights review (SIG-LIC-003).

## derivative_permitted analysis

Not asserted (UNDETERMINED). Request-text contents are user-authored —
SIG-INGEST-048b linking hazard is plausible at this posture; LINK is the safe
default until reviewed.

## ODbL compartment implications

Not OSM-derived.

## Custody posture recommendation

LINK (a public register to link out to, not re-host). `ingestion_permitted=false`
by design; `compact_status = not_contacted` stands until HG-04 outreach records
contact.

## Decision

- [ ] permit ingestion — reviewer: ____ date: ____
- SPDX to record: `UNDETERMINED`   rights_reviewed_by (role, never a name): ____   rights_reviewed_on: ____

## Counsel-needed flag

**YES — user-authored request text; licence terms unfetched; compact posture
`not_contacted` (HG-04 outreach owed before any flip).**
