<!--
  P31.18 — Resolution eval from first principles + camera-site rules v3 (Round 9 / EVAL.1).
  DRAFT on devin/round9-planning. SKELETON (BM-TICKET-03): the body depends on the human labels P31.17 produces
  (size, strata, κ), so only the header + scope + spec §§ + requirement coverage are written. There is NO run line;
  implement-spec must refuse to run it until it is fleshed out after the gate.
-->
# P31.18 — Resolution eval from first principles + rules v3 (P31)

> **MOVED TO ROUND 10 — re-confirmed at the Wave-B seed 2026-09-25 (`devin/round9-waveb-seed`).** Not seeded; manifest
> row 159 stays unused. The rewritten P31.17 (LLM first pass) was **DROPPED** by the operator on 2026-09-25, so Round 9
> runs **no** annotation or review campaign at all. Round 10 schedules the human review campaign (using the P31.10
> review surface + sampler, which Round 9 builds as tooling) and then this re-derivation. It closes D-R6.1-EVAL,
> D-P30.2b-2 and the human-decision verification of D-P30.2b-1 (P31.11 lands the wiring in Round 9).

> **MOVED TO ROUND 10 (operator ratification 2026-09-24, design §6 Q11 — CHANGED).** Human review is deferred;
> Round 9 runs only an LLM annotation first pass (`claude-opus-5-5`, P31.17 as rewritten), which is **not** human
> ground truth. This skeleton is therefore **not** a Round-9 chain row and will not be seeded with Wave B. It is kept
> here, unchanged below, as the Round-10 starting point: Round 10 schedules the human review campaign (the former
> `HUMAN-H4` marker) and then this eval re-derivation from the human labels, with a new id/row at that seed.
> The references to "P31.17 (HUMAN-H4)" below mean that Round-10 human campaign, not the rewritten P31.17.

- **Semantic id:** `EVAL.1` · **Phase:** 31 · **Kind:** skeleton · **Tag:** p31-followups · **Wave:** gated tail
- **base_branch:** (set when fleshed out; stacks on the then-current chain tip) · **Depends on:** P31.17 (HUMAN-H4 dispositioned), P31.11

> **Skeleton only — gated on `HUMAN-H4` (P31.17).** Written in full after the human review campaign is recorded.
> Do not run.

## Scope (one line)
Re-derive the auto-vs-human resolution balance from a **human** holdout: re-estimate P/R/F1 + B-cubed + LLM-vs-human κ,
re-tune `auto_write_precision_threshold` and the tier cutoffs (and consider a decision-theoretic loss where a false
merge costs more than a false split), and derive camera-site **rules v3** (name-level device-kind/place soft
conflicts) from training data measured on that fresh human holdout. Record it in a **new ADR** (ADR-R9-EVAL, never a
silent threshold edit). Drop the PROVISIONAL disclosure only if the human eval supports it.

It also runs the hosted camera-sites re-run (`materialize.sh --apply run camera-sites`, a Cloud Run job) so the
campaign's accepted decisions cluster (N drops per accept, re-run +0). That closes D-P30.2b-1 against its own
verification rule.

## Spec §§
§2.6 of `ROUND6_7_DEPTH_ACTIVATION_DESIGN.md` (first-principles re-derivation); spec §14.7 (SIG-IDENT-024/027/028/029),
§32 / SIG-METRIC-008b (published holdout metrics); ADR-099, ADR-105 §5 (holdout discipline).

## REQ coverage
Closes **D-R6.1-EVAL** (first-principles half), **D-P30.2b-2** and **D-P30.2b-1** (its human-decision verification). Touches SIG-IDENT-024/027/028/029 and SIG-METRIC-008b
(re-measured, not re-defined).
