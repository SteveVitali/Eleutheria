# P28.1 — Resolution eval report (Round 6 keystone)

> **PROVISIONAL** — the gold set is LLM-bootstrapped with a small maintainer seed and the auto-write floor is set against provisional labels (D-R6.1-EVAL, OPEN). A launch claim of "resolved sites" discloses this until the deferral closes.

## Calibration (LLM adjudicator vs maintainer seed)

- gold set version: `1`
- seed adjudicator: `seed:maintainer@sig`
- LLM adjudicator: `llm:rulebased@v1`
- Cohen's κ (LLM vs seed): **0.714** (bar ≥ 0.70)
- LLM trusted as gold: **yes**
- labelled pairs: 17; frozen holdout: 6
- disputed (adjudicators disagree) → review: 4

## Quality gates (frozen holdout)

- auto-write precision floor: **0.980**
- pairwise @ tier ≤ 3: P=1.000 R=1.000 F1=1.000 (tp=1, pp=1, ap=1)
- pairwise @ tier ≤ 4: P=0.500 R=1.000 F1=0.667 (tp=1, pp=2, ap=1)
- pairwise @ tier ≤ 5: P=0.500 R=1.000 F1=0.667 (tp=1, pp=2, ap=1)
- B-cubed cluster (holdout): P=1.000 R=0.952 F1=0.976 (tp=42, pp=42, ap=42)
- auto-write tier 0 holdout precision: 1.000

### Auto-write demotion

- tier 0: precision 1.000 vs floor 0.980 → keeps auto-writing

### Cluster-shape alerts (bad-merge signatures)

- none

## Review routing (active learning)

- pairs routed to review: **14**
- active-learning-selected (disagreement + near-boundary): 13

## Scale (materialization over the spine)

- observations considered: 230267
- resolved sites / decisions: 0
- resolution envelopes materialized: 8
- dedup ratio: 0.000

## Notes

- Methodology numbers below are reproducible from this committed bootstrap gold set (scripts/eval/run_resolution_eval.py). The write path is proven over real PG18+PostGIS in tests/db/test_resolution_materialize.py (append-only, idempotent +0).
- HOSTED SCALE (P30.2, 2026-09-24): the resolution materializer ran over the complete settled hosted spine as a Cloud Run job next to Cloud SQL (execution sig-materialize-725gc, 4m19s, role sig_materialize). It considered 1,955,004 (subject, predicate) groups over 2,280,784 tier-0 claims and materialized 8 envelopes (7 resolved, 1 unresolved_conflict — the OKC claimed_device_count 299-vs-190, kept visible); re-run +0.
- Resolved sites = 0 of 230,267 observation-level sites. This is the honest measured number, not a failure of the write path: 1,954,996 of the 1,955,004 groups carry predicates with no row in the resolver's predicate registry (camera_latitude / camera_longitude / camera_operator / external_id / ... — the camera-registry predicates that make up almost the whole spine), so no claim in them is admissible and nothing is adjudicated (SIG-RECON-013: silence in the ruleset is not resolvable). Registering those predicates is an ontology decision owned by the follow-up D-P30.2-3; until it lands the public surface keeps the observation-level framing (P28.5 degrades honestly — no fabricated resolved-site count).
- Dedup ratio 0.000 = 0 resolved sites of 230,267 observation-level sites; no entity-level merge exists on the hosted spine (entity.merged_into = 0). The methodology numbers above (kappa, floor, holdout P/R/F1) remain the PROVISIONAL LLM-bootstrap eval — the first-principles re-derivation against human ground truth is D-R6.1-EVAL's still-OPEN half.

