# Appendix F — Architecture Decision Record index

**SIG-STORE-006** requires each decision below to be written as an ADR under `docs/adr/`,
using a consistent template: context, decision, status, consequences, alternatives considered, and —
mandatory per SIG-STORE-007 — a **revisit trigger**.

This index is the **repository ADR numbering** and lists **every** `docs/adr/ADR-*.md` by its
repository number, title, and owning phase. Its single source of truth is `docs/adr/README.md`
(SIG-ENG-039): any pull request that adds an ADR MUST add its row here in the same PR, and
`docs/build/tools/check_spec_src.py` asserts the ADR-file set and this table are equal.

> **Numbering note (LD-X04 / LD-D03).** Early drafts of this appendix used a *logical* numbering
> for the eighteen §15.5 + stack decisions that does **not** match the repository ADR numbers. That
> logical scheme is retired: the repository numbering above is canonical. A few historical
> equivalences a reader may meet in older ledgers/PR bodies: logical "ADR-013 (Apache-2.0 code /
> CC-BY data / CC0 ontology, §42.2)" and logical "ADR-016 (Splink)" and logical "ADR-017 (no direct
> automated OSM writes, §35.2)" and logical "ADR-024/025 (the P06.1 count-reconciliation /
> dossier-renderer decisions)" all refer to *design points* now recorded under the repository ADRs
> that own them — e.g. Splink is repository **ADR-029** (P05.1), the no-OSM-writes decision is
> repository **ADR-055** (P16.2, which itself records the spec's Appendix-F "ADR-017"), and the
> P06.1 seeds are repository **ADR-031/032**. When in doubt, cite the repository number.

| ADR | Decision | Phase |
|---|---|---|
| ADR-001 | PostgreSQL 18 + PostGIS as the canonical store; everything else a projection | P00.2 |
| ADR-002 | Append-only claim table; entity tables hold identity only | P00.2 |
| ADR-003 | Two interval time dimensions plus one ordering scalar | P00.2 |
| ADR-004 | EDTF for uncertain dates | P00.2 |
| ADR-005 | Resolution as a stored decision record, not a view | P00.2 |
| ADR-006 | OCFL 1.1 evidence store on object storage with governance-mode Object Lock | P00.2 |
| ADR-007 | LinkML as the single ontology source of truth | P00.2 |
| ADR-008 | SKOS for published controlled vocabularies | P00.2 |
| ADR-009 | SPDX expressions for per-source licensing, with a build-time compatibility gate | P00.2 |
| ADR-010 | DuckDB/Parquet analytics boundary; no raw audit rows anywhere | P00.2 |
| ADR-011 | The ODbL posture: OSM-derived assets in a separate compartment (Strategy B) | P00.2 |
| ADR-012 | Sensitivity tiers enforced by RLS, applied at the view layer | P00.2 |
| ADR-013 | uv as the workspace and lockfile tool | P00.2 |
| ADR-014 | Astro for the public web surface | P00.2 |
| ADR-015 | AWS S3 for the evidence store and CloudFront for bulk-export delivery | P00.2 |
| ADR-016 | Dagster OSS for orchestration, kept reversible | P00.2 |
| ADR-017 | FastAPI for the read API | P00.2 |
| ADR-018 | MapLibre GL for the web map | P00.2 |
| ADR-019 | pytest + Hypothesis for testing | P00.2 |
| ADR-020 | GitHub Actions for CI | P00.2 |
| ADR-021 | Source registry as seeded data, with a runtime ingestion gate in `connectors` | P00.4 |
| ADR-022 | Defer physical partitioning of `claim` to preserve the `claim_id` FK contract | P02.1 |
| ADR-023 | An `evidence/` package, and content-addressed blob dedup for the capture row | P02.2 |
| ADR-024 | A pinned, deterministic, in-repo EDTF envelope derivation | P02.3 |
| ADR-025 | Temporal invariants as pipeline data-quality checks; as-of as SQL functions | P02.3 |
| ADR-026 | The eight-stage connector framework | P04.1 |
| ADR-027 | The `osm` connector | P04.2 |
| ADR-028 | The `atlas` connector | P04.3 |
| ADR-029 | Splink 4 on DuckDB for the probabilistic ER tiers 4–5, as a fully-specified deterministic model | P05.1 |
| ADR-030 | The review queue and the LLM-extraction scaffolding as library + CLI, in `resolution` and `parsing` | P05.2 |
| ADR-031 | A minimal count-reconciliation seed, and the three missing count predicates, for the P06.1 vertical slice | P06.1 |
| ADR-032 | A minimal slice dossier renderer, with a print-CSS PDF path, ahead of the production surface | P06.1 |
| ADR-033 | The layered document-parsing stack as the parser interface every connector extracts through, in `parsing` | P07.1 |
| ADR-034 | The `records` connector | P07.2 |
| ADR-035 | The `procurement` connector | P07.3 |
| ADR-036 | The §29 reconciliation workflows as value-object modules layered on the resolver | P08.2 |
| ADR-037 | The materialized Contradiction entity, its lifecycle, and the byte-identical L3 rebuild | P08.3 |
| ADR-038 | The coverage-metrics layer, its home in `inference`, and the executable capture–recapture prohibition | P09.1 |
| ADR-039 | The research-task engine | P10.1 |
| ADR-040 | The §33.2 detector catalog and the §31 contradiction→task map | P10.2 |
| ADR-041 | The records-request generator | P10.3 |
| ADR-042 | The `flock_portal` connector | P11.1 |
| ADR-043 | The `audit_structural` connector | P11.2 |
| ADR-044 | The usage-analytics boundary | P12.1 |
| ADR-045 | Access-path closure | P12.2 |
| ADR-046 | Policy/LegalInstrument surfaces, the never-merged invariant, and the SIG-EPIS-030 general form | P13.2 |
| ADR-047 | The public read API | P14.1 |
| ADR-048 | Bulk exports | P14.2 |
| ADR-049 | The epistemic visual language and the no-JS, archivable web shell | P15.1 |
| ADR-050 | The production local-dossier surface | P15.2 |
| ADR-051 | The infrastructure map + network explorer (with the static-PMTiles serving contract) | P15.3 |
| ADR-052 | The renewal watch, evidence recommender, and evidence viewer | P15.4 |
| ADR-053 | The research queue, public corrections log, methodology/metrics pages, and the editorial-standards conformance gate | P15.5 |
| ADR-054 | The contributor system | P16.1 |
| ADR-055 | No direct automated OSM writes (the human-mediated suggestion workflow; records the spec's logical "ADR-017", §35.2) | P16.2 |
| ADR-056 | The jurisdiction adapter framework (country-namespaced vocabularies, BCP-47 labels, `canonical_name` scalar) | P18.1 |
| ADR-057 | The France/Belgium (Technopolice) connectors | P18.2 |
| ADR-058 | Tickets and build memory are committed; agent scratch is unified | P19.1 |
| ADR-059 | Capstone spine wiring (`PgClaimSink`, `PgReadStore`, the compute-on-read seam) | P19.4 |
| ADR-060 | The resolver (P08.1) — retro-fitted record | P08.1 (retro-fitted by P19.5) |
| ADR-061 | Capstone gap closure (export gate, jurisdiction-conditional web render, ER over PostgreSQL) | P19.5 |
| ADR-062 | Spec reconciliation after the 46-ticket build (Appendix F/G, applied amendments A1–A8, fold-back ids) | P20.2 |
| ADR-063 | Registry review metadata and the flip rule (the `rights_reviewed_by`/`rights_reviewed_on`/`review_packet` fields, `review-status`, the 27 rights packets, the Stage-0 outreach record) | P21.1 |
| ADR-065 | Live connector wiring (the `HttpxTransport` over `httpx`, the `OcflCaptureStore` adapter, and the gated `sig-connectors run --mode live\|replay\|shadow` CLI that refuses a live fetch — exit 3 — for any non-green source and owns the content-free fetch-record format) | P21.3 |
| ADR-066 | Runtime composition (`sig-ops` + `ops/docker-compose.yml`) and the export-backed web data layer (`web/src/lib/data.ts`, `SIG_DATA_SOURCE=fixtures\|export`); the jurisdiction export emits `web/dossiers.json`; crosses LD-V08 | P21.4 |
| ADR-067 | Zenodo deposits (`ZenodoHttpTransport`/`sig-exports deposit`, dry-run default, sandbox gated on HG-07), object store = Cloudflare R2 with S3/CloudFront (ADR-015) the documented alternative, content-hash push + `sig-ops egress-report` + `.torrent`, tile generation (tippecanoe else pure-Python PMTiles, ODbL metadata preserved) closing LD-F07/LD-H08, mirrors + Software Heritage (`ops/mirrors.toml`, `sig-ops swh-save`), and the tested degraded mode + monthly keepalive; A1 ticked → no MapLibre island | P21.5 |
| ADR-068 | The authenticated curation service (`api/src/api/curation.py`, `create_curation_app`/`sig-api serve-curation`, mounted only when `SIG_CURATION_ENABLED=1`, second loopback-bound `api-curation` process — never the public API, Part VIII §0.7) and the `/curate/**` web surface (progressively-enhanced no-JS forms, WCAG 2.2 AA, `/curate/**` lighthouse block); bearer-token auth bound to the P16.1 contributor tiers with per-route tier checks; append-only human-attributed writes via the JSONL curation log + `PgReviewQueue`/`review_decision` (ER decisions) and compute-on-read dispositions (P21.2 shrunk → no new persistence); machine suggestions labelled, never auto-applied (amends ADR-030); closes LD-F05/LD-H04; RISK-P21-10/11 | P21.6 |
| ADR-069 | Contribution-back **live** (amends ADR-055): the MapRoulette client (`tasks/src/tasks/maproulette.py`, `cooperativeType=tags` payloads matching API v2, `checkinComment` carrying the changeset hashtag + `#sig-<jurisdiction>`, dry-run without `SIG_MAPROULETTE_API_KEY`); sensitive tiers (`geo_tier>=3`, C3/C4/C5) **excluded** from any push (RISK-P21-12); the **registration gate** `ops/config.toml [tasks.contribution] registered` read by `tasks.contribution.contribution_registered()` (fails closed) → `sig-tasks maproulette push` **refuses exit 3** while false (RISK-P16-14); the OSM changeset feed (`tasks/src/tasks/osm_feed.py`) reading id + `comment` **only** (never `user`/`uid`, Part VIII §0.7), attributing hashtag-bearing changesets to `LeverageLedger` (append-only, idempotent), fixtures-replay default (no live poll, HG-08); the §7 metric web page (`web/src/pages/contribution-back.astro`) via `data.ts` `getLeverageMetric()` fixtures/export; and the opt-in **aggregate-only** onboarding timing (`tasks.onboarding.OnboardingTimingAggregate`, count+median, no per-user rows) with the moderated study protocol landed but **not yet run** (HG-10); closes LD-F11 (gate-pending HG-08), LD-P03/LD-F10-usability (gate-pending HG-10) | P21.7 |
| ADR-070 | The Data Driven release model and the coarse-international capture path: the first-class `data_driven` connector (`connectors/src/connectors/data_driven.py`) for EFF/MuckRock's Data Driven releases (§23.9) — the **release is the unit of ingestion**, pinned by a content digest per version + retrieval date (`ReleaseManifest`, SIG-INGEST-043b), so a versioned re-ingest yields new dated claims and never overwrites (SIG-INGEST-043d/017); **per-agency AGGREGATE rows only** enforced at the ingest boundary (`assert_aggregate_only`, `PerSearchColumnError`, RISK-P0-08/RISK-P21-15) plus a predicate allowlist; **sharing degree only, never the edge list** (SIG-INGEST-043c); per-column retention windows preserved in incommensurable units (SIG-INGEST-043d); agency crosswalk through the P03.2 cascade, tier-labelled (§14.6); records-request linkage to `connectors.records.RecordsRequest` (SIG-INGEST-044); every claim historical. Registered in `list-connectors`; two release fixtures (v1,v2) prove versioning; live refuses exit 3 (HG-03 pending). The coarse-international REFERENCE-capture path (`connectors.coarse_international.CoarseInternationalConnector`) routes every row through `coarse_claim` (anti-disaggregation, SIG-INGEST-042), keeps LINK posture, runs replay/shadow over fixtures. `eff_data_driven` → MIRROR candidate, rights UNDETERMINED; packets for it + the coarse trio; RISK-P21-14/15; closes BL-042 (gate-pending HG-03) | P21.8 |
| ADR-071 | The `pathways` connector (one connector + three extractors: `rtcc_federation`/`fr_css_forensics`/`acoustic_drone_location`), the ADR-033-deferred parser layers realised in `parsing/` (`parsing.tables` layer-4 `pdf_table`, `parsing.clauses` layer-3 `pdf_text`, `parsing.genre` document-genre classification surfaced by `sig-parsing classify`, LD-F17), and the **procured≠deployed** epistemic guard (RISK-P21-16, §46): a `deployment` claim requires a deployment-genre document, enforced by a genre-level guard (`DeploymentInferenceError`) + a predicate-level guard with the genre re-derived from the document text; every claim typed/evidenced/dated; coverage is a test (`tests/connectors/test_pathway_coverage.py` → `docs/build/STAGE5_CONNECTORS.md`) retiring RISK-P17-03; three LINK-posture sources rights UNDETERMINED / live REFUSES exit 3; closes BL-043 (gate-pending HG-03) | P21.9 |
| ADR-072 | Documentation freshness gates: two vendored detectors under `scripts/docs/` (`check-repo-docs-freshness.sh` from P22.1 + `check-agent-docs-freshness.sh`), `make docs-check` runs both structural read-only checks over the whole repo, and a CI `docs-check` step wires them into the gate (SIG-ENG-001/015/016/031); operator chose REFRESH (keep P19.1's `AGENTS.md`) over clean-slate; RISK-P22-01/02/03/04 | P22.2 |
| ADR-073 | Build memory is committed under `docs/build/` (build-memory v2 layout: `LEDGER.md`, `runs/<ID>.md`, `pr/<ID>.md`, generated ADR index, `docs/tickets/DEFERRALS.md`); `.agents/scratch/` retired (move/rename only, byte-identical, mapping in `docs/build/README.md`); the layout validated by `scripts/docs/check-build-memory.sh` in `make docs-check`; supersedes ADR-058 §3 (the gitignored-scratch home for the machine ledger) — ADR-058 body unedited (SIG-ENG-003), the generated index shows both; SIG-ENG-001/003/031 | P22.3 |
| ADR-075 | GCP hosted deployment (P24.1 / DEPLOY.1 / GL-DEPLOY-01): the DECISION is a single always-free `e2-micro` GCE running the `ops/docker-compose.yml` PG18+PostGIS+API stack (vs the smallest Cloud SQL tier, kept documented — the free tier wins under the binding zero-cost posture SIG-STORE-003); the read API on Cloud Run (`min-instances 0`, scales to zero, managed TLS), the static site + **published** export compartment on public-read GCS (`…-sig-web`/`…-sig-public`) with the non-published compartments + backups PRIVATE (`…-sig-restricted`/`…-sig-backups`, §42/Part VIII), secrets from Secret Manager by name only (HG-09); the IaC form is idempotent `gcloud` scripts (`ops/gcp/{config,lib,provision,backup}.sh`) with a `--check`/dry-run path that validates green with NO ADC (terraform not on PATH); `sig-ops deploy --target gcp` builds+pushes the API image (Artifact Registry) + syncs `web/dist`/exports to GCS, dry-run/plan when ADC absent; `pg_dump`→private GCS backup bucket + OCFL sync with the restore drill proven for real locally over Docker (`ops/src/ops/backup.py`, `sig-ops backup-drill`, `tests/db/test_restore_drill.py`) — the real `apply`/deploy + cloud restore gate-pending (`D-DEPLOY.1-1`/`D-ACCT.1-1`, HG-12); no secret/token literal, project id is `$SIG_GCP_PROJECT` (D3) | P24.1 |
| ADR-074 | The three OKC document connectors (`okc_procurement`/`okcpd_policy`/`ok_statute`, LIVE.1a/GL-LIVE-01): one shared `OkcDocumentConnector` base (`connectors/src/connectors/okc_documents.py`) + three registered subclasses, reusing the ADR-033-deferred sig-parsing layers (`parsing.tables` layer-4 `pdf_table` for the procurement table, `parsing.clauses` layer-3 `pdf_text` for policy/statute clauses, `parsing.genre` re-derived from the document text — no relabelling); the **Part VIII guard** in the single `okc_claim` builder — no plate/trip/per-person/per-search data (`assert_part_viii_safe`, predicate+raw-value token scan + categorical exclusion, §0.7/§43.2, RISK-P0-08), the officer-naming gate (`assert_person_naming_permitted`, §43.4 five-prong + two-reviewer, default no-publish), and the sensitivity tier (C1 institutional, §43.3) stamped on every claim; per-connector predicate allowlist as a hard schema gate (SIG-INGEST-033); the three RIGHTS.1-green sources fetch → capture (OCFL, P21.3) → parse → emit the P06.1 fixture-encoded claims, proven by shadow replay diff = 0 (SIG-INGEST-019) over committed fixtures; the real live smoke is deferred (HG-09, `D-LIVE.1a-1`); closes BL-023/BL-024/BL-026 | P23.5 |
| ADR-076 | Re-ingest cadence (P24.2 / SCHED.1 / GL-SCHED-01): the scheduler form is a **minimal GitHub Actions cron** (`.github/workflows/reingest.yml`) driving `sig-connectors run` per due source — **not** Prefect/Dagster (the `orchestration/` Dagster seam stays reserved + reversible, SIG-INGEST-021), free-tier under the binding zero-cost posture (SIG-STORE-003, reuses the ADR-067 keepalive shape); **cadence vs etiquette is two layers** (`orchestration.cadence`): coarse = the per-source re-ingest **interval** (`reingest_interval_days` from the registry `cadence`, default 30 days, clamped to a `MIN_INTERVAL_DAYS=1` etiquette floor; `sig-orchestration due` lists due sources), fine = request pacing by the shared `PoliteFetcher`/`HttpxTransport` (crawl-delay, 429/503/504 back-off, Overpass etiquette, RISK-P21-04/RISK-P24-01) which the scheduler never bypasses (a non-green source is refused exit 3 before any socket, HG-03); **freshness is an append-only JSONL ledger** (`FreshnessLedger` under `docs/build/freshness/`, a dated `FreshnessRecord` per tick, never rewritten) and a changed-upstream re-ingest produces **new dated claims** (the data_driven release model, SIG-INGEST-043d/017) that never overwrite prior claims (P1–P3); **disappearance rides the sweep cadence** (`disappearance_sweep_days`→`evidence.disappearance.sweep_cadence_days`, `detect_disappearance` reuses `connectors.disappearance` to emit the event + research task, SIG-EVID-013/014/015); builds on ADR-065, no duplication; RISK-P24-01 | P24.2 |
| ADR-077 | Observability & alerting (P24.3 / OBS.1 / GL-OBS-01): the notifier seam is **record-first, notify-second** — every alert appends to an append-only recorded-alert JSONL ledger (`.sig/ops/alerts.jsonl`, env `SIG_ALERT_LOG`) before any external delivery, so the deterministic artifact never depends on network or credentials; notifiers = always-on `SIG-ALERT` log line + an **env-configured webhook** (`SIG_ALERT_WEBHOOK_URL`/`SIG_ALERT_WEBHOOK_TOKEN`, HG-09, no paid service, no credential literals, the URL itself never logged); INFRA.1`'`s egress alarm is **consumed** (`sig-ops egress-report --alert` fires on warn/alarm; `ops.egress.build_report` unchanged) not re-implemented; **metrics** = a bounded JSONL probe log (`sig-ops probe`, one ok/latency row per service via the single `observe.http_ok`/`pg_ready` the CLI now reuses); **uptime + error budgets** = `compute_uptime` over the rolling `[observability]` window (`uptime_target_pct=99.0`, `window_days=30`, no-data never fabricated 100%); **bounded log retention** = `prune_jsonl` age+byte caps on a file, not a paid log service; **secret hygiene is structural** (`scrub_secrets` masks env-secret values in every emitted surface); keepalive verification = `verify_keepalive` (workflow intact + real degraded rebuild) firing a recorded critical alert, and `keepalive.yml`/`observability.yml` wire both alerts into the free cron; dashboard = a markdown readout (`sig-ops dashboard`, committed at `docs/build/reports/observability-dashboard.md`); RISK-P24-02 | P24.3 |
| ADR-078 | CI composed hardening (P24.4 / CI.1 / GL-CI-01): a dedicated `composed` CI job runs `tests/e2e` for real with Node + Docker present (`SIG_REQUIRE_DB_TESTS=1`, S8/LD-V08 web build runs — closes CI-RED-01 honestly); scanning is three zero-cost gates — a stdlib tracked-file **secret scan** (superset of the build-memory shape set + `SIG_*` credential literals; never echoes the match), a stdlib **dependency licence scan** mirroring the web gate's excluded categories + review-required on unresolvable + a frozen expected-strong-copyleft set (`docutils`/`igraph`/`rfc3987`; a new GPL/AGPL dep breaks the gate), and a **pip-audit** OSV dependency audit (network — rides the nightly, not the PR gate); `nightly.yml` (03:00 UTC + `workflow_dispatch`) runs composed+scans with `continue-on-error` stages and a report step that IS the gate (`nightly_report.py` writes `nightly-report.md`, uploads `if: always()`, exits non-zero on any failed stage); `make docs-check` + `check-build-memory.sh` enforced on PRs as named `docs`-job steps; the audit's first run surfaced real starlette-0.48.0 CVEs → `fastapi>=0.141,<0.142` + explicit `starlette>=1.3.1` floor (fixed, not allowlisted); RISK-P24-03 | P24.4 |
| ADR-079 | The second jurisdiction (P24.6 / JURIS.2 / GL-JURIS-01): France — Commune de Gex (Ain), vidéoprotection — recorded as the build-time candidate (operator selection deferred, non-blocking); the design's proof, not a copy (arrêté préfectoral authorization, DECP national procurement, fr.cada records — never us.foia). The P18.1 adapter framework reused, not rewritten: `ops.seed` gains a `_SEED_SLICES` dispatch (okc/france — the okc path byte-identical) loading the France slice (france-tokened subjects; resolver-registered predicates for material facts; the §11.14 surface carried for provenance; ODbL-1.0 arrêté claims + UNDETERMINED DECP claims — spine yes, export no); `CONNECTOR_FOR_SOURCE` gains the four france_belgium source ids + committed fixtures (`tests/connectors/fixtures/france/`); `madada` recorded REFUSED at the loader gate (compact not_contacted — the gate working); `_jurisdiction_request`/`build_web_dossiers` dispatch france — `legal_instruments` in the ODbL compartment, DECP content absent as a recorded `UNRESOLVED` gap (§42 fail-closed exercised, never bypassed); `_france_dossier` (FR, `lang=fr`, 12 sections) flags the signing-officer row `isPublicEmployeeName`+`originJurisdiction:FR` — withheld under FR-GDPR (SIG-PUB-017) where the US dossier publishes it; France's own acceptance module `live_api_france.py` (FR-1..FR-10 + arrêté→dossier traversal) and `docs/build/tools/run_france.sh` (the eight-stage run_okc.sh template); rights packets `decp_fr`/`madada` (ingestion_permitted=false, provided:no, LO 2.0 SPDX mapping flagged counsel-needed); HG-03/04 flips + fetches deferred D-JURIS.2-1/-2; RISK-P24-04 | P24.6 |
| ADR-080 | The `government_mandated_disclosure` connector (P24.7 / CCOPS.1 / GL-CCOPS-01; SIG-INGEST-049*): municipal surveillance-ordinance (CCOPS) disclosures registered as **their own source class** — licence public record, format pdf, document pipeline, distinct from `civil_society_dataset`/`vendor_portal` — with the honest depth-not-breadth scope carried as data (~26 jurisdictions, ~5% population, ~0.14% of agencies, ZERO machine-readable outputs, the canonical national list a JPEG; `registry_row_cap: 26`). **One connector, three paying source adapters** (SIG-INGEST-049c; the ADR-071 precedent): `field_questionnaire` (Seattle SMC 14.18 ~50-field SIR), `policy_clauses` (NYC POST Act — layer-3 `pdf_text` clause locator), `inventory_table` (SF Chapter 19B biannual inventory + compliance metric). **Per-agency AGGREGATE rows only** (Part VIII): `assert_aggregate_row` requires `agency`+`reporting_period`+`technology` and refuses forbidden columns/tokens/person-naming — schema-enforced, never a policy note. **`procured≠deployed` is a two-layer guard** (RISK-P21-16 applied to the class): use predicates ride only on `disclosure_use` claims; `disclosure_use`/`compliance_finding` claims require a document that itself reports use (deployment genre, or the `reports_actual_use` mandated-disclosure override — never a procurement/vendor genre however labelled; genre re-derived from text). **Mandated≠populated** is recorded (`answered`/`present_but_empty`/`absent` → `disclosure_field_state` claims, never fabricated values). The non-compliance finding is a first-class `compliance_finding` claim (SIG-INGEST-049e). Sources stay `ingestion_permitted=false`/UNDETERMINED (HG-03); live run refuses exit 3; shadow over committed fixtures diff=0; the NCSL state-ALPR seed labelled `as_of: 2022-02-03` (049f) + the future-due 2027 row (050) land honestly; HG-03/04 flips+fetches+outreach deferred D-CCOPS.1-1/-2; RISK-P24-05 | P24.7 |
| ADR-081 | The real GCP deployment (P24.1 re-run / DEPLOY.1 / GL-DEPLOY-01, `live_verification=true` under operator ADC, HG-12): realize the hosted stack as **Cloud SQL + Cloud Run** — the ADR-075 documented alternative promoted to the executed decision (ADR-075 body unedited, SIG-ENG-003; its e2-micro path stays the zero-cost option) because Cloud Run→private-GCE needs a same-priced (~$9/mo) VPC connector `provision.sh` never wired and COS+compose+sqitch is blind-debugged over SSH. Postgres+PostGIS = Cloud SQL (`sig-pg`, POSTGRES_18, db-f1-micro, us-central1) reached by Cloud Run via the **native connector** (`--add-cloudsql-instances`, `/cloudsql/<conn>` socket) — no VPC connector/VM/startup-script; managed backups. Read API = Cloud Run (`sig-api`, min-instances 0, managed TLS), image `linux/amd64`→Artifact Registry, PG password from Secret Manager (`sig-pg-password`) injected + DSN assembled at runtime (never in config, HG-09), API `SET ROLE sig_read_public` (tier-0 only, RLS binds). **Finish-line A = access-restricted:** Cloud Run `--no-allow-unauthenticated`, all four GCS buckets private (public-read held for Go-public/HG-11+counsel). Three gap-closures landed as code: Dockerfile copies the full uv workspace + assembles the DSN; the `read_surface_grants` sqitch change grants SELECT on the full §37 read surface to `sig_read_public`/`sig_export` (tier ceilings still bind, append-only unaffected). Verified: 20-migration schema + read_surface_grants on Cloud SQL, OKC seed (5 claims/1 entity), API serves search/dossier/contradiction (299-vs-190 preserved). Cost ~$9/mo (conscious SIG-STORE-003 departure). `D-DEPLOY.1-1` → PARTIAL (apply/deploy done; cloud restore drill the remainder); runbook `docs/build/reports/GCP_DEPLOYMENT.md` | P24.1 |
| ADR-082 | Live fetch targets as a declarative table (P24.1 live deploy follow-on; closes the P21.3 deliverable-4 target gap): the live runner shipped a placeholder target (`https://<source>/x`) and never populated `ctx.parameters["targets"]`, so a green source fetched nothing. Add `connectors/src/connectors/data/live_targets.toml` (one row per source, read by `connectors.live_targets.live_targets()`, targets are DATA not code, SIG-ENG-001) — `_run_live` loads it + passes `{"targets": …}`; a green source with NO row raises `NoLiveTargets` (CLI exit 4), never a placeholder fetch; endpoints/tokens from env (HG-09). First row `osm_overpass` (OKC-bbox `man_made=surveillance` Overpass query; response matches the committed fixture shape so the parser is unchanged; ODbL compartment). **Finding (recorded, not bypassed):** a real `osm_overpass --mode live` is refused by SIG's own conduct layer — `overpass-api.de` robots.txt disallows `/api/` and `PoliteFetcher` honours robots/no-circumvention (SIG-INGEST-037) — so the OSM fetch is blocked on a policy/endpoint decision (permitting endpoint via `$SIG_OVERPASS_ENDPOINT`, or a documented rate-limited-API rights determination), opened as P25.1 along with document-connector content-drift hardening; tests `tests/connectors/test_live_targets.py` (network-free) | P24.1 |
| ADR-083 | Crawler conduct — documented API access is not crawling (operator determination 2026-09-15, implemented by P25.1): SIG binds itself to honour robots.txt (SIG-INGEST-037), but that convention targets **crawlers**; a real `osm_overpass` live fetch was refused because `overpass-api.de` robots disallows `/api/` (ADR-082). DECISION: an **auditable, allow-listed carve-out** — robots governs CRAWL (default, unchanged, no circumvention); documented, rate-limited access to a **named allow-listed host** under its ToS is **API mode** (robots not applied to that documented endpoint; rate-limit/backoff/no-circumvention still apply; the allow-list decision stamped in the fetch record). The allow-list (`policy.crawler`/`data/api_allowlist.toml`) names host + endpoint + ToS basis + rate limit + `counsel_reviewed: no`; a host off it stays CRAWL — **not** a blanket bypass. First entries: overpass-api.de, catalog.data.gov/api.data.gov, www.muckrock.com, api.usaspending.gov, the CivicClerk tenant host. Firm decision (not pending-counsel) resting on the standard robots-vs-ToS distinction; counsel (HG-02) confirms before real public exposure (the `counsel_reviewed` flag). Unblocks live fetch across P25.2–P25.5; nothing loosens Part VIII or the rights/licence gates | P25.1 |
| ADR-084 | Accept `LicenceOuverte-2.0` in the SPDX set for DECP (operator determination 2026-09-15, the B rights-review pass): the data.gouv.fr dataset API confirmed `decp_fr` is published under `fr-lo` (Licence Ouverte / Etalab), which the accepted SPDX set in `policy/data/licenses.toml` did not contain — rights were honestly UNDETERMINED and the DECP path was blocked. DECISION: add `LicenceOuverte-2.0` to the accepted set as its **own** expression (data, attribution-required, non-share-alike, `relicensable_to` self-only) — the source records the licence it is actually published under rather than silently mapping to CC-BY-4.0 (Etalab's compatibility claim is a counsel question, not a silent map). `decp_fr` carries the resolved rights block but is **not flipped** — the France-cohort gate (P24.6/D-JURIS.2-1) holds the flip for P25.5. Counsel (HG-02) confirms LO↔CC-BY compatibility before LO content joins a published compartment | P25.5 |
| ADR-085 | Municipal-mandated-disclosure + derived-facts basis for CCOPS and pathways (operator determination 2026-09-15, the blocked-source unblock pass): the three CCOPS sources ingest ordinance-*mandated* public disclosures (Seattle SMC 14.18, NYC POST Act, SF Ch. 19B) and the three pathways sources ingest SIG-authored derived-facts records citing upstream documents — municipal works are not automatically public domain (§105 does not reach municipalities), but the ingestion unit is a derived-facts record, never upstream document bytes; facts are uncopyrightable and the mandate orders the facts published. DECISION: flip `ccops_seattle`/`ccops_nyc_post`/`ccops_sf`/`pathways_rtcc_federation`/`pathways_fr_css_forensics`/`pathways_acoustic_drone_location` on `LicenseRef-DerivedFacts-Citations` (redistributable=false — upstream content is never redistributed; derivative_permitted=true — the derived records are SIG's own); the counsel flag is RETAINED (HG-02 confirms the municipal-copyright reading before a published compartment). Same decision batch: the France-cohort gate went **per-source** — `raa_prefectures` (ODbL-1.0) and `decp_fr` (LicenceOuverte-2.0, ADR-084) flip; `madada`/`declarationcamera_be` stay held (no licence basis — HG-04 outreach owed). Live targets added only where a real supported fetch path exists (`decp_fr` monthly consolidated file); sources whose upstream is PDF/HTML stay replay/shadow-only pending document adapters | P25.5 |
| ADR-086 | HG-02 counsel resolution — derived-facts publication compartment (counsel determination 2026-09-16, operator-reported): counsel approved publishing the derived-facts/citations claims of the eleven `LicenseRef-DerivedFacts-Citations` sources (CCOPS×3, pathways×3, madada, declarationcamera_be, carnegie_ai_gsi, facial_recognition_world_map, aspi_mapping_chinas_tech_giants). DECISION: the P25.7 recorded exclusion (`export_disposition=excluded`/`counsel_pending`) is replaced by a real licence row — `relicensable_to` self-only, `attribution_required` — plus a dedicated `[compartments.derived_facts]` so the basis stays visible end-to-end rather than asserting a CC-BY-4.0 relicensing counsel did not state; `redistributable` flips `false→true` on the eleven records (the flag gates SIG's emitted claim rows — upstream bytes never enter the claim spine, so the ADR-085 "never re-host" invariant is architectural, not flag-borne). Remaining HG-02 scope (publication tiers, sensitive-coordinate rules, Part VIII surface, the ODbL 4.4(b) opinion) is NOT resolved — `D-LEGAL.1-1` stays OPEN | (post-chain) |
| ADR-087 | A 4xx robots.txt response means "no policy exists" — RFC 9309 §2.3.1.4 access-result split (P26.3): the original SIG-INGEST-012 implementation collapsed every non-2xx robots outcome into `RobotsResult.text=None` → refuse, so a host that answers robots.txt with a clean **HTTP 404** (no policy published) refused identically to a connection failure — over-refusing in a way RFC 9309 does not require (P26.2's `robots_unretrievable` CivicClerk refusal was exactly this). DECISION: `RobotsResult` carries `status`; the policy splits access results per §2.3.1.4 — **4xx "no policy"** (404 and the RFC's other no-policy 4xx) permits the fetch with a recorded `no_policy_4xx` outcome; **unavailable** (connection failure, timeout, redirect exhaustion, 5xx, 429) still fails closed; **other 4xx** (401/403/451, challenge/access-control) still refuses; a hard `Disallow` still refuses. Per-host robots status+outcome are recorded in the fetch record (`robots_decisions`) for audit. API mode (ADR-083) unchanged — the allow-list, not robots, governs documented endpoints. The amendment is recorded here + in SIG-INGEST-012's amended text, not silently — the fail-closed default for genuinely-unavailable robots is preserved | P26.3 |
| ADR-088 | Robots verdicts are probed and recorded, never enforced (P26.17 — executes the operator's GL-GATE-08 disposition 2026-09-18, "disregard robots entirely"): ADR-087's RFC 9309 §2.3.1.4 classification layer is KEPT verbatim (`retrieved`/`no_policy_4xx`/`unretrievable` still recorded per host), but a `Disallow` verdict or an unretrievable policy no longer refuses the fetch — `PoliteFetcher` proceeds and marks the fetch `robots_disregarded` (per-URL rows → `conduct_decisions` outcome → `FetchRecord.robots_disregarded`), so provenance says the refusal was ignored. `unreachable` joins the failing-status vocabulary for transport-level failures on now-attempted hosts. `RobotsDisallowed`/`RobotsUnretrievable` retained for record-vocabulary compat + non-standard fetchers; challenges/WAF still fail-closed (SIG-INGEST-013); rate-limits and the ADR-083 API allow-list unchanged. Revisit on egress-IP block, counsel objection, changed audit-value assessment, or an RFC 9309 revision | P26.17 |
| ADR-089 | Bounded, quota-aware SAM.gov sweep (P26.19 / SOURCES.18): the widened sweep is bounded to ONE api.data.gov daily window by a request budget; the driver stops cleanly at the budget or on the first 429 (`quota_reached`) and never re-probes, and the SAM.gov live transport is built `max_retries=0` so a 429 is never retried (the re-burn that SIGKILLed the P26.14 runs); the scheduler fires a fresh Monday window. `procurement_notice` shape / append-only / the X-Api-Key credential header unchanged. Fixes the structural half of D-FEDERAL.1-1; the fresh-window hosted land is the kind-V remainder | P26.19 |
| ADR-090 | National public surface from a spine-built export (P27 launch, operator-ratified 2026-09-22): the zero-JS static public surface (SIG-UI-036/037) is built from a real, national, spine-read export rather than the OKC fixtures, its breadth bounded by the fail-closed licence gate (SIG-LIC-004) with absence shown as an honest gap (never a total); the local dossier becomes one entry in a per-jurisdiction index; fixtures stay the CI default. Generalises ADR-066. The §39–41/§38 spec_src fold-back + new requirement id are deferred to the implementing tickets per SIG-ENG-039 | P27.4 |
| ADR-091 | Static content core + progressive-enhancement React islands — DECISION-SPA=B (P27 launch, operator-ratified 2026-09-22): the zero-JS archivable content core (SIG-UI-036/037) is preserved; React renders static HTML by default and hydrates only three named public islands — map (exercises SIG-UI-047), network graph, search — each keeping its no-JS fallback; extends ADR-068's island allowance from `/curate` to those islands; a full SPA is rejected. The §40 spec_src fold-back is deferred to P27.9 per SIG-ENG-039 | P27.9 |
| ADR-092 | Spine-built export + launch resolution posture (P27 launch, operator-ratified 2026-09-22): the public export reads the spine (generalising ADR-066); the empty modeling tables found at audit are shaped at export-build time, and the launch posture is compute-on-read + honest observation-level framing (the ADR-005/ADR-037 family) — cross-source duplicates shown as "N observations across M sources", not a claimed census; coordinates tier-reduced (§19.4); materialized resolution deferred to a follow-up ADR. The §29/§38 spec_src fold-back is deferred to P27.3/P27.4 per SIG-ENG-039 | P27.3 |
| ADR-093 | Information-architecture redesign — grouped nav, national landing, per-jurisdiction dossier index (P27 launch, operator-ratified 2026-09-22): replaces the flat 13-link nav + the hardcoded OKC dossier link + the map/reference-map duplication with a grouped nav, a national landing (named denominators, no total), and a dossier index over the local dossier; canonical origin + `as_of`/ruleset sourced from the export manifest; zero-JS + WCAG 2.2 AA preserved (SIG-UI-036/037). The §39–41 spec_src fold-back + new requirement id are deferred to P27.6 per SIG-ENG-039 | P27.6 |
| ADR-094 | Maximize publishable scope by evidenced rights resolution, never gate bypass (P27 launch, operator-ratified 2026-09-22): the ~24% UNDETERMINED claims found at audit are made publishable by recording real licence bases through the rights pipeline following the GL-GATE-07 / ADR-086 pattern (US public records / non-US operator DB-right / ODbL own compartment), never by weakening the fail-closed licence gate (SIG-LIC-004); ODbL and share-alike stay compartmented; Part VIII classes (facial-recognition and pathways sources, person/officer naming per §43.4) are held for operator HG-03/HG-02 sign-off. Operational — no requirement id, no spec_src change | P27.2 |
| ADR-095 | Append-only `rights_decision` log for resolving UNDETERMINED-recorded claims (P27.2 — the mechanism ADR-094 lands as): a claim's recorded `rights_id` is assertion-time provenance and is never rewritten (P1–P3), so a licence review is recorded as an append-only adjudication event — `(source_id, prior_rights_id) → resolved rights_id` + basis, reviewer **role** (never a personal name, Part VIII §0.7), review packet, terms URL, archived terms capture (SIG-LIC-002), and DB-clock `decided_at`; an immutability trigger refuses UPDATE/DELETE and a changed disposition is a NEW row whose later `decided_at` wins. The effective-rights rule resolves each claim through the latest decision matching its recorded rights + establishing-artifact source; `prior_rights_id` must be UNDETERMINED, so a decision can only lift unresolved rights and can never relicense an already-resolved claim. The audit reports the as-asserted and effective postures side by side, never conflated. Operational — no requirement id, no spec_src change | P27.2 |
| ADR-097 | Three public interactive islands — map (realising SIG-UI-047), network graph, search — extend the ADR-068 island allowance (P27.9, DECISION-SPA=B; the as-built record of ADR-091): React arrives via `@astrojs/react` rendering static HTML by default, and only these three named islands hydrate (`client:only="react"`); each is progressive enhancement with a preserved no-JS fallback — the located-assets table (map), the edge/path lists (graph), the browse index (search) — never a hard dependency (the new **SIG-UI-050**); each island page carries its own `lighthouserc.json` budget (WCAG 2.2 AA enforced, script size not asserted) as `/curate/**` does, while **every other public page keeps script bytes 0** (SIG-UI-036/037, proven by an e2e no-`<script>` sweep); the map draws only the already tier-reduced published points (§19.4, never full-precision) over a self-hosted renderer with ODbL/OSM attribution shown (§42.3, SIG-UI-038); a fourth island or a removed fallback changes the named set only by a new ADR (SIG-ENG-003); a full SPA stays rejected. Lands the §40 spec_src fold-back (SIG-UI-050 + the SIG-UI-047/A1 revisit annotation) that closes the final share of `D-P27-SPEC-1` | P27.9 |
| ADR-098 | Custom-domain cut-over to `surveillancegraph.org` via an external HTTPS load balancer + serverless NEG (P27.10 / LAUNCH.10): `https://surveillancegraph.org` (+ `www`→apex, HTTP→HTTPS, all at the edge) becomes the canonical public origin fronting the `sig-web` Cloud Run service with Google-managed TLS, chosen over a zero-cost Cloud Run domain mapping because the mapping is hard-blocked on interactive Search Console domain verification the cloud-platform ADC cannot perform (empirically confirmed), whereas the LB needs no verification and reserves a real static IP that IS the apex/`www` `A`-record value — so the mapping is applied now under operator ADC and the exact Squarespace DNS records (`@`/`www` `A` → the LB IP; no verification TXT) are produced with realized values. Landed as idempotent `ops/gcp/domain-mapping.sh` (`--check`/`--dry-run` green without ADC, like `provision.sh`; `--apply` under ADC; `--records` prints the DNS block); the domain + records are PUBLIC config, never secrets (HG-09); the `*.run.app` origin stays the documented fallback. Cost ~$18/mo (forwarding rule + IP) — a conscious low-cost departure from SIG-STORE-003 consistent with the ADR-081 precedent, with a zero-cost-migration revisit trigger (a Cloud Run domain mapping once the operator verifies the domain). Google-managed TLS + `probe-hosted` provision only AFTER the operator adds the DNS records at Squarespace (HG-11 / Go-public, never auto-ticked) → RETURN PASS `D-P27.10-1`; the domain serves the OKC demo until the national deploy `D-P27.8-1` lands (operator-accepted). Deploy reads/serves only; the published-vs-restricted compartment separation (ADR-096) is unchanged | P27.10 |
| ADR-096 | National public cut-over publishes only the non-share-alike published compartment (P27.8): the deploy partitions the licence-compartmented export into `exports/out/public` (public-read) and `exports/out/restricted` (PRIVATE) by a fail-closed, data-driven rule — a compartment is public iff its licence is non-`share_alike` (per `policy/data/licenses.toml`) and carries no recorded export exclusion, so ODbL-1.0 + CC-BY-SA-4.0 (share-alike), every UNDETERMINED/unknown licence, and counsel-pending exclusions stay restricted; a leak guard (`assert_public_clean`) re-scans the built public tree and fails loud on any share-alike/UNDETERMINED byte, and the public build reads the real national export (`SIG_DATA_SOURCE=export`) or fails loud (never fixtures). A conservative tightening over §42.3's per-file ODbL publication for the national surface while HG-11/HG-02 remain owed — the ODbL national layer's public release is a dedicated gated decision (`D-LEGAL.1-1`). Operational — no requirement id, no spec_src change | P27.8 |
| ADR-099 | Materialize entity resolution at scale, governed by a measured eval loop (P28.1 / DEPTH.1, Round 6 keystone): the built-but-dormant ER machinery (deterministic cascade tiers 0–3 + Splink tiers 4–5, gold set, quality gates, review queue) is run over the real ~1.06M-claim spine and the `resolution` envelopes are **materialized** as a new sqitch change (deploy/revert/verify, append-only — a resolution is a stored decision, ADR-005; never an UPDATE/DELETE), superseding ADR-092's compute-on-read posture for the resolved layer (the `exports/shaping.py`/`spine_export.py` observation-envelope path stays the fallback where ER cannot decide a predicate). The auto-vs-human balance is **measured, not asserted**: a maintainer-labelled stratified seed + an LLM adjudicator (`llm:<model>@<version>`) bootstrap the gold set; an LLM-vs-human κ calibration report + a human-verified frozen holdout are cut; every ER run re-measures P/R/F1 + B-cubed on the holdout and publishes them (§32/SIG-METRIC-008b). Lean-auto against a high MEASURED floor (`auto_write_precision_threshold ≥ 0.98`, κ ≥ 0.7, tuned in-ticket); `demote_auto_write_tiers` auto-demotes any tier below floor; `cluster_shape_alerts` + active-learning routing. The gold set is provisional (LLM-bootstrap) — `D-R6.1-EVAL` opened to re-derive the balance from first principles once real ground truth exists; a launch claim of "resolved sites" discloses the provisional basis until closed. BL-057 | P28.1 |
| ADR-100 | Identity minimization for contribution; no homegrown accounts (P29.1 / ACTIVATE.1, Round 7): the human loop turns on with the least identity each channel needs — **anonymous** corrections/disputes (`/dispute`, P15.x, zero identity, the public default), **federated/mediated** OSM contribution-back (identity owned by OSM; SIG stores none; P16.2/P21.7), and a **tiered, invite/issue-based** curator surface (the P16.1 pseudonymous `Contributor` + bearer-token `api/curation.py`, 401/403 tiering, append-only actor id, anti-poisoning, loopback-bound, Part VIII §0.7). P29.1 productionizes the tier-token curator store minimally (replace the demo `CURATION_KEYS`; issue/invite-based, pseudonymous, NOT open signup). **No public username/password account system, ever** — wrong on ethos (minimal PII), attack surface/safety (Sybil/poisoning; contributor account data is itself sensitive in an adversarial domain), and liability. Public authenticated contribution, if it must scale, is deferred to **OAuth against an external IdP** (OSM/GitHub; pseudonymous subject id + tier only, never passwords/PII) behind a moderation/safety plan + threat model — `D-R7.1-AUTH` opened, gated on demonstrated demand, not built now. Operational — no requirement id, no spec_src change. BL-057 | P29.1 |
| ADR-101 | The public surface reads the materialized graph (P28.5 / DEPTH.5, Round 6) — reconciling ADR-092: once P28.1 (resolution, ADR-099), P28.2 (relationships), P28.3 (contradictions) and P28.4 (coverage) materialize their rows into the spine, the P27.4 export (+ P27.9 islands) is re-derived off those materialized rows instead of the compute-on-read observation envelope in `exports/shaping.py`/`spine_export.py` (whose read-only-snapshot + fail-closed licence-compartment invariants are unchanged — only WHAT the producer reads changes). ADR-092's launch framing ("N observations across M sources", compute-on-read) is the pre-materialization posture; ADR-099 is its follow-up ADR and this ADR migrates the public surface onto the materialized layer, so "N resolved sites (from M observations)" replaces observation-level framing **only where ADR-099's measured precision floor is cleared** (the shaper stays the honest fallback for un-materialized predicates; absence stays a gap, never a total, §32). The provisional-eval disclosure (`D-R6.1-EVAL`) carries through; a material change to what is published is an HG-11 (Go-public) decision. BL-057 | P28.5 |
| ADR-102 | Temporary Cloud SQL scale-up for the OSM land, and a settled audit snapshot status (P30.1 / GO-LIVE.1, Round 8): a recorded, operator-approved deviation from the P30 tickets' "no Cloud SQL scaling" default — `sig-pg` patched `db-f1-micro` → `db-custom-2-8192` (2026-09-23) so the I/O-bound `camreg_osm_surveillance` land (D-SOURCES.17-1) completed in one execution; kept through P30.2 and scaled back at P30.4; the write-path root cause stays deferred (D-P30.1-1 batched writes, D-P30.1-2 incremental restarts). The `sig-exports audit` verb gains an additive opt-in `--settled` snapshot status (JSON `snapshot_status`, schema `p30.1/1.0.0`, default `provisional`) that replaces only the in-flight caveat — same read-only queries, same numbers, as-of + named denominators kept (§32 / SIG-METRIC-008). BL-055 / BL-057 | P30.1 |
| ADR-103 | Hosted Round-6 materialization: a least-privilege materialize role, in-GCP execution, and the hosted-scale deviations (P30.2 / GO-LIVE.2, Round 8): the operator-authorized `materialize_role` sqitch change creates `sig_materialize` (NOLOGIN NOBYPASSRLS; READ = `sig_read_public` with the tier-0 RLS ceiling; WRITE = INSERT only on the six Round-6 materialized tables + three resolution vocab tables; no UPDATE/DELETE/TRUNCATE, no claim-spine write), deployed by the schema owner `sig` under operator ADC (`postgres` cannot grant on `sig`-owned tables); every materializer + the P29.2 detector runs as a Cloud Run job execution next to Cloud SQL on a SHA-tagged image (`ops/gcp/materialize.sh`, never retagging `:latest`); hosted coverage runs without negative space (the peer-class rule over an all-`deployment` spine would emit ~26.7M `not_researched` rows — D-P30.2-1); the coverage reconciliation ratio counts only decisions with a winning claim and its read seam returns the latest append-only measurement (finding COVERAGE-RESOLVED-01); the detector jurisdiction is set only for a single-jurisdiction queue. Follow-ups D-P30.2-2 (entity-ref claims) and D-P30.2-3 (predicate-registry coverage). BL-057 | P30.2 |
| ADR-104 | Camera-registry predicates in the predicate registry (P30.2a / GO-LIVE.2a, Round 8): the measured hosted genres `camera_registry` + `connector_run` join the directness axis (every pre-existing predicate reads D6 there — behaviour-preserving; assessment owed, D-P30.2a-1) and the 14 measured camera-registry predicates are registered in the ontology source (`make gen`): coordinates SLOW/2y `latest_observation_wins` with an **absolute 0.0005° `value_tolerance`** (agreeing values pool into one candidate carrying an OBSERVED value — never a mean, §19.4; any spread beyond it, incl. a single-linkage chain, is U4 UNRESOLVED and visible); identifiers IMMUTABLE `authoritative_source_wins` exact match; operator/jurisdiction categorical `max_support`; registry rows D2 (own key + extraction metadata D1; publisher-attributed operator/jurisdiction D3). Undated claims are dated from their earliest capture's `retrieved_at` (labelled `SIG-RECON-008:observed_at=capture_retrieved_at`) instead of the 1970 placeholder that made them HISTORICAL. The resulting numbers are §28 value resolution WITHIN one source's camera record — not cross-source dedup (P30.2b); `resolved_sites_metric` must not be published as a dedup figure until P30.2b redefines it. BL-057 | P30.2a |
| ADR-105 | Geospatial camera-site entity resolution (P30.2b / GO-LIVE.2b, Round 8): a **resolved site** is a post-ER cluster of observation-level camera records (one source's row each) judged to be the SAME PHYSICAL DEVICE — M records, N clusters, N ≤ M, dedup ratio 1 − N/M; `resolved_sites_metric` is redefined over the latest completed run's AUTO-WRITTEN same-device edges and never counts a §28 value decision. Sized geo blocking (`camera_geo_grid`: same/neighbouring cell, different source); tiers 1g shared upstream ref / 3g coincident point (candidate auto-write) and 4g/5g proximate (always review); test-pinned constraints — same-source never merged (pairwise or transitively), recorded device-class conflicts never candidates, soft conflicts (jurisdiction/type/direction) never auto-write, mirror lineages counted once (not independent corroboration); bounded clusters + cluster-shape alerts (chaining). Auto-write only for tiers whose STRICT precision on the frozen agent-verified holdout clears 0.98 (gold `camera-2`, 540 double-adjudicated pairs; v1 rules demoted on the v1 holdout, which was then retired; v2 measured once on a fresh 180-pair holdout: 1g 1.000, 3g 0.986; LLM κ 0.669 over 540 pairs < 0.70 → suggester only, disclosed; ≥ 50 holdout pairs per auto-write tier). Append-only `camera_site_match` + `camera_site_run` (sqitch `camera_site_resolution`); proposals enqueued in `review_item`; points stay one record's own. PROVISIONAL (D-R6.1-EVAL). BL-057 | P30.2b |
| ADR-106 | Share-alike compartments go public as separate, attributed compartments; mixed-licence artifacts never do (P30.3 / GO-LIVE.3, Round 8 — supersedes ADR-096 §Decision 1 only, on the operator's 2026-09-24 share-alike answer with counsel clearance operator-reported, D-P30.3-COUNSEL OPEN): an export artifact is public iff it carries ONE known licence, no recorded export exclusion, and agrees with its registered compartment — so the ODbL `osm_physical` and CC-BY-SA compartments publish as their own downloads beside the CC-BY `sig_graph`, never merged (ODbL 4.4(a)); UNDETERMINED/unknown, excluded and mixed-licence bytes stay in the private restricted bucket; `assert_public_clean` re-proves one licence per public compartment; `LICENCES.json` labels each public compartment with its SPDX licence + attribution; the map render surface is labelled with every licence it draws on (a restricted build input); the website is the 4.4(b) produced work with "© OpenStreetMap contributors" wherever OSM-derived points render; a real-data build never renders the committed demo constants as data. |
| ADR-107 | Cloud SQL steady-state tier after launch (P30.4 / GO-LIVE.4, Round 8 — answers ADR-102's scale-back revisit trigger): `sig-pg` is patched `db-custom-2-8192` → **`db-custom-1-3840`** (1 dedicated vCPU / 3.75 GB), the smallest dedicated-core tier — `db-f1-micro` was measured starved and `db-g1-small` is shared-core (no SLA) with too little memory for the 4.4 GB spine's hot indexes; point reads measured unchanged, full scans ~7–13 % slower; ≈ $101 → ≈ $49/month compute (list-price estimate). The public site is bucket-served and unaffected by a DB restart; `sig-api` does not reconnect after one (D-P30.4-1) and is recycled by redeploying its pinned digest, never `:latest`. BL-057 | P30.4 |
| ADR-108 | Read-API connection pool and the bounded `/v1/search` contract (P31.1 / HARDEN.1, Round 9 — closes the code half of D-P30.4-1 and D-P30.4-2): `PgReadStore` reads through a small `psycopg_pool` pool (min 1 / max 5, 10 s checkout, `application_name = sig-api`) with `SET ROLE` re-applied on every new connection; a read whose connection died purges the dead idle connections (`ConnectionPool.check()`) and is retried ONCE on a fresh one — never a statement timeout, a pool timeout or an error on a live connection (the pool's own checkout check was rolled first and measured a 503 after 10 s when a restart killed every pooled connection); annotation/shaping take their lock before checking out a connection; a store that cannot answer is a 503 (`Retry-After: 5`), never a 500; budget (5 + 1) × max-instances 2 = 12 of `max_connections = 100`. `GET /health` (not `/healthz`, which Cloud Run reserves; never a liveness probe) + a `probe-hosted` target. `/v1/search`: a run of 3 letters/digits required (422), `limit` default 50 / max 200, keyset `next_cursor` (additive `limit` + `next_cursor`), literal matching, set-based labels/sources (4 statements per page), 5 s per-statement timeout (503); a non-transactional sqitch change adds `pg_trgm` + a GIN trigram index on `entity_identifier.value` built CONCURRENTLY (17 MB hosted). `API_VERSION` stays 1.0.0. BL-057 | P31.1 |
| ADR-109 | One `ingest_run` per execution; completion is an appended row (P31.2 / HARDEN.2, Round 9 — the design's ADR-R9-RUNLIFE; closes the engine half of D-P30.3-1): each `PgClaimSink` is one execution (a generated or resumed `execution_id` in `ingest_run.parameters`; no reuse-by-key); a new append-only `ingest_run_completion` table (immutability trigger, one live completion per run, one backfilled row per WORM object, all `ON CONFLICT DO NOTHING`) records status `ok`/`partial`/`quota_reached`/`failed`, DB-clock `finished_at`, claims considered/inserted/duplicate and the WORM run-row URI — `ingest_run` is never updated; `connectors.pipeline.run` records it on every live execution the process survives (failure detail = exception class only; gate refusals, replay and shadow record nothing); `scheduled-ingest` pre-computes the run-row URI so the two records name each other; a one-shot `sig-ops backfill-run-completions` (as `sig_materialize`, INSERT-only) appends `backfilled_from=<gs URI>` completions only where claims in the row's window or a sole candidate prove the run, reporting every other row unmatched with its reason; freshness reads completions (`last_content_change` only from completions that inserted > 0 claims). BL-056 | P31.2 |
| ADR-110 | Entity-identity guard, batched claim writes, and the sink's extension points (P31.3 / HARDEN.3, Round 9 — the design's ADR-R9-SINK; closes D-P30.4-3 ENTITY-RACE-01 and D-P30.1-1): a new `entity_identity_key` side table whose PRIMARY KEY (scheme, value) is the uniqueness the DB enforces for identity-bearing identifiers (`GUARDED_SCHEMES` = `sig.connector.subject`; attribute schemes such as `us.state`/`fr.insee` are never keyed) — guarded writers read existing keys, then claim missing ones `ON CONFLICT DO NOTHING` in one global (scheme, value) order before minting (entity FK DEFERRABLE INITIALLY DEFERRED: no orphan on a lost race; unkeyed legacy identifiers adopted), backfilled to the earliest entity per subject (DISTINCT ON, cannot violate the key over the historical pairs), immutable; a CONCURRENTLY-built btree on `entity_identifier (scheme, value)` so the adoption lookup survives generic plans; both entity writers (`PgClaimSink`, `ops.seed`) guarded; `PgClaimSink` stages claims per chunk and writes each chunk with multi-row `INSERT … SELECT FROM unnest … ON CONFLICT (content_digest) DO NOTHING RETURNING` + one `claim_evidence` INSERT per 2,000-row batch inside the chunk transaction (byte-identical rows, exact counters, journaled cache rollback, chunked resume unchanged); extension points: `on_duplicates` hook (P31.7), `object_resolver` entity-ref seam (P31.5), each `assert_claims` call a commit boundary (P31.4); the 3 historical duplicate pairs recorded append-only as same_as through `PgReviewQueue` (`sig-resolution identity-triage`); measured hosted +0 replay of `camreg_fl511_fl` (103,713 claims) 1,444–1,513 s → 8.6–8.9 s. BL-057 | P31.3 |
| ADR-111 | Restart = resume: per-capture flush, capture marks, a GCS capture store, and jobs pinned by digest (P31.4 / HARDEN.4, Round 9 — the design's ADR-R9-RESUME; closes D-P30.1-2, D-P31.3-1, D-P31.1-2): the pipeline asserts each capture's claims as soon as it is processed (one commit boundary per capture, ADR-110 (c)), and a live PG run keeps only the records its fetch record reports plus one artifact per stage (memory bounded by one capture); a new append-only `ingest_run_capture` table records per-target `captured`/`flushed` marks keyed to P31.2's per-execution run; the logical run = the executions sharing `ingest_run.parameters.logical_run` = `<source>@<last cron fire>` (same connector name + version) after the last live non-failed completion — a restart inside the window skips flushed targets (still counted as seen: digest + record count) and re-processes captured-but-unflushed targets from the stored capture, a fresh window or a completed logical run never skips; hosted `scheduled-ingest` jobs mount the private restricted bucket (gcsfuse) as the OCFL capture store (`SIG_CAPTURE_DIR`), since captures had lived on container disk; `sig-ops roll-jobs` + `lib.sh` `pin_image_digest` deploy every job/service by pinned digest (never `:latest`, before/after recorded, other job settings preserved); `--target-limit` bounded slices complete `partial`; resume keyed to connector name + version + code commit (the rolled digest); a stored capture that no longer yields is re-fetched; hosted: resume proven (30-page OSM slice killed at 15: 14 fetches, 1 re-processed from GCS, +0; re-proven on the final digest: 15 fetches, +0), new-claim land 179.9k claims/min, projected 10-10 OSM replay ≈ 12–15 min; all 76 `sig-api` jobs rolled to `sha256:feff986c…`, batch-05 switched per gate Q4. BL-057 | P31.4 |
| ADR-112 | Partner organisations as entity-ref claims: one identifier scheme, the ambiguity rule, and never a person (P31.5 / DEEPEN.1, Round 9 — the design's ADR-R9-ENTITYREF; advances D-P30.2-2 and the procurement half of D-P30.2a-1; opens D-P31.5-1 + D-P31.5-2): a partner (procurement `buyer`/`seller`/`recipient`/`funder`, accountability `event_organizations`, camera-registry `camera_operator` — the re-confirmed inventory; no procurement/accountability path emits `applies_to`/`operator`) is ALSO written as a separate entity-ref claim whose object is an `organization` entity — the connectors' `link()` stage appends a copy of the untouched text claim plus an `object_ref`, so text-claim digests are unchanged (shadow diff 0 vs the base commit); identity = an external crosswalk id when present (LEI/UEI/DUNS/CAGE/agency code; FIPS excluded as a shared place attribute), else `sig.org.name` = `normalize_org_name(name)`; versioned rules (`resolution/data/partner_identity.toml`) keep bare ids, multi-party strings, sole-trader/individual phrases, honorifics, common given names, role/title words without an institutional head noun, names with no distinguishing word, single words and marker-less multi-word (person-shaped) names text-only — only a positive organisation marker mints (Part VIII; the sink also refuses any `person` object); the sink's production `object_resolver` `record_object_ref` is wired by `make_claim_sink('pg')`, `EntityRef.label` feeds one `organization` row (`unclassified`, identity_basis, `cached_canonical_name`, publication review for name-only bodies); the partner schemes join `GUARDED_SCHEMES` with the `partner_org_identity_key` backfill; 46 procurement/accountability predicates registered with `object_kind` + `maps_to` (connector_run D3 descriptive / D1 identifiers + own-text matches); the P28.6 assembler recognises `camera_operator`/`event_organizations`, types placeholder-`deployment` record subjects by predicate domain and never anchors on them, drops `seller` from the direct-vendor set; the export's sharing-edge read excludes partner predicates; fixture proof: 8 accountability links over real PG from emitted claims, +0 re-run; edges 0 (no in-scope access-edge predicate → P31.6, D-P31.5-1). BL-057 | P31.5 |
| ADR-113 | Asserting replay of persisted captures (P31.6 / DEEPEN.2, Round 9 — the design's ADR-R9-REPLAY; closes D-P30.2-2 with the P31.5 emitters): `connectors.replay.replay()` stays deliberately NON-asserting and network-isolated; the asserting path is a separate decision — `runner.replay_ingest` (+ `sig-ops replay-ingest`, hosted job `sig-replay-ingest`) selects persisted `ingest_run_capture` marks (explicit `--run-id`, else the source's non-replay runs), resolves capture bytes from the OCFL store ONLY (a digest absent from the store is recorded `missing`/partial and never re-fetched — no network in replay), supplies connector target metadata via `ctx.parameters["targets"]`, and asserts through a fresh `is_replay=true` `PgClaimSink` run carrying `replay_source`/`replay_of_runs`/`logical_run` lineage and `replay:<source_uri>#<digest>` capture marks; claim-content dedupe makes re-runs +0; `PgClaimSink.extra_parameters` merges operator parameters without shadowing sink-owned keys. Access edges (`flock_portal`/`audit_structural` `configured_access_edge` records) become `configured_sharing_partner` claims: flock portal *slugs* ref `sig.connector.subject`/`flock_portal:<slug>` (entity type `deployment`, the partner's own portal-subject key); audit organisation *names* route through `partner_identity` (`sig.org.name` org refs; refused names stay literal, never edges); `eff_data_driven` adds a `vendor` claim per agency aggregate + a `configured_sharing_partner` claim for `pooled_lookup_participant` rows (endpoint = the release's single vendor constant; aggregate-only design kept); both predicates registered with `object_kind=entity_ref` and join `PARTNER_PREDICATES`. Public republication of edges/links stays with P31.16 (HG-11). BL-057 | P31.6 |
