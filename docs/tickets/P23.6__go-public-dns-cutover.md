<!--
  Template: docs/tickets/NN[a-z]_GATE-G<k>__<slug>.md — a GATE marker (BM-TICKET-05, BM-GATE-03).
  Written by: decompose-spec mode=extend over docs/3_sig_golive_spec.md (2026-09-09).
  A marker, NOT an implement-spec input: no run line. Executed by orchestrate-build. Never guessed past.
-->
# P23.6 — Go public: DNS/host cutover + v0.2.0 (Go-public, GL-GATE-05)

- **Marker id:** `GATE-G2` (semantic **Go-public** / **GL-GATE-05**) — renumbered from `75_GATE-G2` to `P23.6` for lexical chain order; the semantic/GL ids and the `GATE-G2` readout key are unchanged.
- **Kind:** gate · **Phase:** 21 (go live)
- **Readout:** `docs/build/readouts/GATE-G2.md`
- **Blocks:** LIVE.2 → `v0.2.0` "first public jurisdiction" (the public cutover only; LIVE.2 itself lands to GCP staging/private without this).

> **Milestone gate — NOT an `implement-spec` input.** The chain STOPS here until the operator
> dispositions the readout. Never guessed past.

> **Stays a deliberate human action — GL-GATE-05 (NOT pre-answered).** The chain drives to "OKC
> ingested + published to a GCP staging/private target"; the DNS/public cutover (and the `v0.2.0`
> tag) is the operator's explicit final step, after HG-01 (legal home, real) and HG-11 (governance).

## Criterion (verbatim from the spec)
> **Go-public — DNS/host cutover:** operator decision after HG-01/HG-11. Blocks LIVE.2 → `v0.2.0`.
> On Go-public, cut over and bump `0.2.0` "first public jurisdiction".

## Pre-registered thresholds
- HG-01 (a **real** named legal home, superseding the GL-GATE-01 interim posture) and HG-11
  (governance roles + written concurrence + live takedown contact) are both satisfied.
- LIVE.2 is green to a GCP staging/private target; `PUBLICATION_CHECKLIST.md` complete; `/terms` +
  `robots.txt` served; officer-naming + publication tiers verified on live data.

## DEFERRALS rule
This gate does not pass while any `OPEN` row in `docs/tickets/DEFERRALS.md` scoped to publication
remains (rule 4) — notably `D-P21.4-1` (HG-01), `D-P21.4-2` (HG-11), `D-P21.4-3` (Go-public).

## Disposition
- [ ] Operator disposition recorded in `docs/build/LEDGER.md` GATE DECISIONS and in
      `docs/build/readouts/GATE-G2.md` as PASSED | SKIPPED-BY-OPERATOR | NOT PASSABLE (+ what
      would pass it).
