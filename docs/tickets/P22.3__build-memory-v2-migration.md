# P22.3 — Migrate Eleutheria to build memory v2 (EL.1)

- **Sequence:** 66 of 66 · **Phase:** 22 (migration) · **Kind:** ticket
- **base_branch:** current checkout — the chain tip `devin/sig-postbuild-capstone` (PR #68, commit `625d802`; the round-1 capstone landed on top of P22.2/PR#67) · **worktree:** `~/Eleutheria`
- **Depends on:** SK.1 merged into `~/agent-skills` `main` (`2c59325`) and installed (`~/.claude/skills/build-memory`); P22.2 (AGENTS.md refresh, PR #67) and the round-1 CAPSTONE (PR #68) landed.
- **Run:** `implement-spec spec=~/MetaHarness/build-memory-v2-spec.md#EL.1 worktree=~/Eleutheria live_verification=false`
- **Gate status:** none · **Live stage:** offline-only
- **Chain bookkeeping:** appended to `docs/tickets/00_MANIFEST.md` as row 66, this file `docs/tickets/P22.3__build-memory-v2-migration.md` (legacy naming kept for this repo), phase 22; the ticket file is a copy of the spec's EL.1 section with the Load list resolved to repo paths and the "as-of-2026-09-09" specifics re-derived against the current state (the capstone already ran).

> **Round-2 / current-state note.** The spec's EL.1 header (2026-09-09) named the tip as
> `devin/p22-2-agent-docs-refresh` (PR #67); since then the round-1 **capstone** (PR #68,
> `devin/sig-postbuild-capstone` @ `625d802`) landed and IS the real tip. The live ledger already has
> `projectStatus: DONE`, a full PHASE LOG through the capstone, `docs/build/CAPSTONE_VERIFICATION.md`,
> the `CAPSTONE_CLOSURE.md §(b)` addendum, the fixed `COVERAGE_MATRIX.csv` (671 rows), the reconciled
> `BACKLOG.csv`, and Appendix F with **71 ADRs** (ADR-072 present; ADR-064 skipped). This ticket
> reconciles against that reality: the migrated ledger reflects Round 1 COMPLETE **including the
> capstone**; adding ADR-073 makes **72** ADRs; the tail rows are the Round-2 delta over the completed
> Round 1.

## Goal
Retire `.agents/scratch/`, commit the build's memory under `docs/build/` in the v2 layout, convert the
machine ledger, complete `BUILD_INDEX.md` for rows 47–66, seed `docs/tickets/DEFERRALS.md` with the
currently owed gate-pending work, record the decision as ADR-073, and leave the repo green under
`check-build-memory.sh` — without renaming any historical ticket or rewriting any historical content.

## Load (read these — do not re-read others; SIG-ENG-001)
- `~/MetaHarness/build-memory-v2-spec.md` Part I §3–§10; `~/agent-skills/skills/build-memory/layout.md` and `SKILL.md` (migrate mode).
- `docs/adr/ADR-058-tickets-and-build-memory-are-committed-and-scratch-is-unified.md` (superseded in part; its revisit trigger), `docs/adr/README.md`, `docs/research/_meta/spec_src/99a_appF_adr.md`, `docs/build/tools/check_spec_src.py` (Appendix F ↔ ADR set), `AGENTS.md` (post-P22.2), `docs/README.md` (mode table), `docs/build/README.md`, the retired `.agents/scratch/README.md` 44-row rename mapping (carried into `docs/build/README.md`), the live machine ledger `.agents/scratch/planning/sig-postbuild-build-ledger.md` (→ `docs/build/LEDGER.md`), `docs/tickets/00_MANIFEST.md` (banners, rows 47–65, rules 1–4), `docs/build/BUILD_INDEX.md` (rows 1–46; §A.4 column notes), `docs/build/BACKLOG.csv` (`landing`, `gate` columns), `.gitignore`.
- Ground truth for the index rows 47–66: the ledger's PHASE LOG entries and `gh pr view 47..68`.

## In scope — deliverables
1. **Migrate scratch** (BM-MIGE-01): `build-memory migrate from=.agents/scratch apply=true` — run ledgers → `docs/build/runs/<ID>.md`, PR/commit drafts → `pr/`, generators → `tools/`, fixtures → `fixtures/`, machine ledger → `LEDGER.md` (3 keys added), logs dropped, mapping into `docs/build/README.md`. Leftover root PR bodies and the three ledger-less units' bodies normalized to `pr/<ID>.md`. Existing `docs/build/` root reports relocated into `docs/build/reports/` (move-only) to satisfy the v2 root allowlist. `.agents/` removed; `.gitignore` gains `docs/build/logs/`. Contents byte-identical (`cmp`/`sha256`); nothing edited.
2. **Ledger state** (BM-MIGE-02): `LEDGER.md` reflects Round 1 COMPLETE (incl. the capstone); the entire existing PHASE LOG carried verbatim; `manifest:`/`memoryRoot:`/`round: 2` in CURRENT STATE; legacy PHASE PLAN/invariants/SETUP/CAPSTONE under `## LEGACY PLAN`; `returnPass` unchanged; `updatedAt` bumped.
3. **`BUILD_INDEX.md` rows 47–66** (BM-MIGE-03): one row per PHASE LOG "done" entry (incl. inserts P20.4, P22.0, and the CAPSTONE), evidence → `runs/<ID>.md` or `pr/<ID>.md`.
4. **`docs/tickets/DEFERRALS.md`** (BM-MIGE-04): one OPEN row per RETURN PASS ticket/gate pair, `D-P21.n-*` ids, `kind` column, each citing its `BL-` id where one exists; header points at `docs/build/BACKLOG.csv`.
5. **Manifest and tail** (BM-MIGE-05): banners updated (ledger committed at `docs/build/LEDGER.md`; run ledgers at `docs/build/runs/`; DEFERRALS companion); row 66 = this ticket; `## Plan extensions`; tail rows `P23.1…P23.7` (CAP.1/CAP.2/CAP.3/GATE-ACCEPT/REC.1/REC.2/REC.3) instantiated from the v2 templates as a Round-2 delta over the completed Round-1 (Load lines point at the existing capstone/P19.x/P20.x artifacts; `> Round 2` banner). `nextTicket → P23.1` after this ticket.
6. **ADR-073** (BM-MIGE-06): "Build memory is committed under docs/build; agent scratch is retired" — Accepted, Ticket P22.3, ids SIG-ENG-001/003/031, states what it supersedes in ADR-058 §3 (ADR-058 body NOT edited); Appendix F row in `spec_src/99a_appF_adr.md`, `BUILD.sh` run, `check_spec_src.py` green (**72** ADRs); `docs/adr/README.md` regenerated by `build-memory adr-index`.
7. **Docs** (BM-MIGE-07): `AGENTS.md` "Where things live" → v2 paths + "Deferred obligations — read first" gotcha; `docs/README.md` rows for `build/` (run ledgers), `tickets/DEFERRALS.md` (append-only), `adr/README.md` (generated); `docs/build/README.md` rewritten from template (layout + rename mapping + legacy-artifacts list + sha256).
8. **Registers** (project rule): `docs/risk_register.md` `## Phase 22 — Build memory v2 (P22.3)` with `RISK-P22-05`/`RISK-P22-06`; `docs/traceability.md` section for the stamped ids; `make docs-check` gains `scripts/docs/check-build-memory.sh .` (vendored, MIT header).

## Out of scope
- Renaming any `docs/tickets/P*.md`. Editing any historical ledger, PR body, ADR body, or register row. Running the tail tickets. Merging anything (operator). Changing code.

## Acceptance criteria
- [ ] `bash scripts/docs/check-build-memory.sh .` exits 0; `test ! -d .agents`; `git ls-files docs/build/runs | wc -l` ≥ 62; `find docs/build -name '*.log' | wc -l` = 0; `docs/build/logs/.gitignore` exists. *(deterministic)*
- [ ] Every moved file `cmp`-clean vs its pre-move content (pre-move `sha256sum` list recorded in `docs/build/README.md`). *(deterministic)*
- [ ] `grep -c '^| ' docs/build/BUILD_INDEX.md` grew by the number of PHASE LOG "done" entries (rows 47–66); every new row's evidence path exists. *(deterministic)*
- [ ] `docs/tickets/DEFERRALS.md` has one OPEN row per RETURN PASS ticket/gate pair; each cites a `BL-` id where one exists. *(deterministic)*
- [ ] `python docs/build/tools/check_spec_src.py` exits 0 (**72** ADRs); `docs/adr/README.md` carries the generated marker and lists ADR-001…073 (minus 064) in numeric order. *(deterministic)*
- [ ] `docs/build/LEDGER.md` CURRENT STATE has the BM-LEDGER-02 key set in order; `nextTicket: P23.1`; `manifest:`/`memoryRoot:` set; PHASE LOG has P22.1/P22.2/P22.3 (and the capstone). *(deterministic)*
- [ ] `make check` green (0 failed, 0 xfailed; no code change); `make docs-check` green incl. the new validator. *(deterministic)*
- [ ] **Phase gate (§51.3):** CI green; ADR-073 indexed; risk register + traceability appended; `BUILD_INDEX.md` row for P22.3 and `LEDGER.md` closed by the worker. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
`BM-MIGE-01..07`, `BM-COMPAT-04..05`; project ids SIG-ENG-001, SIG-ENG-003, SIG-ENG-031, SIG-ENG-039.

## Cross-cutting invariants
- **Append-only & provenance (P1–P3):** move/rename only; mapping recorded; nothing historical edited; no `docs/tickets/P*.md` renamed; no ADR body/register row rewritten.
- **Part VIII is binding:** scan every newly committed file for private individuals' personal data before staging; record the scan in the PR body (expected: only public officials / sources / gate ids).
- **No code change; `make check` green; secrets never in a file.**

## Notes
- This ticket owns the `docs/build/` v2 layout in this repo and the `D-P21.n-*` deferral ids. P23.x consume `DEFERRALS.md` and `LEDGER.md`.
- Re-confirmed at build time: chain tip = `devin/sig-postbuild-capstone` (PR #68); PR numbers 47–68; the capstone already ran (ledger `projectStatus: DONE`); ADR count 71 → 72 with ADR-073; validator root allowlist required relocating pre-v2 `docs/build/` root reports into `reports/`.
