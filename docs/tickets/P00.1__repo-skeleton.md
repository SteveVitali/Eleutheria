# P00.1 — Repository skeleton and CI

- **Sequence:** 1 of 46
- **Phase:** 0 — Foundations, governance, ecosystem coordination
- **base_branch:** current checkout — the branch the previous ticket left checked out (stacked PR chain); `main` for P00.1
- **Depends on (must land earlier in the stack):** none (this ticket creates the repository structure)
- **Run:** `implement-spec spec=docs/tickets/P00.1__repo-skeleton.md`

## Goal
Stand up the monorepo skeleton every later ticket forks from: the §47 package layout, a Python
workspace with a committed lockfile, CI, and licence headers. **Write no ingestion or domain code.**

## Load
- `docs/2_canonical_design_spec.md` §47 (stack & repository), §0 (execution model), Part I §3 (invariants).
- Appendix E glossary.

## In scope — deliverables
1. Monorepo as a **uv** workspace with a committed lockfile + standards-based lock export + an SBOM target — SIG-ENG-011.
2. The exact package layout of §47 as empty-but-real packages (`ontology/ db/ connectors/ parsing/ resolution/ reconcile/ inference/ tasks/ api/ web/ exports/ orchestration/ policy/ ops/ docs/ tests/`) — SIG-ENG-012.
3. Every pipeline stage invocable as a plain CLI; the orchestrator import confined to `orchestration/` — SIG-ENG-013 (establish the convention + a lint/test that enforces the import boundary).
4. `policy/` scaffolded as a real, tested Python package (no logic yet) — SIG-ENG-014.
5. CI (GitHub Actions): lint, type-check, test, and the "generated artifacts match a fresh generation" gate stub — SIG-ENG-015/016 wiring.
6. Licence headers + repository `LICENSE` posture placeholder (final licences decided in P00.2).

## Out of scope (do NOT do here)
- Any policy logic, source registry, ontology, schema, or connector — those are P00.2, P00.4, P01.1, P02.1.
- Choosing/authoring ADRs — P00.2 owns ADR-001..012 and the stack ADRs.

## Acceptance criteria
- [ ] `uv` workspace resolves and the lockfile is committed; CI installs from the lockfile. *(deterministic)*
- [ ] All §47 packages exist and import cleanly; a test asserts the package set matches §47. *(deterministic)*
- [ ] A test asserts **no package outside `orchestration/` imports the orchestrator** (SIG-ENG-013). *(deterministic)*
- [ ] CI runs lint + type-check + tests green on an empty skeleton. *(deterministic)*
- [ ] **Phase-gate (§51.3):** CI green; the requirement IDs below appear in the PR; risk register R-11 (zero-cost mode) noted.

## Requirement IDs to satisfy and stamp in the PR
SIG-ENG-010, SIG-ENG-011, SIG-ENG-012, SIG-ENG-013, SIG-ENG-014.

**Spec reconciliation (P20.2, A7):** the frozen §47 layout (SIG-ENG-012) now names `evidence/` explicitly (ADR-023); cite SIG-ENG-012 as amended — see `docs/build/SPEC_RECONCILIATION_PLAN.md` A7.

## Cross-cutting invariants — re-check before opening the PR
- Defining standard (§3.1) and Part VIII (§0.7) apply to every ticket; here they are structural (no data yet), but the package boundaries (`policy/`, evidence separation) must not be collapsed.
- Additive/back-compat: this ticket defines the wire-name conventions later tickets depend on; document them.

## Notes
This ticket **owns** the repo conventions (package names, CLI-stage pattern, lockfile tool = uv, CI shape). Every later ticket references them; do not rename packages afterward without an ADR.
