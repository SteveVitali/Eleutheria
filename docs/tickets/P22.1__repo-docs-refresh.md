# P22.1 — Human-facing documentation: full audit + refresh of every reader-facing doc against the finished code (`refresh-repo-docs`)

- **Sequence:** 64 of 65
- **Phase:** 22 — Documentation (added 2026-09-08 by operator request after the P19–P21 chain was cut; not in the spec's Part X)
- **base_branch:** current checkout — the P21.9 branch (or the latest chain tip if tickets were inserted before this one)
- **Depends on (must land earlier in the stack):** P21.9 (the last functional ticket — docs are refreshed against the *final* code, once).
- **Run:** `implement-spec spec=docs/tickets/P22.1__repo-docs-refresh.md live_verification=false`
- **Gate status:** none. Skill used: `refresh-repo-docs` (source: `~/agent-skills/skills/refresh-repo-docs/SKILL.md`; also installed as `~/.claude/skills/refresh-repo-docs`) — invoke it **by name** and follow it completely; this ticket only fixes its inputs and boundaries.

## Goal
Every document a human reader might open — README(s), `CONTRIBUTING.md`, `CHANGELOG.md`, `docs/governance/`, `docs/studies/`, `docs/slice/`, the index READMEs — describes the repository as it actually is after 65 tickets: every command runs, every path exists, every claim was verified against code, cruft is gone, gaps are filled at proportional depth, and a committed report records what changed and what could not be fixed without a human. Generated, frozen, and historical documents are audited but **never edited**.

## Load (read these — do not re-read others; SIG-ENG-001)
- `~/agent-skills/skills/refresh-repo-docs/SKILL.md` (all phases; the detector `scripts/check-repo-docs-freshness.sh`), especially the scope boundary (agent docs are **not** this skill's) and the editing standards (minimal diffs, no placeholders, never hand-edit generated docs, no fabricated changelog entries).
- `docs/build/DECISION_MEMO.md` §9 (the operator runbook — the README must point readers at it), `docs/build/README.md`, `docs/tickets/00_MANIFEST.md` (post-build preamble), `docs/build/INTEGRATION_PLAN.md` §(d) (how releases are cut), `docs/build/OPERATIONAL_READINESS.md` §(b)/(d) ("what is not live" — the README must not overclaim).
- `README.md` "Regenerating the specification" (must survive verbatim in meaning: `spec_src` → `BUILD.sh` is the only way to change the spec), `docs/adr/README.md` (Appendix-F index), `Makefile` targets, `.github/workflows/ci.yml` jobs, `pyproject.toml` workspace members (14), `web/package.json` scripts, `ops/README.md` + `sig-ops` commands (P21.4), `docs/build/tools/*.sh|*.py` (each has a one-line purpose the docs may cite).
- Anchors for the "how do I run it" claims (re-confirm at build time): `Makefile:20-73`; `ops/docker-compose.yml` (P21.4); `docs/build/tools/run_okc.sh` (P21.4); `exports/src/exports/cli.py` (`build|deposit|push|tiles`); `connectors/src/connectors/cli.py` (`validate|review-status|run|gate|export-check`); `api` serve entry (P19.4); `web/README.md`.

## In scope — deliverables
0. **Commit the contracts first.** `docs/tickets/P22.1__repo-docs-refresh.md`, `docs/tickets/P22.2__agent-docs-refresh.md` and the manifest rows 64–65 were placed in the tree on 2026-09-09 (untracked / uncommitted). If a P21.x worker has not already committed them, commit them in this branch's first commit (`chore(tickets): add P22.1/P22.2 contracts + manifest rows 64–65`). If they were committed earlier by another ticket, verify they match and skip.
1. **Detector run + worklist** with the skill's Phase 0: `bash <skill>/scripts/check-repo-docs-freshness.sh .` → `/tmp/repo-docs-freshness.json`; **`audit: full`** (this is the first whole-repo pass since the build began; every in-scope doc gets a claim-by-claim audit, not just flagged ones). Findings ledger at `/tmp/repo-docs-findings.md` per the skill.
2. **Scope boundary (the one input this ticket fixes).** *In scope (audit + fix):* `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `web/README.md`, `db/README.md`, `evidence/README.md`, `ops/README.md`, any other package `README.md`, `docs/governance/**`, `docs/studies/**`, `docs/slice/**` (the P06.1 retrospective is historical — fix only broken references, never its findings), `docs/adr/README.md` (index only), `docs/build/README.md` (index only), `docs/research/README.md` (index only). *Audit-and-report only — never edit:* `docs/2_canonical_design_spec.md` (generated from `docs/research/_meta/spec_src/` — a spec change is P20.2's procedure, not a docs fix), `docs/research/**` (frozen research cache), `docs/1_deep_research_overview.md`, `docs/adr/ADR-*.md` bodies (append-only; the only permitted edit is a "Superseded by …" annotation per the skill), `docs/risk_register.md` and `docs/traceability.md` (spec-mandated registers maintained by tickets), `docs/build/*.md|*.csv` other than `README.md` (planning/capstone **records** — historical evidence; broken references inside them are reported, not fixed), `docs/tickets/**` (executed contracts — historical). *Not this skill's:* `AGENTS.md`, `CLAUDE.md` (→ P22.2). If the detector flags a doc in the report-only set, the finding goes in the report §"excluded-scope findings" with the recommended owner (P20.2 procedure / next ticket / accepted).
3. **Fixes per the skill's Phase 2** (stale → correct, cruft → delete, gap → fill, mode-drift → relocate), foundational docs first. Gaps this pass is expected to fill (verify each against code before writing): (a) `README.md` — the 14 workspace members + `web/` + `ops/` with one line each; the three ways to run SIG (`make check`; `sig-ops up` + `run_okc.sh`; `sig-ops degraded`); what is live vs fixture-backed (from `OPERATIONAL_READINESS.md`, phrased without overclaiming); where the build memory lives (`docs/build/`, `docs/tickets/00_MANIFEST.md`); how releases are cut (`INTEGRATION_PLAN.md` §(d)); the licence posture sentence (from `LICENSE`); (b) **`docs/README.md` (new)** — a one-screen map of the `docs/` tree stating for each subtree whether it is *generated* (spec), *frozen* (research), *append-only* (ADRs, risk register, traceability), *historical record* (build, tickets, slice), or *living* (governance, studies), and how each is changed; (c) `CONTRIBUTING.md` — the stacked-ticket workflow as it actually ran, the `make check` gate, the docs-freshness gates (P22.2 adds them to CI — describe as "planned" only if P22.2 has not landed; this ticket must not promise what does not exist), the spec-amendment rule, licence headers; (d) `CHANGELOG.md` — backfilled **only** from `git log` evidence (tags, merged PR titles, ticket ids), in Keep-a-Changelog format; `0.1.0` stays "unreleased" until the operator tags; (e) `docs/governance/README.md` — index of the eight governance documents with their status (adopted / adopted-pending-counsel / template) taken from the documents themselves.
4. **Validation + sweep per the skill's Phase 3**: detector re-run until 0 critical issues in the in-scope set (max 2 extra iterations, then report), cross-doc consistency sweep (same feature named the same everywhere; README claims agree with `ops/README.md`, `web/README.md`, `INTEGRATION_PLAN.md`).
5. **`docs/build/DOCS_REFRESH_REPORT.md`** (committed): per doc — stale fixed / cruft removed / gaps filled / mode fixes, one line each; **remaining gaps** (need a human: e.g. legal-home text, contact channels); **code suspects** (places where a doc described intent and the code looks wrong — each becomes a `docs/build/BACKLOG.csv` row with `type=docs-drift` or `defect`); **excluded-scope findings** (the report-only set, with owners); the detector's before/after counts; and one line recommending `agent-docs` (P22.2) for any `AGENTS.md` drift noticed in passing.
6. **Vendor the detector (MIT):** copy `check-repo-docs-freshness.sh` to `scripts/docs/check-repo-docs-freshness.sh` with its licence header + a one-line provenance comment (source path + date), so the repo can run the check without the skill installed; `make docs-check-repo` target runs it on `.`. (CI wiring is P22.2's, so both detectors are added to CI once.)
7. **Traceability + risk register**: `## Phase 22 — Documentation (P22.1)` with `RISK-P22-01` (docs drift resumes after this pass → the vendored detector + P22.2's CI gate; refresh on every release per `INTEGRATION_PLAN.md`), `RISK-P22-02` (a "fix" to a historical record would falsify evidence → report-only set enforced by the scope rule above, checked in review). Traceability section "Human-facing documentation".

## Out of scope (scope-creep guard — do NOT do these here)
- `AGENTS.md`, `CLAUDE.md`, any agent-facing doc — **P22.2**. Editing anything in the report-only set (spec, research, ADR bodies, registers, build records, ticket contracts). Any code change (the vendored script and the Makefile target are the only non-doc files). Rewriting voice/format of text whose content is correct (minimal diffs). Inventing changelog entries. Merging, tagging, releasing (operator, after the chain).

## Acceptance criteria
- [ ] `bash scripts/docs/check-repo-docs-freshness.sh .` exits 0 (0 critical issues) **when the report-only paths are excluded from the count**; the report §"excluded-scope findings" lists every remaining critical issue inside the report-only set with an owner. *(deterministic)*
- [ ] `git diff --name-only <base>..HEAD` contains **no** path under `docs/research/`, `docs/tickets/` (other than the P22.x contracts + manifest), `docs/build/` (other than `README.md`, `DOCS_REFRESH_REPORT.md`, `BACKLOG.csv`), `docs/adr/ADR-*.md` (unless the only added line is a "Superseded by" annotation), `docs/risk_register.md`/`docs/traceability.md` (other than the appended P22.1 sections), or `docs/2_canonical_design_spec.md`. *(deterministic)*
- [ ] Every shell command that appears in `README.md`, `CONTRIBUTING.md`, `ops/README.md`, `web/README.md` was executed once during the run (or is marked as requiring a gate/credential, with the gate id); the PR body lists each command and its exit code. *(deterministic — evidence in PR body)*
- [ ] `docs/README.md` exists and classifies every top-level entry of `docs/` (`ls docs` names all present). *(deterministic)*
- [ ] `CHANGELOG.md` entries each cite a PR number, tag, or ticket id that exists (`gh pr view N` / `git tag -l` / `ls docs/tickets`). *(deterministic)*
- [ ] `README.md` names all 14 workspace members (`grep -c` each `pyproject.toml` member name = 14 hits) and `docs/build/`, and contains no claim that a source is "live" unless `OPERATIONAL_READINESS.md` says so. *(deterministic)*
- [ ] `DOCS_REFRESH_REPORT.md` exists with the five sections; every "code suspect" has a `BACKLOG.csv` row. *(deterministic)*
- [ ] A reader who has never seen the repo can, from `README.md` alone, run `make check` and find the spec, the tickets, the build memory and the governance docs in ≤3 clicks (reviewer walkthrough). *(agentic)*
- [ ] **Phase-gate (§51.3):** `make check` green (unchanged test count); `make docs-check-repo` runs; traceability + risk register sections appended. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
No new canonical requirement ids (documentation hygiene). Stamp: SIG-ENG-001 (the repo is executable from its documents), SIG-ENG-003 (spec amendment path respected — the spec was not hand-edited), SIG-ENG-031 (risk register maintained), SIG-LIC-005 (licence posture stated correctly in README).

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** no claim without verification — every command run, every path checked; "no synthetic certainty" applies to docs too (fixture-backed is stated as fixture-backed).
- **Part VIII is binding (§0.7):** no personal data or private contact details enter any doc; governance docs name roles/organisational channels only.
- **Append-only & provenance (P1–P3):** historical records (build, tickets, slice retrospective, ADR bodies) are not rewritten; changelog is evidence-backed.
- **Additive/back-compat:** no code change; the vendored script is additive.

## Notes
- This ticket **owns** `docs/README.md`, `CHANGELOG.md`/`CONTRIBUTING.md` content, the in-scope/report-only boundary above, and `scripts/docs/check-repo-docs-freshness.sh`. P22.2 owns the agent docs and the CI wiring of both detectors.
- Run this ticket **last-but-one on purpose**: it documents the finished chain. If tickets are later appended after P22.2 (P22+ planning), re-run this ticket file — it is idempotent (`audit: incremental` is then sufficient; say so in the PR title).
