<!--
  P28.3 — Contradictions & count reconciliation surfaced (Round 6 / DEPTH.3).
  DRAFT on devin/round6-planning — seeded post-P27. Design: ROUND6_7 §4 P28.3.
-->
# P28.3 — Contradictions & count reconciliation surfaced (P28)

- **Semantic id:** `DEPTH.3` · **Phase:** 28 · **Kind:** ticket · **Tag:** p28-depth
- **base_branch:** `devin/p28-2-sharing-relationship-network` · **Depends on:** P28.1 (resolved entities), P28.2 (edges)
- **Run:** `implement-spec spec=docs/tickets/P28.3__contradictions-reconciliation.md live_verification=true`
- **Gate status:** engineering — materialises contradictions (new sqitch change, append-only).

## Goal
Run §29 count reconciliation + the contradiction object over the real spine (portal-vs-contract device
counts, the 299-vs-190 pattern at scale) and **materialise contradictions — kept visible, never resolved
away.** This is SIG's signature: disagreement is data, not noise.

## Load (cite; do not copy)
- `docs/2_canonical_design_spec.md` §29 (reconciliation), §8.x + the Contradiction first-class entity (P08.3), §3.1 (contradictions stay visible).
- Code: `reconcile/counts.py`, `reconcile/contradiction.py`, `reconcile/lifecycle.py`, `db` `contradiction` table (empty), `api/store_pg._persisted_contradictions` (compute-on-read today).

## In scope — deliverables
1. Run count reconciliation + contradiction detection over the resolved spine; **materialise `contradiction`
   rows** (new sqitch change; append-only; lifecycle-aware — a contradiction resolved by later evidence is
   a new state, never a deletion).
2. The public surface (via P28.5) shows real, live contradictions with their evidence on both sides.

## Out of scope
- Coverage (P28.4); the surface refresh (P28.5); silently reconciling any contradiction.

## Acceptance criteria
- [ ] contradictions materialised over the resolved spine (new sqitch change; append-only; idempotent) *(deterministic + live-gated)*
- [ ] real contradictions (e.g. count disagreements) are detected + kept visible with both evidence sides *(deterministic)*
- [ ] `make check` + `make test-db` + build-memory green *(deterministic)*

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: contradictions stay visible (§3.1) — never silently reconciled;
append-only; every inference labelled.
