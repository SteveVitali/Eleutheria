# P19.1 — Build memory & repo hygiene (post-build chain opener)

- **Sequence:** 47 of 63
- **Phase:** 19 — Post-build capstone & consolidation (not in the spec's Part X; defined by the planning ledger and `DECISION_MEMO.md` §3 — after this ticket lands both live under `docs/build/`)
- **base_branch:** current checkout — `devin/p18-2-france-belgium` (tip of the 46-ticket stacked chain, PR #46)
- **Depends on (must land earlier in the stack):** P18.2 (the whole 46-ticket chain).
- **Run:** `implement-spec spec=docs/tickets/P19.1__build-memory-and-hygiene.md live_verification=false`
- **Gate status:** none (scope fully decided by the operator: ledger §4.1, §4.2, §4.7)

## Goal
Make the build's memory durable and its workspace coherent before any capstone work: commit the ticket contracts and every non-regenerable planning artifact under `docs/build/`, unify the agent scratch directories, give agents an `AGENTS.md` hierarchy, and fix the three small manifest/template defects — so every later ticket cites committed paths.

## Load (read these — do not re-read others; SIG-ENG-001)
- `.agents/scratch/NEXT-PHASE_planning-ledger_20260908.md` — §1 (T1, T3, T4, T15, T16), §4.1, §4.2, **§4.7** (artifact home), §9.G item 7 (build-ledger exception). **This ledger + `DECISION_MEMO.md` §3 row 47 are the spec for this ticket.**
- `.agents/scratch/planning/LEDGER_DEFERRALS.md` rows **LD-X03, LD-X07, LD-X09, LD-X10, LD-X11** (the defects this ticket fixes) and `SCOPING_NUMBERS.md` §(vi) "Scratch" row (sizes).
- `docs/tickets/00_MANIFEST.md` L34–35 (the "gitignored" paragraph), L71 (row 25 "32/34"), L93 (row 47); `docs/tickets/_TEMPLATE.md:9` ("of 43").
- `.gitignore` (the `scratch/` and `docs/tickets/` lines), `.devinignore` (untracked).
- `docs/2_canonical_design_spec.md` §47 (frozen layout — SIG-ENG-012), §51.3 (phase gate), §0.6 (Definition of Done). `README.md` "Repository layout & development", "Regenerating the specification".
- ADR format: `docs/adr/ADR-057-*.md` header (Status/Date/Phase/Requirement ids) + `docs/adr/README.md` index table.

## In scope — deliverables
1. **Tickets committed (T4, §4.2).** Delete the `docs/tickets/` line from `.gitignore`; `git add docs/tickets/` (46 contracts + `00_MANIFEST.md` + `_TEMPLATE.md` + P19.1–P21.9 = **65 files**). Replace manifest L34–35 with: "These ticket docs are **committed** as the build's contract record (decision 2026-09-08, ledger T4 / ADR-058); P19+ tickets are appended in table order." Fix row 25 "(32/34 detectors)" → "(all 34 detectors)" (LD-X10). Fix `_TEMPLATE.md:9` "of 43" → "of N" (LD-X11). Remove the two `docs/tickets/` lines from `.devinignore` (LD-X09; file stays untracked). Add one README sentence under "Repository layout & development" pointing to `docs/tickets/00_MANIFEST.md` and `docs/build/`.
2. **Planning artifacts committed under `docs/build/` (§4.7).** Create `docs/build/` and copy (not move — the scratch copies are deleted in step 3 once `git status` shows them staged): `.agents/scratch/NEXT-PHASE_planning-ledger_20260908.md` → `docs/build/PLANNING_LEDGER.md`; `.agents/scratch/planning/{BUILD_INDEX,LEDGER_DEFERRALS,SCOPING_NUMBERS,SCOPING_ID_LISTS,DECISION_MEMO}.md` → `docs/build/` (same names). Add `docs/build/README.md` (≤40 lines): what each file is, that later tickets append `COVERAGE_MATRIX.csv`, `CAPSTONE_GAP_ANALYSIS.md`, `COMPOSED_E2E_REPORT.md`, `CAPSTONE_CLOSURE.md`, `BACKLOG.*`, `OPERATIONAL_READINESS.md`, `TICKET_VS_SPEC.md`, `SPEC_RECONCILIATION_PLAN.md`, `INTEGRATION_PLAN.md`, `CI_STATUS.md`, and the P21 reports. In `PLANNING_LEDGER.md` prepend one line: "Committed copy; the live machine build ledger stays gitignored at `.agents/scratch/planning/sig-postbuild-build-ledger.md`." Fix internal path references in the copied files from `.agents/scratch/planning/` to `docs/build/` (sed; list the substitutions in the PR body). Scan every copied file for personal data (names of private individuals, emails) before staging — the planning artifacts quote only public officials and project ids; if anything else is found, redact and note it.
3. **Scratch unified (T3, §4.1 minus `planning/`).** Under `.agents/scratch/`: create `ledgers/ pr/ tools/ fixtures/ logs/ planning/` + `README.md` (naming rule `implement-spec_<PXX.Y>.md`; retention: logs deleted freely, ledgers kept). Move: all 44 `implement-spec_*`/`ledger_*` files (incl. `scratch/implement-spec_p00-1_ledger.md`, `scratch/ledger_p00-2.md`) → `ledgers/` renamed to `implement-spec_<PXX.Y>.md` (mapping table in `README.md`; the ticket id comes from `BUILD_INDEX.md` §A.2 column "Ledger"); `*_pr_body.md`, `*commit_msg*.txt`, `pr_body.md` → `pr/`; `*.py`, `*.sh` one-offs (`drive_gates.py`, `gen_l3_sample.py`, `scratch/{build_predicates,build_tech,gen_adrs,relicense}.py`, `scratch/scaffold_pkgs.sh`) → `tools/`; `er_fixture.json`, `scratch/p052_live/`, `p14-2_build_request.json`, `p12-1.diff`, `p09-1_code_diff.txt` → `fixtures/`; delete all 16 `*.log` (7.3 MB; reproducible by `make check`). `planning/` keeps **only** `sig-postbuild-build-ledger.md` (the orchestrate-build machine ledger — never committed) after step 2's copies are staged; the planning ledger and the five artifacts are then deleted from scratch. Remove the emptied root `scratch/` directory. `.gitignore` keeps `scratch/` (it also covers `.agents/scratch/`); nothing else added.
4. **`AGENTS.md` hierarchy (T15).** Invoke the `agent-docs` skill (bootstrap). Root `AGENTS.md` must state: `make check` is the gate (lint/format/typecheck/`uv run pytest`/`verify-gen`); `make test-db` needs Docker (`postgis/postgis:18-3.6`) and `SIG_REQUIRE_DB_TESTS=1` fails loudly; **`verify-gen` fails on uncommitted generated artifacts — commit `ontology/generated` first** (LD-X03); RDF generators are canonicalised via `rdflib.compare.to_canonical_graph` (LD-X07); the §47 layout is frozen (SIG-ENG-012); spec amendments go through `docs/research/_meta/spec_src/*.md` → `sh docs/research/_meta/spec_src/BUILD.sh` → ADR (SIG-ENG-003); tickets live in `docs/tickets/` (committed, manifest order), build memory in `docs/build/`, agent scratch in `.agents/scratch/` (gitignored, layout above); the stacked-PR rule (fork from current checkout). Per-package `AGENTS.md` only where the skill finds non-obvious conventions (expected: `web/`, `db/`, `connectors/`).
5. **Local housekeeping (T1, T16).** `git fetch origin && git branch -f main origin/main` (fast-forward local `main` without checking it out; verify `git rev-parse main == origin/main`). Do not switch branches.
6. **ADR-058** "Tickets and build memory are committed; scratch unified" — Status Accepted, Phase P19.1, Requirement ids SIG-ENG-001/003/012/031; Context = T3/T4/§4.7 decisions; Consequences = `docs/tickets/` no longer derived-only; Revisit trigger = if raw ledgers are ever wanted in git. Add to `docs/adr/README.md` index.
7. **Traceability + risk register.** Append `## Phase 19 — Post-build capstone (P19.1 — build memory)` at the end of `docs/risk_register.md` with rows `RISK-P19-01` (committed digests could carry personal data → scan step in 2), `RISK-P19-02` (scratch rename loses provenance → mapping table in scratch README + `BUILD_INDEX.md` links PR bodies). Append a short "Build memory (docs/build, docs/tickets)" section to `docs/traceability.md` naming the stamped ids.

## Out of scope (scope-creep guard — do NOT do these here)
- Any coverage/gap analysis — **P19.2**. Any test run beyond `make check`, any Docker composed run — **P19.3**. Any code or schema change — **P19.4**. Any `spec_src` edit — **P20.2**. Merging PRs or tagging — the operator, after the chain (no ticket merges); README overhaul (`refresh-repo-docs`) — **P20.3**. Backlog files — **P20.1**. Committing raw ledgers, PR bodies, or logs.

## Acceptance criteria
- [ ] `git ls-files docs/tickets | wc -l` = 65; `grep -c 'docs/tickets' .gitignore` = 0; manifest L34–35 replaced; row 25 reads "all 34"; `_TEMPLATE.md` has no "of 43". *(deterministic)*
- [ ] `git ls-files docs/build` lists exactly `README.md PLANNING_LEDGER.md BUILD_INDEX.md LEDGER_DEFERRALS.md SCOPING_NUMBERS.md SCOPING_ID_LISTS.md DECISION_MEMO.md`; `grep -rc '.agents/scratch/planning/' docs/build/*.md` is 0 except the two deliberate mentions (the build-ledger exception line and DECISION_MEMO §8). *(deterministic)*
- [ ] Root `scratch/` does not exist; `.agents/scratch/{ledgers,pr,tools,fixtures,logs,planning}` exist; `ls .agents/scratch/ledgers | wc -l` = 44 and every name matches `implement-spec_P[0-9][0-9]\.[0-9]\.md`; `ls .agents/scratch/planning` = `sig-postbuild-build-ledger.md` only; `find .agents/scratch -name '*.log' | wc -l` = 0; `git status --short` shows no scratch paths. *(deterministic)*
- [ ] Root `AGENTS.md` exists and contains the strings `make check`, `verify-gen`, `BUILD.sh`, `docs/tickets/`, `docs/build/`, `.agents/scratch/`. *(deterministic)* Its guidance is accurate against `Makefile` and `README.md` (spot-check 5 claims). *(agentic)*
- [ ] `git rev-parse main` == `git rev-parse origin/main`; current branch is still the P19.1 branch. *(deterministic)*
- [ ] `docs/adr/ADR-058-*.md` exists with the four header fields and a `## Revisit trigger`; `docs/adr/README.md` lists it. *(deterministic)*
- [ ] **Phase-gate (§51.3):** `make check` green with the **same test count as P18.2 (2363 local)** — no code changed; `verify-gen` clean; risk register has `RISK-P19-01/02`; traceability has the new section. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
No new canonical requirement ids (housekeeping). Stamp: SIG-ENG-001, SIG-ENG-003, SIG-ENG-012, SIG-ENG-031. Reference planning items T1, T3, T4, T15, T16 and LD-X03/X07/X09/X10/X11 in the PR body.

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** every committed digest row cites a path/PR/sha — no unexplained cells.
- **Part VIII is binding (§0.7):** no personal data of private individuals in anything committed (step 2 scan); raw ledgers/PR bodies stay uncommitted.
- **Append-only & provenance (P1–P3):** never edit the content of historical ledgers or PR bodies; rename/move only; the rename mapping is recorded.
- **Additive/back-compat:** zero code or schema change; `make check` result identical to P18.2's.

## Notes
- This ticket **owns**: `docs/tickets/` is committed from here on; `docs/build/` is the home of build memory; `.agents/scratch/` is the sole scratch root with the layout above. P19.2+ cite `docs/build/*` paths.
- The `orchestrate-build` machine ledger (`.agents/scratch/planning/sig-postbuild-build-ledger.md`) is the single §4.7 exception — it is live state and stays gitignored (ledger §9.G item 7).
- After this PR is opened, the next fresh session runs `implement-spec spec=docs/tickets/P19.2__capstone-gap-analysis.md` from this branch.
