# P23.6 — spec reconciliation refresh (Round 2, REC.2)

- **Sequence:** 72 of 73 · **Phase:** 23 (reconcile) · **Kind:** reconcile · **Tag:** reconcile
- **base_branch:** current checkout (the branch P23.5 left checked out)
- **Depends on:** P23.5 (backlog refresh)
- **Run:** `implement-spec spec=docs/tickets/P23.6__spec-reconciliation.md live_verification=false` (invokes `reconcile-build mode=spec`)
- **Gate status:** none · **Live stage:** none

> **Round 2 — delta over Round 1.** Round-1 reconciliation is `docs/build/TICKET_VS_SPEC.md` +
> `docs/build/SPEC_RECONCILIATION_PLAN.md` (P20.2, applied amendments A1–A8, fold-backs 668→671, ADR-062).
> This refreshes them for the post-P20.2 tickets (P21.x/P22.x/P22.3) and folds back any newly agreed
> amendment via `spec_src` → `BUILD.sh` (only where ticked). ADR-073 is already recorded (Appendix F, 72 ADRs).

## Goal
Keep the ticket-vs-spec map and the reconciliation plan current for the tickets landed since P20.2, and
apply any newly-ticked amendment through `spec_src` (never editing the canonical spec directly).

## Load (read these — do not re-read others)
- `docs/build/TICKET_VS_SPEC.md`, `docs/build/SPEC_RECONCILIATION_PLAN.md` (Round-1 baseline).
- `docs/research/_meta/spec_src/*` + `BUILD.sh`, `docs/build/tools/check_spec_src.py`, `docs/research/_meta/spec_src/99a_appF_adr.md` (Appendix F; now 72 ADRs incl. ADR-073).
- The P21.x/P22.x/P22.3 tickets + their PR bodies (`docs/build/pr/<ID>.md`).

## In scope — deliverables
1. Refresh `docs/build/TICKET_VS_SPEC.md` with the post-P20.2 tickets (each deliverable tagged in-spec / spec-implied / ticket-added).
2. Refresh `docs/build/SPEC_RECONCILIATION_PLAN.md` (any new amendments with target file/anchor/before-after/tick state); apply only ticked amendments through `spec_src` → `BUILD.sh` with a manifest amendment line + an ADR; keep `check_spec_src.py` exit 0.

## Out of scope
- Backlog/readiness (P23.5), integration plan (P23.7). Editing the canonical spec directly (SIG-ENG-003).

## Acceptance criteria
- [ ] `check_spec_src.py` exits 0 after any `spec_src` change (ADR set == Appendix F; id count consistent) *(deterministic)*
- [ ] every post-P20.2 ticket is dispositioned in `TICKET_VS_SPEC.md`; applied amendments have a manifest line + an ADR *(deterministic)*
- [ ] **Phase-gate:** verification green; requirement ids stamped; `DEFERRALS.md` maintained; ADRs for deviations; `BUILD_INDEX.md` row and `LEDGER.md` advanced. *(agentic)*

## Requirement IDs to satisfy and stamp in the PR
The reconciliation/fold-back ids (matching `SIG-[A-Z]+-[0-9]+`) this refresh touches.

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`; the spec changes only through `spec_src` → `BUILD.sh` (SIG-ENG-003).

## Notes
- HG-13 gates any normative amendment; with no new amendment this ticket is a documentation refresh only.
