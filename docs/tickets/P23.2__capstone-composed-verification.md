# P23.2 — capstone composed verification (Round 2, CAP.2)

- **Sequence:** 68 of 73 · **Phase:** 23 (capstone) · **Kind:** capstone · **Tag:** capstone
- **base_branch:** current checkout (the branch P23.1 left checked out)
- **Depends on:** P23.1 (Round-2 gap analysis)
- **Run:** `implement-spec spec=docs/tickets/P23.2__capstone-composed-verification.md live_verification=true`
- **Gate status:** none · **Live stage:** operator-gated: Docker (only if a seam was re-touched)

> **Round 2 — delta over the completed Round-1 capstone.** PR #68 exercised the whole build as one
> unit with Docker up (`make check` 2718 passed, 0 failed, 0 xfailed; `make test-db` 120; `tests/e2e`
> 16 passed, 0 xfailed; `run_okc.sh` 8/8; OKC dossier 299-vs-190 from export bytes). Re-run the composed
> verification **only if** P23.1 found a seam the Round-2 change could have broken; otherwise cite PR #68.

## Goal
Keep the composed-build proof current: either re-drive the composed stack (Docker) if a seam changed, or
record that PR #68's green composed run still holds because nothing on a composed path changed.

## Load (read these — do not re-read others)
- `docs/build/COMPOSED_E2E_REPORT.md`, `docs/build/reports/CAPSTONE_VERIFICATION.md` (Round-1 composed proof).
- `docs/build/CAPSTONE_GAP_ANALYSIS.md` P23.1 delta section (what, if anything, needs re-running).
- `docs/build/tools/run_okc.sh`, `tests/e2e/`. **Do not fabricate green** — a blocked seam is an `xfail`/skip whose reason starts with a `DEFERRALS` id.

## In scope — deliverables
1. A delta section appended to `docs/build/COMPOSED_E2E_REPORT.md`: either the re-run command output (if a seam changed) or a cited statement that PR #68's composed run remains valid (with the git range proving no composed-path file changed).

## Out of scope
- Gap analysis (P23.1), closure (P23.3), reconciliation (P23.5–P23.7).

## Acceptance criteria
- [ ] the composed proof is current: a green re-run if a seam changed, else a cited no-change statement referencing PR #68 *(deterministic)*
- [ ] any unwired seam is an `xfail`/skip whose reason begins with a `DEFERRALS`/`LD-` id — never a loosened assertion *(deterministic)*
- [ ] **Phase-gate:** verification green; requirement ids stamped; `DEFERRALS.md` rows for anything not automatically verifiable; ADRs for deviations; `BUILD_INDEX.md` row and `LEDGER.md` advanced. *(agentic)*

## Requirement IDs to satisfy and stamp in the PR
The composed-verification ids (matching `SIG-[A-Z]+-[0-9]+`) the Round-2 delta touches.

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`.

## Notes
- If Docker is absent, the composed re-run is `gate-pending` (a `DEFERRALS.md` row), never a fabricated pass.
