<!--
  P30.2 — Hosted Round-6 materialization + detector research queue (Round 8 / GO-LIVE.2).
  DRAFT on devin/p30-golive-seed — stacks on the P30.1 tip.
  HOSTED-EXECUTION + GATED tail: deploys the 6 Round-6 sqitch changes to the hosted spine and
  runs every materializer over the complete spine, then the P29.2 detector research queue.
  It PAUSES at a real human gate: the operator must grant the read/materialize DB role
  (Cloud SQL `postgres` is not a superuser and cannot assume the read roles).
-->
# P30.2 — Hosted Round-6 materialization + detector research queue (P30)

- **Semantic id:** `GO-LIVE.2` · **Phase:** 30 · **Kind:** ticket · **Tag:** p30-golive
- **base_branch:** `devin/p30-1-osm-land-and-settled-reaudit` · **Depends on:** P30.1 (settled hosted spine) + the P28.1–P28.6 materializers + P29.2 detector engine (all landed, proven over real PG18+PostGIS)
- **Run:** `implement-spec spec=docs/tickets/P30.2__hosted-round6-materialization.md live_verification=true`
- **Gate status:** **operator DB-role grant (pause for it).** The Cloud SQL `postgres` user is NOT a
  superuser and cannot assume the read/materialize roles the materializers run as — the operator must
  provision + GRANT the read/materialize role (e.g. `sig_read_public` / a dedicated materialize role) on
  the hosted `sig-pg` before the materializers can run. The ticket deploys the sqitch changes and prepares
  the run, then **pauses** for the grant; it never bypasses RLS or self-grants superuser. HG-11 (resolution
  publication) is already ANSWERED "sign off, proceed" (LEDGER GATE DECISIONS 2026-09-23) — only the DB-role
  grant is outstanding here.

## Goal
Deploy the **6 Round-6 sqitch changes** — `resolution_materialize`, `relationship_materialize`,
`contradiction_materialize`, `coverage_materialize`, `research_task_trigger`, `accountability_link_materialize`
— to the hosted `sig-pg` spine, provision the operator-granted read/materialize DB role, then run **every
materializer over the complete (post-OSM-land) spine in dependency order**: resolution (P28.1) → edges
(P28.2) → contradictions (P28.3) → coverage (P28.4) → accountability links (P28.6) → then the **P29.2
detector research queue** (P29.2). This turns the built-but-dormant depth machinery — proven only over seeded
DBs so far — into the REAL materialized graph the national export (P30.3) reads.

## Load (cite; do not copy)
- `docs/tickets/DEFERRALS.md` — the live halves this closes: **D-R6.1-EVAL** (hosted-scale resolution materialization + eval), **D-R6.2-EDGES**, **D-R6.3-CONTRADICTIONS**, **D-R6.4-COVERAGE**, **D-R6.6-ACCOUNTABILITY**, **D-R7.2-DETECTORS** — each row has the exact `cloud-sql-proxy` + `PGPASSWORD` + materializer command + the "operator-held read/materialize role" finding.
- `docs/2_canonical_design_spec.md` §14.7 (ER), §29/§29.3 (reconciliation, sharing edges), §31 (contradictions kept visible), §32 (honest coverage, no total), §11.11–11.18 (accountability layer), §33.2 (detector catalog).
- Code: `reconcile/` (`materialize-edges`, `materialize-contradictions`), `inference/` (`materialize-coverage`, `materialize-accountability-links`, `access-paths`), the P28.1 resolution materializer (`sig-reconcile materialize`), `tasks/` (`tasks.detect` + `tasks.research_pg` + `sig-tasks detect --dsn`), `db/` sqitch changes for the six Round-6 tables; `scripts/eval/run_resolution_eval.py`.
- `docs/build/reports/P28.1_resolution_eval.md` (the PROVISIONAL first-pass eval to re-run at hosted scale).

## In scope — deliverables
1. **Deploy the 6 Round-6 sqitch changes** to the hosted spine (append-only; deploy/verify; no in-place edits).
2. **Provision the read/materialize role** — prepare the GRANT, PAUSE for the operator to grant it (Cloud SQL
   `postgres` cannot assume the read roles), then run under the granted role over cloud-sql-proxy + ADC.
3. **Run every materializer over the complete spine in order:** resolution → relationship edges → contradictions
   → coverage → accountability links; each append-only + idempotent (`+0` on re-run). Record real counts per
   table (resolved sites N-of-M, edges by access kind, contradictions by type, coverage metrics + negative
   space, accountability links by link type) in dated reports — never a fabricated hosted count.
4. **Re-run the resolution eval at hosted scale** over the ~75k+ hosted observations; refresh
   `P28.1_resolution_eval.md` with the scale numbers (thresholds stay PROVISIONAL per D-R6.1-EVAL's
   first-principles remainder — that half stays OPEN).
5. **Run the P29.2 detector research queue** (`tasks detect --dsn <hosted> --jurisdiction … --out …`) over the
   now-materialized contradiction/coverage/relationship tables → append-only `research_task` rows, each citing
   its trigger; records requests DRAFTED not sent (D-R7.2-SEND stays operator-gated, untouched here).

## Out of scope
- Building the national export or the public cut-over (P30.3).
- SENDING any drafted records request (D-R7.2-SEND — operator-gated, stays deferred).
- The from-first-principles re-derivation of the resolution balance against human ground truth (D-R6.1-EVAL's
  second half — needs real human ground truth that does not exist yet; stays OPEN).
- Any bypass of RLS / self-grant of superuser; any UPDATE/DELETE against the spine.

## Acceptance criteria
- [ ] the 6 Round-6 sqitch changes deployed + verified on hosted `sig-pg` (append-only) *(live-verified)*.
- [ ] operator read/materialize role granted (gate) + all five materializers run over the complete spine, append-only + idempotent `+0`, with real per-table counts recorded *(live-verified)* — closes **D-R6.2-EDGES**, **D-R6.3-CONTRADICTIONS**, **D-R6.4-COVERAGE**, **D-R6.6-ACCOUNTABILITY**, and the hosted-scale half of **D-R6.1-EVAL**.
- [ ] the resolution eval re-run at hosted scale; `P28.1_resolution_eval.md` refreshed with scale numbers + the PROVISIONAL-eval disclosure retained (D-R6.1-EVAL's first-principles remainder stays OPEN, honestly noted).
- [ ] the P29.2 detector run over the materialized graph writes real `research_task` rows (each citing its trigger; requests drafted, `sent`=0) — closes **D-R7.2-DETECTORS**; D-R7.2-SEND untouched.
- [ ] `make check` + build-memory green; each closed deferral flipped to `DONE` with date + evidence; no fabricated hosted count.

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: append-only materialization (new rows, never UPDATE/DELETE);
idempotent `+0` re-run; contradictions kept VISIBLE, never reconciled away (§3.1/§31); never a coverage total
(SIG-METRIC-008); DRAFT-never-send for records requests (SIG-TASK-018); no RLS bypass; no fabricated hosted
count; no merge/tag/push-main.
