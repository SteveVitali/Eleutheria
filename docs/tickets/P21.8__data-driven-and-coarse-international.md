# P21.8 — Ecosystem connectors: EFF/MuckRock Data Driven releases as a first-class source; the coarse-international content path

- **Sequence:** 62 of 63
- **Phase:** 21 — Operationalization (`docs/build/DECISION_MEMO.md` §3 row 62)
- **base_branch:** current checkout — the P21.7 branch
- **Depends on (must land earlier in the stack):** P21.3 (`sig-connectors run`, transports, gate refusal), P21.1 (packet template + review-metadata rule).
- **Run:** `implement-spec spec=docs/tickets/P21.8__data-driven-and-coarse-international.md live_verification=true`
- **Gate status:** **GATE HG-03/HG-04 per source** — the connector ships over committed fixtures; a live run happens only for sources the operator flipped after reading the packets this ticket produces. Operator lists flipped sources: **NONE — SKIP (no source flipped).** Ship the `data_driven` connector + the coarse-international content path over committed fixtures and produce the rights packets (`eff_data_driven`, `carnegie_ai_gsi`, `facial_recognition_world_map`, `aspi_mapping_chinas_tech_giants`; MuckRock is the release co-publisher, not a distinct registry source — ADR-070). No live fetch. Every new/updated `sources.toml` row stays rights UNDETERMINED / `ingestion_permitted=false`; `run --mode live` for these sources REFUSES (exit 3, gate reasons) — asserted. Per-source HG-03/HG-04 recorded **pending** → RETURN PASS.
  - *Gate answers may arrive via the build ledger's `GATE DECISIONS` table instead of an edit to this file (the orchestrator pauses before this ticket and records the operator's answers there). In that case copy them into this block in your first commit. An answer of "skip" means: run ungated as described, and list the ticket in the ledger's RETURN PASS table.*

## Goal
Implement the one ingestion source the spec names as first-class but no ticket ever built — EFF/MuckRock **Data Driven** releases (`SIG-INGEST-043, 043a–d, 044`, referenced nowhere in the build) — with versioned re-ingest and agency crosswalk, and give the three coarse-international sources (LINK-only today) a gated content path, all through the eight-stage framework and the loader gate.

## Load (read these — do not re-read others; SIG-ENG-001)
- Spec: §24.3 Data Driven releases (SIG-INGEST-043, 043a, 043b, 043c, 043d, 044 — read the exact obligations: first-class source, release versioning, per-release provenance, agency crosswalk, records-request linkage), §22.5 Stage-5, §47 international coarse sources (P18.1), §21, §26, §42, §43.2a (aggregates only — RISK-P0-08).
- `docs/build/SCOPING_ID_LISTS.md` L1 (the INGEST-043* ids), `docs/build/COVERAGE_MATRIX.csv` rows `routing=P21.8`, `docs/build/BACKLOG.csv` rows `landing=P21.8`; `docs/build/RIGHTS_REVIEW_INDEX.md`; ADR-034, ADR-056.
- Anchors (re-confirm): `connectors/src/connectors/{stages.py:336, pipeline.py, records.py, coarse_international.py:16, data/sources.toml, data/records_vocab.toml}`; the `run` CLI and `HttpxTransport` (P21.3); `resolution/` cascade entry points (P03.2 — `grep -n "def cascade\|normalize_org_name" resolution/src/resolution/*.py`); `tasks/src/tasks/records_request.py`; `docs/build/rights/_TEMPLATE.md` (P21.1).

## In scope — deliverables
1. **Registry + packets:** `sources.toml` rows for EFF Data Driven (`eff_data_driven`, `source_kind=upstream_project`, `custody_posture=MIRROR` candidate, rights UNDETERMINED until reviewed) and, if distinct, the MuckRock release channel; rights packets for them and for the coarse-international trio (`aspi_mapping_chinas_tech_giants`, `carnegie_ai_gsi`, `facial_recognition_world_map`) using P21.1's template; `RIGHTS_REVIEW_INDEX.md` updated.
2. **`data_driven` connector:** release manifest ingestion — each release is a versioned artifact with digest + retrieval date (`043b` provenance); per-agency aggregate rows only (never per-search/person — `043`, RISK-P0-08); agency crosswalk to SIG identities through the P03.2 cascade (`043c`); versioned re-ingest yields new dated claims, never overwrites (`043d`); records-request linkage into `tasks.records_request` where a release documents a request (`044`). Fixtures: two small release snapshots (v1, v2) proving versioning. Registered in `list-connectors`; runs in `replay`/`shadow`/`live` via `run`.
3. **Coarse-international content path:** keep LINK posture; add the `REFERENCE`-capture path so that, once a packet + flip changes a row, the existing `coarse_international.py` extractor runs through `run --mode live`; tests over fixtures for the three.
4. **Run what is permitted:** flipped sources → live run into the composed PG with fetch records; others → fixture replay; `docs/build/ECOSYSTEM_CONNECTORS.md` (per source: ids satisfied, fixtures, live state, claims produced).
5. **ADR-070** (Data Driven release model; coarse-international capture path), risk register `RISK-P21-14` (release schema changes between versions → per-release parser pinned by digest), `RISK-P21-15` (aggregate-only guarantee → schema test that rejects any per-search column), traceability; `COVERAGE_MATRIX.csv` INGEST-043*/044 → MET with test evidence.

## Out of scope (scope-creep guard — do NOT do these here)
- Stage-5 connectors for the P17 pathways and the deferred parser layers — **P21.9**. Flipping sources (HG-03/HG-04). FR/BE live (design-gated). Flock portal live client (RISK-P0-17). A second jurisdiction (P22+).

## Acceptance criteria
- [ ] `uv run sig-connectors list-connectors` includes `data_driven`; `validate` passes with the new rows. *(deterministic)*
- [ ] `uv run pytest tests/connectors -k "data_driven or coarse"` green: two release snapshots yield versioned claims (v1 rows retained, v2 rows added, 0 overwrites); the aggregate-only schema test rejects a fixture with a per-search column; crosswalk resolves ≥1 agency to an existing SIG identity. *(deterministic)*
- [ ] `shadow` over all new fixtures: 0 diffs on re-run. *(deterministic)*
- [ ] `COVERAGE_MATRIX.csv`: `SIG-INGEST-043, 043a, 043b, 043c, 043d, 044` verdict MET with test evidence; `check_coverage_matrix.py` exits 0. *(deterministic)*
- [ ] If any source flipped: `ECOSYSTEM_CONNECTORS.md` records ≥1 live run with captures + claims in PG. *(deterministic, conditional on HG-03)*
- [ ] Packets are factual (reviewer spot-checks the quoted terms). *(agentic)*
- [ ] **Phase-gate (§51.3):** `make check` green; ADR-070; traceability + risk register; `BACKLOG.csv` rows closed. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
SIG-INGEST-043, SIG-INGEST-043a, SIG-INGEST-043b, SIG-INGEST-043c, SIG-INGEST-043d, SIG-INGEST-044, SIG-INGEST-021/027/028 (framework + gate), the P18.1 coarse-source ids routed `P21.8`.

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** every release version is a dated claim set with provenance; crosswalk matches are labelled with their cascade level.
- **Part VIII is binding (§0.7):** per-agency aggregates only — never per-search or per-person rows (§43.2a, RISK-P0-08).
- **Append-only & provenance (P1–P3):** versioned re-ingest appends.
- **Additive/back-compat:** existing connectors and fixtures untouched.

## Notes
- This ticket **owns** the `data_driven` connector and its release-version model; P21.9 reuses the packet + `run` workflow for the pathway connectors.
