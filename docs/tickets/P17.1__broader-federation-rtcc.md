# P17.1 — Private-camera federation and RTCC integration

- **Sequence:** 42 of 46
- **Phase:** 17 — Broader surveillance technologies (Stage 5)
- **base_branch:** current checkout — the branch the previous ticket left checked out (stacked PR chain); `main` for P00.1
- **Depends on (must land earlier in the stack):** P16.2 (predecessor). Substantive: P01.1 (ontology + the generalization conformance suite this phase must not regress), P12.2 (access edges + access-path closure).
- **Run:** `implement-spec spec=docs/tickets/P17.1__broader-federation-rtcc.md`

## Goal
Extend the populated graph to **private-camera federation** and **RTCC integration** with **no
schema change** — any required change is a Phase-1 defect and MUST be recorded as one. Model the
commercial **data-broker chain** with distinct **aggregator vs productizer** roles where relevant,
and model integration hubs that **consume other systems** as directional integration edges, not a
generic `integrates_with`.

## Load
- `docs/2_canonical_design_spec.md` §5.2 (the generalization requirement — SIG-CHART-027/028), §11 (entity catalog — esp. §11.7 Deployment, §11.8 PhysicalAsset, §11.10 DataSystem), §13.1 (technology vocabulary — `surveillance-video` private-camera registry/integration, `integration-platform` RTCC / camera-federation hub), §23 (connector specifications — §23.1 universal rules; integration-hub / RTCC ingestion).
- Companions: §12.3 (integration edges — `enrolls_asset_into`, `federates_search_to`, `resells_data_from`), §12.4 (roles — owner ≠ operator for private cameras), Appendix D.5 pathway 1 (private camera → fusion center) and pathway 3 (six-layer broker chain, aggregator ≠ productizer).
- Part 0, Part I §3, Appendix E glossary.

## In scope — deliverables
1. **Private-camera federation** populated using the existing schema: `owner ≠ operator` for a private camera federated into a police RTCC; `enrolls_asset_into` (the object is a device) kept distinct from a live-feed fact with its own consent gate — SIG-ONTO-047/048, SIG-ONTO-045/046.
2. **RTCC integration hubs** that consume other systems, modelled as `DataSystem` + directional integration edges (`ingests_feed_from`, `federates_search_to`, `pushes_alerts_to`), never a stored `integrates_with` — SIG-ONTO-045/046; reference databases as infrastructure — SIG-ONTO-031.
3. The **commercial data-broker chain** representable with distinct **aggregator vs productizer** roles (`subscribes_to` → `resells_data_from` → `resells_data_from`), six layers not five — SIG-ONTO-046 + Appendix D.5 pathway 3.
4. A **Phase-1-defect record** path: if populating any of the above requires a schema change, it is filed as a Phase-1 defect (not patched silently here).

## Out of scope (do NOT do here)
- Facial recognition, cell-site simulators, mobile-device forensics, and federal authorization datasets — **P17.2**.
- Gunshot detection, drones, and commercial location data — **P17.3**.
- Any schema/ontology change (LinkML source is owned by P01.1; a required change is a defect, not a deliverable here).

## Acceptance criteria
- [ ] Private-camera federation and RTCC integration are **populated with no schema change**; the generalization conformance suite still passes and any required change is recorded as a Phase-1 defect (SIG-CHART-027/028). *(deterministic)*
- [ ] The **commercial data-broker chain is representable with distinct aggregator and productizer roles** — test constructing the six-layer chain with `resells_data_from` between separate broker and productizing-platform organizations (SIG-ONTO-046, Appendix D.5 pathway 3). *(deterministic)*
- [ ] Integration hubs that consume other systems are expressed as directional edges (no stored `integrates_with`); the private-camera `owner ≠ operator` separation is independently representable — test (SIG-ONTO-045/047/048). *(deterministic)*
- [ ] **Phase-gate (§51.3):** CI green incl. data-quality checks; new requirements have automated tests (SIG-ENG-004); ADRs written for any deviation; traceability matrix updated; risk-register entries for this phase updated.

## Requirement IDs to satisfy and stamp in the PR
SIG-CHART-027, SIG-CHART-028, SIG-ONTO-045, SIG-ONTO-046, SIG-ONTO-047, SIG-ONTO-048, SIG-ONTO-031 — plus the §11/§13.1/§23 entity/vocab/connector ids these deployments realize (enumerate in the PR table). Each must appear in the commit/PR and be covered by an automated test.

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** no unexplained dots/edges; no silent overwrites; no synthetic certainty; every node has identity; every state has time; every claim has evidence; every inference is labelled; every contradiction stays visible.
- **Part VIII is binding (§0.7):** no plate/trip/per-person storage or lookup; officer-naming test gates any person-named claim; sensitivity tiers + coordinate rules enforced; licence gate + ODbL separation hold.
- **Append-only & provenance (P1–P3):** no writable "current value" columns; raw_value preserved; corrections are new assertions, never overwrites.
- **Additive/back-compat:** do not break a prior ticket's wire names, IDs, or schema contracts; new fields optional with today's-behavior defaults.

## Notes
This phase is the standing proof of §5.2: **the schema built in Phase 2/4 must absorb Stage-5
technologies with no change.** This ticket does not own the ontology — a schema change here is a
Phase-1 defect, recorded as one, per the Phase-17 acceptance gate. The aggregator-vs-productizer
split matters because collapsing it (the outline's five-step diagram) hides a real institutional
layer in the chain that turns an observation into searchable power (§30.2 access-path closure).
