<!--
  P28.4 — Honest coverage computed (Round 6 / DEPTH.4).
  DRAFT on devin/round6-planning — seeded post-P27. Design: ROUND6_7 §4 P28.4.
-->
# P28.4 — Honest coverage computed (P28)

- **Semantic id:** `DEPTH.4` · **Phase:** 28 · **Kind:** ticket · **Tag:** p28-depth
- **base_branch:** `devin/p28-3-contradictions-reconciliation` · **Depends on:** P28.1 (resolved entities)
- **Run:** `implement-spec spec=docs/tickets/P28.4__honest-coverage.md live_verification=true`
- **Gate status:** engineering — materialises coverage (new sqitch change, append-only).

## Goal
Populate `coverage_record` / the §32 metrics from real data — counted quantities with **named
denominators**, records-derived bounds, negative space, per-jurisdiction site counts — and **never a
total or a population estimate** (SIG-METRIC-008). Back the coverage + "what we don't know" pages with
real numbers, including the P28.1 resolution eval metrics.

## Load (cite; do not copy)
- `docs/2_canonical_design_spec.md` §32 (coverage/completeness/negative space; SIG-METRIC-008/008b — no total, no capture-recapture), §32.4 (freshness).
- Code: `inference/coverage.py`, `inference/denominators.py`, `inference/completeness.py`, `inference/freshness.py`, `db` `coverage_record` table (empty).

## In scope — deliverables
1. Compute + **materialise** coverage records from the resolved spine (new sqitch change; append-only):
   counted quantities with named denominators, records-derived bounds, per-agency reconciliation ratios,
   measured survey recall — each with its denominator, **never extrapolated to a total**.
2. Surface the P28.1 resolution eval (holdout P/R/F1, κ) on the coverage/methodology page (SIG measuring
   its own method).

## Out of scope
- The surface refresh (P28.5); any total/population estimate; extrapolating a named-subset recall.

## Acceptance criteria
- [ ] coverage records materialised with named denominators + negative space; NO total/population estimate emitted (test-pinned) *(deterministic + live-gated)*
- [ ] the resolution eval metrics are surfaced honestly on the coverage/methodology page *(deterministic)*
- [ ] `make check` + `make test-db` + build-memory green *(deterministic)*

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: never publish a total (§32); named denominators; absence is a
visible gap; append-only; no synthetic certainty.
