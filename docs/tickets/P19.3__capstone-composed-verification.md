# P19.3 — Capstone composed verification + retroactive live verification (CAPSTONE step 3)

- **Sequence:** 49 of 63
- **Phase:** 19 — Post-build capstone & consolidation (`docs/build/DECISION_MEMO.md` §3 row 49; orchestrate-build CAPSTONE step 3)
- **base_branch:** current checkout — the P19.2 branch
- **Depends on (must land earlier in the stack):** P19.2 (`COVERAGE_MATRIX.csv` routing `P19.3`; seam list §(e)).
- **Run:** `implement-spec spec=docs/tickets/P19.3__capstone-composed-verification.md live_verification=true`
- **Gate status:** none. Requires a local Docker daemon (present: `docker info` → 29.1.3). **Never fabricate green** — a seam that cannot be crossed is recorded as an `xfail` with its LD id and the exact blocker.

## Goal
Drive the whole build as one unit for the first time — PG18+PostGIS claim spine → OCFL evidence store → connector replay over committed fixtures → resolution → reconcile → API → exports → `web/` build — commit that path as a Docker-gated composed test, and retroactively perform the implement-spec 5.3 live verification that 28 tickets skipped or ran over fixtures only. The output tells P19.4 exactly which seams are CODE gaps.

## Load (read these — do not re-read others; SIG-ENG-001)
- `docs/build/BUILD_INDEX.md` §A.3 (the seam list and the 5.3 roll-up: 18 fixture-only, 10 not-recorded), `docs/build/LEDGER_DEFERRALS.md` §1 rows LD-V01, LD-V02, LD-V03, LD-V04, LD-V05, LD-V06, LD-V07, LD-V08, LD-V09, LD-V11 (**not** LD-V10 = accepted, **not** LD-V12 = P19.5), LD-X06, §7 rows LH-01, LH-02, LH-03, LH-04, LH-05, LH-06, LH-07, LH-08, LH-09, LH-10, LH-11, LH-12, LH-13, LH-14, LH-15, `docs/build/SCOPING_NUMBERS.md` §(vi) (anchors), `docs/build/CAPSTONE_GAP_ANALYSIS.md` §(e) and rows routed `P19.3`.
- Anchors (re-confirm at build time): `tests/db/conftest.py:30,48-49,54,127` (`PG_IMAGE`, `_require_or_skip`, `sig_database`, `conn`); `db/sqitch.plan`; `evidence/src/evidence/ocfl.py:88` (`OcflStore`); `connectors/src/connectors/{stages.py:190,269,280, replay.py:32,98, pipeline.py:125, loader.py:76,93, net.py:76,153}`; `tests/acceptance/okc_slice.py` + `fixtures/okc_sources.json`; `resolution/src/resolution/{probabilistic.py:199,257, review_queue.py:199}`; `reconcile/src/reconcile/resolve.py:291`; `api/src/api/{store.py:108,147, app.py:42}`; `exports/src/exports/cli.py:23-64`; `web/src/lib/fixtures.ts`; `web/package.json` scripts.
- Spec: §2.2 J-1 + §2.3 acceptance queries (SIG-CHART-009), §16 (claim spine), §17 (evidence store), §21.1 (eight connector stages), §37 (API), §38 (exports), §48 (DB), §51.3.

## In scope — deliverables
1. **`tests/e2e/test_composed_stack.py`** (new dir `tests/e2e/`, picked up by `testpaths=["tests"]`; module-level `pytestmark` gated by the same `_require_or_skip` logic as `tests/db/conftest.py` — import or copy its helper; skips without Docker, fails with `SIG_REQUIRE_DB_TESTS=1`). One ordered test per seam, sharing a session fixture: (S1) deploy `db/sqitch.plan` into a fresh `postgis/postgis:18-3.6` container; (S2) `OcflStore` at a tmp path; capture the 10 OKC fixture artifacts as evidence objects; (S3) replay the `atlas` and `osm` connectors over their committed fixtures via `replay()` with a **claim sink** — if the only sink is `InMemoryClaimSink`, assert claims in memory and `pytest.xfail("LD-F06b: no PG ClaimSink — P19.4")` for the PG-row assertion; (S4) ER: `ProbabilisticMatcher.match` over the slice orgs; `ReviewQueue.enqueue/decide` round-trip; assert against PG or `xfail("LD-F04: ER not DB-wired — P19.4")`; (S5) `RESOLVE` over the claims; assert the 299-vs-190 `claimed_device_count` contradiction stays visible (P06.1 retrospective); (S6) `create_app(store)` with a store built from **PG** — if only `InMemoryStore` exists, serve from it and `xfail("LD-F06: no DB-backed ReadStore — P19.4")` for the PG-backed variant; hit `/v1/resolution/...`, `/v1/dossier/...`, `/v1/coverage/...`, `/v1/contradiction`, `/v1/task`, `/id/...` with `httpx` and assert 200 + envelope shape; (S7) `sig-exports build --zenodo-dry-run` into a tmp dir; assert the licence split (ODbL compartment separate) and PMTiles archive present; (S8) `npm --prefix web run build` succeeds and `web/dist` contains the dossier route for the slice jurisdiction — `xfail("LD-V08: web reads fixtures, not exports — P21.4")` for any "rendered from export" assertion. Every `xfail` reason begins with an LD id.
2. **Retro 5.3 CLI matrix** (deterministic script `docs/build/tools/retro_cli_matrix.sh`, committed): drives each CLI once with a real argument and records exit code + first line: `python -m policy validate`, `python -m policy jurisdiction`, `sig-connectors validate|stages|list-connectors|gate --source osm_overpass|export-check`, `sig-ontology generate --check`, `sig-parsing classify <fixture>`, `sig-resolution normalize LAPD`, `sig-resolution review list`, `python -m reconcile --help`, `python -m inference --help`, `python -m tasks --help`, `sig-db analytics assert-schema`, `sig-exports --help`, `sig-ops --help`. (LD-V02 evidence; P19.5 correlates the `inference` row to its own work.)
3. **ISOLATION re-proof** (LD-X06): run the P04.1 network-isolation test set under the composed stack (`uv run pytest tests/connectors -k isolation`) and add one test that the `getaddrinfo`/`gethostbyname*` guard still blocks DNS from inside a connector run.
4. **`tests/db` locally**: `make test-db` (LD-V11 local half; CI half already green — SCOPING §(iv)).
5. **`docs/build/COMPOSED_E2E_REPORT.md`**: (a) environment (Docker version, image digest, `uv` version, node version); (b) seam table S1–S8: crossed / xfail(LD id) / blocked(reason + closing command); (c) retro 5.3 table — one row per ticket in BUILD_INDEX §A.3 "fixture-only" (18) and "not-recorded" (10): what was driven now, command, result — LH-01…15 rows explicitly say "evidence regenerated here, ledger not trusted"; (d) CLI matrix output; (e) the **CODE-gap handoff list for P19.4 (spine seams) and P19.5 (the rest)** (each xfail → item); (f) timings.
6. **Traceability + risk register**: `## Phase 19 — Composed verification (P19.3)` with `RISK-P19-05` (composed test depends on Docker → skips locally, required in CI via `SIG_REQUIRE_DB_TESTS`), `RISK-P19-06` (fixture replay ≠ live fetch; live is P21.3). Traceability section "Composed end-to-end (tests/e2e)".

## Out of scope (scope-creep guard — do NOT do these here)
- Writing the PG `ClaimSink`, DB-backed `ReadStore`, or any production code — **P19.4** (spine) / **P19.5** (rest) (this ticket only adds tests + the two tool scripts). Real network fetches (all connectors stay on fixtures; `assert_loadable` would refuse anyway — 0 sources permitted) — **P21.3**. Real Zenodo/object store — **P21.5**. Web reading exports — **P21.4**.

## Acceptance criteria
- [ ] `SIG_REQUIRE_DB_TESTS=1 uv run pytest tests/e2e -ra` runs S1–S8 in order; result is only `passed` and `xfailed`; **0 failed, 0 skipped**; every xfail reason matches `^LD-[A-Z]+[0-9]+[a-z]?:`. *(deterministic)*
- [ ] Without Docker (`DOCKER_HOST=tcp://127.0.0.1:1 uv run pytest tests/e2e`) the module skips cleanly. *(deterministic)*
- [ ] `sh docs/build/tools/retro_cli_matrix.sh` exits 0 and prints ≥14 rows with exit codes; output pasted in the report §(d). *(deterministic)*
- [ ] `COMPOSED_E2E_REPORT.md` §(c) has 28 rows (18 + 10), §(b) has 8 rows, §(e) lists every xfail once. *(deterministic)*
- [ ] `make test-db` green locally; count recorded. *(deterministic)*
- [ ] S5 asserts the contradiction is **visible** in the resolver output (not collapsed to one number). *(deterministic)*
- [ ] **Phase-gate (§51.3):** `make check` green (test count = 2363 + new e2e/isolation tests); CI (`ubuntu-latest`, Docker present) runs `tests/e2e` for real — confirm in the PR's CI log; ADR only if a deviation was needed (none expected); risk register + traceability updated. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
SIG-CHART-009 (acceptance queries executable end-to-end), SIG-ENG-005 (no Done on manual check alone — the composed path is now a test), SIG-ENG-034 (vertical-slice gate retro-fit), SIG-STORE-024 (DB tests against real PG), SIG-INGEST-021 (plain-CLI stages driven). Stamp also the ids of every spec § whose composed path is crossed (list from the report §(b)).

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** every contradiction stays visible (S5); no synthetic certainty — xfail, never a loosened assertion.
- **Part VIII is binding (§0.7):** fixtures only; no plate/person data; officer-naming gate untouched.
- **Append-only & provenance (P1–P3):** the test asserts append-only behaviour is preserved through the composed path (reuse `tests/db/test_append_only.py` assertions on the composed DB).
- **Additive/back-compat:** tests and tool scripts only; no production code.

## Notes
- This ticket **owns** `tests/e2e/` and the xfail-by-LD-id convention; P19.4/P19.5 flip xfails to asserts and P21.2/P21.4 extend the same module for live data.
- If Docker is unavailable in the run, stop and record `blockedOn: docker` — do not ship a fixture-only "composed" test.
