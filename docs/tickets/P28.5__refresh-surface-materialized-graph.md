<!--
  P28.5 — Refresh the public export/surface off the materialized graph (Round 6 / DEPTH.5).
  DRAFT on devin/round6-planning — seeded post-P27. Design: ROUND6_7 §4 P28.5.
-->
# P28.5 — Refresh the public surface off the materialized graph (P28)

- **Semantic id:** `DEPTH.5` · **Phase:** 28 · **Kind:** ticket · **Tag:** p28-depth
- **base_branch:** `devin/p28-4-honest-coverage` · **Depends on:** P28.1–P28.4, P27.4/P27.5 (the export + web data layer)
- **Run:** `implement-spec spec=docs/tickets/P28.5__refresh-surface-materialized-graph.md live_verification=true`
- **Gate status:** engineering — **republish is auto** (best-judgment, ADR-R6-MATERIALIZE-092): same evidence, better-shaped; NOT a fresh HG-11 UNLESS resolution changes what a claim *asserts* (e.g. a merged "N resolved sites" headline) — then HG-11.

## Goal
Re-derive the P27.4 national export (+ the web islands) from the now-**materialised** resolution /
relationships / contradictions / coverage instead of compute-on-read; reconcile the ADR-092 posture to
"materialised". The public surface shows resolved sites, a real network, live contradictions, honest
coverage.

## Load (cite; do not copy)
- `docs/2_canonical_design_spec.md` §38 (exports), §37/§39–41 (API/UI), §29 (resolution), §3.1.
- Code: `exports/` (the P27.4 spine-read export), `web/src/lib/data.ts` (the P27.5 data layer), `api/store_pg.py` (reads the materialised layer now), ADR-092 (compute-on-read posture being reconciled), ADR-090.

## In scope — deliverables
1. Point the export + read paths at the **materialised** resolution/relationship/contradiction/coverage
   tables; retire the compute-on-read shim for these where materialised.
2. The map shows **resolved sites** with the honest "N resolved sites (from M observations)" framing
   (replacing the observation-level framing); the network shows the P28.2 edges; contradictions + coverage
   are the P28.3/P28.4 real data.
3. **ADR-R6-MATERIALIZE-092** reconciling ADR-092 (materialised now). Republish per the gate rule above.

## Out of scope
- New IA/design (P27.6 already did it); new data (P28.1–P28.4); the domain (P27.10).

## Acceptance criteria
- [ ] export + read paths read the materialised graph; the surface renders resolved sites / real network / live contradictions / honest coverage *(deterministic + live-gated)*
- [ ] the observation-vs-resolved framing is honest ("N resolved sites from M observations"); no total *(deterministic)*
- [ ] `npm --prefix web run check` + `make check` + build-memory green; ADR-R6-MATERIALIZE-092 written *(deterministic)*

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: zero-JS + WCAG 2.2 AA on public pages; contradictions stay
visible; never a total; publication changes that alter what a claim asserts re-check HG-11 (§ publication).
