# P19.5 — Capstone gap closure: remaining CODE gaps, documentation gaps, the ACCEPTED-deviations list (CAPSTONE step 2, part 2)

- **Sequence:** 51 of 63
- **Phase:** 19 — Post-build capstone & consolidation (`docs/build/DECISION_MEMO.md` §3 row 51; orchestrate-build CAPSTONE step 2 closeout)
- **base_branch:** current checkout — the P19.4 branch
- **Depends on (must land earlier in the stack):** P19.2 (`CAPSTONE_GAP_ANALYSIS.md` §(i) — the `P19.4:S/M` work list, minus what P19.4 closed), P19.3, P19.4.
- **Run:** `implement-spec spec=docs/tickets/P19.5__capstone-gap-closure.md live_verification=true`
- **Gate status:** **GATE HG-14 (post-ticket).** The ACCEPTED-deviations list (deliverable 7) is *proposed* here; the orchestrator pauses **after** this ticket, presents §(b) to the operator, and records accept/reject per item in the build ledger `GATE DECISIONS`; **P20.1** writes the signature into `CAPSTONE_CLOSURE.md` §(e) and turns rejected items into backlog rows. This ticket itself needs no operator input.

## Goal
Finish the capstone's closure step: fix the remaining small/medium CODE gaps the whole-spec analysis routed to closure (export gate honours `derivative_permitted`, jurisdiction-conditional web render exists, `inference` CLI is wired), repair the documentation gaps (P08.1's missing ADR and §53 section; the un-ADR'd hardening commit), and record — for the operator's signature — exactly which deviations from the spec are accepted as sound.

## Load (read these — do not re-read others; SIG-ENG-001)
- `docs/build/CAPSTONE_GAP_ANALYSIS.md` §(i) (**the authoritative work list**: every `P19.4:S/M` row not marked `closed_by=P19.4` in `COVERAGE_MATRIX.csv`), §(e) seams **LD-H09, LD-H11** (handoffs received here); `docs/build/COMPOSED_E2E_REPORT.md` §(e) remaining items; `docs/build/LEDGER_DEFERRALS.md` rows **LD-V12, LD-F08, LD-F15, LD-D14, LD-X05** (+ LD-H09/LD-H11 as the handoff records for LD-F08/LD-V12); `.agents/scratch/pr/p_analytics_hardening_commit.txt` (message of `4493b14`).
- Spec: §38.3 + §42 (export gate, `derivative_permitted` — SIG-LIC-004/010, SIG-INGEST-048b), §44 (jurisdiction-conditional publication — SIG-PUB-* from P18.1's PR #45 table), §32 (inference CLI surface), §53 (risk register per phase — SIG-ENG-030/031), §51.3.
- Anchors (re-confirm): `policy/src/policy/licensing.py:111` (`compute_export_license`), `connectors/src/connectors/cli.py` (`export-check`), `registry.py` (`derivative_permitted` in `RightsRecord`); `policy/src/policy/{publication.py:96, jurisdiction.py:266}`; `web/src/lib/fixtures.ts`, `web/src/pages/dossier/`, `web/src/layouts/`, `web/tests/e2e/pages.ts`, `web/lighthouserc.json:15-17`; `inference/src/inference/{cli.py, coverage.py, access_paths.py, completeness.py, freshness.py}`; `docs/adr/ADR-044-*.md`, `docs/adr/README.md`; `docs/risk_register.md` L460 (Phase 8 starts at P08.2); PR #20 body (`gh pr view 20 --json body` — P08.1's requirement table).

## In scope — deliverables
1. **`derivative_permitted` in the export gate (LD-F08 / LD-H09):** `compute_export_license` (or the connectors `export-check` path — whichever the composed run showed is the real gate) partitions or refuses sources with `derivative_permitted=false`; tests with `deflock_app_repo` and `sm_alpr` (AGPL, RISK-P0-18). (SIG-LIC-004/010, SIG-INGEST-048b.)
2. **Jurisdiction-conditional web render (LD-V12 / LD-H11):** dossier and entity pages call `adapter_publication_permitted` in the build-time data path; BCP-47 `lang` + localised labels for the FR/BE fixtures; `web/tests/e2e/jurisdiction.spec.ts` + `.nojs`; zero-JS budget preserved. (P18.1's SIG-PUB-*/SIG-UI-* ids.)
3. **`inference` CLI subcommands (LD-F15):** `coverage`, `access-paths`, `completeness`, `freshness` wired to the existing modules; `python -m inference coverage --help` works; P19.3's CLI matrix row updates.
4. **Every other row in §(i)** not closed by P19.4 and sized S/M: implement + test; list each in `CAPSTONE_CLOSURE.md` §(a). Anything that turns out L is re-routed to the P21 ticket P19.2 would have chosen, recorded in §(c) — never done half-way.
5. **P08.1 documentation gap (LD-X05):** ADR-060 "The resolver (P08.1) — retro-fitted record" (Status Accepted; Phase P08.1; `Requirement ids` from PR #20's table; Deviations = none claimed or the ones the matrix found; Revisit trigger); insert `## Phase 8 — Resolver (P08.1)` into `docs/risk_register.md` **before** L460 with `RISK-P8-00a` (ruleset-as-data drift → `check_ruleset` test) and `RISK-P8-00b` (rationale quotability depends on fixture wording → live test from PR #20).
6. **ADR-044 amendment (LD-D14):** append `## Post-hoc hardening (4493b14, landed on P14.1's branch)` summarising reason_raw / rights_record / forbidden org-id columns / COMPLEMENTARY rationale (SIG-STORE-028/030/031, §11.16, §18.4) and why no separate ADR was written then.
7. **`docs/build/CAPSTONE_CLOSURE.md`:** (a) closed items (LD id / matrix id → commit → test), including P19.4's; (b) **ACCEPTED deviations** — every `MET-DIFFERENTLY` row in the matrix with reasoning; expected members: ADR-022 no partitioning, ADR-030 CLI curation *pending P21.6*, ADR-037/038/039/054 compute-on-read *pending P21.2 verdict*, ADR-051 zero-JS map *pending P20.2 A1*, ADR-023 `evidence/` package; (c) items routed to P21.x (unchanged); (d) `tests/e2e` xfail count before/after; (e) the operator's signature line (ticked in PR review — HG-14).
8. **ADR-061** for this ticket's decisions (gate placement, web render approach); traceability + risk register `## Phase 19 — Gap closure (P19.5)`; `COVERAGE_MATRIX.csv` verdicts updated (`closed_by=P19.5`); `check_coverage_matrix.py` exits 0.

## Out of scope (scope-creep guard — do NOT do these here)
- Spine wiring (`PgClaimSink`, `PgReadStore`, ER over PG) — **P19.4** (done). Annotation persistence — **P21.2**. Live transports — **P21.3**. Tile generation / MapLibre — **P21.5** / **P20.2**. Curation web UI — **P21.6**. Backlog ids — **P20.1**. `spec_src` edits — **P20.2**. Registry rows (OKC sources, `usaspending`) — **P21.1**. Any row P19.2 routed to a P21 ticket, however small. Flipping `ingestion_permitted` (HG-03).

## Acceptance criteria
- [ ] `SIG_REQUIRE_DB_TESTS=1 uv run pytest tests/e2e -ra`: 0 failed; the only remaining xfails carry LD ids routed to P21 tickets in the matrix (expected: `LD-V08` → P21.4; `LD-V05`/`LD-D07` → P21.2). *(deterministic)*
- [ ] `uv run pytest tests/connectors tests/unit -k "derivative or export_gate"`: `sm_alpr` in an export set is refused/partitioned with the reason `derivative_permitted=false`. *(deterministic)*
- [ ] `npm --prefix web run test:e2e` includes `jurisdiction.spec.ts` (+ `.nojs`) green; `npm run check:perf` budgets hold (script size 0). *(deterministic)*
- [ ] `uv run python -m inference coverage --help` and the other three subcommands exit 0. *(deterministic)*
- [ ] `docs/adr/ADR-060-*.md`, `ADR-061-*.md` exist and are indexed in `docs/adr/README.md`; `grep -n '## Phase 8 — Resolver (P08.1)' docs/risk_register.md` hits before the P08.2 heading; ADR-044 contains `4493b14`. *(deterministic)*
- [ ] `python docs/build/tools/check_coverage_matrix.py docs/build/COVERAGE_MATRIX.csv` exits 0; count of rows with `verdict ∈ {PARTIAL, MISSING}` and `routing=P19.4:*` is **0**. *(deterministic)*
- [ ] `CAPSTONE_CLOSURE.md` §(b) row count equals the number of `MET-DIFFERENTLY` rows in the matrix (script equality in the PR body). *(deterministic)* Each accepted deviation's reasoning is sound to a reviewer; the operator signs §(e) in PR review (HG-14). *(agentic)*
- [ ] **Phase-gate (§51.3):** `make check` green; new code tested (SIG-ENG-004); ADR-060/061; traceability + risk register updated. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
Every id in `CAPSTONE_GAP_ANALYSIS.md` §(i) not stamped by P19.4, plus SIG-LIC-004, SIG-LIC-010, SIG-INGEST-048b, SIG-ENG-030, SIG-ENG-031, and P18.1's publication ids (PR #45's table).

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** an accepted deviation is documented reasoning, never a silently loosened requirement.
- **Part VIII is binding (§0.7):** the export gate change can only *reduce* what leaves the system; jurisdiction-conditional render can only *withhold*.
- **Append-only & provenance (P1–P3):** ADR amendments append sections; no ADR text rewritten.
- **Additive/back-compat:** existing exports/pages unchanged where rights and jurisdiction permit.

## Notes
- This ticket **owns** `CAPSTONE_CLOSURE.md` and the ACCEPTED list; P20.1 (backlog) and P20.2 (amendments A5/A6) read §(b) as the signed input.
- If P19.4 deferred its ER backend here (its size guard), do it first — it is on the critical path.
