# P23.5 — backlog and operational readiness refresh (Round 2, REC.1)

- **Sequence:** 71 of 73 · **Phase:** 23 (reconcile) · **Kind:** reconcile · **Tag:** reconcile
- **base_branch:** current checkout (the branch P23.4 left checked out)
- **Depends on:** P23.4 (GATE-ACCEPT)
- **Run:** `implement-spec spec=docs/tickets/P23.5__backlog-and-readiness.md live_verification=false` (invokes `reconcile-build mode=backlog`)
- **Gate status:** none · **Live stage:** none

> **Round 2 — delta over Round 1.** Round-1 produced `docs/build/BACKLOG.csv`/`BACKLOG.md`/`BACKLOG_THEMES.md`
> (in `reports/`) and `OPERATIONAL_READINESS.md`. This refreshes them against the current
> `docs/tickets/DEFERRALS.md` (the `D-P21.n-*` RETURN-PASS rows seeded by P22.3), OPEN FINDINGS, ADR
> revisit triggers, and non-MET matrix rows.

## Goal
Every currently-owed obligation appears in exactly one backlog item, and operational readiness reflects
the current gate/deferral state — a refresh, not a rebuild.

## Load (read these — do not re-read others)
- `docs/build/BACKLOG.csv`, `docs/build/BACKLOG.md`, `docs/build/OPERATIONAL_READINESS.md` (Round-1 baseline).
- `docs/tickets/DEFERRALS.md` (the `D-P21.n-*` rows + their `BL-` cross-refs), `docs/build/LEDGER.md § OPEN FINDINGS / RETURN PASS`, `docs/build/COVERAGE_MATRIX.csv` (non-MET rows), `docs/adr/` (revisit triggers).

## In scope — deliverables
1. Refresh `docs/build/BACKLOG.csv` (+`.md`) so every OPEN/PARTIAL `DEFERRALS.md` row, ADR revisit trigger, OPEN FINDINGS entry and non-MET matrix row appears in exactly one `sources` cell; run the backlog checker (exit 0).
2. Refresh `docs/build/OPERATIONAL_READINESS.md` (capability × {code, infra, human gate, owner}; critical path with `ticket:`/`proof:`; no TBD).

## Out of scope
- Spec reconciliation (P23.6), integration plan (P23.7). Closing the human gates (operator).

## Acceptance criteria
- [ ] the backlog checker exits 0; every owed obligation maps to exactly one item; no duplicate sources *(deterministic)*
- [ ] `OPERATIONAL_READINESS.md` has no `TBD`; every critical-path step names a `ticket:` and `proof:` *(deterministic)*
- [ ] **Phase-gate:** verification green; requirement ids stamped; `DEFERRALS.md` maintained; ADRs for deviations; `BUILD_INDEX.md` row and `LEDGER.md` advanced. *(agentic)*

## Requirement IDs to satisfy and stamp in the PR
The backlog/readiness ids (matching `SIG-[A-Z]+-[0-9]+`) the refresh touches.

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`; append-only registers.

## Notes
- This ticket refreshes (never renames) the `BL-` id space and the `landing` enum owned by P20.1.
