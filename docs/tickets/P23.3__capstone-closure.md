# P23.3 — capstone closure (Round 2, CAP.3)

- **Sequence:** 69 of 73 · **Phase:** 23 (capstone) · **Kind:** capstone · **Tag:** capstone
- **base_branch:** current checkout (the branch P23.2 left checked out)
- **Depends on:** P23.1, P23.2
- **Run:** `implement-spec spec=docs/tickets/P23.3__capstone-closure.md live_verification=false`
- **Gate status:** none · **Live stage:** none

> **Round 2 — delta over the completed Round-1 capstone.** Round-1 closure is `docs/build/CAPSTONE_CLOSURE.md`
> with a signed 76-row ACCEPTED-deviations list plus the append-only §(b) addendum (77th row from the
> capstone). This ticket closes only Round-2-routed gaps (if any) and appends any new proposed deviation.

## Goal
Close the gaps P23.1 routed to closure and propose any new deviation for the operator's Round-2 signature —
append-only over the Round-1 `CAPSTONE_CLOSURE.md`.

## Load (read these — do not re-read others)
- `docs/build/CAPSTONE_CLOSURE.md` (the signed Round-1 list + §(b) addendum — never rewritten).
- `docs/build/CAPSTONE_GAP_ANALYSIS.md` P23.1 delta (the routed gaps).
- `docs/build/COVERAGE_MATRIX.csv`, `docs/tickets/DEFERRALS.md`.

## In scope — deliverables
1. Close each Round-2-routed real gap on a capstone branch (no merge); update its matrix row `closed_by=P23.3`.
2. Append a dated Round-2 addendum to `docs/build/CAPSTONE_CLOSURE.md` listing any newly proposed ACCEPTED deviations (for P23.4 / GATE-ACCEPT). The signed Round-1 table is not rewritten.

## Out of scope
- The operator's signature (P23.4 GATE-ACCEPT owns it); reconciliation (P23.5–P23.7).

## Acceptance criteria
- [ ] every Round-2-routed gap is closed (matrix `closed_by=P23.3`) or moved to `DEFERRALS.md`/proposed-accepted *(deterministic)*
- [ ] the Round-1 signed ACCEPTED list is untouched; new items are an append-only dated addendum *(deterministic)*
- [ ] **Phase-gate:** verification green; requirement ids stamped; `DEFERRALS.md` rows + compensating controls; ADRs for deviations; `BUILD_INDEX.md` row and `LEDGER.md` advanced. *(agentic)*

## Requirement IDs to satisfy and stamp in the PR
The ids of the Round-2 gaps this ticket closes (matching `SIG-[A-Z]+-[0-9]+`).

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`; append-only (P1–P3) — the signed closure list is never edited.

## Notes
- Expected Round-2 delta is small (P22.3 changed no product code); this ticket may legitimately close zero gaps and propose zero new deviations, recording that.
