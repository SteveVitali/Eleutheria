# P19.2 — Capstone gap analysis: the whole spec vs the whole build (CAPSTONE step 1)

- **Sequence:** 48 of 63
- **Phase:** 19 — Post-build capstone & consolidation (`docs/build/DECISION_MEMO.md` §3 row 48; orchestrate-build CAPSTONE step 1)
- **base_branch:** current checkout — the P19.1 branch
- **Depends on (must land earlier in the stack):** P19.1 (committed `docs/build/*` and `docs/tickets/*`).
- **Run:** `implement-spec spec=docs/tickets/P19.2__capstone-gap-analysis.md live_verification=false`
- **Gate status:** none. **Independence rule (anti-bias, orchestrate-build §3.1):** this MUST run in a fresh context that forms each verdict from the spec + code + tests **before** reading any ledger self-assessment. Read `docs/build/LEDGER_DEFERRALS.md` and `BUILD_INDEX.md` only in step 4 below, as a checklist.

## Goal
Produce the first whole-spec coverage judgement of the build: every one of the 668 requirement ids classified MET / MET-DIFFERENTLY / PARTIAL / MISSING / AT-RISK-INTEGRATION with evidence, the inter-ticket seams hunted explicitly, and every gap routed to exactly one later ticket — so P19.3/P19.4 close real gaps and P20.1/P20.2 inherit verdicts instead of re-deriving them. **No code changes.**

## Load (read these — do not re-read others; SIG-ENG-001)
- `docs/2_canonical_design_spec.md` — **all of it**, in this order: Part 0 (§0.2 normative language, §0.3 id grammar, §0.6 DoD), Part I §3 (invariants), then Parts II–IX section by section, Part X §51–§54, Appendix C (DDL). Extract ids with `grep -oE '\*\*SIG-[A-Z]+-[0-9]+[a-z]? \((MUST|SHOULD|MAY|RATIONALE)' docs/2_canonical_design_spec.md | sort -u` (expect **668**; `SIG-ENG-006/009/028/029` are reserved, spec L84).
- `docs/build/SCOPING_NUMBERS.md` §(i) (method incl. **range-notation expansion**), §(vi) (code anchors); `docs/build/SCOPING_ID_LISTS.md` L1–L4 (the 98 / 253 / 250 / 13 seed lists).
- Code: every workspace member's `src/` tree listing + `tests/` + `web/tests/`; `docs/traceability.md`; `docs/adr/*.md` (`Requirement ids` fields); `docs/risk_register.md` (compensating controls); PR bodies `gh pr view N --json body` for N=1..46 (requirement tables).
- Step 4 only: `docs/build/LEDGER_DEFERRALS.md` — P19.2 is the **classify/verify** owner of LD-F02, LD-F12, LD-F13, LD-F17, LD-F18, LD-H01, LD-H02, LD-H03, LD-H04, LD-H05, LD-H06, LD-H07, LD-H08, LD-H09, LD-H10, LD-H11, LD-H12, LD-H13, LD-X01, LD-X08 (it records landed/not-landed and routes; the receiving ticket named in `DECISION_MEMO.md` §5.3 does the implementation); `docs/build/BUILD_INDEX.md` §A.3.
- `~/.claude/skills/orchestrate-build/SKILL.md` §3 item 1 (the classification rubric).

## In scope — deliverables
1. **`docs/build/COVERAGE_MATRIX.csv`** — exactly 668 rows + header. Columns: `id, level (MUST/SHOULD/MAY/RATIONALE), spec_section, class, verdict, evidence, owning_tickets, tests, adrs, risk_rows, routing, note`. `class` ∈ {`covered+tested`, `covered+untested`, `deferred(RISK)`, `deviated(ADR)`, `rationale-only`, `process/governance`, `unreferenced`}. `verdict` ∈ {MET, MET-DIFFERENTLY, PARTIAL, MISSING, AT-RISK-INTEGRATION, N/A-RATIONALE}. `evidence` = ≥1 `path:line` or test node id or ADR id (never blank for MET/MET-DIFFERENTLY). `routing` ∈ {`—`, `P19.3`, `P19.4:S`, `P19.4:M`, `P21.2`, `P21.3`, `P21.4`, `P21.5`, `P21.6`, `P21.7`, `P21.8`, `P20.1:backlog`, `P20.2:spec`, `accepted`}; every non-MET row has a non-`—` routing; `P19.4:L` is **not allowed** (route L-sized items to a P21 ticket instead). **`P19.4:*` means "capstone closure": spine-seam rows (claim sink, read store, ER over PG) are done by P19.4, every other `P19.4:S/M` row by P19.5.** Id → owning ticket comes from PR-body requirement tables and ADR `Requirement ids`, not from `docs/traceability.md` (BUILD_INDEX §A.4).
2. **`docs/build/CAPSTONE_GAP_ANALYSIS.md`** with sections: (a) method + commands; (b) roll-up table by verdict and by prefix; (c) **the 98 nowhere-referenced ids** each classified (many CHART/ENG/EPIS are `process/governance` with a compensating control — say which); (d) **the 250 never-ticketed ids** roll-up; (e) **seam hunt**: for each of LD-H01…H13 and the orphaned seams (curation web UI, tile generation, `derivative_permitted` gate, jurisdiction-conditional web render, osm live wiring, Stage-5 connectors, P08.1 §53 section) state landed / not landed with evidence; plus the composed-path seams from SCOPING §(vi) (no PG `ClaimSink`, in-memory `ReadStore`, no annotation persistence, web reads fixtures) as AT-RISK-INTEGRATION rows; (f) **J-1 registry seam** (7/8 OKC slice sources unregistered — SIG-INGEST-023/038 vs P06.1) and the P07.3 gate bypass (LD-X08: `usaspending` fetched live while `UNDETERMINED`) as process findings; (g) **top-20 gaps** ordered by (MUST first, then critical-path relevance to DECISION_MEMO §6); (h) **spot-check**: 10 rows marked `covered+tested` chosen at random (seeded, list the seed) — run the named tests (`uv run pytest <node ids>`; `npm --prefix web run test:unit` for UI ids) and paste the pass lines; (i) the explicit list of rows routed `P19.4:S/M` (this **is** P19.4's work list) and rows routed to each P21 ticket; (j) dispositions for LD-F02/F12/F13/F17/F18, LD-X01 (MET — BUILD.sh byte-identical), LD-X08.
3. **Traceability + risk register.** Append `## Phase 19 — Capstone gap analysis (P19.2)` to `docs/risk_register.md` with `RISK-P19-03` (verdicts are one reader's judgement → 10-row spot-check + P19.3 composed run as controls) and `RISK-P19-04` (id-to-ticket mapping derived from PR bodies may miss silently-implemented ids → `class=covered+untested` rows listed for P19.4/P20.1). Append a "Whole-spec coverage (docs/build/COVERAGE_MATRIX.csv)" section to `docs/traceability.md`.
4. **Consistency script** `docs/build/tools/check_coverage_matrix.py` (stdlib only; committed): asserts 668 rows, all ids defined in the spec, no duplicate ids, enum values valid, routing non-`—` for non-MET, evidence non-blank for MET. Runs in <1 s; invoked in the PR body.

## Out of scope (scope-creep guard — do NOT do these here)
- Fixing anything (code, tests, docs beyond the deliverables) — **P19.4** (spine seams) / **P19.5** (everything else). Running the composed stack or Docker — **P19.3** (spot-check tests only). Backlog ids — **P20.1**. Spec edits or `ticket-added` tagging — **P20.2**. Re-reading ledger self-assessments before verdicts (independence rule).

## Acceptance criteria
- [ ] `python docs/build/tools/check_coverage_matrix.py docs/build/COVERAGE_MATRIX.csv` exits 0 and prints `668 rows OK`. *(deterministic)*
- [ ] Every id in `SCOPING_ID_LISTS.md` §L1 (98) has `class ≠ covered+tested` **or** an evidence path proving a test exists (the list is a seed, not a verdict). *(deterministic — script cross-check)*
- [ ] `CAPSTONE_GAP_ANALYSIS.md` §(e) has one row per LD-H01…H13 + 7 orphaned seams + 4 composed-path seams = **24 rows**, each with a landed/not-landed verdict and evidence. *(deterministic)*
- [ ] §(h) shows 10 executed test commands with pass output; 0 failures (a failure is itself a finding → routing `P19.4:S`). *(deterministic)*
- [ ] Routing column: 0 rows `P19.4:L`; every `P19.4:S/M` row is listed in §(i). *(deterministic)*
- [ ] Verdicts are defensible: a reviewer sampling 15 rows across prefixes finds the evidence supports the verdict in ≥14. *(agentic)*
- [ ] **Phase-gate (§51.3):** `make check` green (no code changed → 2363 local tests); risk register + traceability sections present. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
SIG-ENG-002 (phase completeness judged against ACs), SIG-ENG-004/005 (Done needs automated evidence — the matrix records where it is missing), SIG-ENG-030/031 (risk register). No new ids implemented.

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** no verdict without evidence; "no synthetic certainty" — PARTIAL when unsure, never MET.
- **Part VIII is binding (§0.7):** the matrix contains ids and paths only, no data.
- **Append-only & provenance (P1–P3):** do not edit earlier docs' claims; add the matrix beside them.
- **Additive/back-compat:** no code change.

## Notes
- This ticket **owns** the verdict vocabulary and the `routing` enum; P19.3/P19.4/P19.5/P20.1/P20.2 consume `COVERAGE_MATRIX.csv` by column name — do not rename columns later.
- Known pre-seeded expectations (verify, don't assume): `SIG-INGEST-043*` MISSING → `P21.8`; `SIG-GOV-022` → `P21.5`; `SIG-STORE-003/004/005` → `P21.5`; `SIG-UI-001` → `P21.7`; `SIG-CONTRIB-012/012a` → `P21.1`; ADR-030 (curation CLI) → verdict decides `P21.6`; ADR-037/038/039/054 (no persistence) → verdict decides `P21.2`; ADR-051 (zero-JS map) → `P20.2:spec`.
