# P21.1 — Rights review packets, registry completion, Stage-0 outreach record

- **Sequence:** 55 of 63
- **Phase:** 21 — Operationalization (`docs/build/DECISION_MEMO.md` §3 row 55; critical path §6 step 4)
- **base_branch:** current checkout — the P20.3 branch (the whole post-build chain stacks on `devin/p18-2-france-belgium`; merging is an operator action after the chain)
- **Depends on (must land earlier in the stack):** P20.3.
- **Run:** `implement-spec spec=docs/tickets/P21.1__rights-review-and-registry-completion.md live_verification=false`
- **Gate status:** **GATE HG-03 (per-source rights flips) and HG-04 (Stage-0 outreach outcomes)** — both are *human edits* this ticket prepares but never performs. Operator fills in after reading the packets:
  - Sources flipped to `ingestion_permitted = true` (id, reviewer role, date): `________`
  - Stage-0 outreach outcomes recorded (project, date, outcome ∈ compact_status enum): `________`
  If both blank, the ticket is still complete (packets + tooling + records template); `sig-connectors validate` will still show `loadable now: 0`.
  - *Gate answers may arrive via the build ledger's `GATE DECISIONS` table instead of an edit to this file (the orchestrator pauses before this ticket and records the operator's answers there). In that case copy them into this block in your first commit. An answer of "skip" means: run ungated as described, and list the ticket in the ledger's RETURN PASS table.*

## Goal
Turn "0 of 109 sources loadable" into a reviewable, one-line-per-source decision: every source on the OKC critical path and every flip-ready source gets a rights-review packet with the facts a reviewer needs, the registry enforces that a flip carries its review metadata, and Stage-0 outreach has a record format that flows into `compact_status` — so a human can unblock ingestion without touching code.

## Load (read these — do not re-read others; SIG-ENG-001)
- `docs/build/SCOPING_NUMBERS.md` §(ii) (the 22/87/18 numbers; the 18 flip-ready ids; the OKC list; `usaspending`), §(v) HG-03/HG-04; `docs/build/OPERATIONAL_READINESS.md` §(a)(b)(d); `docs/build/BACKLOG.csv` rows `landing=P21.1`; `docs/build/LEDGER_DEFERRALS.md` LD-P01, LD-P05; `docs/risk_register.md` RISK-P0-15/16/17/18/19/20 (L45–60).
- Spec: §10.3.1 (registry row fields, SIG-INGEST-023), §22.4–§22.6 (compact statuses, verification — SIG-INGEST-027/028/038), §35.1 (Stage-0 compact, the 19 projects — SIG-CONTRIB-012/012a/013), §42 (SIG-LIC-001 per-source rights review, SIG-LIC-004/009/009a), §26 (crawler conduct incl. robots), Appendix B "Licensing"/"Data access".
- Code anchors (re-confirm): `connectors/src/connectors/registry.py:96-130,170-200` (`SourceRecord`, `_record_from_row`, `_rights_from_row`), `loader.py:43-115`, `cli.py` (`validate` at ~L55–70), `connectors/src/connectors/data/sources.toml` (109 rows; this ticket adds the 6 OKC rows), `data/agenda_tenants.toml:71-77` (OKC CivicClerk), `data/local_groups.toml`; `tests/connectors/test_source_registry.py`, `test_ingestion_gate.py`, `test_registry_export_gate.py`; `docs/slice/P06.1_hardness_precondition.md` §"four checkable properties" (source families F1–F6).

## In scope — deliverables
1. **OKC slice sources registered (SCOPING §(ii)):** add `sources.toml` rows `okc_procurement`, `okc_council` (CivicClerk tenant `oklahomacityok`), `okcpd_policy`, `ok_statute`, `journalrecord`, `oklahoman` — all **without** rights block (`UNDETERMINED`), `ingestion_permitted` absent (false), `compact_status = public_terms_only`, `custody_posture = REFERENCE` (news rows: `LINK`), `verified = false`, notes citing `docs/slice/P06.1_hardness_precondition.md`; add a mapping test from `tests/acceptance/fixtures/okc_sources.json` `source_id`s (`src:okc-procurement` → `okc_procurement`, …) to registry ids (additive; the fixture file is unchanged). `sig-connectors validate` → `registered sources: 115`.
2. **Registry schema (additive):** new optional row keys `rights_reviewed_by` (role string, e.g. `"licensing reviewer"`), `rights_reviewed_on` (date), `review_packet` (path). `_record_from_row` reads them; **validation rule**: `ingestion_permitted = true` ⇒ rights block present **and** `rights_reviewed_by` **and** `rights_reviewed_on` **and** `last_verified` present, else `sig-connectors validate` fails with the offending id (SIG-INGEST-028/038, RISK-P0-15). `usaspending`: add the rights block (US federal public-domain works; `spdx = "CC0-1.0"`-equivalent or `"US-PD"` per the registry's SPDX convention — check `policy.licensing` for the accepted expression) if `terms_url` can be cited; else leave UNDETERMINED.
3. **Review packets** `docs/build/rights/<source_id>.md` for: the 6 OKC rows (`okc_procurement`, `okc_council`, `okcpd_policy`, `ok_statute`, `journalrecord`, `oklahoman`), `deflock_repo`, `osm_overpass`, `usaspending`, and the 18 flip-ready ids (SCOPING §(ii)) = **27 packets**. Template (`docs/build/rights/_TEMPLATE.md`): source id · homepage · terms URL(s) actually fetched *by the agent reading them*, quoted verbatim with retrieval date (reading a terms page is permitted research, not ingestion) · robots.txt summary · SPDX candidate · `redistributable` analysis · `derivative_permitted` analysis · ODbL compartment implications (RISK-P0-01/02) if OSM-derived · custody posture recommendation · **decision line for the reviewer** (`[ ] permit ingestion — reviewer: ____ date: ____`) · counsel-needed flag. No packet asserts a legal conclusion; it lays out facts.
4. **`docs/build/RIGHTS_REVIEW_INDEX.md`:** table of all 109+ sources: rights state, compact status, custody, packet path (or "not on critical path"), gate state; the OKC minimum set highlighted; counts reproduced from `sig-connectors validate`.
5. **`sig-connectors review-status [--source ID]`** subcommand: prints per-source gate breakdown (`ingestion_permitted / compact / custody / rights / reviewed-by`) and, with no arg, the same counts as `validate` plus `flip-ready` (rights block + compact ok + custody ok, flag false). Test in `tests/connectors/test_review_status.py`.
6. **Stage-0 outreach record:** `docs/build/STAGE0_OUTREACH_RECORD.md` — one row per compact project (the 19 from §35.1; cross-check `sources.toml` `source_kind=upstream_project` rows and `local_groups.toml`): contact channel (public address only), date sent, outcome (`not_contacted` / `no_response` / `permission_granted` / `permission_granted_conditional` / `permission_declined` / `partnership_active` / `public_terms_only`), the `sources.toml` id(s) it governs; plus the outreach letter template (`docs/governance/stage0-outreach-letter.md`, citing the compact §35.1 and the archival-succession offer SIG-CONTRIB-013, RISK-P0-17/20). A test asserts every `compact_status ≠ not_contacted/public_terms_only` row in `sources.toml` has a matching record row.
7. **Apply operator inputs from the gate block** (if any): set the flags + review metadata in `sources.toml`; update `compact_status` from the record; re-run `validate`.
8. **ADR-063** (registry review metadata; the flip rule) and risk register `## Phase 21 — Operationalization (P21.1)` with `RISK-P21-01` (packets quote terms that may change → `retrieval_date` + `last_verified`), `RISK-P21-02` (reviewer role recorded, not a legal opinion → counsel flag, SIG-LIC-009); traceability section.

## Out of scope (scope-creep guard — do NOT do these here)
- Deciding any flip yourself (HG-03) or recording any outreach you did not verifiably perform (HG-04). Fetching source *content* (only terms/robots pages). Live transports — **P21.3**. Counsel opinions (HG-02). Sources outside the 27 (their packets are P22+ backlog).

## Acceptance criteria
- [ ] `uv run sig-connectors validate` → `registered sources: 115`, self-checks OK; with a test row `ingestion_permitted = true` lacking `rights_reviewed_by`, validation **fails** naming the id (`tests/connectors/test_review_status.py`). *(deterministic)*
- [ ] `ls docs/build/rights/*.md | grep -v _TEMPLATE | wc -l` = 27; each packet contains the sections `Terms (verbatim)`, `SPDX candidate`, `Decision`. *(deterministic)*
- [ ] `uv run sig-connectors review-status` prints `flip-ready: 18` (or the post-flip number) and `loadable now: N` equal to `validate`'s. *(deterministic)*
- [ ] `STAGE0_OUTREACH_RECORD.md` has 19 project rows; the consistency test passes. *(deterministic)*
- [ ] If the operator flipped ≥1 source: `review-status --source <id>` shows all five gate fields true and `validate` shows `loadable now ≥ 1`. *(deterministic, conditional)*
- [ ] Packets are factual and quote the actual terms text (reviewer spot-checks 3 URLs). *(agentic)*
- [ ] **Phase-gate (§51.3):** `make check` green; ADR-063; traceability + risk register updated; `BACKLOG.csv` rows landing here set `status=closed`. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
SIG-INGEST-023, SIG-INGEST-027, SIG-INGEST-028, SIG-INGEST-038, SIG-LIC-001, SIG-LIC-004, SIG-LIC-009, SIG-LIC-009a, SIG-CONTRIB-012, SIG-CONTRIB-012a, SIG-CONTRIB-013, SIG-INGEST-030 (Eyes on Flock outreach row).

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** packets separate fact (quoted terms) from judgement (decision line); no synthetic certainty about rights.
- **Part VIII is binding (§0.7):** contact channels are organisational addresses only; no personal names in records.
- **Append-only & provenance (P1–P3):** outreach records and flips carry dates; earlier rows are never rewritten.
- **Additive/back-compat:** new registry keys optional; `ingestion_permitted` still defaults false.

## Notes
- This ticket **owns** the review-metadata rule (a flip without metadata is invalid) and the packet format; P21.3 refuses to fetch any source whose `review-status` is not fully green.
- The OKC minimum set for step 4 of the critical path: `okc_procurement`, `okc_council`, `okcpd_policy`, `ok_statute` (government records, R1/R2) + `osm_overpass`/`deflock_repo` (ODbL/community). News sources (`journalrecord`, `oklahoman`) are LINK-posture candidates — the packet should say so.
