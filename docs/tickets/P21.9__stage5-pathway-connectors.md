# P21.9 — Stage-5 connectors for the Phase-17 pathways (federation/RTCC, FR/CSS/forensics, acoustic/drone/location) + the deferred parser layers

- **Sequence:** 63 of 63
- **Phase:** 21 — Operationalization (`docs/build/DECISION_MEMO.md` §3 row 63)
- **base_branch:** current checkout — the P21.8 branch
- **Depends on (must land earlier in the stack):** P21.8 (packet + `run` workflow for new sources), P21.3, P21.1.
- **Run:** `implement-spec spec=docs/tickets/P21.9__stage5-pathway-connectors.md live_verification=true`
- **Gate status:** **GATE HG-03/HG-04 per source** — connectors ship over committed fixtures drawn from public documents already cited in the research cache; live runs only for sources the operator flipped. Operator lists flipped sources: `________`.
  - **OPERATOR ANSWER (recorded P21.9, HG-03/HG-04 per source): SKIP — no source flipped.** Ship the Stage-5 pathway connector(s) + the ADR-033-deferred parser layers over COMMITTED FIXTURES drawn from public documents already cited in the research cache (each fixture directory cites its source URL in `SOURCES.md`). Rights packets produced for each new source (P21.1 template). NO live fetch. New `sources.toml` rows stay rights UNDETERMINED / `ingestion_permitted=false`; `run --mode live` for them REFUSES (exit 3). Per-source HG-03/HG-04 recorded **pending** → RETURN PASS (listed in the ledger's RETURN PASS table).
  - *Gate answers may arrive via the build ledger's `GATE DECISIONS` table instead of an edit to this file (the orchestrator pauses before this ticket and records the operator's answers there). In that case copy them into this block in your first commit. An answer of "skip" means: run ungated as described, and list the ticket in the ledger's RETURN PASS table.*

## Goal
Populate the Phase-17 pathways with ingested claims instead of conformance-suite instance graphs ("expressibility, not ingestion", LD-H12 / RISK-P17-03): connectors that read municipal RTCC/federation disclosures, facial-recognition / cell-site-simulator / mobile-forensics procurement and policy documents, and gunshot-detection / drone / commercial-location-data procurement, emitting the pathway claims the P17 models already express — and implement the parser layers ADR-033 deferred to "connectors" (LD-F17) where they are needed to do it.

## Load (read these — do not re-read others; SIG-ENG-001)
- Spec: §46 broader surveillance pathways (the P17.1/P17.2/P17.3 ids — SIG-ONTO-05x/06x, SIG-INGEST-046b/046c, SIG-INGEST-049a–d; SIG-EPIS rules for "procured ≠ deployed"), §22.5 Stage-5, §23–§25 (records/procurement/agenda connectors reused), §20 parsing stack (SIG-PARSE-001..008; the layers ADR-033 deferred), §21, §26.
- `docs/build/LEDGER_DEFERRALS.md` LD-H12, LD-F17 (verified in P19.2 — read its §(e) row), LD-V10 (accepted: expressibility was the P17 design; ingestion is this ticket); `docs/risk_register.md` RISK-P17-03; `docs/build/COVERAGE_MATRIX.csv` rows `routing=P21.9`; `docs/build/BACKLOG.csv` rows `landing=P21.9`; ADR-033.
- Anchors (re-confirm): P17 models and conformance graphs (`grep -rln "rtcc\|cell_site\|acoustic\|drone\|location_data" ontology/ tests/ontology/`); `connectors/src/connectors/{procurement.py, records.py, stages.py:336}` + the agenda path from P07.3 (`grep -rn civicclerk connectors/src`); `parsing/src/parsing/` (classifier, locators — find which of layers 1–5 exist: `grep -n "layer" parsing/src/parsing/*.py`); `docs/research/R*` (public documents to excerpt as fixtures — cite each fixture's source URL in a `SOURCES.md` beside it); `sig-connectors run` (P21.3); packet template (P21.1).

## In scope — deliverables
1. **Coupling decision (ADR-071):** one `pathways` connector with per-pathway extractors **or** three connectors (`rtcc_federation`, `fr_css_forensics`, `acoustic_drone_location`) — choose by shared code (all three reuse procurement/records/agenda parsing); record the decision and the module layout.
2. **Parser layers (LD-F17):** implement the ADR-033-deferred layers actually needed by the extractors (e.g. table extraction from procurement PDFs, clause locators for policy documents) in `parsing/`; each layer has unit tests over the new fixtures; `sig-parsing classify` recognises the new document genres.
3. **Pathway connectors:** for each pathway, extractors that emit the P17 claims (vendor/product, procurement, policy, deployment *only when evidence says so*) with the P17 epistemic rules — `procured` never implies `deployed`; every claim typed and evidenced; registry rows + packets for each new source (public procurement portals, agenda tenants, policy pages).
4. **Coverage table:** `tests/connectors/test_pathway_coverage.py` asserts every pathway in the P17 conformance suite has ≥1 claim produced by a connector fixture; the table is rendered into `docs/build/STAGE5_CONNECTORS.md`.
5. **Run what is permitted:** flipped sources → live run into the composed PG with fetch records; others → fixture replay; report per connector in `STAGE5_CONNECTORS.md`.
6. **ADR-071**, risk register `RISK-P21-16` (pathway connectors over-infer from procurement → claims typed `procured`; deployment requires a deployment-genre document), `RISK-P21-17` (parser layers regress on real PDFs → fixture corpus + `shadow` diffs), traceability; `COVERAGE_MATRIX.csv` P17 ingestion ids → MET; `RISK-P17-03` gets a `→ retired by P21.9` suffix (no rewrite).

## Out of scope (scope-creep guard — do NOT do these here)
- New ontology classes (P17 classes exist; only instances are produced). Data Driven / coarse-international — **P21.8**. Flipping sources. FR/BE live. A second jurisdiction's full run (P22+). Flock portal live client (RISK-P0-17).

## Acceptance criteria
- [ ] `uv run sig-connectors list-connectors` includes the pathway connector(s); `validate` passes with the new rows. *(deterministic)*
- [ ] `uv run pytest tests/connectors -k "pathway" tests/parsing`: green over fixtures; the coverage test shows every P17 pathway with ≥1 connector-produced claim; no claim of type `deployed` originates from a procurement-genre fixture. *(deterministic)*
- [ ] `shadow` over all new fixtures: 0 diffs on re-run. *(deterministic)*
- [ ] Each fixture directory has a `SOURCES.md` citing the public document URL and retrieval date. *(deterministic)*
- [ ] `COVERAGE_MATRIX.csv`: the P17 ingestion ids routed `P21.9` are MET with test evidence; `check_coverage_matrix.py` exits 0. *(deterministic)*
- [ ] If any source flipped: `STAGE5_CONNECTORS.md` records ≥1 live run with captures + claims in PG. *(deterministic, conditional on HG-03)*
- [ ] Pathway claims never assert deployment from procurement alone (reviewer samples 10 claims). *(agentic)*
- [ ] **Phase-gate (§51.3):** `make check` green; ADR-071; traceability + risk register; `BACKLOG.csv` rows closed. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
SIG-INGEST-046b, SIG-INGEST-046c, SIG-INGEST-049a, SIG-INGEST-049b, SIG-INGEST-049c, SIG-INGEST-049d (as routed), the P17.x SIG-ONTO ids for pathway population, SIG-PARSE-* ids for the implemented layers, SIG-INGEST-021/027/028.

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** every inference labelled (procured ≠ deployed); every claim has evidence and time.
- **Part VIII is binding (§0.7):** no per-person data from forensics/location documents; officer-naming gate on policy documents.
- **Append-only & provenance (P1–P3):** appends only.
- **Additive/back-compat:** existing connectors, parsers and fixtures untouched.

## Notes
- This is the last ticket of the planned chain. New scope discovered after it is recorded in `BACKLOG.csv` as `P22+` and cut by a later planning pass (DECISION_MEMO §7) — not appended here.
