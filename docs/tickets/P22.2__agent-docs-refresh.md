# P22.2 — Agent-facing documentation: converge the `AGENTS.md` hierarchy on the finished code (`agent-docs`), bridge `CLAUDE.md`, and gate both doc sets in CI

- **Sequence:** 65 of 65
- **Phase:** 22 — Documentation (added 2026-09-08 by operator request; not in the spec's Part X)
- **base_branch:** current checkout — the P22.1 branch
- **Depends on (must land earlier in the stack):** P22.1 (human docs current; the vendored repo-docs detector), P19.1 (the first `AGENTS.md`, bootstrapped when the chain had 47 tickets — now 18 tickets stale).
- **Run:** `implement-spec spec=docs/tickets/P22.2__agent-docs-refresh.md live_verification=false`
- **Gate status:** none. Skill used: `agent-docs` (source: `~/agent-skills/skills/agent-docs/SKILL.md` + `modes/bootstrap.md`, `modes/refresh.md`, `guidelines.md`; also installed as `~/.claude/skills/agent-docs`) — invoke it **by name** and follow it completely. **One operator option** (tick before the run, or answer at the orchestrator pause; default = unticked):
  - [ ] **Clean slate** — delete every existing `AGENTS.md`/`CLAUDE.md` first and run `mode=bootstrap depth=hierarchical` so the hierarchy is regenerated from scratch. *Default (unticked):* keep P19.1's docs and run **`mode=refresh deep=true`**, which converges to the same target state (uncovered packages are bootstrapped, severely drifted docs are regenerated with bootstrap's machinery, hand-written gotchas that still hold are preserved). Sanity-check note for the operator: the skill auto-detects mode from whether docs exist; "bootstrap afresh" on a repo that already has `AGENTS.md` is *by the skill's own rule* "fill gaps only, never clobber" — so a literal from-scratch regeneration requires the clean-slate tick.
  - *Gate answers may arrive via the build ledger's `GATE DECISIONS` table instead of an edit to this file; copy them into this block in your first commit.*

## Goal
An agent opening this repo cold — at the root or inside any package — finds an accurate, budget-compliant `AGENTS.md` (nearest-file-wins) whose every build/test command works, whose gotchas are the real ones the 65-ticket build discovered, whose boundaries name what must not be touched (frozen §47 layout, generated spec, append-only registers, gitignored scratch, the ticket/manifest conventions, the human gates), with a `CLAUDE.md` bridge — and both documentation sets (human + agent) are protected from silent drift by deterministic detectors in `make check` and CI.

## Load (read these — do not re-read others; SIG-ENG-001)
- `~/agent-skills/skills/agent-docs/SKILL.md` (Phase 0 detector `scripts/check-agent-docs-freshness.sh`; mode table; Phase 2 shared tail: validate → consistency sweep → `CLAUDE.md` bridge → quality checklist → commit), `modes/refresh.md` (or `modes/bootstrap.md` if clean slate), **`guidelines.md`** (length budgets ~150 lines leaf / ~250 root; standard section order `Purpose → Architecture → Module Layout → Key Files → Build & Test → Code Conventions → Critical Gotchas → Terminology → Do → Don't → Boundaries`; no contradictions across the hierarchy).
- The existing docs: root `AGENTS.md` (P19.1) and any package `AGENTS.md` (`find . -name AGENTS.md -not -path '*/node_modules/*'`); `CLAUDE.md` if present.
- Facts the docs must carry (verify each against the file, do not copy from memory): `Makefile` (all targets incl. `test-db`, `docs-check-repo`, `verify-gen`, `sbom`), `.github/workflows/ci.yml`, `pyproject.toml` (14 members; `evidence/` added by ADR-023; ruff/mypy config), `tests/db/conftest.py:30,48-49` (Docker image + `SIG_REQUIRE_DB_TESTS`), `tests/e2e/test_composed_stack.py` (Docker-gated composed test; xfail-by-LD-id convention), `web/package.json` scripts + `web/lighthouserc.json` (script budget 0, total 150 KB), `docs/research/_meta/spec_src/BUILD.sh` (the only way to change the spec), `docs/tickets/00_MANIFEST.md` (stacked-ticket convention; gates), `docs/build/README.md` (build memory), `.agents/scratch/README.md` (scratch layout + retention, P19.1), `connectors/src/connectors/loader.py:43-115` (the fail-closed ingestion gate — an agent must never bypass it), `policy/` (officer-naming + publication gates), `ops/README.md` + `sig-ops` (P21.4), `db/sqitch.plan` (additive changes only), `ontology/generated` (never hand-edit; `make gen`), `docs/build/DECISION_MEMO.md` §4 (human gates an agent must not tick).
- `docs/build/DOCS_REFRESH_REPORT.md` (P22.1) — its "recommend agent-docs" line and any `AGENTS.md` drift it noticed.

## In scope — deliverables
1. **Detector + mode** (skill Phase 0): `bash <skill>/scripts/check-agent-docs-freshness.sh .` → `/tmp/agent-docs-freshness.json`; resolve mode per the option block (default `refresh deep=true`; clean slate → delete + `bootstrap depth=hierarchical`). Record the detector's before-counts (critical / moderate / minor / coverage gaps).
2. **Converged hierarchy** (skill Phase 1): root `AGENTS.md` (≤~250 lines) + a package `AGENTS.md` for every package the detector's coverage rule names (≥5 source files, no doc) — expected at least `web/`, `db/`, `connectors/`, `api/`, `exports/`, `ops/`, `tests/`; more only if the skill's hierarchy design says so. Content requirements beyond the standard sections (all verified against code): **Build & Test** = `make check` (mirror of CI), `make test-db` (Docker), `SIG_REQUIRE_DB_TESTS=1 uv run pytest tests/e2e` (composed), `npm --prefix web run check`, `make docs-check`; **Critical Gotchas** = `verify-gen` fails on uncommitted generated artifacts (commit `ontology/generated` first); RDF canonicalisation (`rdflib.compare.to_canonical_graph`); `tests/db`/`tests/e2e` skip silently without Docker unless `SIG_REQUIRE_DB_TESTS=1`; `ingestion_permitted` defaults false and `sig-connectors run --mode live` refuses un-reviewed sources — never work around it; `PgReadStore`/`PgClaimSink` insert-only (no UPDATE/DELETE anywhere in `db/`); zero-JS budget on public pages; xfails in `tests/e2e` carry an `LD-`/`accepted:` reason and are flipped only by the ticket that closes them; **Boundaries / Don't** = never edit `docs/2_canonical_design_spec.md` directly (spec_src → `BUILD.sh` → ADR), never rewrite ADR bodies / risk register / traceability history (append), never commit `.agents/scratch/` or `sbom.cdx.json`, never touch the frozen §47 layout without an ADR, never tick a human gate (`HG-nn`) or flip a source, never merge/tag/push `main` (operator action), never store secrets in files (env only); **Terminology** = claim spine, resolution envelope, contradiction, compartment (ODbL), custody posture, compact status, ticket/manifest/build memory, gate; **Do** = read the ticket contract + `docs/build/` before implementing; stack PRs from the current checkout; stamp requirement ids in PRs.
3. **`CLAUDE.md` bridge** at the repo root (`@AGENTS.md` + nothing else unless a Claude-specific note is genuinely needed), per skill Phase 2.3 — unless the operator opted out in the option block.
4. **Consistency sweep + quality checklist** (skill Phase 2.2/2.4): no contradicting commands across root and package docs; every command copy-pasted and run once; every path `ls`-verified; line counts real.
5. **Vendor the detector (MIT)** to `scripts/docs/check-agent-docs-freshness.sh` (licence header + provenance line), alongside P22.1's `check-repo-docs-freshness.sh`; add `make docs-check` = both detectors on `.`, exit non-zero on critical issues; add a `docs` job (or a step in the `python` job) to `.github/workflows/ci.yml` running `make docs-check` on pull requests — structural checks only, no LLM (skill "Optional CI Integration"). `make check` gains `docs-check` **only if** it runs in <10 s locally (measure; else keep it CI-only and say so).
6. **ADR-072** "Documentation freshness gates" (two vendored detectors, `make docs-check`, CI step; refresh cadence = every release per `INTEGRATION_PLAN.md`; Revisit trigger = detector false-positive rate >1 per PR); `docs/adr/README.md` index.
7. **Traceability + risk register**: `## Phase 22 — Documentation (P22.2)` with `RISK-P22-03` (agent docs exceed the harness budgets and stop being read → length budgets asserted by the detector), `RISK-P22-04` (a contradiction between root and package docs → consistency sweep + nearest-file-wins stated). Traceability section "Agent-facing documentation".

## Out of scope (scope-creep guard — do NOT do these here)
- Human-facing docs (README, CONTRIBUTING, CHANGELOG, `docs/**`) — **P22.1**; if drift is found, note it in the PR body. Any product code change (only `scripts/docs/`, `Makefile`, `.github/workflows/ci.yml`, docs). `--cross-repo` integration surface (single repo). Ticking any gate, flipping any source, merging/tagging (operator).

## Acceptance criteria
- [ ] `bash scripts/docs/check-agent-docs-freshness.sh .` exits 0 with **0 critical issues and 0 coverage gaps** (every package with ≥5 source files has an `AGENTS.md`). *(deterministic)*
- [ ] Root `AGENTS.md` ≤ 250 lines; every package `AGENTS.md` ≤ 150 lines (`wc -l`); every file has the standard sections in the guideline order (`grep -n '^## '`). *(deterministic)*
- [ ] Every command under "Build & Test" in every `AGENTS.md` was executed once during the run; exit codes listed in the PR body (Docker-dependent ones may report `skipped: no daemon` only if Docker was genuinely absent — with `docker info` output). *(deterministic — evidence in PR body)*
- [ ] Root `AGENTS.md` contains the strings `BUILD.sh`, `ingestion_permitted`, `SIG_REQUIRE_DB_TESTS`, `verify-gen`, `docs/tickets/`, `docs/build/`, `.agents/scratch/`, `HG-` (gates), and states nearest-file-wins. *(deterministic)*
- [ ] `CLAUDE.md` exists at the root and its first non-empty line is `@AGENTS.md` (unless the operator opted out — then the PR body says so). *(deterministic, conditional)*
- [ ] `make docs-check` runs both detectors and exits 0; `.github/workflows/ci.yml` contains the step; the PR's CI run shows it executed. *(deterministic)*
- [ ] Clean-slate ticked only: `git log --diff-filter=D --name-only` in this branch shows the old `AGENTS.md` files deleted before the regenerated ones were added. *(deterministic, conditional)*
- [ ] An agent given only the root `AGENTS.md` can state the test command, the spec-amendment rule, and three things it must never do — without opening another file (reviewer check). *(agentic)*
- [ ] **Phase-gate (§51.3):** `make check` green (unchanged test count); ADR-072 indexed; traceability + risk register appended. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
No new canonical requirement ids (agent-orientation hygiene). Stamp: SIG-ENG-001 (executable from the documents), SIG-ENG-012 (frozen layout stated as a boundary), SIG-ENG-015/016 (CI gate extended), SIG-ENG-031 (risk register).

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** every statement in an `AGENTS.md` is verified against a file or a run; a gotcha is a fact, not an opinion.
- **Part VIII is binding (§0.7):** the docs restate the Part VIII protections as agent boundaries (no plate/person data, officer-naming gate, ODbL separation); no secrets or private contacts in any doc.
- **Append-only & provenance (P1–P3):** the docs instruct agents to append, never rewrite, the registers and ADRs.
- **Additive/back-compat:** the CI step is additive; `make check` semantics unchanged unless `docs-check` is fast enough to include (measured).

## Notes
- This ticket **owns** the `AGENTS.md` hierarchy, `CLAUDE.md`, `scripts/docs/`, `make docs-check`, and the CI docs step. After it, the CAPSTONE of the post-build chain runs (build ledger), then the operator merges the whole stack per `INTEGRATION_PLAN.md` §(d) — with current docs at the top of the stack.
- Re-run cadence: whenever a later chain lands (P22+ planning), re-run P22.1 then P22.2 (`refresh` mode; both idempotent).
