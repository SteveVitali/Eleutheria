<!--
  Lane C (new code) contract — productionize Round 4. Filled by decompose-spec mode=extend over
  ~/MetaHarness/sig-golive-spec.md (2026-09-09). CITES the spec's §§ and GL-* ids.
-->
# P24.5 — Housekeeping backlog (META.1, GL-META-01)

- **Semantic id:** `META.1` (**GL-META-01**) — renumbered from `83_META.1` to `P24.5` for lexical chain order; the semantic/GL ids are unchanged.
- **Sequence:** P24.5 (was 83 of 87) · **Phase:** 22+ (productionize) · **Kind:** ticket
- **Tag:** golive-round4
- **base_branch:** current checkout
- **Depends on:** CI.1 (row 82) — the previous Round-4 step
- **Run:** `implement-spec spec=docs/tickets/P24.5__housekeeping-backlog.md live_verification=false`
- **Gate status:** none
- **Live stage:** offline-only

## Goal
Clear the standing housekeeping debt: package descriptions, docs-drift, and the P22+ backlog
triage — with real landings, not just re-filing.

## Load (read these — do not re-read others)
- `~/MetaHarness/sig-golive-spec.md` Part II § META.1 (GL-META-01); Appendix A (BL-052).
- `docs/build/BACKLOG.csv` (P22+ rows); the 5 "skeleton" `pyproject.toml` descriptions;
  `docs/build/tools/check_backlog.py`.

## In scope — deliverables
1. `pyproject.toml` descriptions for the 5 packages still marked "skeleton" (GL-META-01; BL-052).
2. Remaining docs-drift fixes (GL-META-01).
3. Triage the P22+ `BACKLOG.csv` rows into DEFERRALS/backlog with real landings (GL-META-01).

## Out of scope
- New features / connectors. Anything owned by DEPLOY.1/SCHED.1/OBS.1/CI.1.

## Acceptance criteria
- [ ] no package description says "skeleton" *(deterministic)*
- [ ] `check_backlog.py` green *(deterministic)*
- [ ] BL-052 closed in `BACKLOG.csv` *(deterministic)*
- [ ] verification green; every new behaviour has a test that fails if it is removed; requirement ids stamped in the PR; anything not automatically verifiable is a `DEFERRALS.md` row with its compensating control; ADRs written for every deviation and owned decision; `BUILD_INDEX.md` row and `LEDGER.md` advanced. *(agentic — the universal phase-gate AC)*

## Requirement IDs to satisfy and stamp in the PR
GL-META-01. Closes BL-052.

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`.

## Notes
- Also the natural home to fix the vendored `check_backlog.py` path drift (it hard-codes
  `docs/build/LEDGER_DEFERRALS.md`, which P22.3 moved to `docs/build/reports/`) as part of
  "docs-drift + `check_backlog.py` green". Re-confirm the 5 skeleton descriptions at build time.
