<!--
  P30.3 — National export build + public cut-over (Round 8 / GO-LIVE.3).
  DRAFT on devin/p30-golive-seed — stacks on the P30.2 tip.
  HOSTED-EXECUTION + GATED tail: builds the national export from the MATERIALIZED hosted spine,
  then the real public cut-over of national surveillance data onto the public buckets +
  surveillancegraph.org. It PAUSES at HG-11 / Go-public — the actual public launch, operator sign-off.
  Merges the proposed "national-export-build" row into the cut-over (5→4 collapse).
-->
# P30.3 — National export build + public cut-over (P30)

- **Semantic id:** `GO-LIVE.3` · **Phase:** 30 · **Kind:** ticket · **Tag:** p30-golive
- **base_branch:** `devin/p30-2b-geospatial-camera-site-resolution` (re-pointed 2026-09-24 when P30.2a/P30.2b were inserted) · **Depends on:** P30.1 (settled spine) + P30.2 (materialized graph) + **P30.2a/P30.2b (camera predicates registered + geospatial camera-site resolution — the public "N resolved sites (from M observations)" headline must use P30.2b's recorded definition and measured N/M/dedup ratio with its PROVISIONAL disclosure; if P30.2b measured N honestly at 0 or near-0, keep observation-level framing rather than print a misleading "0 resolved sites")** + the P27.4 spine-backed export path + P27.8 cut-over machinery + P27.10 domain/TLS (all landed; TLS ACTIVE on surveillancegraph.org)
- **Run:** `implement-spec spec=docs/tickets/P30.3__national-export-and-public-cutover.md live_verification=true`
- **Gate status:** **HG-11 / Go-public — PAUSE for operator sign-off.** This is the ACTUAL public launch of
  real national surveillance data. HG-01 (legal home) / HG-11 (reviewer roles, sole-maintainer posture) /
  HG-02 (interim engineering dispositions) are ANSWERED carry-forward (LEDGER GATE DECISIONS 2026-09-22/23);
  the domain + TLS are live (D-P27.10-1 DONE). The remaining human step is the operator ticking Go-public at
  the cut-over — never auto-ticked. The ticket builds + proves the export and the published/restricted
  partition, then pauses for the Go-public tick before the public sync.
  - **ANSWERED (copied from `docs/build/LEDGER.md` § GATE DECISIONS, first commit of the run):**
    - [x] **2026-09-23 — HG-11 / Go-public launch: APPROVED** — operator, verbatim: *"I give you the launch
      go approval now."* No further pause for the go. **Binding safety framing (not overridden):** the
      cut-over proceeds ONLY if (a) compartment separation holds — no mixed-licence artifact, no
      UNDETERMINED/excluded byte in any public object; (b) the export is built from the settled,
      materialized hosted spine; (c) `probe-hosted` confirms the REAL national data renders on both URLs.
      If any fails, HALT and report — never publish a bad cut-over or fabricate green.
    - [x] **2026-09-24 — launch sequencing:** *"Fix resolution first, then launch."* — done (P30.2a / P30.2b);
      this ticket publishes P30.2b's measured metric as the export computes it over its own sites, with the
      PROVISIONAL disclosure (D-R6.1-EVAL).
    - [x] **2026-09-24 — share-alike layers:** *"counsel says it's okay and we can publish it all together."*
      Implemented as: ALL layers public at launch — ODbL `osm_physical`, CC-BY-SA, and the CC-BY `sig_graph`
      — on ONE public map/site (an ODbL 4.4(b) produced work, "© OpenStreetMap contributors" shown wherever
      OSM-derived points render), while the DOWNLOADABLE datasets remain LICENCE-SEPARATED compartments, each
      labelled with its SPDX licence + attribution. ODbL is NOT merged into the CC-BY graph as one downloadable
      database (4.4(a); breaks the gated separation invariant). Counsel clearance is operator-reported, no
      written opinion on file → `D-P30.3-COUNSEL` stays OPEN. A new ADR supersedes ADR-096's classifier rule.

## Goal
Build the **national export** from the MATERIALIZED hosted spine
(`sig-exports build --from-spine --dsn <hosted> --out exports/out/national`, **D-P27.4-1** + **D-R6.5-SURFACE**)
so the emitted surfaces carry the REAL "N resolved sites (from M observations)" count, the real §29.3 sharing
network, the real §31 contradictions, the real §32 coverage, and the real accountability chain — then execute
the **public cut-over** (**D-P27.8-1**): build `web/dist` in `SIG_DATA_SOURCE=export` mode, sync `web/dist` +
the **published** compartment to the public buckets (keeping `…-sig-restricted` PRIVATE), `probe-hosted`,
confirm the REAL national data renders (not the OKC demo) on both `sig-web…run.app` and
`https://surveillancegraph.org`, and record the served watermark.

## Load (cite; do not copy)
- `docs/tickets/DEFERRALS.md` — **D-P27.4-1** (exact `sig-exports build --from-spine` re-run command + expected compartmented bundle + 10 web/*.json + PMTiles), **D-R6.5-SURFACE** (the materialized-surface republish: `web/coverage.json` resolved_sites N-of-M + contradictions_visible + materialized_* coverage/edges), **D-P27.8-1** (the hosted cut-over: `sig-ops deploy --prepare-only` → `deploy` → `probe-hosted`; published-only sync, restricted stays private; `assert_public_clean`).
- `docs/2_canonical_design_spec.md` §38 (export bundles + licence compartments), §39–41 (public UI / national IA), §42.3 (ODbL compartment separation), §29/§31/§32 (materialized surfaces).
- Code: `exports/spine_export.py` (`run_spine_export`, one `REPEATABLE READ READ ONLY` snapshot; reads the materialized tables), `exports/` compartment/`assert_separated`/fail-closed gate, `ops/` (`sig-ops deploy --target gcp [--prepare-only]`, `sig-ops probe-hosted`, `export_watermark`); `web/src/lib/data.ts` (export-mode fail-loud seam).
- ADR-090/092/096/098/101 (national surface, spine-read export, published-compartment cut-over, domain LB, materialized-graph surface) — **CONSUMED, not amended**.

## In scope — deliverables
1. **National export build** (D-P27.4-1 + D-R6.5-SURFACE): `sig-exports build --from-spine` over the hosted
   `sig-pg` (read-only snapshot) after P30.2's materializations — produce `exports/out/national` + the ten
   `web/*.json` surfaces + per-compartment PMTiles + `provenance.ttl` + `exclusions.json`; confirm
   `assert_separated` + the fail-closed licence gate over the real licence mix (osm_physical ODbL + sig_graph
   CC-BY + derived_facts, each compartment single-licence); confirm the surfaces carry the REAL materialized
   numbers (resolved_sites N-of-M, contradictions_visible, materialized coverage/edges, accountability chain).
2. **Prepare + prove the cut-over** (D-P27.8-1): `sig-ops deploy --target gcp --prepare-only` builds `web/dist`
   from the national export, partitions into `exports/out/{public,restricted}`, PROVES the public compartment
   clean (`assert_public_clean` — fail-loud if the export is absent or a leak is injected).
3. **PAUSE for HG-11 / Go-public** (operator tick).
4. **Execute the public cut-over**: `sig-ops deploy --target gcp` runs the published-only GCS sync to
   `…-sig-web` + `…-sig-public`, keeping `…-sig-restricted` PRIVATE; `probe-hosted` green on `sig-web-root` +
   `sig-public-national-manifest`; confirm the REAL national data renders on both `sig-web…run.app` and
   `https://surveillancegraph.org`; record the served watermark (`release_id`/`as_of`/`ruleset`).

## Out of scope
- Any spine WRITE (the export opens a read-only snapshot; no UPDATE/DELETE).
- Publishing the restricted / ODbL-only or UNDETERMINED compartments to a public bucket (leak — fail-closed).
- Post-launch closeout / whole-build capstone (P30.4).
- Auto-ticking Go-public; a fixtures fall-back for missing national data (forbidden — fail loud).

## Acceptance criteria
- [ ] national export built `--from-spine` over the materialized hosted spine → `exports/out/national` (compartmented, 10 web/*.json, PMTiles, PROV-O, exclusions) with `assert_separated` + fail-closed gate green over the real licence mix; surfaces carry the REAL materialized numbers *(live-verified)* — closes **D-P27.4-1** + **D-R6.5-SURFACE**.
- [ ] cut-over prepared + proven (`--prepare-only` fail-loud + `assert_public_clean`), then — after the operator ticks **HG-11 / Go-public** — the published-only sync runs, restricted stays PRIVATE, `probe-hosted` green, and the REAL national data renders on `sig-web…run.app` + `https://surveillancegraph.org`; served watermark recorded *(live-verified)* — closes **D-P27.8-1**.
- [ ] `make check` + build-memory green; the three closed deferrals flipped to `DONE` with date + evidence; no fabricated URL or watermark; Go-public recorded in LEDGER § GATE DECISIONS.

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: licence compartments never mix (§42.3; ODbL separate); the public
compartment is published-only, restricted stays PRIVATE; export reads a read-only spine snapshot (no write);
zero-JS public budget + WCAG 2.2 AA preserved (ADR-068/091 island allowance the only exception); Go-public is
a human tick, never auto; no fabricated hosted green; no merge/tag/push-main.
