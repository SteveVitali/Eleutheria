<!--
  P28.6 — Accountability linkage (Round 6 / DEPTH.6).
  DRAFT on devin/round6-planning — seeded post-P27. Design: ROUND6_7 §4 P28.6.
-->
# P28.6 — Accountability linkage: deployment↔vendor↔contract↔funding↔policy↔oversight (P28)

- **Semantic id:** `DEPTH.6` · **Phase:** 28 · **Kind:** ticket · **Tag:** p28-depth
- **base_branch:** `devin/p28-5-refresh-surface-materialized-graph` · **Depends on:** P28.1 (resolved entities)
- **Run:** `implement-spec spec=docs/tickets/P28.6__accountability-linkage.md live_verification=true`
- **Gate status:** engineering — derived links (append-only, labelled inference).

## Goal
Connect the accountability layer — the ~205k procurement claims + the P13.x contract/funding/policy/
accountability entities — to the resolved deployments/orgs, so a dossier answers **"who deployed it,
funded how, governed by which policy, with what oversight."** This turns the map into accountability
infrastructure, not a point map.

## Load (cite; do not copy)
- `docs/2_canonical_design_spec.md` §13 (accountability/policy/legal entities), §11.14 (FundingInstrument/LegalInstrument), §12 (relationships), §21.6 (lineage), §3.1.
- Code: `inference/` (derived-claim inference), `reconcile/`, the P13.x entities in `db`, the P28.1 resolved entities + P28.2 edges.

## In scope — deliverables
1. Derive + **materialise** the accountability links (deployment↔vendor↔contract↔funding↔policy↔
   oversight) as **labelled inference** over resolved entities (new sqitch change; append-only; every
   derived edge cites its establishing claims — no synthetic certainty).
2. Enrich the per-jurisdiction dossiers (via the P28.5-refreshed surface) with the governance chain.

## Out of scope
- New source ingestion (P29.3); the surface plumbing (P28.5 owns it); asserting a link the evidence
  doesn't support.

## Acceptance criteria
- [ ] accountability links materialised as labelled inference over resolved entities, each citing its establishing claims (new sqitch change; append-only) *(deterministic + live-gated)*
- [ ] a real dossier shows the deployment→vendor→contract→funding→policy→oversight chain where evidence exists; gaps shown honestly *(deterministic)*
- [ ] `make check` + `make test-db` + build-memory green *(deterministic)*

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: every inference labelled; no assertion beyond committed
evidence (§3.1); append-only; procured ≠ deployed (the predicate-layer guard) preserved.
