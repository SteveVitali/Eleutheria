<!--
  P29.2 — Detector/task engine + records-request loop on real data (Round 7 / ACTIVATE.2).
  DRAFT on devin/round6-planning — seeded post-P27. Design: ROUND6_7 §5 P29.2.
-->
# P29.2 — Detector/task engine + records-request loop on real data (P29)

- **Semantic id:** `ACTIVATE.2` · **Phase:** 29 · **Kind:** ticket · **Tag:** p29-activate
- **base_branch:** `devin/p29-1-contributor-identity-and-ops` · **Depends on:** Round 6 (resolved graph + contradictions + coverage)
- **Run:** `implement-spec spec=docs/tickets/P29.2__detector-records-request-loop.md live_verification=true`
- **Gate status:** engineering, EXCEPT **operator gate to actually SEND records requests** (external side effect).

## Goal
Run the built §33 detector catalog (34 detectors) + records-request generation over the **real** spine to
auto-produce the research queue + records requests — the self-feeding accountability loop (contradictions
+ gaps + renewals → the next round of evidence).

## Load (cite; do not copy)
- `docs/2_canonical_design_spec.md` §33 (detector DSL, lifecycle, dispositions, geo queues, anti-abuse; the §33.2 catalog of 34), §33.4 (records-request generation + statute templates), §32 (coverage/negative space feeds detectors).
- Code: `tasks/` (detector engine, `records_request.py`, `geographic.py`, anti-abuse), the Round-6 materialised contradictions/coverage/relationships, `parsing/` statute templates.

## In scope — deliverables
1. Run the 34-detector catalog over the real spine → materialise the **research queue** (tasks from
   contradictions/gaps/renewals), with geo queues + anti-abuse (P10.1) intact.
2. Generate **records requests** from the gaps/contradictions (statute-templated) — **drafted, not sent**;
   sending is an explicit operator gate (external side effect; never auto-fire).
3. Surface the research queue on the public surface (the honest "what SIG is looking at next").

## Out of scope
- Identity/contribution (P29.1); source breadth (P29.3); auto-sending records requests.

## Acceptance criteria
- [ ] the detector catalog runs over the real spine → a real research queue (tasks with dispositions + geo queues + anti-abuse) *(deterministic + live-gated)*
- [ ] records requests are auto-drafted from gaps/contradictions (statute-templated); sending is operator-gated, never auto *(deterministic)*
- [ ] `make check` + `make test-db` + build-memory green *(deterministic)*

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: anti-abuse on task/detector generation; no external side effect
(records-request send) without an operator gate; append-only; every generated task cites its trigger.
