<!--
  Template: docs/tickets/NN[a-z]_HUMAN-H<k>__<slug>.md — a HUMAN marker (BM-TICKET-05).
  Written by: decompose-spec mode=extend over docs/3_sig_golive_spec.md (2026-09-09).
  A marker, NOT an implement-spec input: no run line. Checkboxes are living state, ticked (with a
  date) by the operator or by orchestrate-build when the ledger records the item done.
-->
# P23.2 — Legal home & operating governance (GOV.1, GL-GOV-01)

- **Marker id:** `HUMAN-H1` (semantic **GOV.1** / **GL-GOV-01**) — renumbered from `68_HUMAN-H1` to `P23.2` for lexical chain order; the semantic/GL ids are unchanged.
- **Kind:** human · **Phase:** 21 (go live)
- **Blocks:** LIVE.2 publish (HG-01 legal home + HG-11 governance); informs RIGHTS.1 (reviewer roles must exist).
- **Gate register:** HG-01 (legal home, SIG-GOV-012), HG-11 (operating governance, SIG-PUB-008).

> **Operator work — NOT an `implement-spec` input.** Actions only the operator can take. The
> chain never silently blocks: a code ticket that reaches this before it is done records the
> gated remainder as `DEFERRALS.md` rows and continues.

> **Pre-answered (interim) at decompose time — GL-GATE-01.** SIG operates as an independent
> open-source project under maintainer stewardship pending a formal legal-home designation. This
> unblocks the artifacts; a **real legal home remains a human action before the actual public
> cutover (Go-public)**. Not a substitute for legal counsel. HG-11 (governance roles/workflow/
> contact) is still genuine operator work, not pre-answered.

## Checklist
- [ ] Name the legal home in `docs/governance/governance-and-code-of-conduct.md` (SIG-GOV-012). <!-- interim posture recorded via GL-GATE-01; supersede with the real home before Go-public -->
- [ ] Define **two reviewer roles** + the written-concurrence workflow (`ReviewerConcurrence`, SIG-PUB-008). <!-- HG-11 -->
- [ ] Stand up a live takedown/corrections contact (org channel only, no personal data). <!-- HG-11 -->
- [ ] Sign the marker; readout to `docs/build/readouts/`.

## Exit criterion
The governance doc names the home + the two roles + the contact; `docs/build/PUBLICATION_CHECKLIST.md`
HG-01/HG-11 rows tick with evidence links.

## DEFERRALS rule for tickets that run before this completes
A ticket that depends on this row but runs before it is ticked opens a `D-<TICKET>-<n>` row in
`DEFERRALS.md` (why deferred = "HG-01/HG-11 pending", unblocked-by = "HUMAN-H1", how-to-verify)
and reports "gate pending" — never a failure, never a fabricated pass. (See existing
`D-P21.4-1` (HG-01), `D-P21.4-2` (HG-11).)
