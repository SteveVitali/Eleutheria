# P23.7 — integration plan refresh (Round 2, REC.3)

- **Sequence:** 73 of 73 · **Phase:** 23 (reconcile) · **Kind:** reconcile · **Tag:** reconcile
- **base_branch:** current checkout (the branch P23.6 left checked out)
- **Depends on:** P23.6 (spec reconciliation)
- **Run:** `implement-spec spec=docs/tickets/P23.7__integration-plan.md live_verification=false` (invokes `reconcile-build mode=integration`)
- **Gate status:** none · **Live stage:** none

> **Round 2 — delta over Round 1.** Round-1 integration plan is `docs/build/INTEGRATION_PLAN.md` +
> `merge_dryrun.sh` (P20.3). This refreshes the PR graph (now #47–#68 plus the Round-2 PRs), re-runs the
> **read-only** merge dry-run (never merges), and drafts the release-notes delta. No ticket merges/tags.

## Goal
Give the operator a current, copy-pasteable bottom-up integration procedure for the full stack including
the Round-2 tail — a read-only refresh; nothing is merged, tagged, or pushed.

## Load (read these — do not re-read others)
- `docs/build/INTEGRATION_PLAN.md`, `docs/build/reports/RELEASE_NOTES_v0.1.0.md`, `docs/build/CI_STATUS.md`? (in `reports/CI_STATUS.md`).
- `docs/build/tools/merge_dryrun.sh` (read-only), `docs/build/BUILD_INDEX.md` (the row set for release notes).
- `git log --oneline --first-parent` and `gh pr list --state all` for the current PR graph.

## In scope — deliverables
1. Refresh `docs/build/INTEGRATION_PLAN.md`: the current PR graph (#47–#68 + Round-2), the read-only merge dry-run output (`merge_dryrun.sh`, conflicts listed, refs unchanged, worktree clean), the bottom-up merge + `v0.1.0` tag + release operator procedure, rollback, and post-merge verification.
2. Draft the release-notes delta from `docs/build/BUILD_INDEX.md`.

## Out of scope
- Merging/tagging/pushing `main` (operator action after the chain). Backlog (P23.5), spec (P23.6).

## Acceptance criteria
- [ ] `merge_dryrun.sh` runs read-only (refs unchanged, worktree clean, conflicts listed) and its output is recorded in `INTEGRATION_PLAN.md` *(deterministic)*
- [ ] the integration procedure covers every open PR in the current graph with a `ticket:`/`proof:` per step *(deterministic)*
- [ ] **Phase-gate:** verification green; requirement ids stamped; `DEFERRALS.md` maintained; ADRs for deviations; `BUILD_INDEX.md` row and `LEDGER.md` advanced. *(agentic)*

## Requirement IDs to satisfy and stamp in the PR
The integration/release ids (matching `SIG-[A-Z]+-[0-9]+`) this refresh touches.

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`; **no merge/tag/push-main** (operator integrates).

## Notes
- On `projectStatus: DONE` (BM-TAIL-03): every chain row landed-or-skipped, `BUILD_INDEX.md` complete, no OPEN deferral without a landing, GATE-ACCEPT signed. The `D-P21.n-*` RETURN-PASS rows remain OPEN pending the operator's HG gates — recorded, not blocking.
