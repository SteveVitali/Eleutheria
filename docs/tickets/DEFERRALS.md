<!--
  docs/tickets/DEFERRALS.md (BM-DEFER-01). Seeded by P22.3 (build-memory v2 migration) from the
  docs/build/LEDGER.md RETURN PASS table + returnPass: list. Append-only companion — NOT a chain
  ticket, never overwritten or deleted. The four rules below are stated verbatim.
-->
# Deferred obligations ledger

> **Maintained companion — NOT a generated ticket.** Committed and hand-maintained; preserved
> across any ticket regeneration; never overwritten or deleted. It has no `NN_` sequence prefix
> so it is not mistaken for a chain ticket.

The rules:
1. **Every run, first:** read this file. If the ticket you are about to implement — or a
   prerequisite it depends on — unblocks any `OPEN` row, **closing that row is part of your
   run**: verify it for real, then flip it to `DONE` with the date and evidence.
2. **Never delete a row.** Flip `OPEN` → `DONE` (verified) or `WONTFIX` (with a reason).
   History stays.
3. **When you defer something new,** append a row here in the same run that defers it. A
   deferral that is not in this file did not happen.
4. **Gates refuse to pass** while any `OPEN` row scoped to that phase remains. Treat an open
   row as gate-blocking.

Status values: `OPEN` (owed) · `PARTIAL` · `DONE` (verified — add date + evidence) ·
`WONTFIX` (add reason) · `ACCEPTED-SKELETON`. `kind`: V (verification) | F (functionality) |
D (deviation) | H (handoff seam) | P (human prerequisite) | X (other).

Pre-existing normalized debt is tracked in `docs/build/BACKLOG.csv`; each row below cites its
`BL-` id where one exists (no duplication). These rows are the **RETURN PASS** obligations from
`docs/build/LEDGER.md` — Phase-21 tickets the operator ran with a human gate skipped. They are
**not** blocks (the chain completed and the round-1 capstone is DONE); they are owed operator
work re-run per each ticket's `Run:` line after the operator acts.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-P21.1-1 | P | HG-03 — flip the reviewed OKC/critical-path sources to `ingestion_permitted=true` | P21.1 shipped the 27 rights packets, review-metadata rule + `review-status` CLI, but flipping a source is a human/rights decision (gate HG-03), not a code change | operator reads the packets in `docs/build/reports/rights/`, picks source ids to flip (each needs reviewer role + date) | `sig-connectors review-status` shows flip-ready sources; `loadable now: 0` until flipped | DONE 2026-09-10 — RIGHTS.1 flip re-run (`devin/p21-1-rights-flip-rerun`) executed operator disposition GL-GATE-03 (GATE DECISIONS 2026-09-09): flipped the OKC critical subset to `ingestion_permitted=true`, reviewer=`maintainer (delegated)`, date 2026-09-10. 6 ids + SPDX basis: `okc_procurement`/`okc_council`/`okcpd_policy`/`ok_statute` = **CC0-1.0** (US/OK government records + statutory text are uncopyrightable/public-domain per their packets; registry-accepted public-domain expression, `policy/data/licenses.toml`); `osm_overpass` = **ODbL-1.0** (redistributable + derivative_permitted per packet, kept in the separate ODbL compartment); `deflock_repo` = **MIT** (redistributable + derivative_permitted per packet). Evidence: `sig-connectors validate` → `ingestion_permitted=true: 6`, `loadable now: 6`, self-checks OK; `review-status` → `flip-ready: 16`, `loadable now: 6`; `review-status --source <id>` → all five gate fields True for each of the 6; `make check` 2720 passed/2 skipped/0 failed. News sources `journalrecord`/`oklahoman` stayed LINK-posture (UNDETERMINED); FR/BE untouched (design-gated false). (cites BL-032) |
| D-P21.1-2 | P | HG-04 — record actual Stage-0 outreach outcomes to the 19 federation-compact projects | P21.1 shipped the outreach record template + letter; no outreach was performed (no outcomes to record) | operator performs/records outreach; rows move off `not_contacted`/`public_terms_only` | `docs/build/reports/STAGE0_OUTREACH_RECORD.md` all rows `not_contacted` | OPEN (cites BL-033) |
| D-P21.3-1 | V | HG-03 — first real LIVE fetch of ≥1 green source (document-connector modules okc_procurement/okcpd_policy/ok_statute) | P21.3 landed `HttpxTransport`/`OcflCaptureStore`/`sig-connectors run --mode live` (refuses exit 3 for non-green sources); no source is green yet | after D-P21.1-1 flips ≥1 source, re-run `implement-spec spec=docs/tickets/P21.3__live-connector-wiring.md live_verification=true` | stub + fixture-replay tested; `run --mode live` refuses exit 3, no socket opened | PARTIAL 2026-09-10 — **connector-code half DONE by P23.5 (LIVE.1a / GL-LIVE-01):** the three document-connector modules `okc_procurement`/`okcpd_policy`/`ok_statute` now exist (`connectors/src/connectors/okc_documents.py` + the three modules), fetch→OCFL→parse→emit implemented and proven by **shadow replay diff = 0** against committed fixtures (`tests/connectors/test_okc_documents.py`); sources are green (RIGHTS.1). The **live-fetch / transport re-run half stays with P21.3** (manifest row 73) and is still owed — blocked only on HG-09 tokens (see `D-LIVE.1a-1`). (cites BL-023; BL-023 connector-code closed) **PARTIAL advanced 2026-09-10 by the P21.3 LIVE.1 fetch re-run (`devin/p21-3-live-wiring-rerun`, prepare-only):** HG-03 now **CLEARED** — `review-status --source <id>` shows all five gate fields True + `loadable now: True` for `okc_procurement`/`okcpd_policy`/`ok_statute`/`osm_overpass`/`deflock_repo`/`okc_council`, and `run --mode live` NO LONGER refuses them on gate grounds (`live_gate_reasons(<id>)` = empty); the still-un-flipped `usaspending` still refuses exit 3 with reasons + no socket. The real fetch is **still pending HG-09 tokens + network egress** (this isolated context has neither) — **NOT DONE, no real fetch happened; no live green fabricated.** Deterministic ACs re-confirmed (gate-refusal exit 3, shadow diff 0 for osm/atlas + the 3 doc connectors, no-token-literal, isolation). Evidence: `docs/build/runs/P21.3.md` (2026-09-10 re-run) + `docs/build/reports/LIVE_WIRING_REPORT.md` (2026-09-10 dated section). |
| D-P21.3-2 | P | HG-09 — API tokens (`SIG_MUCKROCK_TOKEN`/`SIG_DATA_GOV_KEY`/`SIG_OVERPASS_ENDPOINT`/`SIG_CIVICCLERK_BASE`) exported in the worker shell | live fetch needs credentials; `provided: no` at the P21.3 gate (secrets never in a file) | operator exports the env vars in the re-run shell; ledger records only `provided: yes/no` | no live fetch; no-token-literal test passes | OPEN (cites BL-024) — re-confirmed 2026-09-10 by the P21.3 LIVE.1 fetch re-run: HG-03 now cleared but tokens `provided: no` + no network egress in the isolated context → the live fetch still cannot run; stays OPEN |
| D-P21.4-1 | P | HG-01 — name the legal home | P21.4 ended at local staging; public publish is gated on a named legal home | operator names the legal home; `PUBLICATION_CHECKLIST.md` HG-01 ticks | **re-verified 2026-09-10 by the LIVE.2 re-run (`devin/p21-4-live2-rerun`):** local composed staging re-run for real (Docker) — `run_okc.sh` complete, `PUBLICATION_CHECKLIST.md` item 1 = `gate pending: HG-01`. HG-01 stands at the **INTERIM** posture only (GL-GATE-01 / P23.2: maintainer-stewardship, **NOT counsel, NOT a real named home**); a real legal home is still required before Go-public. | OPEN (cites BL-034) — re-confirmed 2026-09-10; interim posture is not a real home |
| D-P21.4-2 | P | HG-11 — establish two reviewer roles + written concurrence + live takedown contact | governance prerequisites for public publish were not established | operator establishes the roles/concurrence/contact; `concurrence.md` filled | **re-verified 2026-09-10 by the LIVE.2 re-run:** the corrections/dispute mechanism is built + served (re-verified in the export-mode e2e); `docs/build/reports/okc/concurrence.md` is still a template only — no two named reviewer roles + written concurrence + live takedown contact (P23.2/HUMAN-H1 recorded HG-11 as OWED). | OPEN (cites BL-036) — re-confirmed 2026-09-10, governance still owed |
| D-P21.4-3 | P | Go-public — DNS/host cut-over to publish the first jurisdiction | impossible while HG-01 + HG-11 are open; P21.4 ends at staging | after D-P21.4-1/2, operator decides go-public and re-runs `implement-spec spec=docs/tickets/P21.4__first-jurisdiction-ingest-and-publish.md live_verification=true` | **re-verified 2026-09-10 by the LIVE.2 re-run:** local staging complete + re-verified (`SIG_STAGING_*` local; OSM-in-exports/HG-02 interim-ticked), but Go-public is **reserved to the human** (GL-GATE-05 / GATE-G2 / P23.6, row 75) and impossible while HG-01 (real home) + HG-11 remain open. No public cut-over, no `0.2.0` bump done. | OPEN (no dedicated BL; depends on BL-034/BL-036) — re-confirmed 2026-09-10, Go-public genuinely gated |
| D-P21.5-1 | V | HG-07 — real Zenodo (sandbox) deposit + object-store push + egress report + SWH save | P21.5 ran dry-run/FakeZenodo/MockTransport; no credentials (`provided: no`), zero-cost posture | operator provides `SIG_ZENODO_SANDBOX_TOKEN`/`SIG_OBJECT_STORE_*` and re-runs `implement-spec spec=docs/tickets/P21.5__infra-deposit-and-tiles.md live_verification=true` | free paths (tiles/.torrent/degraded/SWH-save) real; credentialed steps dry-run/stub tested. **Proxy-now re-verified 2026-09-10 by the INFRA.1 (GL-INFRA-01) re-run (`devin/p21-5-infra-rerun`, base `devin/p23-6-go-public-marker`, prepare-only):** over a freshly-built `okc` export on the current tip — `sig-exports tiles` → valid PMTiles v3, ≥1 layer, `sig:license=ODbL-1.0` + attribution (pure-Python renderer; tippecanoe absent); `sig-exports torrent` → real pure-Python bencode (`infohash=8d1d2b60…`); `sig-ops degraded --data-source fixtures` → `web/dist` with NO API ($0 host); `keepalive.yml` valid + runs the same `sig-ops degraded` command; `sig-exports deposit --dry-run` → exit 0 (FakeZenodo dry-run DOI recorded); `sig-exports deposit --sandbox` w/o token → **exit 4** `gate pending: HG-07`, wrote nothing; `sig-ops egress-report` → `gate-pending` (no usage API) yet computes `level=ok`/`ratio=0.05` for a simulated `--usage-gb 5`; `sig-ops swh-save` (no `--now`) → request built for real, **untriggered** (no network). `tests/exports/{tiles,torrent,zenodo,push}` + `tests/ops/{degraded,egress,swh}` = 43 passed; `SIG_REQUIRE_DB_TESTS=1 make check` 2767 passed/2 skipped/0 failed; no token literal. **The credentialed half — real sandbox `10.5072/…` deposit + object-store push + live egress + SWH save — is STILL blocked on HG-07 (`provided: no`, P23.4/ACCT.1) + network egress; NOT DONE, no credentialed green fabricated.** | OPEN (cites BL-029) — re-confirmed 2026-09-10; free paths re-verified, credentialed steps still gate-pending HG-07 |
| D-P21.7-1 | V | HG-08 — MapRoulette API key + registered OSM Organised-Editing page → real challenge push/pull | P21.7 ran dry-run/stub; the registration gate refuses push (exit 3) while `registered=false`; no creds | operator creates the account, registers the OE page, sets `SIG_MAPROULETTE_API_KEY`/`SIG_OSM_OE_PAGE`/`registered=true`, re-runs P21.7 live | **proxy-now re-verified 2026-09-10 by the CONTRIB.1 (GL-CONTRIB-01) re-run (`devin/p21-7-contrib-rerun`, base `devin/p21-5-infra-rerun`, prepare-only)** on the current tip: `sig-tasks maproulette push --challenge 1 --jurisdiction okc --dry-run` → **REFUSED exit 3** with the OE-registration reason (`ops/config.toml [tasks.contribution] registered=false`); the dry-run challenge JSON build + MapRoulette v2 cooperative-shape payload + sensitive-tier (C3/C4/C5) exclusion re-proven by `tests/tasks/test_tasks_maproulette.py` (11 passed); `sig-tasks osm-feed pull` over the two committed fixtures (`osm_changesets_okc_page1/2.xml`) → 4 hashtag-bearing changesets attributed to `LeverageLedger`, 3 §7 accepted, idempotent on re-run (0 new), NO OSM display names stored (`tests/tasks/test_tasks_osm_feed.py` 9 passed incl. `test_no_osm_user_names_are_stored_anywhere`); no `SIG_MAPROULETTE_API_KEY`/`SIG_OSM_OE_PAGE` token literal anywhere. **The real challenge push/pull is STILL blocked on HG-08** (`provided: no`, P23.4/ACCT.1) — NOT DONE, no MapRoulette push or OSM edit fabricated. | OPEN (cites BL-039) — re-confirmed 2026-09-10; dry-run/fixture paths re-verified, real push/pull still gate-pending HG-08 |
| D-P21.7-2 | P | HG-10 — run the moderated usability study with ≥5 ontology-naïve participants | P21.7 shipped the runnable protocol + opt-in aggregate-only instrumentation; the study was not run | operator schedules ≥5 participants and runs the protocol; results replace "not yet run" | **proxy-now re-verified 2026-09-10 by the CONTRIB.1 re-run (prepare-only):** the runnable protocol (`docs/governance/contributor-onboarding-usability-study.md`) + `docs/build/USABILITY_STUDY.md` still carry the explicit "not yet run — gate pending HG-10" results line; the opt-in **aggregate-only** L0 timing (count + median, no per-user rows, no personal data — Part VIII) re-proven by `tests/tasks/test_tasks_onboarding_timing.py` + `tests/api/test_curation_onboarding_timing.py` (all passed). No participants available; **study genuinely not run — no results fabricated.** | OPEN (cites BL-040) — re-confirmed 2026-09-10; protocol + aggregate-only instrumentation re-verified, results still gate-pending HG-10 |
| D-P21.8-1 | V | HG-03/HG-04 — live fetch of the data-driven / coarse-international sources (eff_data_driven, aspi, carnegie, facial_recognition_world_map) | P21.8 shipped the `data_driven` connector + coarse path over fixtures + 4 rights packets; no source green | operator flips the sources (per-source HG-03/04) and re-runs P21.8 with green sources | **proxy-now re-verified 2026-09-10 by the SOURCES.1 (GL-SOURCES-01) re-run (`devin/p21-8-sources-rerun`, base `devin/p21-7-contrib-rerun`, PREPARE-ONLY)** on the current tip: `data_driven` connector + coarse-international path re-verified over committed fixtures (`tests/connectors/test_data_driven.py` 23 + `test_coarse_international.py` 22 passed) — two release snapshots v1/v2 yield versioned claims (v1 retained + v2 added, 0 overwrites), aggregate-only schema test rejects a per-search-column fixture (RISK-P0-08/P21-15), agency crosswalk resolves ≥1 agency; **shadow diff = 0** (`test_shadow_over_fixture_has_zero_diffs` + `test_shadow_replay_over_coarse_fixture_has_zero_diffs`); `run --mode live` for `eff_data_driven`/`carnegie_ai_gsi`/coarse trio **REFUSES exit 3** (ingestion_permitted=false/UNDETERMINED, refused before any fetch, no socket). The **live fetch is STILL gate-pending on per-source HG-03/HG-04 flips + HG-09 tokens + network egress** — NONE available in the isolated context; no flip performed, no live green fabricated. NEWS sources stay LINK-only. `SIG_REQUIRE_DB_TESTS=1 make check` 2767 passed/2 skipped/0 failed; no token literal. | OPEN (cites BL-042) — re-confirmed 2026-09-10; connector code + coarse path re-verified (shadow diff 0, exit 3), live fetch still gate-pending HG-03/HG-04 + HG-09 + network |
| D-P21.9-1 | V | HG-03/HG-04 — live fetch of the Stage-5 pathway sources (rtcc_federation / fr_css_forensics / acoustic_drone_location) | P21.9 shipped the `pathways` connector + 3 extractors + parser layers over fixtures + 3 rights packets; sources LINK-posture, UNDETERMINED | operator flips the sources and re-runs P21.9 with green sources | **proxy-now re-verified 2026-09-10 by the SOURCES.1 (GL-SOURCES-01) re-run (`devin/p21-8-sources-rerun`, base `devin/p21-7-contrib-rerun`, PREPARE-ONLY)** on the current tip: the one `pathways` connector + its 3 per-pathway extractors + the ADR-033-deferred parser layers re-verified over committed fixtures (`tests/connectors/test_pathways.py` 17 + `test_pathway_coverage.py` 4 + `tests/parsing` green) — every P17 pathway (federation/RTCC, FR/CSS/forensics, acoustic/drone/location) has ≥1 connector-produced claim, a `deployed` claim from a procurement-genre fixture is refused (procured≠deployed, RISK-P21-16, Part VIII binding); **shadow diff = 0** for all three families (`test_shadow_replay_over_fixture_has_zero_diffs[federation|forensics|acoustic]`); `run --mode live` for `pathways_rtcc_federation`/`pathways_fr_css_forensics`/`pathways_acoustic_drone_location` **REFUSES exit 3** (ingestion_permitted=false/LINK-only/UNDETERMINED, refused before any fetch, no socket). The **live fetch is STILL gate-pending on HG-03/HG-04 flips + HG-09 tokens + network egress** — the D-P21.9-1 sources stay LINK-posture/UNDETERMINED, NOT flipped; FR/CSS/forensics stay design-gated; no live green fabricated. `SIG_REQUIRE_DB_TESTS=1 make check` 2767 passed/2 skipped/0 failed; no token literal. | OPEN (cites BL-043) — re-confirmed 2026-09-10; pathways connector + extractors + parser layers re-verified (shadow diff 0, exit 3), live fetch still gate-pending HG-03/HG-04 + HG-09 + network |

## Go-live round (Round 3/4) reconciliation — GL-* cross-reference (2026-09-09)

Seeded by `decompose-spec mode=extend` over `docs/3_sig_golive_spec.md`. The existing
`D-P21.*` rows above are the go-live return-pass obligations; this maps each to its spec `GL-*`
id and the manifest chain row that re-runs it (Lane B — no new contract). Append-only: existing
rows are **not** rewritten. (First column is the GL id so these are not parsed as `D-` rows.)

| GL id | existing DEFERRALS row(s) | manifest row (re-run) | gate(s) | pre-answered? |
|---|---|---|---|---|
| GL-RIGHTS-01 | D-P21.1-1, D-P21.1-2 | 71 (`P21.1`) | HG-03, HG-04 | GL-GATE-03 (flip OKC subset first, phased) |
| GL-LIVE-01 (fetch) | D-P21.3-1, D-P21.3-2 | 73 (`P21.3`) | HG-03, HG-09 | flips via GL-GATE-03; HG-09 tokens = human (ACCT.1) |
| GL-LIVE-02 | D-P21.4-1, D-P21.4-2, D-P21.4-3 | 74 (`P21.4`) | HG-01, HG-11, HG-02, Go-public | HG-01 interim (GL-GATE-01), HG-02 interim (GL-GATE-02); Go-public human (GL-GATE-05) |
| GL-INFRA-01 | D-P21.5-1 | 76 (`P21.5`) | HG-07, HG-12, HG-02 | HG-12 host = GL-GATE-04; HG-07 creds = human (ACCT.1) |
| GL-CONTRIB-01 | D-P21.7-1, D-P21.7-2 | 77 (`P21.7`) | HG-08, HG-10 | none — creds + participants are human (ACCT.1) |
| GL-SOURCES-01 | D-P21.8-1, D-P21.9-1 | 78 (`P21.8`/`P21.9`) | HG-03/HG-04 per source | GL-GATE-03 (remainder after the OKC subset; news LINK-only) |

New OPEN rows for go-live gates not yet represented above (HG-02, HG-12). HG-01/HG-07/HG-08/
HG-09/HG-10/HG-11/Go-public are already covered by the `D-P21.*` rows (not duplicated):

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-LEGAL.1-1 | P | HG-02 — counsel opinions (ODbL 4.4(b) RISK-P0-01, officer-naming gate, publication tiers + sensitive-coordinate rules, Part VIII of the published surface) | no counsel engaged; GL-GATE-02 records an interim **engineering disposition** (publish-permitting, "not a legal opinion; counsel review recommended before real public exposure"), resting on the built structural safeguards | operator/counsel records dated opinions in a governance doc + GATE DECISIONS (opinion summary); LEGAL.1 (HUMAN-H2) signed | GL-GATE-02 interim engineering disposition in GATE DECISIONS; every artifact labelled "pending counsel" | OPEN |
| D-ACCT.1-1 | P | HG-12 — a real (zero/low-cost) host + object store + PG target beyond local, and the GCP `apply` | LIVE.2/INFRA.1/DEPLOY.1 need a real host; GL-GATE-04 pre-answers the **host choice** (GCP `$SIG_GCP_PROJECT`) but the accounts + the `gcloud apply` are operator work; DEPLOY.1 lands IaC in prepare mode | operator provisions GCP + Secret Manager, exports `gcloud` ADC in the run shell; DEPLOY.1 re-run with ADC applies the validated IaC | IaC written+validated (`terraform validate`/dry-run) by DEPLOY.1; `apply` gate-pending; no token literal in any file | OPEN |
| D-DEPLOY.1-1 | P | HG-12 — the real GCP `apply`/deploy of the validated IaC + the real cloud restore (GCS / Cloud SQL) under operator Application Default Credentials | P24.1 (DEPLOY.1 / GL-DEPLOY-01) wrote + validated the `ops/gcp/` infra-as-code (idempotent `gcloud` scripts, ADR-075) and the `sig-ops deploy --target gcp` path, but there are **no** `gcloud` ADC and **no** GCP account in this isolated context and creds are `provided: no` (P23.4 / ACCT.1). No `gcloud apply` is run by an isolated subagent; no deployed URL fabricated. Cross-ref `D-ACCT.1-1` (HG-12 host/accounts). | operator exports ADC (`gcloud auth application-default login`) + `SIG_GCP_PROJECT` in the run shell, then re-runs `bash ops/gcp/provision.sh --apply`, `sig-ops deploy --target gcp` (no `--dry-run`), and the cloud restore (`bash ops/gcp/backup.sh --apply` → `gcloud sql backups restore` or a `pg_restore` from `gs://…-sig-backups`) — expect the OKC dossier serving from the GCS/Cloud-Run URL and the restored graph reproducing claim/evidence counts | **validated-IaC proxy is green** — `bash ops/gcp/provision.sh --check` + `bash ops/gcp/backup.sh --check` print the full plan, use NO ADC, open NO network, exit 0; `sig-ops deploy --target gcp --dry-run` prints the plan + exits 0 (`tests/ops/test_gcp_iac.py`, `tests/ops/test_deploy.py`); the **local restore drill runs for real over Docker** — `pg_dump` → restore into a fresh DB → claim/evidence/entity counts reproduce (`tests/db/test_restore_drill.py`, `sig-ops backup-drill`); no secret/token literal, project id is `$SIG_GCP_PROJECT` (`tests/connectors/test_secrets.py`) | OPEN — opened 2026-09-10 by P24.1 (DEPLOY.1 / GL-DEPLOY-01); real apply/deploy + cloud restore gate-pending on operator ADC (HG-12), cross-ref D-ACCT.1-1 |
| D-LIVE.1a-1 | V | HG-09 — the real LIVE smoke of the three now-green OKC document sources (`okc_procurement`/`okcpd_policy`/`ok_statute`): fetch the cited public PDFs/HTML → capture to OCFL → parse → emit the expected claim shape into PG with a fetch record; idempotent re-run adds 0 new claims | P23.5 (LIVE.1a) implemented the connector code and proved it via shadow replay (diff = 0), but the live fetch needs `SIG_*` API tokens which are **not provided** (`provided: no`, P23.4 / ACCT.1); secrets are env-only and none are available in this isolated context | operator exports the required `SIG_*` token(s) in the run shell and runs `sig-connectors run --source okc_procurement --mode live` (then `okcpd_policy`, `ok_statute`) — expect a fetch record written under the OCFL capture dir, the fixture-shaped claims asserted, and a second run adding 0 new claims (idempotent); the ledger records only `provided: yes/no` | **shadow-replay proxy is green** — `tests/connectors/test_okc_documents.py::test_shadow_replay_over_fixture_has_zero_diffs` (all 3) + `test_run_source_shadow_and_replay` prove fetch→capture→parse→emit is deterministic and byte-identical over the committed fixtures; `run --mode live` for these green sources is wired (P21.3 transport) and would fetch once tokens are present; no live green fabricated | OPEN — cross-ref updated 2026-09-10 by the P21.3 LIVE.1 fetch re-run: HG-03 CLEARED (`review-status` all-green + `loadable now: True` for all three; `run --mode live` no longer refuses them on gate grounds), live smoke still blocked on HG-09 tokens + network egress; shadow-replay diff=0 proxy re-verified. Stays OPEN. |
| D-OBS.1-1 | V | OBS.1 live-side verification — a real webhook delivery (a configured `SIG_ALERT_WEBHOOK_URL`) and the first real probe/uptime data against the hosted stack | P24.3 ran offline-only: the notifier seam is proven by a stubbed-transport test + the always-on recorded ledger, and `compute_uptime` is proven over synthetic rows; no live endpoint exists yet (HG-12) and no webhook URL is configured (env-only, HG-09) | operator sets `SIG_STAGING_*` (+ optional `SIG_ALERT_WEBHOOK_URL`) once a host exists (D-ACCT.1-1) — then `sig-ops probe --alert` against the real stack and one real webhook delivery of `sig-ops alert`; the daily `observability.yml` sweep picks both up automatically | stubbed `urlopen` POST test asserts the scrubbed body + Bearer header; the recorded-alert ledger fires with no notifier configured; `no-data` (never a fabricated 100%) until real probes land | OPEN |
