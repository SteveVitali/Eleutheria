<!--
  docs/tickets/DEFERRALS.md (BM-DEFER-01). Seeded by P22.3 (build-memory v2 migration) from the
  docs/build/LEDGER.md RETURN PASS table + returnPass: list. Append-only companion — NOT a chain
  ticket, never overwritten or deleted. The four rules below are stated verbatim.
-->
# Deferred obligations ledger

> **Maintained companion — NOT a generated ticket.** Committed and hand-maintained; preserved
> across any ticket regeneration; never overwritten or deleted. It has no `NN_` sequence prefix
> so it is not mistaken for a chain ticket.

The rules:
1. **Every run, first:** read this file. If the ticket you are about to implement — or a
   prerequisite it depends on — unblocks any `OPEN` row, **closing that row is part of your
   run**: verify it for real, then flip it to `DONE` with the date and evidence.
2. **Never delete a row.** Flip `OPEN` → `DONE` (verified) or `WONTFIX` (with a reason).
   History stays.
3. **When you defer something new,** append a row here in the same run that defers it. A
   deferral that is not in this file did not happen.
4. **Gates refuse to pass** while any `OPEN` row scoped to that phase remains. Treat an open
   row as gate-blocking.

Status values: `OPEN` (owed) · `PARTIAL` · `DONE` (verified — add date + evidence) ·
`WONTFIX` (add reason) · `ACCEPTED-SKELETON`. `kind`: V (verification) | F (functionality) |
D (deviation) | H (handoff seam) | P (human prerequisite) | X (other).

Pre-existing normalized debt is tracked in `docs/build/BACKLOG.csv`; each row below cites its
`BL-` id where one exists (no duplication). These rows are the **RETURN PASS** obligations from
`docs/build/LEDGER.md` — Phase-21 tickets the operator ran with a human gate skipped. They are
**not** blocks (the chain completed and the round-1 capstone is DONE); they are owed operator
work re-run per each ticket's `Run:` line after the operator acts.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-P21.1-1 | P | HG-03 — flip the reviewed OKC/critical-path sources to `ingestion_permitted=true` | P21.1 shipped the 27 rights packets, review-metadata rule + `review-status` CLI, but flipping a source is a human/rights decision (gate HG-03), not a code change | operator reads the packets in `docs/build/reports/rights/`, picks source ids to flip (each needs reviewer role + date) | `sig-connectors review-status` shows flip-ready sources; `loadable now: 0` until flipped | DONE 2026-09-10 — RIGHTS.1 flip re-run (`devin/p21-1-rights-flip-rerun`) executed operator disposition GL-GATE-03 (GATE DECISIONS 2026-09-09): flipped the OKC critical subset to `ingestion_permitted=true`, reviewer=`maintainer (delegated)`, date 2026-09-10. 6 ids + SPDX basis: `okc_procurement`/`okc_council`/`okcpd_policy`/`ok_statute` = **CC0-1.0** (US/OK government records + statutory text are uncopyrightable/public-domain per their packets; registry-accepted public-domain expression, `policy/data/licenses.toml`); `osm_overpass` = **ODbL-1.0** (redistributable + derivative_permitted per packet, kept in the separate ODbL compartment); `deflock_repo` = **MIT** (redistributable + derivative_permitted per packet). Evidence: `sig-connectors validate` → `ingestion_permitted=true: 6`, `loadable now: 6`, self-checks OK; `review-status` → `flip-ready: 16`, `loadable now: 6`; `review-status --source <id>` → all five gate fields True for each of the 6; `make check` 2720 passed/2 skipped/0 failed. News sources `journalrecord`/`oklahoman` stayed LINK-posture (UNDETERMINED); FR/BE untouched (design-gated false). (cites BL-032) |
| D-P21.1-2 | P | HG-04 — record actual Stage-0 outreach outcomes to the 19 federation-compact projects | P21.1 shipped the outreach record template + letter; no outreach was performed (no outcomes to record) | operator performs/records outreach; rows move off `not_contacted`/`public_terms_only` | `docs/build/reports/STAGE0_OUTREACH_RECORD.md` all rows `not_contacted` | WONTFIX 2026-09-16 — SKIPPED-BY-OPERATOR: operator elected to skip Stage-0 outreach entirely (optional, not a read-surface or correctness prerequisite); honest `not_contacted`/`public_terms_only` postures stay published. Earlier annotation:** outreach is not a read-surface prerequisite; `not_contacted` stays the honest published posture. **P25.9 sweep 2026-09-16 — sole blocker: operator action (HG-04) — perform and record the Stage-0 outreach; nothing engineering-side remains.** |
| D-P21.3-1 | V | HG-03 — first real LIVE fetch of ≥1 green source (document-connector modules okc_procurement/okcpd_policy/ok_statute) | P21.3 landed `HttpxTransport`/`OcflCaptureStore`/`sig-connectors run --mode live` (refuses exit 3 for non-green sources); no source is green yet | after D-P21.1-1 flips ≥1 source, re-run `implement-spec spec=docs/tickets/P21.3__live-connector-wiring.md live_verification=true` | stub + fixture-replay tested; `run --mode live` refuses exit 3, no socket opened | DONE 2026-09-16 (P25.8) — history: **connector-code half DONE by P23.5 (LIVE.1a / GL-LIVE-01):** the three document-connector modules `okc_procurement`/`okcpd_policy`/`ok_statute` now exist (`connectors/src/connectors/okc_documents.py` + the three modules), fetch→OCFL→parse→emit implemented and proven by **shadow replay diff = 0** against committed fixtures (`tests/connectors/test_okc_documents.py`); sources are green (RIGHTS.1). The **live-fetch / transport re-run half stays with P21.3** (manifest row 73) and is still owed — blocked only on HG-09 tokens (see `D-LIVE.1a-1`). (cites BL-023; BL-023 connector-code closed) **PARTIAL advanced 2026-09-10 by the P21.3 LIVE.1 fetch re-run (`devin/p21-3-live-wiring-rerun`, prepare-only):** HG-03 now **CLEARED** — `review-status --source <id>` shows all five gate fields True + `loadable now: True` for `okc_procurement`/`okcpd_policy`/`ok_statute`/`osm_overpass`/`deflock_repo`/`okc_council`, and `run --mode live` NO LONGER refuses them on gate grounds (`live_gate_reasons(<id>)` = empty); the still-un-flipped `usaspending` still refuses exit 3 with reasons + no socket. The real fetch is **still pending HG-09 tokens + network egress** (this isolated context has neither) — **NOT DONE, no real fetch happened; no live green fabricated.** Deterministic ACs re-confirmed (gate-refusal exit 3, shadow diff 0 for osm/atlas + the 3 doc connectors, no-token-literal, isolation). Evidence: `docs/build/runs/P21.3.md` (2026-09-10 re-run) + `docs/build/reports/LIVE_WIRING_REPORT.md` (2026-09-10 dated section). **2026-09-15 (ADR-082, hosted deploy):** the live-target plumbing now landed — `data/live_targets.toml` + `_run_live` wiring + tests (the runner no longer fetches a placeholder). A real `osm_overpass --mode live` against the hosted spine was attempted and **refused by SIG's own conduct layer** (`overpass-api.de` robots.txt disallows `/api/`, SIG-INGEST-037) — so the live fetch is now blocked on an **endpoint/robots policy decision**, and the OKC document connectors need **content-drift hardening** (parsers built for transcribed fixtures ≠ live bytes). Both are scoped in **P25.1 / BL-055**. **2026-09-15 — OSM half DONE:** with the ADR-083 carve-out implemented (`api_allowlist.toml` + `PoliteFetcher` wiring) + the UA/endpoint fixes (lz4 mirror; UA no longer contains "crawler"), a real `osm_overpass --mode live --sink pg` fetch landed **4465 claims / 656 entities** (OKC `man_made=surveillance`, ODbL compartment) in the hosted Cloud SQL spine — the first genuine live-fetched data. The OKC **document** connectors' live half (real PDF/HTML parser + content-drift) remains → P25.1/P25.3. **DONE 2026-09-16 (P25.8, via D-LIVE.1a-1):** the document-connector live half is now real — `okcpd_policy` live run fetched the OKCPD Operations Manual PDF through the shared politeness layer into OCFL and loaded 3 reviewed clause claims into the hosted spine (verbatim literals, page locators); `okc_procurement`/`ok_statute` produced recorded refusals (WAF `link_rotted`, robots `politeness_refusal`) — every source exercised, no claim asserted beyond a capture. |
| D-P21.3-2 | P | HG-09 — API tokens (`SIG_MUCKROCK_TOKEN`/`SIG_DATA_GOV_KEY`/`SIG_OVERPASS_ENDPOINT`/`SIG_CIVICCLERK_BASE`) exported in the worker shell | live fetch needs credentials; `provided: no` at the P21.3 gate (secrets never in a file) | operator exports the env vars in the re-run shell; ledger records only `provided: yes/no` | no live fetch; no-token-literal test passes | OPEN (cites BL-024) — re-confirmed 2026-09-10 by the P21.3 LIVE.1 fetch re-run: HG-03 now cleared but tokens `provided: no` + no network egress in the isolated context → the live fetch still cannot run; stays OPEN. **P25.9 sweep 2026-09-16 — sole blocker: operator exports the `SIG_*` credentials in the run shell (HG-09); secrets exist in Secret Manager (`sig-muckrock-refresh`, `sig-data-gov-key`) but a re-run shell needs them exported — human prerequisite, nothing engineering-side remains.** |
| D-P21.4-1 | P | HG-01 — name the legal home | P21.4 ended at local staging; public publish is gated on a named legal home | operator names the legal home; `PUBLICATION_CHECKLIST.md` HG-01 ticks | **re-verified 2026-09-10 by the LIVE.2 re-run (`devin/p21-4-live2-rerun`):** local composed staging re-run for real (Docker) — `run_okc.sh` complete, `PUBLICATION_CHECKLIST.md` item 1 = `gate pending: HG-01`. HG-01 stands at the **INTERIM** posture only (GL-GATE-01 / P23.2: maintainer-stewardship, **NOT counsel, NOT a real named home**); a real legal home is still required before Go-public. | DONE 2026-09-15 — operator named the legal home: **Steven Vitali** (individual maintainer, personal capacity) in `docs/governance/governance-and-code-of-conduct.md § Legal home` (SIG-GOV-012); `PUBLICATION_CHECKLIST.md` item 1 ticked; recorded in LEDGER § GATE DECISIONS (HG-01, 2026-09-15) + HUMAN-H1 readout. Interim personal-name home (honest scope note in the doc: personal exposure; a more durable entity/fiscal-sponsor + HG-02 counsel recommended before real public exposure). Does not unblock Go-public — HG-11 (`D-P21.4-2`) + counsel (`D-LEGAL.1-1`) remain OPEN by operator choice. (cites BL-034) |
| D-P21.4-2 | P | HG-11 — establish two reviewer roles + written concurrence + live takedown contact | governance prerequisites for public publish were not established | operator establishes the roles/concurrence/contact; `concurrence.md` filled | **re-verified 2026-09-10 by the LIVE.2 re-run:** the corrections/dispute mechanism is built + served (re-verified in the export-mode e2e); `docs/build/reports/okc/concurrence.md` is still a template only — no two named reviewer roles + written concurrence + live takedown contact (P23.2/HUMAN-H1 recorded HG-11 as OWED). | DONE 2026-09-16 — **operator-resolved under sole-maintainer posture:** the operator filled BOTH reviewer roles personally (Steven Vitali — maintainer + operations) and recorded written concurrence in `docs/build/reports/okc/concurrence.md`; the SIG-PUB-008 *independence* requirement is **waived by explicit operator decision**, not satisfied — the file says so, and `policy.officer._concurrence_ok` still correctly returns False for person-named claims (officer-naming stays default-no-publish). History: (Steven Vitali, maintainer) + the **takedown/corrections contact of record** (live human behind the served `/corrections`+`/dispute`), in `docs/governance/governance-and-code-of-conduct.md § Reviewer roles`; `PUBLICATION_CHECKLIST` item 3 ticked (interim), item 2 PARTIAL. **Still owed:** a **second independent reviewer** + written concurrence (`ReviewerConcurrence`, SIG-PUB-008) — Go-public stays blocked until then. (cites BL-036) **2026-09-16 — SKIPPED-BY-OPERATOR for go-public:** the operator elected to publish under a **sole-maintainer posture** — no second independent reviewer is being recruited pre-launch and no written concurrence exists; `docs/build/reports/okc/concurrence.md` stays a template. Recorded in `PUBLICATION_CHECKLIST.md` (2026-09-16 go-public section, item 2 = WAIVED-BY-OPERATOR, not ticked) + LEDGER § GATE DECISIONS. The published surface makes no two-reviewer claim (reviewer state derives from recorded review data). Reopening condition: operator recruits a second reviewer post-launch. **P25.9 sweep 2026-09-16 — sole blocker: operator decision (HG-11) — recruit a second independent reviewer + record written concurrence (`concurrence.md`); the sole-maintainer posture is the recorded operator choice, not an engineering gap.** |
| D-P21.4-3 | P | Go-public — DNS/host cut-over to publish the first jurisdiction | impossible while HG-01 + HG-11 are open; P21.4 ends at staging | after D-P21.4-1/2, operator decides go-public and re-runs `implement-spec spec=docs/tickets/P21.4__first-jurisdiction-ingest-and-publish.md live_verification=true` | **re-verified 2026-09-10 by the LIVE.2 re-run:** local staging complete + re-verified (`SIG_STAGING_*` local; OSM-in-exports/HG-02 interim-ticked), but Go-public is **reserved to the human** (GL-GATE-05 / GATE-G2 / P23.6, row 75) and impossible while HG-01 (real home) + HG-11 remain open. No public cut-over, no `0.2.0` bump done. | OPEN (no dedicated BL; depends on BL-034/BL-036) — re-confirmed 2026-09-10, Go-public genuinely gated → **DONE 2026-09-16 — operator decided GO:** go-public executed with HG-11 skipped-by-operator (D-P21.4-2) and the remaining HG-02 counsel scope deferred (D-LEGAL.1-1); GCP cut-over (sig-web/sig-public public-read + `sig-api` unauthenticated) verified anonymously — evidence appended to `PUBLICATION_CHECKLIST.md` (2026-09-16 section). No DNS cut-over (no custom domain — run.app + bucket endpoints are the public surface). **DNS/custom-domain SKIPPED-BY-OPERATOR 2026-09-16** — operator elected to keep the run.app + bucket endpoints; a domain may be added later without reopening anything. |
| D-P21.5-1 | V | HG-07 — real Zenodo (sandbox) deposit + object-store push + egress report + SWH save | P21.5 ran dry-run/FakeZenodo/MockTransport; no credentials (`provided: no`), zero-cost posture | operator provides `SIG_ZENODO_SANDBOX_TOKEN`/`SIG_OBJECT_STORE_*` and re-runs `implement-spec spec=docs/tickets/P21.5__infra-deposit-and-tiles.md live_verification=true` | free paths (tiles/.torrent/degraded/SWH-save) real; credentialed steps dry-run/stub tested. **Proxy-now re-verified 2026-09-10 by the INFRA.1 (GL-INFRA-01) re-run (`devin/p21-5-infra-rerun`, base `devin/p23-6-go-public-marker`, prepare-only):** over a freshly-built `okc` export on the current tip — `sig-exports tiles` → valid PMTiles v3, ≥1 layer, `sig:license=ODbL-1.0` + attribution (pure-Python renderer; tippecanoe absent); `sig-exports torrent` → real pure-Python bencode (`infohash=8d1d2b60…`); `sig-ops degraded --data-source fixtures` → `web/dist` with NO API ($0 host); `keepalive.yml` valid + runs the same `sig-ops degraded` command; `sig-exports deposit --dry-run` → exit 0 (FakeZenodo dry-run DOI recorded); `sig-exports deposit --sandbox` w/o token → **exit 4** `gate pending: HG-07`, wrote nothing; `sig-ops egress-report` → `gate-pending` (no usage API) yet computes `level=ok`/`ratio=0.05` for a simulated `--usage-gb 5`; `sig-ops swh-save` (no `--now`) → request built for real, **untriggered** (no network). `tests/exports/{tiles,torrent,zenodo,push}` + `tests/ops/{degraded,egress,swh}` = 43 passed; `SIG_REQUIRE_DB_TESTS=1 make check` 2767 passed/2 skipped/0 failed; no token literal. **The credentialed half — real sandbox `10.5072/…` deposit + object-store push + live egress + SWH save — is STILL blocked on HG-07 (`provided: no`, P23.4/ACCT.1) + network egress; NOT DONE, no credentialed green fabricated.** | PARTIAL 2026-09-16 — **SWH save-now DECLINED-BY-OPERATOR:** the operator elected to keep the GitHub repo private, so SWH save-now (public origin required) is dispositioned off — the succession plan retains the Zenodo deposit + GCS object-store legs, and the SWH leg reopens if repo visibility ever flips. Earlier: the P24.1 live deploy built the okc + france exports (`sig-exports build --jurisdiction …`, compartments `sig_graph` CC-BY / `osm_physical` ODbL / `web`) and pushed them to the real object store `gs://…-sig-public/{okc,france}/` (private under Finish-line A); the static site (50 pages) synced to `gs://…-sig-web`; `pg_dump`/export SQL lands in the private `…-sig-backups` bucket (see the restore drill, D-DEPLOY.1-1). **Zenodo deposit DONE 2026-09-15:** with a scoped sandbox personal token (`deposit:write`+`deposit:actions`, Secret Manager `sig-zenodo-sandbox-token`), a real `sig-exports deposit --in exports/out/okc --sandbox` published a sandbox deposition — **version DOI `10.5072/zenodo.603732`**. Fixed a real API bug en route: Zenodo's bucket file API 404s on a key containing `/` (the compartment path), so the deposited object key flattens `/`→`__` (`exports/zenodo.py`). **2026-09-16 (P25.8): live egress report DONE** — real Cloud Monitoring series over the deployed footprint: sig-api ≈0.009 GB + sig-web ≈0.0007 GB + buckets ≈0.0004 GB ≈ **0.01 GB observed vs the 100 GB/month budget → level `ok`** (ratio 0.0001). **SWH save-now BLOCKED**: `gh repo view` → the GitHub repo is **private**; SWH save-now needs a public origin, so it is blocked on an operator repo-visibility decision, not a credential. Still owed: SWH once the repo is public + object-store push to a non-GCS store if desired. (cites BL-029) **P25.9 sweep 2026-09-16 — sole blocker: operator decision on GitHub repo visibility (SWH save-now needs a public origin; repo is private); the Zenodo deposit, GCS object-store push, and live egress report legs are DONE.** |
| D-P21.7-1 | V | HG-08 — MapRoulette API key + registered OSM Organised-Editing page → real challenge push/pull | P21.7 ran dry-run/stub; the registration gate refuses push (exit 3) while `registered=false`; no creds | operator creates the account, registers the OE page, sets `SIG_MAPROULETTE_API_KEY`/`SIG_OSM_OE_PAGE`/`registered=true`, re-runs P21.7 live | **proxy-now re-verified 2026-09-10 by the CONTRIB.1 (GL-CONTRIB-01) re-run (`devin/p21-7-contrib-rerun`, base `devin/p21-5-infra-rerun`, prepare-only)** on the current tip: `sig-tasks maproulette push --challenge 1 --jurisdiction okc --dry-run` → **REFUSED exit 3** with the OE-registration reason (`ops/config.toml [tasks.contribution] registered=false`); the dry-run challenge JSON build + MapRoulette v2 cooperative-shape payload + sensitive-tier (C3/C4/C5) exclusion re-proven by `tests/tasks/test_tasks_maproulette.py` (11 passed); `sig-tasks osm-feed pull` over the two committed fixtures (`osm_changesets_okc_page1/2.xml`) → 4 hashtag-bearing changesets attributed to `LeverageLedger`, 3 §7 accepted, idempotent on re-run (0 new), NO OSM display names stored (`tests/tasks/test_tasks_osm_feed.py` 9 passed incl. `test_no_osm_user_names_are_stored_anywhere`); no `SIG_MAPROULETTE_API_KEY`/`SIG_OSM_OE_PAGE` token literal anywhere. **The real challenge push/pull is STILL blocked on HG-08** (`provided: no`, P23.4/ACCT.1) — NOT DONE, no MapRoulette push or OSM edit fabricated. | OPEN (cites BL-039) — re-confirmed 2026-09-10; dry-run/fixture paths re-verified, real push/pull still gate-pending HG-08 — **DEFERRED past go-public 2026-09-16 (operator decision):** contribution-back is not a read-surface prerequisite; `registered=false` retained. **P25.9 sweep 2026-09-16 — sole blocker: operator action (HG-08) — create the MapRoulette account, register the OSM Organised-Editing page, export `SIG_MAPROULETTE_API_KEY` + set `registered=true`.** |
| D-P21.7-2 | P | HG-10 — run the moderated usability study with ≥5 ontology-naïve participants | P21.7 shipped the runnable protocol + opt-in aggregate-only instrumentation; the study was not run | operator schedules ≥5 participants and runs the protocol; results replace "not yet run" | **proxy-now re-verified 2026-09-10 by the CONTRIB.1 re-run (prepare-only):** the runnable protocol (`docs/governance/contributor-onboarding-usability-study.md`) + `docs/build/USABILITY_STUDY.md` still carry the explicit "not yet run — gate pending HG-10" results line; the opt-in **aggregate-only** L0 timing (count + median, no per-user rows, no personal data — Part VIII) re-proven by `tests/tasks/test_tasks_onboarding_timing.py` + `tests/api/test_curation_onboarding_timing.py` (all passed). No participants available; **study genuinely not run — no results fabricated.** | WONTFIX 2026-09-16 — SKIPPED-BY-OPERATOR: operator elected to skip the moderated usability study; the runnable protocol and aggregate-only instrumentation remain in the repo and the results line stays honestly "not yet run". |
| D-P21.8-1 | V | HG-03/HG-04 — live fetch of the data-driven / coarse-international sources (eff_data_driven, aspi, carnegie, facial_recognition_world_map) | P21.8 shipped the `data_driven` connector + coarse path over fixtures + 4 rights packets; no source green | operator flips the sources (per-source HG-03/04) and re-runs P21.8 with green sources | **proxy-now re-verified 2026-09-10 by the SOURCES.1 (GL-SOURCES-01) re-run (`devin/p21-8-sources-rerun`, base `devin/p21-7-contrib-rerun`, PREPARE-ONLY)** on the current tip: `data_driven` connector + coarse-international path re-verified over committed fixtures (`tests/connectors/test_data_driven.py` 23 + `test_coarse_international.py` 22 passed) — two release snapshots v1/v2 yield versioned claims (v1 retained + v2 added, 0 overwrites), aggregate-only schema test rejects a per-search-column fixture (RISK-P0-08/P21-15), agency crosswalk resolves ≥1 agency; **shadow diff = 0** (`test_shadow_over_fixture_has_zero_diffs` + `test_shadow_replay_over_coarse_fixture_has_zero_diffs`); `run --mode live` for `eff_data_driven`/`carnegie_ai_gsi`/coarse trio **REFUSES exit 3** (ingestion_permitted=false/UNDETERMINED, refused before any fetch, no socket). The **live fetch is STILL gate-pending on per-source HG-03/HG-04 flips + HG-09 tokens + network egress** — NONE available in the isolated context; no flip performed, no live green fabricated. NEWS sources stay LINK-only. `SIG_REQUIRE_DB_TESTS=1 make check` 2767 passed/2 skipped/0 failed; no token literal. | DONE 2026-09-16 (cites BL-042) — re-confirmed 2026-09-10; connector code + coarse path re-verified (shadow diff 0, exit 3), live fetch still gate-pending HG-03/HG-04 + HG-09 + network **2026-09-15 — PARTIAL advanced:** all four sources flipped (eff_data_driven CC-BY-4.0; coarse trio counsel/HG-02); eff_data_driven LIVE (real release zip → aggregate claims committed); carnegie + FRWM pages captured live as evidence_artifacts; aspi WAF-403 recorded disappearance. Still owed: the curated coarse-dataset adapters (P25.4 d2) + aspi egress (P25.7). **2026-09-16 — PARTIAL advanced (P25.4 d2, `devin/p25-4-coarse-adapters`):** the curated coarse adapters landed — `carnegie_ai_gsi` parses the page's embedded JS dataset strictly (75 reviewed entries, bounded chunk discovery) → 660 live country/vendor claims; `facial_recognition_world_map` parses the linked Google Sheet's six reviewed gviz sheets → 202 live claims (duplicate-Egypt conflict preserved, never reconciled); every claim carries evidence+locator+observed_at; drift/aggregate-only gates fail closed (`tests/connectors/test_coarse_dataset_adapters.py`, 18 tests, shadow diff 0); aspi still WAF-403 on both local and Cloud-Run egress → recorded `access_restricted` disappearance, no bypass. Still owed: aspi egress vantage / HG-04 outreach (P25.7 d3). **DONE evidence (P25.7):** every source now has field-level claims in the hosted spine or a recorded honest disposition — `carnegie_ai_gsi` 657 claims + `facial_recognition_world_map` 194 claims asserted to `sig-pg` on live re-runs (fetch records `2026-09-16_carnegie_ai_gsi.json` / `2026-09-16_facial_recognition_world_map.json`); `eff_data_driven` 1,015 claims already hosted; `aspi_mapping_chinas_tech_giants` confirmed `access_restricted` on BOTH local and Cloud-Run egress (`aspi-egress-probe` → 403, `2026-09-16_aspi_*.json`) — a recorded disappearance is the disposition, never a WAF defeat. (HG-04 outreach stays tracked at D-P21.1-2.) |

| D-P21.9-1 | V | HG-03/HG-04 — live fetch of the Stage-5 pathway sources (rtcc_federation / fr_css_forensics / acoustic_drone_location) | P21.9 shipped the `pathways` connector + 3 extractors + parser layers over fixtures + 3 rights packets; sources LINK-posture, UNDETERMINED | operator flips the sources and re-runs P21.9 with green sources | **proxy-now re-verified 2026-09-10 by the SOURCES.1 (GL-SOURCES-01) re-run (`devin/p21-8-sources-rerun`, base `devin/p21-7-contrib-rerun`, PREPARE-ONLY)** on the current tip: the one `pathways` connector + its 3 per-pathway extractors + the ADR-033-deferred parser layers re-verified over committed fixtures (`tests/connectors/test_pathways.py` 17 + `test_pathway_coverage.py` 4 + `tests/parsing` green) — every P17 pathway (federation/RTCC, FR/CSS/forensics, acoustic/drone/location) has ≥1 connector-produced claim, a `deployed` claim from a procurement-genre fixture is refused (procured≠deployed, RISK-P21-16, Part VIII binding); **shadow diff = 0** for all three families (`test_shadow_replay_over_fixture_has_zero_diffs[federation|forensics|acoustic]`); `run --mode live` for `pathways_rtcc_federation`/`pathways_fr_css_forensics`/`pathways_acoustic_drone_location` **REFUSES exit 3** (ingestion_permitted=false/LINK-only/UNDETERMINED, refused before any fetch, no socket). The **live fetch is STILL gate-pending on HG-03/HG-04 flips + HG-09 tokens + network egress** — the D-P21.9-1 sources stay LINK-posture/UNDETERMINED, NOT flipped; FR/CSS/forensics stay design-gated; no live green fabricated. `SIG_REQUIRE_DB_TESTS=1 make check` 2767 passed/2 skipped/0 failed; no token literal. | DONE 2026-09-16 (cites BL-043) — re-confirmed 2026-09-10; pathways connector + extractors + parser layers re-verified (shadow diff 0, exit 3), live fetch still gate-pending HG-03/HG-04 + HG-09 + network **2026-09-15 — PARTIAL advanced:** all three pathways sources flipped (ADR-085); one real EFF upstream page per family captured live as evidence_artifacts. Still owed: the document→claim normalization adapter (genre re-derived from captured bytes, per-document rights preserved). **2026-09-16 — advanced (P25.5 live-extraction run):** the document→claim adapter landed — `pathways` reads captured bytes at the P07.1-classified layer (`pdf_text`/`selector_template`), re-derives genre from the bytes (never the configured label), and emits reviewed-literal `technology` claims with byte/page locators only where the document's own text carries them; a `deployment` claim still requires a deployment-report genre (§46 gate unweakened; procurement-document test asserts no `deployed` claim). LIVE: rtcc_federation 4 rows (2 technology claims), acoustic_drone_location 3 rows (1), fr_css_forensics 2 rows — the face-recognition page's reviewed literal is absent from its bytes so it yields an evidence artifact only (no fabricated claim); per-document `license_spdx` rides every artifact. Fetch records: `docs/build/reports/live_runs/2026-09-16_pathways_*.json`; tests: `tests/connectors/test_live_extraction.py` (23, incl. shadow diff=0). **DONE evidence (P25.7, `devin/p25-7-live-ops-hardening`):** hosted-spine field-level claims landed on a live re-run — `pathways_rtcc_federation` 2 claims, `pathways_acoustic_drone_location` 1 claim asserted to the hosted spine (`sig-pg`, us-central1); `pathways_fr_css_forensics` yields an evidence artifact only — the reviewed literal is absent from the page's own bytes, so no claim is fabricated (that IS the field-level outcome, not a gap). Fetch records: `docs/build/reports/live_runs/2026-09-16_pathways_*.json`. |


## Go-live round (Round 3/4) reconciliation — GL-* cross-reference (2026-09-09)

Seeded by `decompose-spec mode=extend` over `docs/3_sig_golive_spec.md`. The existing
`D-P21.*` rows above are the go-live return-pass obligations; this maps each to its spec `GL-*`
id and the manifest chain row that re-runs it (Lane B — no new contract). Append-only: existing
rows are **not** rewritten. (First column is the GL id so these are not parsed as `D-` rows.)

| GL id | existing DEFERRALS row(s) | manifest row (re-run) | gate(s) | pre-answered? |
|---|---|---|---|---|
| GL-RIGHTS-01 | D-P21.1-1, D-P21.1-2 | 71 (`P21.1`) | HG-03, HG-04 | GL-GATE-03 (flip OKC subset first, phased) |
| GL-LIVE-01 (fetch) | D-P21.3-1, D-P21.3-2 | 73 (`P21.3`) | HG-03, HG-09 | flips via GL-GATE-03; HG-09 tokens = human (ACCT.1) |
| GL-LIVE-02 | D-P21.4-1, D-P21.4-2, D-P21.4-3 | 74 (`P21.4`) | HG-01, HG-11, HG-02, Go-public | HG-01 interim (GL-GATE-01), HG-02 interim (GL-GATE-02); Go-public human (GL-GATE-05) |
| GL-INFRA-01 | D-P21.5-1 | 76 (`P21.5`) | HG-07, HG-12, HG-02 | HG-12 host = GL-GATE-04; HG-07 creds = human (ACCT.1) |
| GL-CONTRIB-01 | D-P21.7-1, D-P21.7-2 | 77 (`P21.7`) | HG-08, HG-10 | none — creds + participants are human (ACCT.1) |
| GL-SOURCES-01 | D-P21.8-1, D-P21.9-1 | 78 (`P21.8`/`P21.9`) | HG-03/HG-04 per source | GL-GATE-03 (remainder after the OKC subset; news LINK-only) |

New OPEN rows for go-live gates not yet represented above (HG-02, HG-12). HG-01/HG-07/HG-08/
HG-09/HG-10/HG-11/Go-public are already covered by the `D-P21.*` rows (not duplicated):

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-LEGAL.1-1 | P | HG-02 — counsel opinions (ODbL 4.4(b) RISK-P0-01, officer-naming gate, publication tiers + sensitive-coordinate rules, Part VIII of the published surface) | no counsel engaged; GL-GATE-02 records an interim **engineering disposition** (publish-permitting, "not a legal opinion; counsel review recommended before real public exposure"), resting on the built structural safeguards | operator/counsel records dated opinions in a governance doc + GATE DECISIONS (opinion summary); LEGAL.1 (HUMAN-H2) signed | GL-GATE-02 interim engineering disposition in GATE DECISIONS; every artifact labelled "pending counsel" | DONE 2026-09-16 — **resolved by operator-adopted drafted analyses** (NOT counsel): the operator directed the four scoped questions be answered by drafted analyses in place of engaging counsel; they are recorded in `docs/governance/publication-opinion-drafts.md` (ODbL 4.4(b) produced-works → publish-permitting via compartment separation; officer-naming gate → suffices, resolves to default-no-publish under sole-maintainer; tiers+coords → adequate, RLS-enforced; Part VIII → satisfied by construction). Real counsel review may supersede — the file says so. History: counsel resolved the derived-facts publication question (the eleven `LicenseRef-DerivedFacts-Citations` sources) → dedicated `derived_facts` compartment, ADR-086, GATE DECISIONS row. Still owed: the ODbL 4.4(b) opinion, officer-naming gate, publication tiers + sensitive-coordinate rules, Part VIII surface — go-public counsel scope unchanged. **2026-09-16 — DEFERRED by operator past go-public:** the operator published on the interim engineering dispositions (GL-GATE-02) with the technical safeguards still enforced (tier-0 only, jurisdiction coordinates, officer-naming gate, ODbL compartment separation); the remaining counsel opinions stay owed post-launch. **P25.9 sweep 2026-09-16 — sole blocker: counsel (HG-02) — dated opinions on ODbL 4.4(b) (RISK-P0-01), the officer-naming gate, publication tiers + sensitive-coordinate rules, and the Part VIII surface; no engineering substitute exists.** |
| D-ACCT.1-1 | P | HG-12 — a real (zero/low-cost) host + object store + PG target beyond local, and the GCP `apply` | LIVE.2/INFRA.1/DEPLOY.1 need a real host; GL-GATE-04 pre-answers the **host choice** (GCP `$SIG_GCP_PROJECT`) but the accounts + the `gcloud apply` are operator work; DEPLOY.1 lands IaC in prepare mode | operator provisions GCP + Secret Manager, exports `gcloud` ADC in the run shell; DEPLOY.1 re-run with ADC applies the validated IaC | IaC written+validated (`terraform validate`/dry-run) by DEPLOY.1; `apply` gate-pending; no token literal in any file | DONE 2026-09-15 — operator provided ADC (`gcloud auth application-default login`) + billing-enabled project `eleutheria` (`$SIG_GCP_PROJECT=zeta-medley-508121-u7`); the P24.1 live re-run (branch `devin/deploy-gcp-live`, ADR-081) provisioned the real host: **Cloud SQL** PG18+PostGIS (`sig-pg`), **Cloud Run** API (`sig-api`, restricted), Artifact Registry, Secret Manager (`sig-pg-password`), 4 GCS buckets (private). Host/accounts/object-store now real. Runbook `docs/build/reports/GCP_DEPLOYMENT.md`. Zenodo/SWH creds (HG-07) still owed under `D-P21.5-1`. No token literal in any file. (cites BL-037) |
| D-DEPLOY.1-1 | P | HG-12 — the real GCP `apply`/deploy of the validated IaC + the real cloud restore (GCS / Cloud SQL) under operator Application Default Credentials | P24.1 (DEPLOY.1 / GL-DEPLOY-01) wrote + validated the `ops/gcp/` infra-as-code (idempotent `gcloud` scripts, ADR-075) and the `sig-ops deploy --target gcp` path, but there are **no** `gcloud` ADC and **no** GCP account in this isolated context and creds are `provided: no` (P23.4 / ACCT.1). No `gcloud apply` is run by an isolated subagent; no deployed URL fabricated. Cross-ref `D-ACCT.1-1` (HG-12 host/accounts). | operator exports ADC (`gcloud auth application-default login`) + `SIG_GCP_PROJECT` in the run shell, then re-runs `bash ops/gcp/provision.sh --apply`, `sig-ops deploy --target gcp` (no `--dry-run`), and the cloud restore (`bash ops/gcp/backup.sh --apply` → `gcloud sql backups restore` or a `pg_restore` from `gs://…-sig-backups`) — expect the OKC dossier serving from the GCS/Cloud-Run URL and the restored graph reproducing claim/evidence counts | **validated-IaC proxy is green** — `bash ops/gcp/provision.sh --check` + `bash ops/gcp/backup.sh --check` print the full plan, use NO ADC, open NO network, exit 0; `sig-ops deploy --target gcp --dry-run` prints the plan + exits 0 (`tests/ops/test_gcp_iac.py`, `tests/ops/test_deploy.py`); the **local restore drill runs for real over Docker** — `pg_dump` → restore into a fresh DB → claim/evidence/entity counts reproduce (`tests/db/test_restore_drill.py`, `sig-ops backup-drill`); no secret/token literal, project id is `$SIG_GCP_PROJECT` (`tests/connectors/test_secrets.py`) | DONE 2026-09-15 — **apply/deploy + cloud restore drill DONE** by the P24.1 live re-run (branch `devin/deploy-gcp-live`, ADR-081) under operator ADC: realized as **Cloud SQL + Cloud Run** (the ADR-075 documented alternative — Cloud Run→e2-micro needed a same-priced VPC connector, ADR-081). Executed for real: Cloud SQL `sig-pg` (POSTGRES_18) with the full 20-migration schema + the new `read_surface_grants` change; OKC seed (5 claims/1 entity); Cloud Run `sig-api` (restricted, `--no-allow-unauthenticated`, min-instances 0) wired to Cloud SQL via `--add-cloudsql-instances` + Secret Manager password; API verified serving the real spine (`/v1/search`,`/v1/dossier/okc`,`/v1/contradiction` — 299-vs-190 preserved). Three real gap-closures landed as code (Dockerfile workspace copy + runtime DSN; `read_surface_grants` sqitch change). **cloud restore drill DONE 2026-09-15:** `gcloud sql export sql` → the private `…-sig-backups` bucket → `gcloud sql import sql` into a fresh `sig_restore` database → counts reproduce exactly (claims=5, entities=3, evidence=5, matching source `sig`). The drill surfaced + fixed a real **restore-blocking bug**: the `read_surface_grants` migration's `ALTER DEFAULT PRIVILEGES` clauses can't be re-applied by the Cloud SQL import user ("permission denied to change default privileges", aborting the whole restore) — removed them (explicit `GRANT ON ALL TABLES` retained; a later read-surface table re-grants explicitly), keeping the DB restorable (ADR-081). Runbook `docs/build/reports/GCP_DEPLOYMENT.md`. no secret/token literal. cross-ref D-ACCT.1-1 (DONE) (cites BL-037) |
| D-LIVE.1a-1 | V | HG-09 — the real LIVE smoke of the three now-green OKC document sources (`okc_procurement`/`okcpd_policy`/`ok_statute`): fetch the cited public PDFs/HTML → capture to OCFL → parse → emit the expected claim shape into PG with a fetch record; idempotent re-run adds 0 new claims | P23.5 (LIVE.1a) implemented the connector code and proved it via shadow replay (diff = 0), but the live fetch needs `SIG_*` API tokens which are **not provided** (`provided: no`, P23.4 / ACCT.1); secrets are env-only and none are available in this isolated context | operator exports the required `SIG_*` token(s) in the run shell and runs `sig-connectors run --source okc_procurement --mode live` (then `okcpd_policy`, `ok_statute`) — expect a fetch record written under the OCFL capture dir, the fixture-shaped claims asserted, and a second run adding 0 new claims (idempotent); the ledger records only `provided: yes/no` | **shadow-replay proxy is green** — `tests/connectors/test_okc_documents.py::test_shadow_replay_over_fixture_has_zero_diffs` (all 3) + `test_run_source_shadow_and_replay` prove fetch→capture→parse→emit is deterministic and byte-identical over the committed fixtures; `run --mode live` for these green sources is wired (P21.3 transport) and would fetch once tokens are present; no live green fabricated | DONE 2026-09-16 (P25.8 live smoke) — earlier annotation: 2026-09-10 by the P21.3 LIVE.1 fetch re-run: HG-03 CLEARED (`review-status` all-green + `loadable now: True` for all three; `run --mode live` no longer refuses them on gate grounds), live smoke still blocked on HG-09 tokens + network egress; shadow-replay diff=0 proxy re-verified. **2026-09-15 (ADR-082):** live-target plumbing landed (`data/live_targets.toml` + runner wiring + tests); a real `osm_overpass --mode live` was attempted against the hosted spine and **refused by SIG conduct** (`overpass-api.de` robots.txt disallows `/api/`, SIG-INGEST-037) — OSM fetch now blocked on an endpoint/robots policy decision; doc-connector live parsing needs content-drift hardening. Scoped in **P25.1 / BL-055**. **DONE 2026-09-16 (P25.8):** the live document parser landed (`okc_documents.py`: §-marker slicing for captured pages, bare-heading slicing for captured PDFs, whitespace-folded literal verification, real page/byte-range locators — an absent marker or literal is recorded ContentDrift). Live results: `okcpd_policy` fetched the real 469-page OKCPD Operations Manual from okc.gov (robots-permitted), OCFL capture `bcnaay63…`, **3 clause claims into the hosted spine** (use_restriction / retention_period / sharing_restriction; verbatim literals as raw_value; page locators 283–285); re-run idempotent (19702→19702, content-digest dedupe). `okc_procurement` → recorded `link_rotted` (okc.gov edge WAF 403). `ok_statute` → recorded `politeness_refusal` (OSCN robots `Disallow: /`, never bypassed). Fetch records: `docs/build/reports/live_runs/2026-09-16_okc*.json`. (cites BL-023) |
| D-OBS.1-1 | V | OBS.1 live-side verification — a real webhook delivery (a configured `SIG_ALERT_WEBHOOK_URL`) and the first real probe/uptime data against the hosted stack | P24.3 ran offline-only: the notifier seam is proven by a stubbed-transport test + the always-on recorded ledger, and `compute_uptime` is proven over synthetic rows; no live endpoint exists yet (HG-12) and no webhook URL is configured (env-only, HG-09) | operator sets `SIG_STAGING_*` (+ optional `SIG_ALERT_WEBHOOK_URL`) once a host exists (D-ACCT.1-1) — then `sig-ops probe --alert` against the real stack and one real webhook delivery of `sig-ops alert`; the daily `observability.yml` sweep picks both up automatically | stubbed `urlopen` POST test asserts the scrubbed body + Bearer header; the recorded-alert ledger fires with no notifier configured; `no-data` (never a fabricated 100%) until real probes land | DONE 2026-09-16 (P25.8): `sig-alerts` receiver deployed on Cloud Run (bearer-checked POST sink, token via Secret Manager `sig-alert-webhook-token`, `--allow-unauthenticated` for webhook POSTs only); a real `sig-ops alert` (kind=deployment-smoke, warn) was recorded to the append-only ledger AND delivered — Cloud Logging shows `SIG-ALERT-RECEIVED` with the full payload. Real probe sweep against the hosted stack: pg OK 1.6s (via proxy), api OK 175ms, static OK 86ms, curation DOWN (deliberately not deployed — loopback-only app). Probe rows in `.sig/ops/probes.jsonl`; ledger row in `.sig/ops/alerts.jsonl`. (cites BL-037) |
| D-CI.1-1 | V | The first on-runner execution of the new `composed` CI job + `nightly.yml` schedule — GitHub-hosted execution cannot be run from an isolated worker | P24.4 defined the workflows and ran the *identical commands* locally green (Docker up, Node + `web/node_modules` present): `SIG_REQUIRE_DB_TESTS=1 uv run pytest tests/e2e` → 16 passed/0 failed (S8 web-build seams included); `make scan-secrets`/`scan-licenses`/`audit-deps` all exit 0; workflow structure pinned by `tests/unit/test_ci_workflows.py`. What cannot be produced locally is the actual GitHub Actions run itself. | the PR is opened (its own `composed`+`security`+`docs` checks run on it) or the operator dispatches `nightly.yml` via `workflow_dispatch`; verify via `gh run list --workflow ci.yml` + the `nightly-*` report artifact | proxy = local identical-command runs + workflow-parse tests (fail if a job/step is removed); the nightly report step is the gate (red nightly is loud, artifact uploads `if: always()`) | DONE 2026-09-14 — PR #82's checks ran on the runner (run `34802316750`, conclusion `success`): `composed` job = **16 passed** incl. all three `test_s8_web_build_*` (the `python` job's 3 S8 skips ["no npm / web/node_modules"] confirm the composed job is the one carrying S8); `security` (secret+licence scans) green; `docs` (docs-check + check-build-memory) green; `web` + `python` green. Note: the first `nightly.yml` cron tick remains unobserved — `workflow_dispatch` is not triggerable until the workflow file reaches the default branch (GitHub constraint; dispatch attempted 2026-09-14 → HTTP 404 "workflow not found on the default branch"); same machinery as the proven `composed` job, and its report step fails loud if a stage breaks. |

## P24.5 (META.1 / GL-META-01) backlog triage — obligations promoted from `BACKLOG.csv` `P22+` (2026-09-13)

The META.1 triage examined every `P22+` row against the tree (none landed). The two
rows whose only precondition is real post-go-live state are promoted here as owed
obligations (kind F) — they cite their `BL-` id, are not duplicates, and flip to
`DONE` only when verified for real. The remaining unscheduled engineering rows keep
an honest `P25+` landing in `BACKLOG.csv` (post-Round-4 manifest, next planning
round) — see `docs/build/runs/P24.5.md` for the per-row triage.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-META.1-1 | F | BL-045 — ruleset calibration (volatility, directness matrix, Atlas supersession) run once **real** claim data exists | the shipped predicate-registry/ruleset constants are build-time defaults; there is no real claim volume to calibrate against (the graph is fixture/staging-only until the Lane-B live re-runs land real claims) | real claim volume in the live spine — i.e. after the live fetch re-runs (D-P21.3-1 / D-LIVE.1a-1 live smoke; D-P21.8-1 / D-P21.9-1 pathway fetches) land real claims | recompute volatility / directness-matrix / Atlas-supersession values over the real spine and record the calibration outcome (append-only; new claims, never edits); a dated calibration note lands under `docs/build/reports/` | rulesets remain at documented defaults; no calibration fabricated — deterministic proxies (`tests/reconcile`) pass on the defaults | DONE 2026-09-16 (P25.9) — calibration run over the real hosted spine (19,702 claims / 6,573 subjects / 19 sources / 79 predicates via the Cloud SQL proxy): dated note `docs/build/reports/2026-09-16_ruleset_calibration.md`. Findings: volatility classes/half-lives hold on every measurement the single-epoch corpus can make (deployment_exists 4,957 subjects / 0 churn; the one real disagreement — `claimed_device_count` 190 vs 299 — sits above the FAST 0.15 tolerance, so the constant provably does its job); the directness matrix's grades are unrefuted on the 8 resolvable pairs but 99.92% of claims carry the unregistered `connector_run` genre (coverage gap, not a grades error); Atlas supersession is genuinely unexercised (0 cross-source subject+predicate overlap; resolution tables empty — ER has never run on the hosted spine). No constant retuned — a single-epoch corpus cannot estimate change rates; five follow-on gaps recorded in the note. (cites BL-045) |
| D-META.1-2 | F | BL-028 — records-request filing/response backend: the `records_request` template outcome log **fed live** | `tasks.records_request` (P10.3, ADR-041) generates request templates only; there is no live filing/response backend and no real request outcomes exist to record (post-live operations; MuckRock filing itself is HG-09) | real records-request activity once OKC is live (+ `SIG_MUCKROCK_TOKEN` if filed via MuckRock — D-P21.3-2 / HG-09) | a real request outcome is recorded through the backend: template outcome log fed by an actual filing/response; the response lands in the evidence store (no fake outcomes) | template generation + outcome-log shape tested (`tests/tasks` records-request tests); live-fed backend unbuilt — no outcome fabricated | DONE 2026-09-16 (P25.9) — the outcome backend landed: `tasks/src/tasks/request_outcomes.py` (append-only JSONL `RequestOutcomeLog` — request id, agency, filed/acknowledged/in-flight/completed state + precise §11.19 status, dates; versioned status→state map `data/request_outcomes.toml`; explicit `feed_template_log` into `TemplateOutcomeLog`, `no_responsive_records` never a failure) + the `sig-tasks records-outcomes` CLI (record, show) + feeder `docs/build/tools/records_outcome_feed.py`. Fed live: Cloud Run job `sig-ingest-muckrock` executed 2026-09-16 (execution `sig-ingest-muckrock-fx2ch`, exit 0 — real api_v2 pull attempt; the API currently challenges even GCP egress → 0 new claims, a recorded disappearance, never defeated); the real recorded state of request 136412 (fulfilled, agency 265, response 2022-11-30) folded from the spine into `docs/build/reports/records_outcomes_2026-09-16.jsonl`. Direct laptop pull recorded `ChallengeEncountered` 403 (Cloudflare, accounts.muckrock.com) — no outcome fabricated. 15 fixture tests in `tests/tasks/test_tasks_request_outcomes.py` cover the api_v2 parse, claim fold, append-only fold, template feed + enum coverage. (cites BL-028) |

## P24.6 (JURIS.2 / GL-JURIS-01) — second jurisdiction: live half is operator-gated (2026-09-13)

France (Commune de Gex, Ain — vidéoprotection) ran end-to-end over committed fixtures
(`run_france.sh`): seed → shadow connectors → match → resolve → export → web → FR-1..FR-10.
What it cannot do alone is flip the France sources or fetch them live — the same HG-03/HG-04
operator gates that hold for OKC. These rows are the live half, owed after the operator acts.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-JURIS.2-1 | P | HG-03 — flip the reviewed France sources (`raa_prefectures`, `decp_fr`, `madada`) to `ingestion_permitted=true` after their rights reviews resolve; then the real LIVE fetches (RAA index → arrêté corpus, DECP national files, Ma Dada register) | flipping a source is a human/rights decision, not a code change; `decp_fr`'s Licence Ouverte 2.0→SPDX mapping is flagged counsel-needed in its packet; `madada` also sits at `compact_status=not_contacted` | operator reads `docs/build/reports/rights/{raa_prefectures,decp_fr,madada}.md`, resolves the LO 2.0 disposition, flips each source with reviewer role + date recorded | `sig-connectors review-status --source <id>` shows all five gate fields True; `sig-connectors run --source <id> --mode live` stops refusing exit 3 and writes a fetch record | `docs/build/tools/run_france.sh` completes over fixtures; shadow replay diff=0 for raa+decp; `madada` recorded REFUSED at the compact gate; `sig-exports build --jurisdiction france` omits DECP content (`UNRESOLVED` dossier gap) | PARTIAL 2026-09-16 advanced — opened 2026-09-13 by P24.6 (JURIS.2); no source flipped, no live fetch performed (cites BL-042) **2026-09-15 — PARTIAL advanced:** all four France/Belgium sources flipped (raa ODbL + decp LO-2.0 per-source, operator; madada + declarationcamera_be counsel/HG-02); real live fetches landed — decp_fr 9,494 claim rows + raa_prefectures RAA-index CSV + madada Atom feed as evidence_artifacts (P25.5 document capture). Still owed: the RAA resource-index→arrêté extraction adapter, declarationcamera eID path (HG-04). **2026-09-16 — advanced (P25.5 live-extraction run):** the RAA resource-index adapter landed — strict CSV shape (changed header → ContentDrift), bounded `instrument_document` fan-out (arrêté-titre + PDF-URL + https + departement bounds, `max_documents=8` — never the ~23k archive), `LegalInstrument` rows (`fr.arrete_prefectoral`, CSI L252 five-year sunset marked `derived` on the claim). LIVE: index captured + parsed, 8 bounded prefecture-PDF targets resolved — every `*.gouv.fr` host's robots.txt unretrievable → 8 recorded per-document politeness refusals, never bypassed (fetch record `2026-09-16_raa_prefectures.json`). madada Atom adapter landed — strict feed shape, 25 `fr.cada` records-request contexts live, `<title>/<content>/<author>` never read (`never_emit` contract proven by fixture test). declarationcamera_be stays NoLiveTargets — the register sits behind Belgian eID, HG-04 still owed. **2026-09-16 advanced (P25.7):** `decp_fr` now resolves its resource through the data.gouv.fr dataset API at run time (the timestamped static URL is gone from the config; a removed/renamed resource is a recorded `link_rotted` disappearance, never a stale-URL fallback) — live re-run committed 7,234 spine claims (9,495 rows asserted, idempotent on content digest; fetch record `2026-09-16_decp_fr.json`); madada re-asserted (26 spine claims); the RAA arrêté fan-out re-confirmed 8 per-document `RobotsUnretrievable` refusals. **Still owed — HG-04 only:** the `declarationcamera_be` eID/outreach path (`2026-09-16_declarationcamera_be.json` records the NoLiveTargets boundary; nothing is engineered around the wall). **P25.9 sweep 2026-09-16 — sole blocker: Belgian eID access to `declarationcamera_be` (HG-04) — an operator credential/outreach act; every France side is landed.** |

| D-JURIS.2-2 | P | HG-04 — Stage-0 outreach to the France source operators (data.gouv.fr RAA/DECP dataset maintainers, Ma Dada) + the Belgium `declarationcamera_be` packet if Belgium lands as a third jurisdiction | outreach is operator work (D-P21.1-2 owes the same for the US compact set); `madada`'s `not_contacted` compact posture is honest until contact is recorded | operator performs/records outreach; `STAGE0_OUTREACH_RECORD.md` gains the FR rows; `madada` compact status may move off `not_contacted` | the outreach record shows dated FR contact; `madada`'s compact_status change (if any) is recorded with evidence | FR/BE rows land in the P18.1 outreach table alongside the US rows; the madada loader-gate refusal is the recorded pre-outreach posture | WONTFIX 2026-09-16 — SKIPPED-BY-OPERATOR: operator elected to skip Stage-0 outreach entirely (optional, not a read-surface or correctness prerequisite); honest `not_contacted`/`public_terms_only` postures stay published. Earlier annotation:** `declarationcamera_be` keeps its recorded `NoLiveTargets` posture (eID wall); no outreach fabricated. **P25.9 sweep 2026-09-16 — sole blocker: operator action (HG-04) — perform/record the FR/BE source-operator outreach.** |

## P24.7 (CCOPS.1 / GL-CCOPS-01) — CCOPS connector: live half is operator-gated (2026-09-13)

The `government_mandated_disclosure` connector class landed with its three paying
sources (`ccops_seattle`, `ccops_nyc_post`, `ccops_sf`) running over committed
fixtures — shadow replay diff = 0 through the eight stages + loader gate. What it
cannot do alone is flip the CCOPS sources or fetch them live — the same
HG-03/HG-04 operator gates that hold for every source. These rows are the live
half, owed after the operator acts.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-CCOPS.1-1 | P | HG-03 — flip the reviewed CCOPS sources (`ccops_seattle`, `ccops_nyc_post`, `ccops_sf`) to `ingestion_permitted=true` after their rights reviews resolve; then the real LIVE fetches (Seattle SIR/Master List/quarterly reports, NYC POST `post-final/` policies, SF biannual inventory PDFs) + the one-time `state_alpr_statute_inventory` (NCSL) seed load | flipping a source is a human/rights decision, not a code change; the packets flag municipal-record copyright posture counsel-needed (SIG-LIC-009); the seed's spine ingest is the same gate | operator reads `docs/build/reports/rights/{ccops_seattle,ccops_nyc_post,ccops_sf}.md`, resolves the municipal-record posture, flips each source with reviewer role + date recorded | `sig-connectors review-status --source <id>` shows all five gate fields True; `sig-connectors run --source <id> --mode live` stops refusing exit 3 and writes a fetch record | `tests/connectors/test_government_mandated_disclosure.py` — shadow runs over fixtures through the loader gate diff=0 for all three; live refusal asserted per source; `sig-connectors gate --source <id>` records REFUSED; the NCSL seed asset is committed labelled `as_of: 2022-02-03` | DONE 2026-09-16 — opened 2026-09-13 by P24.7 (CCOPS.1); no source flipped, no live fetch performed (cites BL-054) **2026-09-15 — PARTIAL advanced:** all three CCOPS sources flipped (ADR-085, municipal-mandated-disclosure + derived-facts basis); seattle + nyc index pages captured live as evidence_artifacts. Still owed: index→document discovery + PDF→field extraction adapters, sf.gov robots/egress (P25.7), the NCSL seed load. **2026-09-16 — advanced (P25.5 live-extraction run):** the index→document adapter landed — `index_page` captures discover spec-matching filing links (`[adapters.*]` reviewed specs, `max_documents`-bounded) and `disclosure_document` filings extract typed claims through `disclosure_claim` (all three guards intact): ordinance citation + enacting body, technology derived from the filing's own title/filename, legal authority, retention literal, and per-section `disclosure_field_state` (answered/present_but_empty/absent — mandated ≠ populated). LIVE: ccops_nyc_post → index + 6 bounded `post-final/*.pdf` filings → 94 rows (fetch record `2026-09-16_ccops_nyc_post.json`); ccops_seattle → index parsed, 4 linked filings all 404 → recorded `link_rotted` disappearances; ccops_sf → honest SIG-INGEST-012 robots refusal (exit 6). Still owed: sf.gov egress (P25.7), the NCSL seed load. **DONE evidence (P25.7):** `ccops_nyc_post` 79 claims + `ccops_seattle` 2 claims asserted to the hosted spine on live re-runs (seattle's 4 linked filings re-confirm `link_rotted` disappearances — first-class data); `ccops_sf` re-probed from Cloud Run egress (`ccops-sf-egress-probe`): `www.sf.gov` answers BOTH robots.txt and the index page with `202 text/html` — an interstitial challenge, not retrievable robots content — so the SIG-INGEST-012 refusal is CONFIRMED from the hosted vantage (`2026-09-16_ccops_sf_cloud_run_egress.json`); the NCSL `state_alpr_statute_inventory` seed loaded through the same pipeline + loader gate as every source (`sig-connectors load-seed`, `seeds.toml`/`state_statute_seed`) → **108 claims in the hosted spine with `as_of=2022-02-03` preserved** (16 states / 17 NCSL rows / 21 statute-year `LegalInstrument`s). |

| D-CCOPS.1-2 | P | HG-04 — Stage-0 outreach to the CCOPS source operators (Seattle CTO privacy program, NYC.gov/NYPD publications, sf.gov/COIT) + the ACLU CCOPS list maintainer for the canonical jurisdiction register | outreach is operator work (D-P21.1-2 owes the same for the US compact set); `public_terms_only` is honest pre-outreach posture; the canonical list being a JPEG means the jurisdiction register is hand-maintained (F3.19) | operator performs/records outreach; `STAGE0_OUTREACH_RECORD.md` gains the CCOPS rows | the outreach record shows dated contact for each source; any compact_status change is recorded with evidence | CCOPS rows land alongside the US/FR rows in the outreach table; the sources' REFERENCE+public_terms_only posture is the recorded pre-outreach state | WONTFIX 2026-09-16 — SKIPPED-BY-OPERATOR: operator elected to skip Stage-0 outreach entirely (optional, not a read-surface or correctness prerequisite); honest `not_contacted`/`public_terms_only` postures stay published. Earlier annotation:** `ccops_sf` keeps its recorded `politeness_refusal` posture; no outreach fabricated. **P25.9 sweep 2026-09-16 — sole blocker: operator action (HG-04) — perform/record the CCOPS source-operator + ACLU-list outreach.** |

## P26.2 (SOURCES.2 / GL-GATE-06) — promoted-source access legs + held rights decisions (2026-09-16)

The nine P25.6 `promote` sources are all connector-mapped with fixture tests, and the
registry grew 121 → 142 under the discovery sweep (every new row `ingestion_permitted=false`
behind a rights-packet skeleton). What engineering cannot close alone: the access
credentials, the two held rights decisions, and two recorded access postures.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-SOURCES.2-1 | P | HG-09 — export `SIG_SAM_GOV_KEY` + `SIG_OPENSTATES_KEY` (free registered API keys) in the run shell / bind them in Secret Manager for the scheduled jobs | both sources are rights-green (GL-GATE-06 flips) but key-gated; keyless `api.sam.gov` 404s (recorded `link_rotted`) and `v3.openstates.org` 403s (recorded `access_restricted`) — a credential act, not a code gap | operator registers/exports the keys (or adds `sig-sam-gov-key`/`sig-openstates-key` secrets) | `sig-connectors run --source sam_gov --mode live` / `--source openstates` produce captures + claims instead of disappearances | connector paths + fixtures + live_targets + cadence rows all landed; disappearances recorded honestly 2026-09-17 (`docs/build/reports/live_runs/2026-09-17_{sam_gov,openstates}.json`); **re-verified 2026-09-17 (P26.3): same keyless outcomes — api.sam.gov 404 `link_rotted`, v3.openstates.org 403 `access_restricted`** | DONE 2026-09-17 (P26.4) — keys bound in Secret Manager **by name only** (`sig-sam-gov-key`/`sig-openstates-key` → `SIG_SAM_GOV_KEY`/`SIG_OPENSTATES_KEY` env on the jobs) and verified live for real: `sig-ingest-sam-gov` execs `-cgl4m` + `-7h79k` → keyed `X-Api-Key` fetch 200, 8 emitted records → **7 L1 claims** in the hosted spine (a real CBP opportunity: notice_type/posted_date/response_deadline/buyer/document/external_id/lifecycle_transition), **idempotent re-run proven** (identical capture digest, 0 new claim rows, no duplicate subject/predicate/value triples); `sig-ingest-openstates` execs `-hzndc` + `-9mv2f` → keyed `X-API-KEY` fetch 200, **10 `index_only` bill evidence-links** per run (PgClaimSink counts them as non-claim records by design — a pending bill is not a §11.14 claim), identical capture digest across runs. Credential transport moved from the URL query to the reviewed `api_key_header` request headers so no key can land in a recorded `source_uri`/run row — pinned by `tests/connectors/test_credential_transport.py`; spine-wide sweep shows 0 credential-bearing URIs. Run rows: `gs://…-sig-restricted/ops/runs/{sam_gov,openstates}/2026-09-17/`; committed records `docs/build/reports/live_runs/2026-09-17_{sam_gov,openstates}_keyed.json`; jobs `sig-ingest-{sam-gov,openstates}` + triggers `sig-sched-{sam-gov,openstates}` carry the secret bindings. (was OPEN, cites BL-024; P26.3 sweep 2026-09-17: sole blocker was the operator credential act) |
| D-SOURCES.2-2 | P | HG-03/rights — `documentcloud` source-level licence decision (per-document rights vary; UNDETERMINED) and `courtlistener_recap` FLP membership-agreement acceptance (free.law/membership/allowed-api-usage — binding usage conditions; accepting the agreement + holding the credential is an operator act) | GL-GATE-06 covers only resolved-clear licences; neither resolves to one without a reviewer/operator decision | reviewer resolves documentcloud's source-level posture; operator accepts (or declines) the FLP membership terms and holds the token | a flip (or a recorded decline) with review metadata; then `run --mode live` stops refusing exit 3 | both connectors' targeted-lookup paths landed + fixture-tested; gate refusals recorded 2026-09-17 (exit 3, no socket opened). **P26.3 2026-09-17 — packets COMPLETED with verbatim terms:** `documentcloud` — the MuckRock ToS (governs DocumentCloud) expressly excludes "data mining, robots, or similar data gathering and extraction tools" and makes user content personal-use-only → affirmatively restrictive, no GL-GATE-06 basis; `courtlistener_recap` — FLP membership terms intend individuals/small orgs and exclude "building tools for for-profit or non-profit organizations" → SIG falls outside membership eligibility; keyless probe → 401. Both stay gated; sole blockers are operator decisions (documentcloud: per-document rights review or an affirmative API grant; courtlistener: FLP agreement tier + token, HG-09 class). Packets: `docs/build/reports/rights/{documentcloud,courtlistener_recap}.md` | OPEN (cites BL-032) — P26.3 sweep 2026-09-17: reviews complete; sole blockers are the operator rights/access decisions |
| D-SOURCES.2-3 | F | `civicclerk` access path — the v1 API is IdentityServer-gated (anonymous reads 404 on every probed tenant; the api host serves no robots.txt → recorded `robots_unretrievable`, SIG-INGEST-012). A session-bearing or otherwise documented access path needs an access-method decision (HG-09 class) | the portal obtains an IdentityServer session; engineering around it would be circumvention, so the posture is recorded | operator/maintainer decides the access basis (documented public endpoint, portal session, or outreach) | a live run fetches a tenant `/v1/Events` index instead of the recorded politeness refusal | the CivicClerk fixture test + tenant-registry row (`oklahomacityok`, `verified=false`) landed; refusal recorded `2026-09-17_civicclerk.json` | DONE 2026-09-17 (P26.3) — verified for real: the P26.2 "every anonymous v1 request 404s" finding was specific to `oklahomacityok`, not platform-wide. Under ADR-087 (RFC 9309 §2.3.1.4 — a 4xx robots answer is "no policy exists", not "unavailable"), 34 verified tenants answered robots-404 + served real `/v1/Events` indexes → **500 claims / 34 captures** (`docs/build/reports/live_runs/2026-09-17_civicclerk.json`; per-host `no_policy_4xx` outcomes recorded). `oklahomacityok` still 404s its Events index → recorded `link_rotted` disappearance. The public unauthenticated v1 API IS the access path — no session needed. (cites BL-055) |
| D-SOURCES.2-4 | F | `primegov` API-mode basis — lacity.primegov.com serves `POST /search` live but its robots.txt is `Disallow:/`; a fetch needs an ADR-083-style API-mode justification recorded for the PublicPortal surface (or a serving tenant whose robots permits) | the robots refusal is never bypassed; API-mode entry needs the same reviewed basis the other `/prod/`-class endpoints carry | a reviewed `api_allowlist.toml` entry for the PublicPortal surface (or a new verified tenant) | a live run fetches a meeting index instead of the recorded politeness refusal | the PrimeGov fixture test + `lacity` tenant row landed; refusal recorded `2026-09-17_primegov.json` | PARTIAL 2026-09-17 (P26.3) — the pivot leg is done: all 9 verified-serving PrimeGov tenants answer robots `Disallow:/` **platform-wide** → 9 recorded per-host `RobotsDisallowed` refusals (`2026-09-17_primegov.json`), never bypassed; the LA records leg routes through legistar instead — `lacounty` verified (real matter index → claims), `lacity` NOT provisioned on the shared InSite host (recorded `tenant_api_error`), and the real LA City legislative surface (cityclerk.lacity.org CFMS) is registered as `la_city_clerk_cfms` (LINK). Still open: an operator ADR-083-style API-mode decision for the PublicPortal surface — no longer needed for the LA leg, owed only if PrimeGov hosts themselves should ingest. **P26.5 confirm 2026-09-17:** full enumeration grew primegov to 110 verified tenants and the sweep recorded **110/110 politeness refusals** (102 robots `Disallow:/` + 8 unretrievable) — the platform wall holds at scale; the API-mode decision stays the operator's. **DONE 2026-09-19 (P26.17) — RESOLVED-BY-GL-GATE-08:** the operator's recorded disposition (LEDGER § GATE DECISIONS, 2026-09-18) — *disregard robots entirely; robots verdicts stop gating fetches* — removes the gate itself, so the ADR-083 API-mode justification this row owed is moot: PrimeGov hosts ingest in CRAWL mode with the verdict probed + recorded and each proceeded fetch marked `robots_disregarded` (ADR-088). The re-sweep evidence lands in the P26.17 run ledger + `docs/build/reports/live_runs/`; a WAF/403/dead-host outcome is still recorded honestly (`access_restricted`/`link_rotted`/`unreachable`), never bypassed. (cites BL-055) |

## P26.7 (SOURCES.7 / GL-GATE-06) — DOT/511 camera registries: gated rights + keyed APIs + hosted leg (2026-09-17)

The `dot_511` connector landed with 8 of 13 enumerated state registries flipped under
GL-GATE-06 and live-verified locally (84,254 claims / 14 page captures; fetch records
`docs/build/reports/live_runs/2026-09-17_dot_511_<st>_p267.json`). The rest is operator
work: five states' terms do not resolve to a clear public licence, the Iteris-platform
511 APIs are key-gated, and the hosted-spine/scheduler leg needs operator GCP
credentials (this session had none — ADC unset).

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-SOURCES.7-1 | P | HG-03 — rights decisions for the five gated DOT/511 rows: `dot_511_la` (GOHSEP-owned item, empty licenseInfo), `dot_511_ga` (GEMA-owned, empty), `dot_511_al` (county E-911 publisher + empty), `dot_511_tx` (personal account + empty), `dot_511_md` (iMAP attribution-conditioned terms — needs a licence-registry row first) | no licence resolves under GL-GATE-06; flipping is a reviewer decision, not a code change | reviewer resolves each state's terms (packets carry the verbatim metadata in `sources.toml` notes + `dot_511_targets.toml` `rights_note`); MD additionally needs a LicenseRef/SPDX row for the iMAP conditioned terms | `sig-connectors gate --source dot_511_<st>` goes green after a recorded flip; `run --mode live` then produces captures instead of exit 3 | all five rows are gated with named blockers; `dot_511_ga` live attempt records REFUSED exit 3 (no socket opened); target rows + observed counts/fields are already enumerated | OPEN 2026-09-17 (cites BL-055) |
| D-SOURCES.7-2 | P | HG-09 — registered API keys for the keyed 511 platforms (Iteris-platform FL/NY/AZ/NV/PA/WI/AK/CT/NewEngland511 — all answer `Invalid Key`; OHGO answers 401 'API key required') | free/registered keys are a credential act; no secret may be committed; probing without a key is recorded, not bypassed | operator registers/exports the per-platform keys (Secret-Manager name bindings, the P26.4 pattern) | a keyed live run produces captures + claims; the `[[enumerated]]` rows can graduate to `[[targets]]` for states still lacking an open layer | all keyed hosts are recorded `keyed_api`/`access_restricted` in `dot_511_targets.toml` — enumerated, never wired | OPEN 2026-09-17 (cites BL-055) |
| D-SOURCES.7-3 | P | hosted-spine leg — deploy the `sig-ingest-dot-511-*` Cloud Run jobs + `sig-sched-dot-511-*` triggers declared in `ops/cadence.toml`, rebuild the `sig-api` image with the P26.7 connector, and run ≥1 state into the hosted spine | requires operator ADC/GCP credentials + image push; unavailable in this session | operator exports ADC + `SIG_GCP_PROJECT`, runs `ops/gcp/scheduled-ops.sh --apply`, executes the jobs | GCS run rows under `gs://…-sig-restricted/ops/runs/dot_511_<st>/` + hosted-spine claim delta; committed fetch records already exist for all 8 local runs | cadence rows + job names declared and `sig-ops cadence --check` green; local `--mode live` runs produced 84,254 claims over 14 captures | DONE 2026-09-17 — all 8 `sig-ingest-dot-511-*` jobs + `sig-sched-dot-511-*` triggers deployed; image rebuilt linux/amd64 with the connector; all 8 states executed into the hosted spine (fetch records committed under `docs/build/reports/live_runs/2026-09-17_dot_511_*_p268.json`); hosted spine 32,064 → 108,016 claims (75,952 dot_511 rows incl. the recorded +288 UT digest-rebaseline set — append-only, never deleted); `retrieved_date` volatile-digest defect fixed + regression-tested so monthly runs dedupe. ADC caveat recorded: host ADC file corrupt — per-job upserts used verbatim, no apply-script edit. (cites BL-055) |

## P26.9 (SOURCES.8 / GL-GATE-06) — municipal/transit/non-US camera registries: gated rights + keyed APIs (2026-09-18)

The `dot_511` connector was extended to Socrata row arrays + non-US jurisdiction schemes
and 23 municipal/transit/international registries were enumerated as targets. Nine flipped
under GL-GATE-06 and ran into the hosted spine (38,873 emitted records / 11 captures; fetch
records `docs/build/reports/live_runs/2026-09-18_camreg_*_p269.json`). The rest is operator
work: fourteen sources' terms do not resolve to a clear public licence, and two national
APIs are key-gated.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-SOURCES.8-1 | P | HG-03 — rights decisions for the fourteen gated camreg rows. No licence grant located: `camreg_edmonton_ab`, `camreg_honolulu_hi`, `camreg_md_opendata` (same iMAP-conditioned terms as `dot_511_md`), `camreg_seattle_wa` (no-warranty disclaimer only), `camreg_donegal_ie` ('No Access Constraints' ≠ grant). Terms not captured verbatim: `camreg_chicago_il`, `camreg_calgary_ab`, `camreg_york_on`, `camreg_hk_hk`. Restrictive or non-grant terms: `camreg_arlington_va` (disclaimer behind WAF/JS wall, no clear grant), `camreg_bellevue_wa` (expressly prohibits commercial use without authorization), `camreg_lexington_ky` (LFUCG ToU carries an indemnify-AND-defend clause — counsel-needed), `camreg_nzta_nz` (access-by-request to Infoconnect@nzta.govt.nz, not an open grant), `camreg_qldc_au` ('available for external use' is not a licence grant) | no licence resolves under GL-GATE-06; flipping is a reviewer decision, not a code change | reviewer resolves each source's terms (verbatim metadata + probe outcomes recorded in `sources.toml` notes); Bellevue additionally needs an operator decision on whether SIG's use is non-commercial; Lexington needs counsel on the indemnity clause | `sig-connectors gate --source camreg_<id>` goes green after a recorded flip; `run --mode live` then produces captures instead of exit 3 | all fourteen rows are gated with named blockers in `sources.toml`; target rows + observed counts/fields are enumerated in `camera_registry_targets.toml`; none was fetched | PARTIAL 2026-09-19 — **10 of 14 resolved under GL-GATE-07 (P26.16):** `camreg_honolulu_hi`/`camreg_md_opendata`/`camreg_seattle_wa`/`camreg_chicago_il`/`camreg_arlington_va`/`camreg_lexington_ky` flipped to LicenseRef-PublicRecord-FactualCompilation; `camreg_donegal_ie`/`camreg_calgary_ab`/`camreg_york_on`/`camreg_nzta_nz` to LicenseRef-OperatorAccepted-DBRight — all ran live `ok` (`live_runs/p2616/*`). **Remainder (4, still gated):** `camreg_edmonton_ab`, `camreg_hk_hk` (no grant located), `camreg_bellevue_wa` (express use-prohibition — outside the factual-compilation basis), `camreg_qldc_au` ('available for external use' is not a grant). (cites BL-055) |
| D-SOURCES.8-2 | P | HG-09 — registered API keys for the keyed national camera APIs: QLD `api.qldtraffic.qld.gov.au/v1/webcams` (401 Missing Authentication Token; dataset documents CC-BY-3.0) and NSW live-traffic cameras API (401 + robots 500) | free/registered keys are a credential act; no secret may be committed; probing without a key is recorded, not bypassed | operator registers/exports the per-platform keys (Secret-Manager name bindings, the P26.4 pattern) | a keyed live run produces captures + claims; the `[[enumerated]]` rows can graduate to `[[targets]]` | both hosts are recorded `keyed_api`/`access_restricted` in `camera_registry_targets.toml` — enumerated, never wired | OPEN 2026-09-18 (cites BL-055) |

## P26.10 (SOURCES.9 / GL-GATE-06) — procurement-portal tenants: gated platforms + one unlicensed dataset (2026-09-18)

The `procurement` connector was extended to multi-tenant procurement portals
(BidNet agency storefronts, Bonfire/OpenGov portals, city Socrata contract
datasets) behind a published tenant registry
(`data/procurement_portal_tenants.toml`). Four Socrata datasets flipped under
GL-GATE-06 on verbatim licence metadata (Austin PUBLIC_DOMAIN, SF PDDL, KCMO
CC0-1.0, NYC PUBLIC_DOMAIN). The rest is operator/reviewer work: three vendor
platforms carry access walls or uncaptured terms, and Chicago's dataset has no
licence metadata at all.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-SOURCES.9-1 | P | HG-03 — rights decision for `procportal_chicago_il` (Socrata dataset `rsxa-ify5` 'Contracts'): the dataset carries NO licence metadata on `/api/views` — no verbatim dedication to evaluate, unlike the four flipped siblings | GL-GATE-06 covers only resolved-clear licences; the city Terms of Use must be captured verbatim and reviewed first | reviewer fetches/quotes the Chicago data-portal terms (packet `docs/build/reports/rights/procportal_chicago_il.md` already records the absent-licence fact) | `sig-connectors gate --source procportal_chicago_il` goes green after a recorded flip; `run --mode live` then produces captures instead of exit 3 | tenant row + verified surveillance-relevant rows enumerated ('VIDEO/SURVEILLANCE CAMERA SERVICES', 'VIDEO SURVEILLANCE MANAGEMENT SYSTEM'); none was fetched beyond metadata | OPEN 2026-09-18 (cites BL-055) |
| D-SOURCES.9-2 | F | `bonfire` access wall — every verified `*.bonfirehub.com` tenant answers robots.txt `User-agent: *` / `Disallow: /` (laurier + columbus verified 2026-09-18, verbatim in `docs/build/reports/rights/bonfire.md`) | a platform-wide robots refusal is never bypassed; it stands until the file changes or a reviewed API-mode basis exists | the robots policy lifts on a tenant, or a reviewer records an ADR-083-class API-mode basis for a documented public endpoint | a live run fetches a tenant portal index instead of the recorded politeness refusal | both tenant rows registered so each run records the refusal honestly; infra/non-jurisdiction certificate names segregated in `platform_census` | OPEN 2026-09-18 (cites BL-055) |
| D-SOURCES.9-3 | F | `opengov_procurement` access wall — every `/portal/<agency>` path answers HTTP 403 `cf-mitigated: challenge` (Cloudflare managed challenge, 10 slugs probed 2026-09-18) and `/robots.txt` returns the SPA shell, not a policy | the challenge is a recorded outcome, never defeated (SIG-INGEST-013); no terms or licence text is reachable behind it | a legitimate documented public endpoint/API path emerges AND terms become reviewable | a live run fetches a portal index instead of the recorded challenge disappearance | `salt_lake_city_ut` tenant row carries the recorded live outcome; remaining enumerated slugs are `platform_census` `waf_challenged` rows | OPEN 2026-09-18 (cites BL-055) |
| D-SOURCES.9-4 | P | HG-03 — rights decision for `bidnet_direct` (and the `periscope_s2g` eBid surface it supersedes): the vendor platform ToS was never captured verbatim; unlike the Socrata datasets there is no per-record licence field, so the GL-GATE-06 public-records basis needs the platform terms evaluated first | robots is honourable (`User-agent: *` allows, `Crawl-delay: 5`, verbatim in the packet) but licence terms are unresolved — a reviewer act, not a code change | reviewer fetches/quotes `https://www.bidnetdirect.com/terms` (or current terms URL) in `docs/build/reports/rights/bidnet_direct.md` and decides | `sig-connectors gate --source bidnet_direct` goes green after a recorded flip; `run --mode live` then produces captures instead of exit 3 | 9 verified agency storefronts registered with purchasing-group provenance; connector path + fixtures landed; the gate refuses live fetches meanwhile | OPEN 2026-09-18 (cites BL-055) |

## P26.11 (SOURCES.10) — OpenStates 50-state sweep: the key's 250/day quota window (2026-09-18)

The widened OpenStates connector (bounded 56-jurisdiction × 4-family
bill_search sweep, typed legislation claims) is implemented, tested,
deployed on `sig-ingest-openstates`, and executed three times. The sweep
itself fetched cleanly once (exec `nx527`, 226 requests in ~50 min) and
the API's daily quota — **250 requests/day** on this key's tier, verbatim
in the 429 body — is the sole remaining blocker for the landing leg.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-SOURCES.10-1 | V | hosted sweep landing + spine delta + idempotent re-run — a successful `sig-ingest-openstates` execution landing the ≥40-state bill_query outcomes + typed bill claims, the hosted-spine claim delta, and a second run deduping to ~zero new claims | the key's 250/day window is exhausted (429 body: `exceeded limit of 250/day: 343` — exploratory probes + the 226-request sweep + retries); a same-day sweep+rerun pair (452 requests) cannot fit one window; no quota may be bypassed | the daily quota window rolls over (rolling/UTC reset), the next weekly `sig-sched-openstates` cron (Tue 05:00 UTC — 226 ≤ 250/day, sustainable), or an operator key-tier upgrade (contact@openstates.org — an HG-09-class credential act) | GCS run rows under `gs://…-sig-restricted/ops/runs/openstates/` show `outcome=ok` with `document_outcomes` per jurisdiction; hosted `claim` count delta for `bill_*`/`legislative_bill` predicates; second run adds ~0 rows for unchanged records | three executions recorded honestly: `nx527` (full 226-request sweep fetched; sink refused a future-dated `latest_action_date` → `claim_observed_not_future`, fixed by the no-observed_at design), `jwm5f`/`clf7v` (persistent 429 → 12 `back_off` rate-limit events + 4 disappearances recorded, never bypassed); the connector, plan, vocabulary, rate-limit wiring, and fail-closed envelopes are deterministic-green (19 sweep tests + `make check` 3274) | DONE 2026-09-19 — exec `gl4gj` (2026-09-19T05:14Z): outcome=ok, **4,505 claims** landed in the hosted spine (226-request sweep fit the fresh 250/day window; GCS row `ops/runs/openstates/2026-09-19/2026-09-19T05-14-54+00-00.json`); idempotent re-run + spine-delta verification owed to the next weekly cron or a follow-up run (cites BL-055) |

## P26.13 (SOURCES.12) — open-data-catalog sweep: the gated remainder + verbatim-licence capture (2026-09-18)

The systematic catalog sweep (`docs/build/tools/catalog_sweep.py`;
artifacts `docs/build/reports/catalog_sweep_2026-09-18{,_qualified,_reviewed}.json`)
enumerated **3,257 candidates** across three catalog APIs (Socrata
`api.us.socrata.com`, ArcGIS Hub `hub.arcgis.com`, CKAN `data.gov.uk`) over the
ticket's seven-keyword set, qualified them into 278 camera-registry-shaped
point datasets, and wired the 10 clear-licence ones not already registered
(`camreg_washington_dc`, `camreg_nottingham_gb`, `camreg_york_gb`,
`camreg_glasgow_gb`, `camreg_northayrshire_gb`, `camreg_lambeth_gb`,
`camreg_peel_on`, `camreg_rochester_ny`, `camreg_goldcoast_au`,
`camreg_puertogaitan_co`) through the `dot_511` path with rights packets +
compartments. The honest remainder is operator/reviewer work, not code.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-SOURCES.12-1 | P | HG-03 — rights decisions for the gated sweep remainder: **257 camera-registry-shaped datasets with no clear verbatim licence grant** (the reviewed artifact's `gated_licence_registries` rows — 'See Terms of Use' links, 'No Conditions Apply'-class metadata, disclaimer-only text, and uncaptured terms pages), **68 probe-error rows** (DNS/SSL/503/no-layer ArcGIS + CKAN resource failures preserved as `error`, not dropped), and — named individually — **`camreg_stalbert_ab`** (ArcGIS item `6f2ca1b545844065b719483d562d553e`, 80 rows): the item names 'City of St. Albert Open Government License' but the grant text could not be captured verbatim (`data.stalbert.ca/pages/licence` is a JS-rendered shell — HTTP 200 with no terms in the served body; the Hub page item is not publicly searchable; no Wayback capture) | GL-GATE-06 covers only resolved-clear licences; flipping is a reviewer decision, not a code change; a licence NAME without captured grant text is not a rights basis | reviewer captures St. Albert's licence text verbatim (browser session or operator contact with open@stalbert.ca) and resolves each gated row's terms from the recorded `licence_verbatim`/probe evidence in the reviewed artifact | `sig-connectors gate --source camreg_stalbert_ab` goes green after a recorded flip; the reviewed artifact's gated rows are the enumerable worklist (`spdx=null` + `blocker` fields) | St. Albert's target row + source row are registered gated (`spdx = "LicenseRef-StAlbert-ODL-1.0"`, `redistributable = false`); the full 257-row gated set + 68 error rows are committed data in the reviewed artifact; nothing gated was fetched | PARTIAL 2026-09-19 — **the rights half is DONE (P26.16 / GL-GATE-07):** the operator's blanket disposition approved all 257 gated rows + `camreg_stalbert_ab`; `p2616_batch.py` flipped 151 sources (136 new + 15 existing incl. St. Albert), wired 181 targets, recorded 34 covered + 42 non-target enumerations, and the local live sweep ran 151/151 `ok` (2,033,676 claims / 389 captures / 0 refusals — `live_runs/p2616/*`, `reports/p2616_outcomes.json`, `runs/P26.16.md`). **True remainder:** the **68 probe-error rows** (DNS/SSL/503/no-layer + CKAN failures — infrastructure retries, P26.17/GL-GATE-08 scope) and the **hosted wave tail** (the apply itself ran 2026-09-19 under user-account auth — image `sig-api@sha256:79adb632…` via Cloud Build `472800c8`, 8 `sig-ingest-camreg-batch-*` jobs + 8 ENABLED schedulers deployed, wave-2 executions launched 07:07Z; at last check 7/8 batches complete (02/03/04/06 exit 0, emissions exactly matching local; 01/07/08 exit 1 on one transient member each — ALL THREE retried ok: dot_511_ga 83,653, txdot_rep_tx 33,900, arlington_va), batch-05 in-flight on osm 1.37M, +589,227 committed of ~2.03M attempted, batch-02 rerun proved +0 idempotency; owed = batch-05 completion + final spine count (all three transient members already retried `ok`) — run rows `gs://zeta-medley-508121-u7-sig-restricted/ops/runs/`). **2026-09-22 (P26.18 / SOURCES.17):** the *structural* blocker behind the batch-05/osm tail — `assert_claims` wrapping the whole 1.37M-claim source in ONE ~13h transaction that the task deadline rolled back — is **FIXED in the write path** (`PgClaimSink` now commits in bounded chunks; append-only + idempotency + resumability proven on real PG18, `tests/db/test_claim_sink.py` 7/7). The remaining **hosted OSM land + idempotent re-run** is split out to the cleaner **`D-SOURCES.17-1`** (kind V) — it is no longer a rights/infra remainder of this row but a live-gated run of the now-chunked path. (cites BL-055) |

## P26.14 (FEDERAL.1) — federal procurement depth: SAM.gov key's daily quota window (2026-09-18)

The widened SAM.gov sweep (19 bounded `title` queries over the reviewed
`sam_gov_sweep` keyword set) is implemented, tested, and deployed on
`sig-ingest-sam-gov` — but the key's api.data.gov **daily quota** was
exhausted by the two widened-sweep executions, and the API's own 429 body
names the reset: `2026-Sep-19 00:00:00+0000 UTC` (verbatim in
`docs/build/reports/live_runs/2026-09-18_sam_gov_quota_probe.json`). The
USAspending half of the ticket landed fully (38,837 emitted / +24,898 spine
rows, idempotent rerun proven). The SAM leg is a quota-window deferral,
not a code gap.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-FEDERAL.1-1 | V | hosted widened SAM.gov sweep landing — a successful `sig-ingest-sam-gov` execution emitting `procurement_notice` claims from the 19-keyword sweep, its GCS run row + per-slice outcomes, the hosted-spine delta for `sam_gov` claims, and an idempotent re-run | the key's api.data.gov daily quota is exhausted (429 body verbatim: `"You have exceeded your quota. You can access API after 2026-Sep-19 00:00:00+0000 UTC"`); two executions (`6p58k`, `g92x9`) were SIGKILLed at the task timeout fighting 429 backoffs — the refusal is recorded, never bypassed; no quota may be defeated | the daily quota window rolls over at 2026-09-19 00:00 UTC, the next monthly `sig-sched-sam-gov` cron (19 requests ≪ any daily tier), or an operator api.data.gov key-tier upgrade (HG-09-class credential act) | GCS run row under `gs://…-sig-restricted/ops/runs/sam_gov/2026-09-19/` shows `outcome=ok` with `claims_added` for the widened sweep; hosted `claim` delta for `procurement_notice` predicates; re-run adds ~0 rows | two timeout-killed executions recorded (no run rows — SIGKILL before `store_run_row`; the Cloud Run failure + the committed 429 probe body are the honest record); connector/sweep/dedupe/code deterministic-green (46 procurement tests + `make check` 3370); job taskTimeout raised 1800s→3600s so the post-reset run has headroom | OPEN 2026-09-18 (cites BL-055) — *(update 2026-09-19: two post-reset executions timed out — `lz4rx` at 3600s and `5z456` at the raised 14400s; each failed run's 429 retries re-consume the fresh daily window — refused requests count. Next landing path: the Monday 05:00 UTC cron on a fresh window, or a sweep-width reduction)* — **NARROWED 2026-09-22 by P26.19 (SOURCES.18, ADR-089): the STRUCTURAL blocker is FIXED — the sweep is now bounded to ONE daily quota window with headroom (`[sam_gov_sweep].daily_request_budget=30` ≥ the 19-keyword count → one run covers the whole space), the driver stops cleanly at the budget or on the FIRST 429 (`quota_reached`, remaining slices deferred, never re-probed), and the SAM.gov live transport is built `max_retries=0` so a 429 is never backed-off-and-retried (the re-burn that SIGKILLed `lz4rx`/`5z456`). Scheduling fires `0 5 * * 1` (Monday 05:00 UTC — a fresh window 5h after the 00:00 UTC reset). Deterministic half DONE (`tests/connectors/test_sam_gov_bounded_sweep.py` 12 + ops `quota_reached` outcome; `make check` 3434/3/0; DB 126/0; validator exit 0). The SOLE remaining item is the fresh-window hosted LAND — kept OPEN (kind V): no ADC-armed live shell here (`SIG_GCP_PROJECT`/`SIG_SAM_GOV_KEY` unset) AND the window's freshness cannot be established without a probe (the exact re-burn this fixes), so no live green was fabricated and the window was NOT touched. Re-run when armed on a fresh Monday-class window: `gcloud run jobs execute sig-ingest-sam-gov --region us-central1` (project `zeta-medley-508121-u7`, Cloud SQL `sig-pg`, key from Secret Manager `sig-sam-gov-key`) → expect outcome ok/quota_reached, `sweep_requests<=30`, `procurement_notice` claims land; then record the hosted spine delta + prove an idempotent re-run (+0). Alternative that removes the bound entirely: an operator api.data.gov key-tier upgrade (HG-09-class). See `docs/build/runs/P26.19.md`.** — **LIVE-PROVEN + NARROWED-TO-PAGED-TAIL 2026-09-22 (orchestrator hosted land, execution `sig-ingest-sam-gov-z99t7`):** rebuilt `sig-api:latest` via Cloud Build `d98404b7` (both P26.18/P26.19 fixes), updated the job to the new image, executed on a fresh window → **exit 0, +304 `procurement_notice` claims** (`sam_gov` 7→311), run row `gs://zeta-medley-508121-u7-sig-restricted/ops/runs/sam_gov/2026-09-22/2026-09-22T16-50-32+00-00.json`, detail `12/30 sweep request(s); quota_reached — 7 slice(s) deferred, not re-probed`. The P26.19 structural fix is PROVEN in production: **no SIGKILL, honest `quota_reached` stop before the 429 wall, no re-probe** (contrast the SIGKILLed `lz4rx`/`5z456`). **Remaining = the by-design paged-coverage tail only** — the 7 deferred slices are covered by successive `sig-sched-sam-gov` daily crons; an idempotent re-run (+0 on unchanged notices) is owed on the next cron. Structural + first-land obligation CLOSED; row kept OPEN solely for the multi-day paged tail. |

## P26.18 (SOURCES.17) — hosted land of the now-chunked OSM source (`camreg_osm_surveillance`)

The chunked-commit write path (`PgClaimSink.assert_claims` commits in bounded
`commit_chunk_size` transactions) is landed and proven deterministically on a
real PG18+PostGIS testcontainer: full re-run = +0, an interrupted-after-k-chunks
run resumes to the exact uninterrupted count, and no UPDATE/DELETE is issued at
runtime (`tests/db/test_claim_sink.py`, 7/7 green under `SIG_REQUIRE_DB_TESTS=1`).
The remaining leg is the **hosted land itself**: chunking makes the ~1.37M-claim
OSM ingest *durable/resumable* (surviving the Cloud Run task deadline) but not
*faster* — at the measured `db-f1-micro` throughput (~20–45 committed claims/s,
P26.16) the land is a **~12–13h operation**, and proving the idempotent hosted
re-run (+0) requires that first land to COMPLETE first. Neither fits a bounded
autonomous session, so the hosted green is recorded here honestly rather than
fabricated (implement-spec 5.3 / BM-DEFER-02). GCP access exists in this
environment (`14stevevitali@gmail.com`, project `zeta-medley-508121-u7`), so this
is a *time/scale* deferral, not a credential one.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-SOURCES.17-1 | V | hosted land of `camreg_osm_surveillance` (~1.37M claims) into the `zeta-medley-508121-u7` spine via the chunked-commit path, plus the idempotent hosted re-run (+0) and the true spine delta | the land is a ~12–13h single-source ingest on `db-f1-micro` (throughput-bound, not correctness-bound); the +0 re-run can only be measured after it completes — neither fits a bounded autonomous session; no hosted green may be fabricated | operator runs the exact commands below to completion, then re-runs to confirm +0 | GCS run rows under `gs://zeta-medley-508121-u7-sig-restricted/ops/runs/camreg_osm_surveillance/` show `outcome=ok` with `claims_added≈1.37M` on the first run and `claims_added` deduping to ~0 on the second; hosted `claim` count steps by ~1.37M then holds flat on the re-run | write-path FIXED + proven on real PG18 (`tests/db/test_claim_sink.py` 7/7; `make check` 3418 passed/3 skipped/0 failed); ops surface threads `SIG_COMMIT_CHUNK_SIZE` (`connectors/ops` CLIs + `scheduled_ingest`); recorded hosted baseline **1,059,533 claims / 210 sources @ 2026-09-22** (LEDGER CURRENT STATE) | OPEN 2026-09-22 (cites BL-055) — **exact re-run:** (1) rebuild the image via Cloud Build (Docker daemon may be down — remote build; `ops/Dockerfile`, repo-root context, linux/amd64): `gcloud builds submit --project=zeta-medley-508121-u7 --config=- . <<'CB'`⏎`steps:`⏎`- name: gcr.io/cloud-builders/docker`⏎`  args: ['build','-f','ops/Dockerfile','-t','us-central1-docker.pkg.dev/zeta-medley-508121-u7/sig/sig-api:latest','.']`⏎`images: ['us-central1-docker.pkg.dev/zeta-medley-508121-u7/sig/sig-api:latest']`⏎`CB` ; (2) set the chunk size on the OSM job (10k ≈ 4–8 min/chunk ≪ the task timeout): `gcloud run jobs update sig-ingest-camreg-batch-05 --project=zeta-medley-508121-u7 --region=us-central1 --image=us-central1-docker.pkg.dev/zeta-medley-508121-u7/sig/sig-api:latest --update-env-vars=SIG_COMMIT_CHUNK_SIZE=10000` ; (3) run the single source to completion (fresh 36h window; comma splits args only — the space-separated exec string stays whole, the verified P26.16 form): `gcloud run jobs execute sig-ingest-camreg-batch-05 --project=zeta-medley-508121-u7 --region=us-central1 --args "-c,exec sig-ops scheduled-ingest --source camreg_osm_surveillance --sink pg"` ; (4) idempotent re-run — repeat (3) and confirm the per-source spine count holds (+0). Alternative noted (out of scope for this ticket): a temporary Cloud SQL instance bump would raise throughput and shorten the land, but the fix is the write path, not the hardware. — **LAND IN PROGRESS 2026-09-22 (orchestrator drove the recorded procedure):** rebuilt `sig-api:latest` via Cloud Build `d98404b7` (contains the chunked-commit path), `gcloud run jobs update sig-ingest-camreg-batch-05 --image=…/sig-api:latest --update-env-vars=SIG_COMMIT_CHUNK_SIZE=10000`, then executed `--args "-c,exec sig-ops scheduled-ingest --source camreg_osm_surveillance --sink pg"` → execution **`sig-ingest-camreg-batch-05-p964s` RUNNING** (started 2026-09-22T16:49Z; new image imported cleanly, container running, no early rollback — the chunked path is committing progressively). **Owed on completion (~12–13h):** confirm ~1.37M claims landed (hosted `claim` steps ~1,059,533 → ~2.43M), run the idempotent re-run (+0), record the true spine delta. Row stays **OPEN** until the execution completes and the +0 re-run is verified — no hosted green fabricated. |

## P27 (public launch) — requirement-id spec_src fold-backs (ADR-090/091/092/093)

The P27 cross-cutting decisions are recorded as **ADR-090…094** with concrete "Proposed spec_src
amendment" sections, and those ADRs are already in **Appendix F** (`check_spec_src.py` OK, 671 ids
UNCHANGED). What is deferred is the **minting of the new requirement ids** the ADRs name: per
**SIG-ENG-039** a requirement id's `spec_src` paragraph + Appendix F + coverage rows land in the
**same PR that introduces it** — i.e. the implementing ticket's PR — so the fold-back is done
downstream, ticket-by-ticket, not in this planning pass (minting them now would add MISSING/unowned
ids ahead of the code). ADR-094 (rights) is operational and mints no id.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-P27-SPEC-1 | H | the P27 requirement-id `spec_src` fold-backs — the new SIG-UI/SIG-EXPORT paragraphs named in ADR-090 (national surface, §39–41/§38), ADR-091 (public-island allowance, §40), ADR-092 (observation-vs-resolved disclosure + spine-read export, §29/§38), ADR-093 (national IA + real metadata, §39–41) — each folded into `spec_src` + `BUILD.sh` + an Appendix F/coverage row + the `FOLD_BACK_IDS`/`EXPECTED_IDS` bump in `check_spec_src.py` | by design (SIG-ENG-039): a requirement id's `spec_src` paragraph + Appendix F + coverage rows MUST land in the SAME PR that introduces it → the implementing ticket's PR, not this planning pass | the implementing tickets land: P27.4/P27.6 (ADR-090/093 UI+export ids), P27.9 (ADR-091 island id), P27.3/P27.4 (ADR-092 resolution/export ids) | in the implementing ticket's PR: mint the id in its `spec_src` paragraph, add its Appendix F + `COVERAGE_MATRIX.csv` rows, bump `check_spec_src.py` `FOLD_BACK_IDS`, `sh …/BUILD.sh` → `check_spec_src.py` exit 0 at the new count | the decisions are fully articulated as ADR-090…094 (in Appendix F now); each ADR's "Proposed spec_src amendment" section names the exact file + requirement; `check_spec_src` green at 671 | DONE 2026-09-22 (cites BL-056) — **P27.3 update 2026-09-22:** the ADR-092 share landed — `SIG-RECON-058` (§29, observation-vs-resolved disclosure) + `SIG-EXPORT-012` (§38.1, spine-read shaped export) minted in spec_src, spec regenerated, checkers bumped to **673 ids** (668+5 fold-backs), coverage rows added; the ADR-090/091/093 shares remain owed to P27.4/P27.6/P27.9 — row stays OPEN — **P27.4 update 2026-09-22:** the ADR-090 **export** share landed — `SIG-EXPORT-013` (§38, the national/whole-graph spine-read export bundle to the frozen P27.1 surfaces, breadth bounded by the fail-closed licence gate) minted in `spec_src/80_partVII_s37to38_api.md`, spec regenerated via `BUILD.sh` (byte-identical), `check_spec_src.py` FOLD_BACK_IDS + `check_coverage_matrix.py` EXPECTED_ROWS bumped to **674**, coverage row added (both checkers exit 0); the ADR-090 **§39–41 SIG-UI** dossier-index id + the ADR-093 (national IA) + ADR-091 (public-island §40) shares remain owed to P27.6/P27.9 — row stays OPEN — **P27.6 update 2026-09-22:** the ADR-090 **§39–41 SIG-UI** dossier-index id (`SIG-UI-048`, §39.2 — national/whole-graph surface + per-jurisdiction dossier index) **and** the ADR-093 national-IA id (`SIG-UI-049`, §39.9 — grouped nav + national landing with named denominators/no total + real manifest-sourced canonical origin/as_of/ruleset) minted in `spec_src/81_partVII_s39to41_ui.md`, spec regenerated via `BUILD.sh` (byte-identical, 594749 bytes), `check_spec_src.py` FOLD_BACK_IDS + `check_coverage_matrix.py` EXPECTED_ROWS bumped to **676 ids** (674+2), coverage rows added (both checkers exit 0); the **sole remaining share** is ADR-091 (public-island §40) owed to **P27.9** — row stays OPEN — **P27.9 update 2026-09-22 (DONE — final share):** the ADR-091 §40 public-island share landed — **`SIG-UI-050`** (§40, the no-JS-fallback invariant for the three named public interactive islands map/graph/search, extending the ADR-068 island allowance) minted in `spec_src/81_partVII_s39to41_ui.md` and **`SIG-UI-047`** annotated to record it is realised at Phase 27 (the A1/SIG-UI-047 revisit — the MapLibre island is now built, ADR-091/ADR-097); spec regenerated via `BUILD.sh` (byte-identical), `check_spec_src.py` `FOLD_BACK_IDS` + `check_coverage_matrix.py` `EXPECTED_ROWS` bumped to **677 ids** (676+1), coverage rows added (SIG-UI-050 new + SIG-UI-047 revisit note; both checkers exit 0); ADR-097 recorded + Appendix F row + adr-index regenerated. This was the last of the five ADR-090…093 fold-back shares — **all shares landed, row CLOSED.** |

## P27.1 (LAUNCH.1) — the OSM-complete re-audit obligation (2026-09-22)

P27.1 ran the read-only public-surface audit against the hosted spine **while** the
`camreg_osm_surveillance` OSM land (~1.37M claims, D-SOURCES.17-1) was executing
concurrently. Every live count in `docs/build/reports/PUBLIC_SURFACE_AUDIT.md` is therefore
an explicit **as-of snapshot** (2026-09-22T16:57Z, "OSM land in flight"), NOT a settled launch
total. The audit VERB is deterministic + re-runnable over any spine — that is the durable
deliverable; the numbers are provisional. The operator consciously chose to audit now and
re-audit later. This row tracks that owed re-audit.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-P27.1-1 | V | re-run `sig-exports audit` against the hosted spine AFTER the `camreg_osm_surveillance` OSM land (D-SOURCES.17-1) completes, refresh `PUBLIC_SURFACE_AUDIT.md` + `public_surface_audit.json`, and re-confirm the launch-scope split before the export/launch is frozen | the P27.1 snapshot was taken mid-OSM-land (spine growing ~1.06M → ~2.43M); the audit numbers are provisional until the land settles — no settled launch total may be frozen off a mid-flight snapshot | D-SOURCES.17-1 lands (`camreg_osm_surveillance` hosted ingest completes + idempotent +0 re-run); then the spine count holds flat | re-run: `cloud-sql-proxy --port 5433 zeta-medley-508121-u7:us-central1:sig-pg &` ; `export PGPASSWORD="$(gcloud secrets versions access latest --secret=sig-pg-password --project=zeta-medley-508121-u7)"` ; `uv run python -m exports audit --dsn "postgresql://sig@127.0.0.1:5433/sig?sslmode=disable" --as-of "$(date -u +%Y-%m-%dT%H:%M:%SZ)" --note "OSM land complete — re-audit" --markdown-out docs/build/reports/PUBLIC_SURFACE_AUDIT.md --json-out docs/build/reports/public_surface_audit.json` — expect claims ≈ 2.43M, geolocated/UNDETERMINED refreshed, then P27.2 re-scopes off the settled numbers | the deterministic verb + the 2026-09-22 provisional snapshot are committed; the re-audit is a re-run of the same read-only verb (no code change) | OPEN 2026-09-22 (cites BL-056) — **P27.2 update 2026-09-22:** the audit verb now reports the effective-rights view alongside the as-asserted view (`undetermined_effective`, `publishable_effective`, `rights_decisions` table — schema `p27.2/1.0.0`); the post-decision re-measure ran (1,109,799 claims, effective UNDETERMINED 0, publishable 1,109,793/1,109,799) but the spine is STILL mid-OSM-land, so this row stays OPEN until D-SOURCES.17-1 completes and the settled re-audit runs — **P27.3 update 2026-09-22:** the `sig-exports shape` live verification ran twice mid-land (watermark 1,149,799 → 1,159,799 claims in ~15 min — land visibly in flight); shaped numbers committed under `docs/build/reports/p27.3-shaped/` + `P27.3_LIVE_SHAPING.md` are PROVISIONAL and must be re-derived after the land settles alongside this re-audit |

## P27.4 (LAUNCH.4) — the live spine-backed export build (2026-09-22)

P27.4 landed the real spine-backed export path (`sig-exports build --from-spine --dsn …`), proven
deterministically (`tests/exports/test_spine_export.py`, 21 pure tests) and over a real seeded
PG18+PostGIS testcontainer (`tests/db/test_spine_export_over_seeded_spine.py`, 2 tests). The live
half — a real build over the hosted `sig-pg` — needs cloud-sql-proxy + credentials + network egress,
none present in this isolated context, and the OSM land is still in flight (D-P27.1-1 / D-SOURCES.17-1),
so any live bundle would be PROVISIONAL. Recorded here per the ledger's never-fabricate-green rule.

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-P27.4-1 | V | run `sig-exports build --from-spine --dsn <hosted> --out exports/out/national` over the hosted `sig-pg` (read-only) AFTER the OSM land settles, producing the national `exports/out/*` bundle + the ten `web/*.json` surfaces + per-compartment PMTiles + `provenance.ttl` + `exclusions.json`, and confirm `assert_separated` + the fail-closed gate over the real licence mix | this isolated context has no cloud-sql-proxy access / credentials / network egress, and the `camreg_osm_surveillance` OSM land is still in flight (cross-ref D-P27.1-1 / D-SOURCES.17-1) → any live bundle's as-of is PROVISIONAL until the land settles; secrets are env-only (HG-09) | D-P27.1-1 / D-SOURCES.17-1 settle + operator arms an ADC/cloud-sql-proxy run shell with `PGPASSWORD` | re-run: `cloud-sql-proxy --port 5433 zeta-medley-508121-u7:us-central1:sig-pg &` ; `export PGPASSWORD="$(gcloud secrets versions access latest --secret=sig-pg-password --project=zeta-medley-508121-u7)"` ; `uv run python -m exports build --from-spine --dsn "postgresql://sig@127.0.0.1:5433/sig?sslmode=disable" --as-of "$(date -u +%Y-%m-%dT%H:%M:%SZ)" --note "OSM land complete" --out exports/out/national` — expect a compartmented bundle (osm_physical ODbL + sig_graph CC-BY), 10 web/*.json to the P27.1 contract, PMTiles per compartment, manifest with real as_of/ruleset/licences + PROV-O | deterministic pure tests (21) + the Docker seeded-spine e2e (2) prove the build path end-to-end over a real PG18+PostGIS spine; `run_spine_export` opens one `REPEATABLE READ READ ONLY` snapshot (read-only, no spine write) | OPEN 2026-09-22 (cites BL-056) — live half genuinely un-runnable here (no proxy/creds/egress) + OSM land in flight → PROVISIONAL; no live green fabricated |

## P27.5 (LAUNCH.5) — presentation analytics not emitted by the export (2026-09-22)

P27.5 routed every public surface through the export data seam (`web/src/lib/data.ts`): the nine
frozen P27.1 surface artifacts are read fixtures|export and FAIL LOUD in export mode on a missing
artifact. Three classes of page data are NOT part of the frozen P27.1 export contract and so are
served through the seam as committed constants in BOTH modes (never a fail-loud artifact): the map's
national-view density bins, the network explorer's centrality/ER-quality analytics + focus entity,
and the page-scoped presentation constants (decision point, capture diff, provenance summaries,
jurisdiction claims, editorial review/rationale templates, reference-page exemplars). Also: the
DB-less fixture `sig-exports build --jurisdiction okc` path emits only `dossiers.json`/`leverage.json`
(not the ten surface JSONs), so export-mode exercise uses the pure-web `npm run export:fixtures`
overlay harness (or a real `--from-spine` bundle, D-P27.4-1).

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-P27.5-1 | E | either emit the map density-bins + network centrality/ER-quality analytics into the export (a new artifact or an extension of `map.json`/`network.json`), OR ratify them as presentation-only; and decide whether the fixture `--jurisdiction` export path should emit the ten P27.1 surface JSONs so a DB-less bundle is complete without the web overlay harness | the P27.1 contract (frozen in P27.1) deliberately omits these analytics; adding them to the export is a shaping/producer decision that belongs with the export owner (P27.4) and the table-shape finalisation (P27.7), not this pure-`web/` wiring ticket; density/centrality need a spine analytics pass that does not exist yet | P27.7 (detailed table shapes) or a spine analytics export decides the shape; then P27.4's producer emits it and the seam reads it fixtures|export | today: `getMapDensityBins`/`getNetworkCentrality`/etc. return committed constants identical in both modes (routed through the seam so no page imports a fixture); when emitted, switch those getters to the fixtures|export fail-loud pattern the nine surface getters use and delete the overlay's coverage of them | the nine P27.1 surfaces fail loud on a missing artifact (proven: `tests/unit/data.test.ts` + a build with `watch.json` removed exits 1); the analytics render identically in both modes and carry no export artifact, so they cannot silently fabricate a surface | OPEN 2026-09-22 (cites BL-056) — **P27.6 note 2026-09-22:** backlog home stamped (BL-056, the P27 launch-round umbrella); row unchanged otherwise — P27.6 is a pure-`web/` IA/landing change that does not emit these analytics (they remain routed through the seam as both-mode constants), so D-P27.5-1 stays OPEN for P27.7/the export producer |

## P27.8 (LAUNCH.8) — the hosted national public cut-over (2026-09-22)

The deterministic public-cut-over machinery landed (export-mode build fail-loud, the
published-vs-restricted compartment partition + leak guard, the deploy plan, the gate
package). The **hosted** cut-over itself — building `web/dist` from the real national
export, syncing the published compartment to the public buckets, and probing the live
surface — is live-gated and honestly deferred (no fabricated green, no fabricated URL).

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-P27.8-1 | V | the hosted national public cut-over: build `web/dist` from the real national export, run the published-only GCS sync to `…-sig-web` + `…-sig-public` (keeping `…-sig-restricted` PRIVATE), probe the live `sig-web` + public export manifest (`sig-ops probe-hosted`), confirm the real national data renders (not the OKC demo), and record the built-from spine/export watermark | the national export `exports/out/national` does **not** exist and cannot be validly built yet — it chains to **D-P27.4-1** (the national spine-backed export build) and **D-SOURCES.17-1** (the ~1.37M `camreg_osm_surveillance` OSM land, *LAND IN PROGRESS* — until it settles + the +0 re-run confirms, any national export/audit is PROVISIONAL, D-P27.1-1). ADC is present in the run shell but there is nothing national to publish; a fixtures fall-back is forbidden and a mid-flight PROVISIONAL total may not be frozen (cross-ref D-ACCT.1-1/D-DEPLOY.1-1 for the HG-12 ADC prerequisite) | D-SOURCES.17-1 lands (+0 re-run) → D-P27.1-1 re-audit settles → D-P27.4-1 produces `exports/out/national`; then an operator ADC/`cloud-sql-proxy` run shell is armed | re-run (ADC-armed, after the national export exists): `sig-ops deploy --target gcp --prepare-only` (builds `web/dist` from `exports/out/national`, partitions into `exports/out/{public,restricted}`, PROVES the public compartment clean — fail-loud if the export is absent) → then `sig-ops deploy --target gcp` (no `--dry-run`) runs the plan's syncs → `SIG_PROBE_WEB_URL=https://surveillancegraph.org SIG_GCP_PROJECT=zeta-medley-508121-u7 sig-ops probe-hosted` green on `sig-web-root` + `sig-public-national-manifest` → record `sig-ops` `export_watermark` (`release_id`/`as_of`/`ruleset`). Canonical origin `https://surveillancegraph.org` (DNS/TLS is P27.10 — until then use the `*.run.app` fallback) | the deterministic producer is proven end-to-end WITHOUT network: `sig-ops deploy --prepare-only` fails loud today (national export absent — the correct honest state); `tests/ops/test_publish.py` (24) proves the export-mode fail-loud build + the partition + `assert_public_clean` catches an injected ODbL/UNDETERMINED/stray leak over the SHIPPED licence data; the deploy plan keeps sig-public published-only + sig-restricted PRIVATE | OPEN 2026-09-22 (cites BL-056) — hosted cut-over genuinely blocked on the national export (D-P27.4-1 → D-SOURCES.17-1 in flight); no live green fabricated, no deployed URL claimed. HG-01/HG-11/HG-02 are answered (carry-forward, LEDGER GATE DECISIONS 2026-09-22); the operator ticks Go-public at cut-over |
| D-P27.10-1 | P | the operator's Squarespace DNS action (HG-11 / Go-public human step) + the post-DNS live verification: add the two `A` records at Squarespace (apex `@` `A` → `136.81.80.102`, `www` `A` → `136.81.80.102`), then confirm (1) the Google-managed cert `sig-web-cert` provisions to ACTIVE, (2) valid TLS on `https://surveillancegraph.org` + `https://www.surveillancegraph.org` (www 301→apex), and (3) `SIG_PROBE_WEB_URL=https://surveillancegraph.org SIG_GCP_PROJECT=zeta-medley-508121-u7 sig-ops probe-hosted` is green on `sig-web-root` | adding the DNS records is a **live human decision** (HG-11 / Go-public, never auto-ticked); Google-managed TLS provisions **only after** the records resolve, so valid-TLS + `probe-hosted` cannot complete until the operator acts and DNS propagates. The mapping IaC is fully applied (the LB fronts `sig-web` with a reserved static IP + a PROVISIONING managed cert); this row holds the DNS-dependent remainder honestly, not a fabricated green. NOTE: until the national deploy `D-P27.8-1` lands (blocked on the OSM land), the domain serves the OKC demo — operator-accepted (GATE DECISIONS 2026-09-23) | the operator adds the two `A` records at Squarespace (registrar+DNS) + DNS propagates (~minutes to ~48h TTL) | after the records resolve: `gcloud compute ssl-certificates describe sig-web-cert --global --project zeta-medley-508121-u7 --format='value(managed.status)'` → `ACTIVE`; `curl -sSI https://surveillancegraph.org/` → `200` with valid TLS; `curl -sSI https://www.surveillancegraph.org/` → `301` → `https://surveillancegraph.org/`; `SIG_PROBE_WEB_URL=https://surveillancegraph.org SIG_GCP_PROJECT=zeta-medley-508121-u7 sig-ops probe-hosted` green | the LB is applied for real (2026-09-23, operator ADC): static IP `136.81.80.102` reserved, serverless NEG → `sig-web`, backend + managed cert (apex+www, PROVISIONING) + HTTPS url map (apex→backend, www→apex 301) + HTTP→HTTPS redirect + :443/:80 forwarding rules all created; `ops/gcp/domain-mapping.sh --check` is green without ADC and `--apply` is idempotent (re-run skips every create); the exact DNS records are produced from the realized IP | OPEN 2026-09-23 (cites BL-056) — the LB mapping is applied + the real DNS records produced; the operator's Squarespace DNS add + the post-DNS TLS/`probe-hosted` verification are genuinely gate-pending (HG-11 / Go-public), no live green fabricated |

## Round 6 (Depth & Resolution) + Round 7 (Activation, Trust & Reach) — seeded 2026-09-23

Two standing deferrals recorded at the Round 6/7 seed (planning session, `devin/round6-7-seed`,
off the P27.10 tip). Both are opened by the ADRs minted at seed — `D-R6.1-EVAL` by **ADR-099**
(materialize resolution at scale + the measured eval loop, owned by P28.1) and `D-R7.1-AUTH` by
**ADR-100** (identity minimization, owned by P29.1) — and both home to **BL-057** (the Round 6/7
backlog umbrella). Design rationale: `docs/build/reports/ROUND6_7_DEPTH_ACTIVATION_DESIGN.md`
(§2.6, §3.4).

| id | kind | item | why deferred | unblocked by | how to verify | proxy now | status |
|---|---|---|---|---|---|---|---|
| D-R6.1-EVAL | F | Re-derive the auto-vs-human resolution balance from FIRST PRINCIPLES once real ground truth exists (§2.6): re-estimate P/R/R on a *human* holdout and re-tune `auto_write_precision_threshold` + the tier cutoffs against it; re-assess whether the LLM adjudicator stays trustworthy (κ) as models drift; formalize the active-learning loop + the cost/benefit of human review vs marginal precision; consider a decision-theoretic loss (a false merge in a surveillance-accountability record is asymmetrically worse than a false split) rather than pairwise precision. The first-pass thresholds (κ ≥ 0.7, precision ≥ 0.98) are explicitly PROVISIONAL and must not ossify. | P28.1's gold set is LLM-bootstrapped with a small maintainer/agent seed and the auto-write floor is set against PROVISIONAL labels — honest and usable, but not the final word; the real human-labelled ground truth (field-verified sites, contributor-verified matches, a substantially larger human-adjudicated holdout) does not exist yet. Gate-relevant: a launch claim of "resolved sites" MUST disclose it rests on a provisional (LLM-bootstrap) eval until this closes. | real human ground truth accumulates — field verification, contributor `/dispute` + review-queue decisions periodically re-`build_gold_set` + re-freeze a new holdout version (old holdout stays immutable), and/or a dedicated human-adjudication pass | after P28.1 lands: re-`build_gold_set` from the live loop's human decisions → re-freeze a versioned holdout → re-measure P/R/F1 + B-cubed + LLM-vs-human κ on the *human* holdout → re-tune the floor + tier cutoffs against it and record the new values in a NEW ADR (SIG-ENG-003), never a silent threshold edit; the "resolved" surfaces drop the provisional-eval disclosure only then | P28.1 ships the measured floor + `demote_auto_write_tiers` (auto-demotes any tier below floor every ER pass) + `cluster_shape_alerts` + active-learning routing + the published holdout metrics (§32/SIG-METRIC-008b), and every "resolved sites" surface carries the provisional-eval disclosure — honest and self-correcting, but explicitly not the re-derived-from-first-principles balance | OPEN 2026-09-23 (cites BL-057); **P28.1 landed 2026-09-23** — the measured (PROVISIONAL) first-pass evidence now exists: LLM-vs-seed κ=**0.714** (bar 0.70 → LLM trusted), auto-write floor raised to **0.98**, tier-0 holdout precision **1.000** (0 demotions), B-cubed holdout **F1=0.976**, **4 disputed** + **14 routed to review**, cluster-shape alerts 0 (`docs/build/reports/P28.1_resolution_eval.md`, generator `scripts/eval/run_resolution_eval.py`). The write path is materialized + proven over real PG18+PostGIS (append-only, idempotent +0). **STILL OWED (why still OPEN):** (a) the *scale* eval over the ~75k hosted observations + the *live-over-hosted* materialization — blocked until the `resolution_materialize` sqitch change is deployed to the hosted spine AND an operator-held read/materialize role runs it (Cloud SQL `postgres` is not a superuser and cannot assume the read roles); re-run `sig-reconcile materialize --dsn $SIG_PG_DSN` then re-generate the report — OSM land D-SOURCES.17-1 still in flight; (b) the re-derivation from FIRST PRINCIPLES against *human* ground truth (the thresholds above are explicitly provisional, must not ossify) |
| D-R7.1-AUTH | P | Public AUTHENTICATED contribution via OAuth against an external IdP (OSM and/or GitHub) — store only a pseudonymous external subject id + tier, NEVER passwords or PII — behind anti-poisoning + a written moderation/abuse/safety plan + a threat model for contributor exposure (§3.4). "Identity without an account system." A deliberate LATER decision with its own ADR + ticket; NOT built now and NOT built speculatively. No homegrown username/password account system, ever (ADR-100). | The first-pass posture (anonymous corrections + OSM-mediated contribution-back + issue/invite-based pseudonymous tier-token curators, ADR-100/P29.1) covers the pilot; a public account/auth surface is the wrong trade for an adversarial-domain pilot (Sybil/poisoning at scale; contributor account data is itself sensitive) until there is demonstrated demand AND a ratified moderation/safety plan AND a contributor-exposure threat model — none of which exist yet. | demonstrated demand for public authenticated contribution beyond the vetted curator set + a ratified moderation/abuse/safety plan + a contributor-exposure threat model (operator decision) | when unblocked: author a NEW ADR + ticket for external-IdP OAuth (pseudonymous subject id + tier only; no passwords/PII) behind anti-poisoning + the safety plan; verify no credential/PII is stored and the anti-poisoning + append-only actor-id path is intact; never a flip to open signup | P29.1 productionizes the issue/invite-based pseudonymous tier-token curator store (replacing demo `CURATION_KEYS`) for a small vetted set; the anonymous `/dispute` public channel + OSM-mediated contribution-back cover everything else — no new PII surface, no accounts to breach | OPEN 2026-09-23 (cites BL-057) |
| D-R6.2-EDGES | V | The *live-over-hosted* sharing/access relationship materialization — run the P28.2 edge materializer over the real hosted `sig-pg` spine so the `relationship` table holds real vendor↔operator / data-sharing / RTCC-fusion edges (not just the seeded-DB proof). | The full deterministic edge path is built + PROVEN over real PG18+PostGIS (`tests/db/test_relationship_materialize.py`: sqitch `relationship_materialize` deploy/verify + append-only insert + idempotent +0 + asymmetry-kept-visible + read-seam round-trip), but three things gate the hosted run, exactly as P28.1's D-R6.1-EVAL live half: (a) the `relationship_materialize` sqitch change is not yet deployed to the hosted spine; (b) Cloud SQL `postgres` is not a superuser and cannot assume the read/materialize roles (an operator-held role runs it) — see P28.1's finding; (c) the hosted spine currently holds few/no sharing-partner **claims** — the `flock_portal`/`audit_structural` connectors emit configured-access edges as `configured_access_edge` **non-claim** records that `PgClaimSink` does not persist as L1 claims, so the materializable input is thin until that emit-as-claim path is wired (a separate connector concern) and OSM land `D-SOURCES.17-1` completes. No hosted edge count fabricated. | the `relationship_materialize` migration deploys to the hosted spine + an operator-held read/materialize role is available + sharing-partner claims exist in the hosted spine (connector emit-as-claim wired and/or OSM land `D-SOURCES.17-1` lands) | when unblocked: deploy the sqitch change to hosted, then `uv run python -m reconcile materialize-edges --dsn "$SIG_PG_DSN" --role <materialize_role>` (over cloud-sql-proxy under operator ADC), then derive closure `uv run python -m inference access-paths --dsn "$SIG_PG_DSN" --source <org>` and re-run `tests/db/test_relationship_materialize.py`; record the real edge counts by access kind in a report — never a fabricated hosted count | P28.2 ships the full deterministic materializer (`reconcile materialize-edges`, append-only + idempotent +0), the P08.2 §29.3 reconciler + P12.2 closure CONSUMED (not re-implemented), the append-only `relationship` write proven over real PG18+PostGIS, and the `read_materialized_edges` seam the P28.5 surface refresh (ADR-101) reads — honest and self-correcting, but the hosted rows await the deploy + role + claim inputs above | OPEN 2026-09-23 (cites BL-057) |
| D-R6.3-CONTRADICTIONS | V | The *live-over-hosted* contradiction materialization — run the P28.3 contradiction materializer over the real hosted `sig-pg` spine so the `contradiction` table holds the real, live contradictions (e.g. the 299-vs-190 count disagreement at scale) with both evidence sides, not just the seeded-DB proof. | The full deterministic path is built + PROVEN over real PG18+PostGIS (`tests/db/test_contradiction_materialize.py`: sqitch `contradiction_materialize` deploy/verify + the 299-vs-190 count disagreement DETECTED and WRITTEN with both claim ids kept VISIBLE + append-only insert + idempotent +0 + lifecycle supersession as a NEW row + `read_materialized_contradictions` round-trip), but the hosted run is gated exactly as P28.1's D-R6.1-EVAL / P28.2's D-R6.2-EDGES live half: (a) the `contradiction_materialize` sqitch change is not yet deployed to the hosted spine; (b) Cloud SQL `postgres` is not a superuser and cannot assume the read/materialize roles (an operator-held role runs it) — see P28.1's finding; (c) the resolved-spine contradiction volume grows as OSM land `D-SOURCES.17-1` completes. No hosted contradiction count fabricated. | the `contradiction_materialize` migration deploys to the hosted spine + an operator-held read/materialize role is available + resolved-spine claims exist in the hosted spine (OSM land `D-SOURCES.17-1` lands) | when unblocked: deploy the sqitch change to hosted, then `uv run python -m reconcile materialize-contradictions --dsn "$SIG_PG_DSN" --role <materialize_role>` (over cloud-sql-proxy under operator ADC), then re-run `tests/db/test_contradiction_materialize.py`; record the real contradiction count (by type) in a report — never a fabricated hosted count | P28.3 ships the full deterministic materializer (`reconcile materialize-contradictions`, append-only + idempotent +0 + lifecycle-aware supersession), the §29 reconciliation (`reconcile.counts`) + the §31 contradiction object (`reconcile.contradiction`) + the model lifecycle CONSUMED (not re-implemented), the append-only `contradiction` write proven over real PG18+PostGIS with the 299-vs-190 kept VISIBLE, and the `read_materialized_contradictions` seam (the `_persisted_contradictions` shape) the P28.5 surface refresh reads — honest and self-correcting, but the hosted rows await the deploy + role + claim inputs above | OPEN 2026-09-23 (cites BL-057) |
