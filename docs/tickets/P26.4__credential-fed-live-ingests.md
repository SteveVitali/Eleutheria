<!--
  P26.4 — credential-fed live ingests. Drafted 2026-09-17 after the operator
  provided the OpenStates + SAM.gov API keys (stored in Secret Manager as
  sig-openstates-key / sig-sam-gov-key — never in files). Closes D-SOURCES.2-1.
  Registered in the manifest chain table (row 101).
-->
# P26.4 — Credential-fed live ingests: OpenStates + SAM.gov (P26)

- **Semantic id:** `SOURCES.4` · **Phase:** 26 · **Kind:** ticket · **Tag:** p26-sources
- **base_branch:** `devin/p26-3-ungate-expansion` · **Depends on:** P26.3 (the connectors + targets exist; both sources are `ingestion_permitted=true` and refused only for want of a key)
- **Run:** `implement-spec spec=docs/tickets/P26.4__credential-fed-live-ingests.md live_verification=true`
- **Gate status:** engineering — the operator supplied the keys (HG-09 satisfied for these two); GL-GATE-06 flips already landed.
- **Live stage:** real keyed API fetches into the hosted spine.

## Goal
`openstates` and `sam_gov` are permitted, connector-implemented, target-registered —
and refused only because the key env vars were absent. The keys now exist in Secret
Manager (`sig-openstates-key`, `sig-sam-gov-key`). Wire them, run the real ingests,
close `D-SOURCES.2-1`.

## In scope — deliverables
1. **Secret wiring.** `sig-ingest-openstates` + `sig-ingest-sam-gov` Cloud Run jobs
   created on the established pattern (`SIG_PG_*` env + `--set-secrets
   SIG_OPENSTATES_KEY=sig-openstates-key:latest` /
   `SIG_SAM_GOV_KEY=sig-sam-gov-key:latest`); cadence rows in `ops/cadence.toml`
   already exist — attach scheduler triggers (monthly per the cadence table).
2. **Real live runs** — `sig-ops scheduled-ingest --source openstates` and
   `--source sam_gov` through the gated runner against the hosted spine:
   - OpenStates: bounded state-scoped bill queries (v3; `per_page` bound per the
     target row); bills index as `index_only` evidence links — a pending bill is
     not a §11.14 LegalInstrument.
   - SAM.gov: bounded keyword+window opportunity search (`limit` + posted window
     per the target row) → `procurement_notice` records.
   - Idempotent re-run proven (no duplicate claims).
3. **Records.** Outcome rows in `docs/build/reports/live_runs/` + run rows under
   `gs://…-sig-restricted/ops/runs/`; `D-SOURCES.2-1` flipped to DONE with the real
   claim counts; the permitted→configured→ingested ladder re-measured.
4. **Key-hygiene.** A test asserting no key literal appears anywhere in the repo or
   committed reports (the P21.3 no-token-literal convention), and the secrets are
   referenced only by Secret Manager name.

## Out of scope
- documentcloud/courtlistener (rights stay unresolved — operator deferred);
  primegov (hard robots wall); any new source classes.
- Changing what the connectors emit — the vocab/target rows already define the
  bounded query shapes.

## Acceptance criteria
- [ ] both jobs exist with secret bindings; scheduler triggers attached per cadence *(live-gated)*
- [ ] real keyed runs land claims in the hosted spine with evidence+locators; outcomes recorded *(live-gated)*
- [ ] idempotent re-run verified; `D-SOURCES.2-1` → DONE *(deterministic + live-gated)*
- [ ] no key literal anywhere; `make check` + build-memory checks green *(deterministic)*

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`: secrets are
environment/Secret-Manager only (HG-09); append-only spine; honest outcome
recording; the ingestion gate is never bypassed — these sources were already green.
