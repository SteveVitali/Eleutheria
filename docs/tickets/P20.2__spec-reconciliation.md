# P20.2 — Spec reconciliation: ticket-added scope, ADR dispositions, Appendix F/G, spec_src amendments (planning Phase D)

- **Sequence:** 53 of 63
- **Phase:** 20 — Reconciliation & release (`docs/build/DECISION_MEMO.md` §3 row 53)
- **base_branch:** current checkout — the P20.1 branch
- **Depends on (must land earlier in the stack):** P20.1 (`BACKLOG.csv` landings; `CAPSTONE_CLOSURE.md` ACCEPTED list signed under HG-14).
- **Run:** `implement-spec spec=docs/tickets/P20.2__spec-reconciliation.md live_verification=false`
- **Gate status:** **GATE HG-13 — normative amendments.** The block below is edited by the operator **before** the run; only ticked amendments are applied to `spec_src`. Unticked ones are still written into `SPEC_RECONCILIATION_PLAN.md` as proposals with the exact diff. Non-normative corrections (Appendix F index, Appendix G corrections, §52 phase-text alignment with what ADRs already record) need no tick.
  - *Gate answers may arrive via the build ledger's `GATE DECISIONS` table instead of an edit to this file (the orchestrator pauses before this ticket and records the operator's answers there). In that case copy them into this block in your first commit. An answer of "skip" means: run ungated as described, and list the ticket in the ledger's RETURN PASS table.*

  **Approved normative amendments (operator ticks):** — *HG-13 decided by the operator (build ledger `GATE DECISIONS`); all eight TICKED — APPLY.*
  - [x] A1 — §40 / ADR-018 MapLibre interactive map → allow a zero-JS static map as the conforming default with MapLibre as a progressive-enhancement island (ADR-051, LD-F09). *If unticked, P21.5 must build the MapLibre island.* **TICKED — APPLIED.**
  - [x] A2 — §32.2 absence vocabulary: add `not_researched` (ADR-041, LD-F14) or reword the residency barrier requirement. **TICKED — APPLIED.**
  - [x] A3 — §11.2 `canonical_name` scalar (ADR-056, LD-D12). **TICKED — APPLIED.**
  - [x] A4 — §16.2 item 6 claim-table partitioning → "MAY partition; MUST keep the PK/FK contract" (ADR-022, LD-F01). **TICKED — APPLIED.**
  - [x] A5 — §30/§32/§33 persistence of contradiction/coverage/task objects: accept compute-on-read as conforming (ADR-037/038/039, only if P19.5's ACCEPTED list kept it; else no amendment and P21.2 persists). **TICKED — APPLIED.**
  - [x] A6 — §34 curation UI: accept CLI+JSONL as the Phase-5 conforming form with the web surface in Phase 21 (ADR-030, LD-F05) — *or* leave the MUST and let P21.6 satisfy it. **TICKED — APPLIED.**
  - [x] A7 — §47 layout: add `evidence/` as a member (ADR-023; currently a recorded deviation in `pyproject.toml`). **TICKED — APPLIED.**
  - [x] A8 — any further normative item P19.2/P19.5 routed `P20.2:spec` (list them in the plan; the operator ticks per item). **TICKED — APPLIED (enumerated set is empty: the only `P20.2:spec`-routed id in `COVERAGE_MATRIX.csv`/`CAPSTONE_*` is `SIG-UI-038`, which is A1; see `SPEC_RECONCILIATION_PLAN.md §(b)`).**

## Goal
Make the canonical spec and the built system tell the same story: tag every ticket deliverable as in-spec / spec-implied / ticket-added, disposition all 61 ADRs against Appendix F/G, apply the non-normative corrections at the source (`spec_src` → `BUILD.sh`), fold the *approved* ticket-added requirements back into the spec with new ids, and leave every unapproved change as a precise, reviewable proposal.

## Load (read these — do not re-read others; SIG-ENG-001)
- `docs/build/PLANNING_LEDGER.md` §2.3 (how the spec is assembled), §1 T6/T7/T11; `docs/build/LEDGER_DEFERRALS.md` rows LD-D01, LD-D02, LD-D03, LD-D04, LD-D05, LD-D06, LD-D07, LD-D08, LD-D09, LD-D10, LD-D11, LD-D12, LD-D13, LD-F09, LD-F14, LD-X04; `docs/build/COVERAGE_MATRIX.csv` rows `routing=P20.2:spec` and every `MET-DIFFERENTLY`; `docs/build/CAPSTONE_CLOSURE.md` §(b); `docs/build/BACKLOG.csv` rows `landing=P20.2`.
- `docs/research/_meta/spec_src/` — all 36 section files' first 20 lines (to map § → file); `BUILD.sh`; especially `96_partX_s51to54_plan.md`, `99a_appF_adr.md`, `99c_appG_corrections.md`; `README.md` "Regenerating the specification".
- `docs/2_canonical_design_spec.md` Part 0 §0.3 (id grammar — new ids continue each prefix's sequence; never reuse), §0.1 (superset claim), §52 (19 phases), Appendix F, Appendix G (G.1–G.4 structure); the §§ named in A1–A8.
- `docs/adr/ADR-001..061` headers + `## Deviations`/`## Revisit trigger`; `docs/adr/README.md`.
- `docs/tickets/P00.1…P18.2` — each file's "In scope — deliverables" and "Requirement IDs" sections (46 files; this is the tagging input).

## In scope — deliverables
1. **`docs/build/TICKET_VS_SPEC.md`** — one table per ticket P00.1–P18.2: each deliverable line tagged `in-spec` (a § + id says so), `spec-implied` (needed to satisfy a § but not named), or `ticket-added` (decomposition-time scope beyond the spec text — e.g. P00.4 source-registry seed breadth, P06.1 hard-gate retrospective format, P08.3 contradiction object shape, P10.2 catalog form, P15.x splits, P14.1 `/id/{type}/{uuid}`, P12.1 DuckDB analytics boundary). Count per tag at the end. `ticket-added` rows get a proposed disposition: `fold-back as new id` / `note in Appendix G` / `leave as implementation detail`.
2. **`docs/build/SPEC_RECONCILIATION_PLAN.md`** — (a) ADR disposition table ADR-001…061: `no-op` / `F-index` (Appendix F entry missing or mis-numbered — fixes LD-X04/LD-D03: the ledgers' "ADR-013/016/017/024/025" logical numbers vs repo numbers) / `G-correction` / `amendment (A1–A8)`; (b) the amendment list with **target `spec_src` file, anchor line, exact before/after text, new ids allocated**, and tick state from the gate block; (c) fold-back list of approved `ticket-added` items with their new ids (continue the prefix sequence: e.g. next `SIG-INGEST-084`, `SIG-API-014`, …); (d) order of application; (e) what stays a proposal.
3. **Apply non-normative + approved edits at the source:** edit `spec_src/*.md` (never the assembled file), then `sh docs/research/_meta/spec_src/BUILD.sh`, commit both. Appendix F: make `99a_appF_adr.md` list every repo ADR by repo number with title and phase (single source of truth = `docs/adr/README.md`); Appendix G: add `G.5 Post-build reconciliation (2026-09)` entries for each correction; §52: where an ADR recorded a deviation the phase text now describes (e.g. "34 task types" precedent), align wording and cite the ADR. New ids introduced by fold-back get their `**SIG-XXX-nnn (MUST).**` paragraph in the owning § and a Part X §52 mention.
4. **ADR-062** "Spec reconciliation after the 46-ticket build" (Status Accepted; lists applied amendments and proposals; Revisit trigger = next planning pass).
5. **Consistency checks committed** (`docs/build/tools/check_spec_src.py`): BUILD.sh output is byte-identical to the committed spec; every `docs/adr/ADR-*.md` appears in Appendix F; every new id is unique and follows the grammar; the id count printed (expect 668 + fold-backs).
6. **Downstream updates:** `docs/traceability.md` gains rows for new ids (pointing at the code that already satisfies them); `COVERAGE_MATRIX.csv` gains rows for new ids (`verdict=MET`, `class=covered+tested` only if a test exists — else `covered+untested` + a `BACKLOG.csv` row); manifest "Spec amendments applied" gets one line per applied amendment; affected ticket files' Load/AC lines updated to match (cite, don't copy).
7. **Traceability + risk register** section for P20.2 (`RISK-P20-02` unapproved proposals drift from code → listed in `SPEC_RECONCILIATION_PLAN.md` §(e) and `BACKLOG.csv`).

## Out of scope (scope-creep guard — do NOT do these here)
- Any code change (except tests for `check_spec_src.py`). Applying an **unticked** A1–A8 amendment. Editing `docs/2_canonical_design_spec.md` directly (always via `spec_src`). Rewriting ADR bodies (only Appendix F/G and the new ADR-062). Integration — **P20.3**. Backlog re-numbering — items are referenced by existing `BL-` ids.

## Acceptance criteria
- [ ] `sh docs/research/_meta/spec_src/BUILD.sh && git diff --exit-code docs/2_canonical_design_spec.md` → clean after commit (byte-identical reproduction). *(deterministic)*
- [ ] `python docs/build/tools/check_spec_src.py` exits 0: all ADR files present in Appendix F; id count = 668 + N fold-backs; no duplicate/malformed ids; no `SIG-ENG-006/009/028/029` used. *(deterministic)*
- [ ] `TICKET_VS_SPEC.md` has 46 ticket tables and a totals line; every `ticket-added` row has a disposition. *(deterministic)*
- [ ] `SPEC_RECONCILIATION_PLAN.md` §(a) has 61 ADR rows, none blank; §(b) has A1–A8 rows each with target file + anchor + before/after + tick state; every ticked amendment is applied and every unticked one is **not** (grep the spec for the after-text). *(deterministic)*
- [ ] `git diff --stat` touches only `docs/**`, `docs/tickets/**`, and `docs/build/**`. *(deterministic)*
- [ ] The fold-back ids read as requirements a fresh implementer could satisfy without the ticket (reviewer judgement). *(agentic)*
- [ ] **Phase-gate (§51.3):** `make check` green; ADR-062 present; traceability + risk register + manifest amendment log updated. *(deterministic)*

## Requirement IDs to satisfy and stamp in the PR
SIG-ENG-003 (spec amendment path), SIG-ENG-001 (phases executable from the spec), SIG-ENG-002, SIG-ENG-031, plus every new id introduced by fold-back (list them).

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** no requirement is weakened silently — every softening is a ticked amendment with an ADR.
- **Part VIII is binding (§0.7):** no amendment touches Part VIII protections without an explicit tick and counsel note (none proposed).
- **Append-only & provenance (P1–P3):** Appendix G records what changed and why; earlier G entries untouched.
- **Additive/back-compat:** new ids only append; no id renumbering.

## Notes
- This ticket **owns** the id allocations for fold-backs and the Appendix F ↔ `docs/adr/` equivalence. Later ADRs must be added to Appendix F in the same PR that adds them. **This rule is now normative — `SIG-ENG-039` (§47) — and machine-enforced by `docs/build/tools/check_spec_src.py`; ADR-062 records it.** The one-line follow-up for root `AGENTS.md` (its "Amending the specification (SIG-ENG-003)" section) is **deferred to P20.3** because this ticket's `git diff` is scoped to `docs/**` and root `AGENTS.md` is outside that scope; the rule is fully captured by SIG-ENG-039 + the manifest "Spec amendments applied" follow-up rule in the meantime.
- If the operator has ticked nothing, the ticket is still complete: non-normative corrections + plan + proposals.
