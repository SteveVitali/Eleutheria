<!--
  P25.9 — post-live deferral closures: the meta / backend half. Drafted
  2026-09-16 after go-public. P25+ backlog. Registered in the manifest chain
  table (row 96). These rows were deferred because there was no real corpus and
  no real records-request activity — both exist now.
-->
# P25.9 — Post-live deferral closures: ruleset calibration + records-request backend (P25+ backlog)

- **Semantic id:** `META.2` · **Phase:** 25 · **Kind:** ticket · **Tag:** p25-backlog
- **base_branch:** current checkout · **Depends on:** P25.1–P25.8; the hosted spine carries ~27k real claims
- **Run:** `implement-spec spec=docs/tickets/P25.9__post-live-deferral-closures-meta.md live_verification=true`
- **Gate status:** engineering — no human gate.
- **Live stage:** reads the hosted spine (Cloud SQL via the documented proxy/DSN path); the MuckRock API is reachable under its existing credential + allow-list posture.

## Goal
Close the two META deferrals that were blocked on "no real data": BL-045 ruleset calibration (needs real claim volume — the spine now has ~27k claims) and BL-028 records-request filing/response backend (needs real request activity — MuckRock is live with real request ids). Finish with a DEFERRALS register sweep so every remaining OPEN row carries its precise human blocker.

## In scope — deliverables
1. **Ruleset calibration on the real corpus (`D-META.1-1`, BL-045).** Run the documented calibration analysis (volatility, directness matrix, Atlas supersession) over the hosted spine's real claims. The outcome is a **recorded recommendation**: either tuned ruleset constants (a data change with justification) or a written finding that the documented defaults hold on real data. Either closes the row — an unrun calibration is the only failure.
2. **Records-request filing/response backend (`D-META.1-2`, BL-028).** Feed `tasks.records_request`'s outcome log from real MuckRock activity: read the SIG account's real requests (the API token exists; requests like 136412 already returned documents) and record their real statuses/outcomes into the outcome-log shape — request id, agency, filed/acknowledged/completed state, dates. No fabricated outcomes; a request still in flight is recorded as in-flight. Add the test coverage the AGENTS convention expects (fixtures for the API shape; the live read is the evidence).
3. **DEFERRALS register sweep.** Re-read every OPEN/PARTIAL row after this run and annotate each with its *precise* remaining blocker in one line (e.g. `D-JURIS.2-1` → "sole blocker: declarationcamera_be eID, HG-04"; outreach rows → "operator action: send letters"; `D-LEGAL.1-1` → "counsel opinions X/Y/Z"). The register must let a reader tell at a glance which rows are engineering-closeable vs human-gated — none of the former may remain after this run without a stated reason.

## Out of scope
- The live-ops closures (P25.8) and the contradiction serve-path (P25.10).
- Tuning that changes claim semantics without justification; new sources; gate flips.
- Records-request *filing* (submitting new requests is an operator/legal act — HG-04 posture); this ticket reads and records existing requests only.

## Acceptance criteria
- [ ] a committed calibration report over the real spine — tuned constants with justification, or a finding that defaults hold (`docs/build/reports/`); `D-META.1-1` → DONE *(deterministic)*
- [ ] the outcome log records real MuckRock request statuses pulled live from the API (real request ids, real states, dates); fixture tests cover the parse; `D-META.1-2` → DONE *(deterministic, live-gated)*
- [ ] every OPEN/PARTIAL DEFERRALS row carries a one-line precise blocker after the sweep *(deterministic)*
- [ ] `make check` green; build-memory + backlog checks green; no token literal *(deterministic)*

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`: no fabricated outcomes; append-only; secrets env-only; a deferral flips to DONE only on evidence.
