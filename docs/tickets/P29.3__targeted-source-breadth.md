<!--
  P29.3 — Targeted, relationship-weighted source breadth (Round 7 / ACTIVATE.3).
  DRAFT on devin/round6-planning — seeded post-P27. Design: ROUND6_7 §5 P29.3.
  This is the DISCIPLINED form of the operator's "expand sources" instinct:
  targeted + resolution-gated, NOT undifferentiated maximal ingestion.
-->
# P29.3 — Targeted, relationship-weighted source breadth (P29)

- **Semantic id:** `ACTIVATE.3` · **Phase:** 29 · **Kind:** ticket · **Tag:** p29-activate
- **base_branch:** `devin/p29-2-detector-records-request-loop` · **Depends on:** P28.1 (resolution) — new sources dedup INTO the resolved graph
- **Run:** `implement-spec spec=docs/tickets/P29.3__targeted-source-breadth.md live_verification=true`
- **Gate status:** **HG-03 per source — ANSWERED 2026-09-23 (operator, LEDGER GATE DECISIONS):
  "Flip under GL-GATE-07 + fetch."** The 8 new accountability/governance sources are flipped to
  `ingestion_permitted=true` under the standing GL-GATE-07 basis — US → `LicenseRef-PublicRecord-
  FactualCompilation` (federal-government works record their 17 U.S.C. §105 public-domain basis →
  `CC0-1.0`, the per-source packet winning); non-US → `LicenseRef-OperatorAccepted-DBRight` — each via a
  rights packet + reviewer role + date + the P27.2 dispositions machinery (never a raw registry bypass).
  **Operational refinement:** the OSM ingest is in flight on the shared `db-f1-micro`, so heavy
  CONCURRENT hosted ingestion + the dedup-INTO-resolved-graph (needs the deferred hosted resolution
  materialization D-R6.1) are recorded as honest deferrals (D-R7.3-BREADTH); the flips + deterministic
  proof are done now; no fabricated live green.

## Goal
Expand sources in the **accountability/governance dimension** — more contract/funding/policy/oversight
sources, fusion-center/RTCC governance, under-covered jurisdictions — **not** maximal commodity camera
points; each new source runs through P28.1 resolution so it **deepens** the graph rather than enlarging an
unresolved pile.

## Load (cite; do not copy)
- `docs/2_canonical_design_spec.md` §22 (source registry), §4 (connector framework + ingestion gate), §42 (rights/licensing), §14.7 (ER — new sources dedup in).
- Code: `connectors/` (registry + framework + loader gate), `db.rights_decisions` + `sig-db rights-decisions` (P27.2 rights machinery), the P28.1 resolution pipeline.

## In scope — deliverables
1. Enumerate + add **high-signal, relationship-weighted** sources (accountability/governance first),
   each `ingestion_permitted=false` behind a rights packet; flips are HG-03 (operator).
2. Each yielding source runs through the **P28.1 resolution** pipeline (dedup into resolved entities) and
   the P27.2 rights pipeline (evidenced licence) — breadth that compounds the resolved graph.
3. Honest recorded outcomes per source; cadence rows for yielding sources; ladder re-measured.

## Out of scope
- Maximal/undifferentiated commodity ingestion; bypassing the ingestion or rights gate; adding sources
  that don't dedup into the resolved graph (they'd re-create the unresolved-pile problem).

## Acceptance criteria
- [x] N targeted accountability/governance-weighted sources added (rights-packeted; flipped under GL-GATE-07 per the HG-03 answer) *(deterministic)* — **8 sources** (5 new: gao_surveillance_reports, dhs_oig_reports, dhs_fusion_center_assessments, fema_hsgp_allocations, uk_surveillance_camera_commissioner; 3 pre-registered gated CCOPS discovery rows flipped: ccops_oakland, ccops_cambridge, ccops_somerville), each with a rights packet under `docs/build/reports/rights/annex/p293/` + a `[rights]` block.
- [~] each yielding source dedups INTO the resolved graph (P28.1) + carries an evidenced licence (P27.2) *(deterministic + live-gated)* — **evidenced-licence half DONE** (the p293 dispositions artifact + packets + `[rights]` blocks, proven by `tests/unit/test_p293_dispositions.py`); the **dedup-into-resolved half is live-gated and DEFERRED** (D-R7.3-BREADTH) under the OSM-land contention + the empty hosted resolved graph (D-R6.1). Each source is relationship-weighted (resolves onto an existing entity via the P28.6 accountability chain — deepen, not enlarge).
- [x] per-source outcomes recorded honestly; cadence + ladder updated; `make check` + build-memory green *(deterministic)* — ladder registered 334→339, loadable 228→236 (`sig-connectors validate`); cadence rows deferred with the connector wiring (no live targets until wired — D-R7.3-BREADTH).

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: ingestion gate fails closed; a flip is HG-03 (operator);
append-only; no assertion beyond committed evidence; new sources resolution-gated (deepen, not enlarge).
