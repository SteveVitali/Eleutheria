<!--
  P28.2 — Sharing & relationship network from evidence (Round 6 / DEPTH.2).
  DRAFT on devin/round6-planning — seeded post-P27. Design: ROUND6_7 §4 P28.2.
-->
# P28.2 — Sharing & relationship network from evidence (P28)

- **Semantic id:** `DEPTH.2` · **Phase:** 28 · **Kind:** ticket · **Tag:** p28-depth
- **base_branch:** `devin/p28-1-entity-resolution-at-scale` · **Depends on:** P28.1 (resolved entities are the nodes)
- **Run:** `implement-spec spec=docs/tickets/P28.2__sharing-relationship-network.md live_verification=true`
- **Gate status:** engineering — materialises the edge layer (new sqitch change, append-only).

## Goal
Populate the empty relationship/access-edge layer — vendor↔operator, data-sharing, RTCC/fusion
federation — from real claims, so the network surface renders a **real** graph instead of fixtures.

## Load (cite; do not copy)
- `docs/2_canonical_design_spec.md` §29.3/§29.7 (sharing-edge + snapshot-diff logic — owned by P08.2), §12 (relationships), §3.1.
- Code: `reconcile/sharing.py`, `reconcile/snapshot_diff.py`, `inference/access_paths.py` (P12.2 access-path closure), `db` `relationship`/`organization_relation` tables (empty), the P28.1 resolved entities.

## In scope — deliverables
1. Derive sharing/access edges (the 3 access-edge types + access-path closure) from real claims over the
   **resolved** entity set; **materialise** them (new sqitch change; append-only).
2. Each edge carries its support glyph + evidence classes; disagreements surface as visible contradictions
   (not silently reconciled — that's P28.3's object).
3. Feed the web network island/list-equivalent a real edge dataset (consumed by the P28.5 refresh).

## Out of scope
- Contradiction materialisation (P28.3); coverage (P28.4); the surface refresh (P28.5); re-running ER (P28.1).

## Acceptance criteria
- [ ] sharing/access edges materialised over resolved entities (new sqitch change; append-only; idempotent) *(deterministic + live-gated)*
- [ ] edges carry support glyphs + evidence provenance; the §29.3/§29.7 logic is reused, not re-implemented *(deterministic)*
- [ ] `make check` + `make test-db` + build-memory green *(deterministic)*

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: append-only; every inference labelled; contradictions stay
visible; the §29.3/§29.7 logic is owned by P08.2 and consumed here, never re-implemented (SIG-ENG-035).
