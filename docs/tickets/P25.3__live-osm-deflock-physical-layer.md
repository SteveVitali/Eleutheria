<!--
  P25.3 — live OSM/DeFlock physical layer. P25+ backlog. Standalone contract;
  registered in the manifest chain table + BACKLOG.csv (BL-023 (live-fetch backlog)) +
  SOURCE_LIVE_OPS_MATRIX.md. Depends on P25.1 (carve-out impl + live_targets).
-->
# P25.3 — Live fetch: the OSM/DeFlock physical ALPR layer (ODbL)

- **Semantic id:** `LIVE-OPS.3` · **Phase:** 25 · **Kind:** ticket · **Tag:** p25-backlog
- **base_branch:** current checkout · **Depends on:** P25.1 (ADR-083 carve-out impl; the `osm_overpass` `live_targets.toml` row already exists, ADR-082)
- **Run:** `implement-spec spec=docs/tickets/P25.3__live-osm-deflock-physical-layer.md live_verification=true`
- **Gate status:** `osm_overpass`/`osm_element_history`/`deflock_repo` green; `eff_atlas_of_surveillance` HG-03. No tokens (keyless).
- **Live stage:** operator-gated real fetches into the hosted Cloud SQL spine (ODbL compartment).

## Goal
Land the physical ALPR/surveillance-asset layer live, keeping it in the physically separate ODbL compartment (§42.3): OSM via Overpass under the ADR-083 carve-out (`overpass-api.de` allow-listed), the DeFlock MIT source (robots-clean), and the EFF Atlas.

## In scope — deliverables
1. Run `osm_overpass` live (its OKC-bbox `man_made=surveillance` target already exists) under the carve-out → OCFL → ODbL-compartment claims; verify against the fixture shape (parser unchanged); idempotent; fetch record with the allow-list decision stamped. This closes the OSM half of `D-LIVE.1a-1`/`D-P21.3-1`.
2. `data/live_targets.toml` + connector confirmation for `deflock_repo` (MIT — fetch the DeFlock data export, robots-clean, no carve-out needed) and `osm_element_history` (per-element history walks for first-observed).
3. `eff_atlas_of_surveillance` (`atlas` connector): live target + HG-03 flip after rights review; content-drift guard.
4. Reconcile the OSM + DeFlock + Atlas physical points (the existing `reconcile` sharing-edge/snapshot logic — consume, don't re-implement); ODbL attribution + share-alike preserved in exports.

## Out of scope
- The carve-out mechanism (P25.1). Records/procurement (P25.2), data-driven (P25.4). Any non-physical layer.

## Acceptance criteria
- [ ] `osm_overpass` live fetch lands ODbL-compartment claims in the hosted spine; second run adds 0 *(deterministic, live-gated)*
- [ ] DeFlock + Atlas live fetches land claims (or fail loud on drift); ODbL compartment kept separate with attribution/share-alike *(deterministic)*
- [ ] shadow replay diff=0 for all; no robots bypass (Overpass via the allow-list only); `make check` green; DEFERRALS advanced *(deterministic)*

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`: ODbL compartment separation (Part VIII §42.3); every live claim has evidence+time; append-only; ADR-083 conduct.
