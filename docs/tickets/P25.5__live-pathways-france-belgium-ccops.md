<!--
  P25.5 — live Stage-5 pathways + France/Belgium + CCOPS expansion. P25+ backlog.
  Standalone contract; registered in the manifest chain table + BACKLOG.csv
  (BL-043/BL-054 (pathways/CCOPS backlog)) + SOURCE_LIVE_OPS_MATRIX.md. Depends on P25.1 (carve-out + live_targets).
-->
# P25.5 — Live fetch: Stage-5 pathways + France/Belgium + CCOPS expansion (design/counsel-gated)

- **Semantic id:** `LIVE-OPS.5` · **Phase:** 25 · **Kind:** ticket · **Tag:** p25-backlog
- **base_branch:** current checkout · **Depends on:** P25.1 (carve-out + live_targets), P21.9 (pathways connector), P24.6 (France adapter, ADR-079), P24.7 (CCOPS connector, ADR-080)
- **Run:** `implement-spec spec=docs/tickets/P25.5__live-pathways-france-belgium-ccops.md live_verification=true`
- **Gate status:** RESOLVED 2026-09-15 — France per-source (operator) + CCOPS/pathways (ADR-085) + the five held sources (counsel, HG-02). `declarationcamera_be` alone has no path (Belgian eID; HG-04).
- **Live stage:** partially landed 2026-09-15 — the document-capture path (non-JSON capture → P07.1 classify → `evidence_artifact` row, bytes never re-hosted) is live for `raa_prefectures` (RAA index CSV), `madada` (Atom feed), `ccops_seattle`/`ccops_nyc_post` (index pages), `pathways×3` (EFF pages). Honest non-fetch: `aspi` WAF 403, `ccops_sf` robots unretrievable.

## Goal
Bring the remaining connector-backed classes live to the extent their gates permit: the Stage-5 pathway sources, the France/Belgium sources, and the CCOPS disclosure sources — each honouring its design/counsel/rights gate (this ticket does **not** force a gate).

## In scope — deliverables
1. ~~`data/live_targets.toml` rows~~ **DONE 2026-09-15** — document-capture targets registered for all reachable sources (see live_targets.toml §document-capture).
2. **Curated extraction adapters** (the owed deep work — the document-capture path records provenance only; field claims need per-source extraction):
   - `raa_prefectures`: **resource-index adapter** — the captured index CSV lists per-prefecture gazette PDFs; resolve bounded rows → fetch each PDF → the P07.1 document pipeline → emit `LegalInstrument` rows (`fr.arrete_prefectoral`, CSI L252 five-year derived sunset) per arrêté. Fail closed on a changed index shape.
   - `ccops_*`: **index→document discovery** — each index page links the mandated filings (Seattle SIR/Master List/quarterly, NYC POST `post-final/` policies, SF biannual inventory PDFs); bounded discovery → capture each as an artifact → PDF/table/clause layers → `disclosure_claim` fields. Aggregate + use-predicate + mandated≠populated guards stay enforced.
   - `pathways_*`: **document→claim normalization** — re-run genre classification on captured bytes (never trust fixture labels); per-document rights stay visible; procured≠deployed enforced.
   - `madada`: **Atom→records_requests adapter** — feed entries become records-request contexts (`fr.cada` regime), never request text.
   - `declarationcamera_be`: eID access path — HG-04 outreach, no anonymous path exists.
3. Per-source gate handling (record, never bypass): `procured≠deployed` (RISK-P21-16) stays enforced on any live claim; a changed upstream shape raises `ContentDrift`, never a fabricated claim.
4. Advance `D-P21.9-1`, `D-JURIS.2-1/2`, `D-CCOPS.1-1/2` per source (all PARTIAL as of 2026-09-15); CCOPS-beyond-three expansion stays BL-054.

## Out of scope
- The carve-out mechanism (P25.1). Records/procurement (P25.2), OSM (P25.3), data-driven/international (P25.4). Forcing any design/counsel gate. Officer-naming or per-person data (forbidden).

## Acceptance criteria
- [x] each source is either fetched live with provenance rows (document capture) OR its gate/block is recorded — never a bypass *(deterministic; landed 2026-09-15)*
- [ ] each curated adapter emits typed/evidenced field claims from real upstream bytes — `fr.arrete_prefectoral` instruments from RAA PDFs, `disclosure_claim` fields from CCOPS filings, pathway claims from classified documents *(deterministic)*
- [ ] `procured≠deployed` + aggregate/Part VIII guards stay enforced on any live claim *(deterministic)*
- [ ] content drift raises `ContentDrift` (recorded); shadow diff=0; `make check` green; DEFERRALS advanced per source *(deterministic)*

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`: Part VIII binding (design gates, procured≠deployed, officer-naming); every live claim has evidence+time; append-only; ADR-083 conduct; no gate bypass.
