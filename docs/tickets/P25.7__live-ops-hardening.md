<!--
  P25.7 — live-ops hardening: the leftover seams surfaced by the 2026-09-15
  unblock + document-capture pass. P25+ backlog. Standalone contract; registered
  in the manifest chain table + BACKLOG.csv (BL-055 umbrella) +
  SOURCE_LIVE_OPS_MATRIX.md.
-->
# P25.7 — Live-ops hardening: indirection, licence math, egress, cadence (P25+ backlog)

- **Semantic id:** `LIVE-OPS.7` · **Phase:** 25 · **Kind:** ticket · **Tag:** p25-backlog
- **base_branch:** current checkout · **Depends on:** P25.1–P25.6 (all landed or dispositioned 2026-09-15)
- **Run:** `implement-spec spec=docs/tickets/P25.7__live-ops-hardening.md live_verification=true`
- **Gate status:** the `LicenseRef-DerivedFacts-Citations` export-compartment item is **counsel/policy-needed** (HG-02); the eID item is HG-04. The rest are engineering.
- **Live stage:** operator-gated where a fetch or a Cloud Run job is involved.

## Goal
Close the seams the 2026-09-15 live pass surfaced but deliberately did not absorb: upstream indirection that would silently 404, a licence expression that fails export closed, two honest non-fetch egress blocks, an eID-walled register, and the recurring-cadence + seed-load plumbing that turns one-off live fetches into an operating system.

## In scope — deliverables
1. **DECP resource indirection** — `decp_fr`'s target is pinned to a timestamped `static.data.gouv.fr` URL that 404s when Etalab regenerates the resource. Follow the dataset-API indirection (`www.data.gouv.fr /api/1/datasets/...`, already allow-listed) at run time to resolve the current resource URL; a missing/changed resource is a recorded disappearance, never a silent fallback.
2. **`LicenseRef-DerivedFacts-Citations` export compartment** — the expression is deliberately absent from `policy/data/licenses.toml`, so licence math fails closed. Resolve its compartment treatment (counsel/policy, HG-02): either a recorded compartment mapping or a documented permanent exclusion from public export. No silent mapping.
3. **Egress for the two honest blocks** — `aspi_mapping_chinas_tech_giants` (host WAF 403) and `ccops_sf` (robots.txt unretrievable): run the document-capture fetch from Cloud Run egress (the MuckRock precedent — a normal alternate vantage, never a WAF defeat); a still-challenged fetch stays a recorded disappearance. `declarationcamera_be` stays NoLiveTargets until an HG-04 eID/outreach path exists — record it, do not engineer around the wall.
4. **NCSL `state_alpr_statute_inventory` seed load** — the one-time seed named in `D-CCOPS.1-1` (`as_of: 2022-02-03` label preserved); ingest through the same gate as every source.
5. **MuckRock cadence** — the `sig-ingest-muckrock` Cloud Run job ran once (2026-09-15, request 136412 → 6 claims); wire the recurring/scheduled form (Cloud Scheduler or the documented cadence), keeping the targeted-lookup posture (never a listing crawl, SIG-INGEST-036/037).
6. **Return-pass bookkeeping** — flip the PARTIAL DEFERRALS rows (D-JURIS.2-1, D-CCOPS.1-1, D-P21.9-1, D-P21.8-1) to DONE only when each row's *full* evidence bar is met (field-level claims, not just artifact captures); the hosted-spine counts + fetch records are the evidence.

## Out of scope
- The curated extraction adapters (P25.5 deliverable 2) and coarse dataset adapters (P25.4 deliverable 2). OKC document-connector live smoke (D-LIVE.1a-1). The deployment return-pass items (cloud restore drill, OBS.1 live-side verification — tracked under their own rows). Any WAF/robots defeat — a challenged host is a disappearance (SIG-INGEST-012/013).

## Acceptance criteria
- [ ] `decp_fr` resolves its current resource URL through the dataset API at run time; a regenerated/removed resource is a recorded disappearance *(deterministic)*
- [ ] `LicenseRef-DerivedFacts-Citations` has a recorded export-compartment disposition (mapped, or documented exclusion) — the fail-closed gap is resolved by a decision, not by silence *(counsel/policy)*
- [ ] aspi/ccops_sf are fetched from Cloud Run egress OR their block is a recorded disappearance/robots refusal — both are acceptable outcomes *(deterministic)*
- [ ] the NCSL seed loads with its `as_of` label; MuckRock runs on a recorded cadence with fetch records *(deterministic, live-gated)*
- [ ] `make check` green; DEFERRALS rows advanced with evidence; no gate bypass *(deterministic)*

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`: no WAF/robots/ingestion-gate bypass; no fabricated green; licence compartments fail closed; append-only; HG-02/HG-04 are human gates.
