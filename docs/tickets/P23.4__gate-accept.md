# GATE-ACCEPT (P23.4) — operator signs the Round-2 accepted-deviations delta

> **Milestone gate — NOT an `implement-spec` input.** The chain STOPS here after P23.3 until the
> operator dispositions the Round-2 accepted-deviations delta. Never guessed past.

- **Kind:** gate · **Phase:** 23 (capstone) · **Sequence:** 70 of 73
- **Depends on:** P23.3
- **Blocks:** the Round-2 `REC.*` tickets (P23.5–P23.7) that assume signed deviations.

> **Round 2.** The Round-1 ACCEPTED list is already signed (76 rows + the §(b) addendum,
> `docs/build/CAPSTONE_CLOSURE.md`, HG-14, recorded in `docs/build/LEDGER.md § GATE DECISIONS`). This
> marker covers only any **new** Round-2 deviation P23.3 proposed (expected: none).

## Criterion (verbatim)
The operator has reviewed the Round-2 addendum to the ACCEPTED-deviations list in
`docs/build/CAPSTONE_CLOSURE.md` (from P23.3) and signed each new row as accepted, or sent it back to
closure. If P23.3 proposed no new deviation, the operator confirms that.

## Readout
`docs/build/readouts/GATE-ACCEPT.md` — append-only: the delta presented, the operator's per-row
disposition (ACCEPTED / SEND-BACK + what would accept it), verdict (PASSED | NOT PASSABLE |
SKIPPED-BY-OPERATOR), date, and the disposition line. `orchestrate-build` records the answers in
`docs/build/LEDGER.md § GATE DECISIONS` and commits on the chain tip.

## Pre-registered thresholds
Every new row in the P23.3 Round-2 ACCEPTED-deviations addendum is signed (accepted) or returned to
closure; no proposed Round-2 deviation is left unsigned.

## Deferrals rule
This gate refuses to pass while any `OPEN` row in `docs/tickets/DEFERRALS.md` scoped to the capstone
phase remains (`DEFERRALS.md` rule 4). Note: the `D-P21.n-*` RETURN-PASS rows are scoped to their P21
tickets and the operator's HG gates, not to the capstone phase.

## Disposition
- [ ] Operator disposition recorded in `docs/build/LEDGER.md § GATE DECISIONS` and `docs/build/readouts/GATE-ACCEPT.md`.
