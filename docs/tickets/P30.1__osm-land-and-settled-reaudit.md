<!--
  P30.1 — OSM land completion + settled launch re-audit (Round 8 / GO-LIVE.1).
  DRAFT on devin/p30-golive-seed — seeded post-P29.3 at the exhausted-manifest boundary.
  This is a HOSTED-EXECUTION tail: real GCP execution (Cloud SQL sig-pg via cloud-sql-proxy)
  under operator ADC. It closes the two deferrals that gate every downstream launch number:
  D-SOURCES.17-1 (the OSM land) and D-P27.1-1 (the settled re-audit).
-->
# P30.1 — OSM land completion + settled launch re-audit (P30)

- **Semantic id:** `GO-LIVE.1` · **Phase:** 30 · **Kind:** ticket · **Tag:** p30-golive
- **base_branch:** `devin/p30-golive-seed` · **Depends on:** the P26.18 chunked-commit write path (landed) + the P27.1 audit verb (landed); the whole P27+P28+P29 chain (rows 117–135)
- **Run:** `implement-spec spec=docs/tickets/P30.1__osm-land-and-settled-reaudit.md live_verification=true`
- **Gate status:** **none.** This is a hosted verification/measurement ticket under operator ADC — no human decision gate. It DOES pause on the OSM execution: if `sig-ingest-camreg-batch-05-p964s` is still RUNNING when P30.1 starts, monitor/await it to completion (or record the honest in-flight state and re-run); never freeze a settled launch total off a mid-flight snapshot.

## Goal
Confirm the OSM land (`camreg_osm_surveillance`, ~1.37M claims, **D-SOURCES.17-1**) has COMPLETED on the
hosted `sig-pg` spine — the ingest execution `sig-ingest-camreg-batch-05-p964s` was RUNNING at seed time
(started 2026-09-22T16:49Z, ~14h budget, ~2/3 landed) and will likely complete before this ticket runs.
Handle **both** states: (a) **still running →** monitor/await via the GCS run rows + the hosted per-source
`claim` count, do not fabricate green; (b) **done →** verify the final claim/entity counts and the
idempotent `+0` re-run. Then run the **settled re-audit** (`sig-exports audit` over the complete hosted
spine via cloud-sql-proxy, **D-P27.1-1**) and freeze the launch numbers that every downstream row
(P30.2 materialization, P30.3 export/cut-over) reads.

## Load (cite; do not copy)
- `docs/tickets/DEFERRALS.md` — **D-SOURCES.17-1** (the exact rebuild/update/execute/re-run commands + the recorded baseline 1,059,533 claims / 210 sources @ 2026-09-22) and **D-P27.1-1** (the exact `sig-exports audit` re-run command + expected ≈2.43M claims).
- `docs/2_canonical_design_spec.md` §16 (claim spine, append-only), §38 (export/audit surfaces), §3.1 (defining standard — no fabricated total).
- Code: `db/src/db/claim_sink.py` (`PgClaimSink` chunked-commit path, insert-only), `exports/` (`sig-exports audit`, the read-only public-surface audit verb), `ops/` (`sig-ops scheduled-ingest`, run-log rows).
- `docs/build/reports/PUBLIC_SURFACE_AUDIT.md` + `public_surface_audit.json` (the P27.1 provisional mid-land snapshot to be superseded).

## In scope — deliverables
1. **OSM land completion** (D-SOURCES.17-1): determine the state of `sig-ingest-camreg-batch-05-p964s`.
   If RUNNING → monitor to completion (GCS run rows under `gs://…-sig-restricted/ops/runs/camreg_osm_surveillance/`
   `outcome=ok`; hosted `claim` count steps ~1,059,533 → ~2.43M then holds). If it failed/timed out → re-run
   the recorded procedure (rebuild image via Cloud Build, `SIG_COMMIT_CHUNK_SIZE=10000`, execute the single
   source to completion). Then the **idempotent re-run** (`+0`) confirming the per-source spine count holds flat.
2. **Settled re-audit** (D-P27.1-1): re-run `sig-exports audit` over the complete hosted spine via
   cloud-sql-proxy; refresh `docs/build/reports/PUBLIC_SURFACE_AUDIT.md` + `public_surface_audit.json` with the
   settled (no longer "OSM land in flight") numbers — geolocated / UNDETERMINED / publishable_effective refreshed.
3. **Freeze the launch numbers**: record the settled claim/entity/source totals + the launch-scope split as the
   canonical launch baseline in a dated report, so P30.2/P30.3 build off settled numbers.

## Out of scope
- Deploying the Round-6 materializers or running any materializer (that is P30.2).
- Building the national export or the public cut-over (P30.3).
- Any spine WRITE beyond the OSM ingest itself (the audit is read-only; `REPEATABLE READ READ ONLY`); no
  UPDATE/DELETE ever; no source flip; no fabricated total off a mid-flight snapshot.

## Acceptance criteria
- [ ] `camreg_osm_surveillance` OSM land COMPLETE on hosted `sig-pg` — final claim count recorded (~2.43M, or the true settled number), GCS run row `outcome=ok`, and the idempotent re-run confirmed `+0` *(live-verified; D-SOURCES.17-1 → DONE)*. If genuinely still running at ticket end, record the honest in-flight state + the await procedure and keep D-SOURCES.17-1 OPEN — never a fabricated completion.
- [ ] settled `sig-exports audit` re-run over the complete spine; `PUBLIC_SURFACE_AUDIT.md` + `public_surface_audit.json` refreshed with settled numbers (no "in flight" caveat); launch-scope split re-confirmed off the settled totals *(live-verified; D-P27.1-1 → DONE)*.
- [ ] the frozen launch baseline recorded in a dated report under `docs/build/reports/`; `make check` + build-memory green; the two closed deferrals flipped to `DONE` with date + evidence in `DEFERRALS.md`.

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: the claim spine is insert-only (no UPDATE/DELETE); no assertion
beyond committed evidence; never a population total (SIG-METRIC-008); never a fabricated hosted count or a
settled total off a mid-flight snapshot; append-only build memory; no merge/tag/push-main.
