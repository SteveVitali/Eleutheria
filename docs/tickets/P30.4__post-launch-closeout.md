<!--
  P30.4 — Post-launch closeout + whole-build capstone (Round 8 / GO-LIVE.4).
  DRAFT on devin/p30-golive-seed — stacks on the P30.3 tip. FINAL row of the GO-LIVE tail.
  Verifies every go-live deferral is DONE, sweeps the remaining live deferrals HONESTLY (run or
  record — some stay operator-gated / deferred by design), records the launch, runs a whole-build
  capstone verification, and sets projectStatus per BM-TAIL-03.
-->
# P30.4 — Post-launch closeout + whole-build capstone (P30)

- **Semantic id:** `GO-LIVE.4` · **Phase:** 30 · **Kind:** ticket · **Tag:** p30-golive
- **base_branch:** `devin/p30-3-national-export-and-public-cutover` · **Depends on:** P30.1–P30.3 (land+audit, materialization, export+cut-over all landed)
- **Run:** `implement-spec spec=docs/tickets/P30.4__post-launch-closeout.md live_verification=true`
- **Gate status:** **engineering** (no hard human gate). A final operator confirmation of the closeout /
  `projectStatus` is welcome but is not a blocking gate; the work here is verification + honest bookkeeping.

## Goal
Close out the launch: (1) verify **every go-live deferral is DONE** (the D-SOURCES.17-1 / D-P27.1-1 / D-R6.x /
D-R7.2-DETECTORS / D-P27.4-1 / D-R6.5-SURFACE / D-P27.8-1 rows P30.1–P30.3 closed); (2) sweep the remaining
live deferrals HONESTLY; (3) record the launch; (4) run a **whole-build capstone verification**
(composed end-to-end + coverage); (5) set `projectStatus` per **BM-TAIL-03** (DONE only if every manifest row
is landed or consciously-skipped AND no OPEN deferral lacks a landing/precise blocker).

## Load (cite; do not copy)
- `docs/tickets/DEFERRALS.md` — the remaining live deferrals to sweep: **D-R7.3-BREADTH** (the P29.3 8-source hosted fetch + dedup-into-resolved + `sig-db rights-decisions --apply` + connector wiring — run it now that the OSM land + hosted resolution are done, OR record the precise honest remainder); **D-R7.2-SEND** (records-request SEND — **STAYS operator-gated**, never auto-fired); **D-P21.7-1** (contribution-back — **STAYS deferred** pending OSM-wiki OE-page registration / HG-08); plus a sweep so every remaining OPEN row names its precise blocker.
- `skills/build-memory/layout.md` (BM-TAIL-03 projectStatus rule) + `docs/build/README.md` (memory-root, committed tree).
- Code/verbs: `sig-connectors run --mode live --sink pg` + `CONNECTOR_FOR_SOURCE` (D-R7.3-BREADTH wiring), `sig-db rights-decisions --apply` (ADR-095), the P28.1 resolution + P28.6 accountability materializers (re-run to dedup the new claims INTO the resolved graph), `make check` / `make test-db` / `SIG_REQUIRE_DB_TESTS=1 pytest tests/e2e` (composed capstone), `check_coverage_matrix.py`.
- `docs/build/LEDGER.md` (CURRENT STATE + PHASE LOG), `docs/build/BUILD_INDEX.md`, `docs/build/reports/CAPSTONE_VERIFICATION.md` (prior capstone format).

## In scope — deliverables
1. **Go-live deferral verification:** confirm every deferral P30.1–P30.3 closed is `DONE` with real evidence;
   if any is genuinely still blocked (e.g. the OSM land or a materialization did not complete), record the
   honest state and keep it OPEN — never a fabricated close.
2. **Honest sweep of the remaining live deferrals:**
   - **D-R7.3-BREADTH** — now that the OSM land (D-SOURCES.17-1) + hosted resolution (D-R6.1-EVAL hosted half)
     are done, either RUN the 8-source hosted fetch → dedup-into-resolved → `rights-decisions --apply` →
     accountability linkage (per-source, shadow-diff=0 over a fixture first), OR record the precise remaining
     blocker (connector-wiring scope) with the exact re-run command. No flip without its packet; no fabricated
     hosted count.
   - **D-R7.2-SEND** — re-confirm it STAYS operator-gated (DRAFT-never-send; `records_requests_sent`=0; no
     transmit path); do not send.
   - **D-P21.7-1** — re-confirm it STAYS deferred pending HG-08 (MapRoulette key + OSM OE-page registration);
     do not register.
   - A sweep so every remaining OPEN deferral names its precise human/engineering blocker.
3. **Record the launch:** a dated launch record under `docs/build/reports/` (served watermark, the settled
   launch numbers, the surfaces live, the domain/TLS state).
4. **Whole-build capstone verification:** composed end-to-end (`SIG_REQUIRE_DB_TESTS=1` — real Docker
   PG18+PostGIS; `make test-db` + `tests/e2e`) + coverage matrix + `make check` all green; record in a dated
   capstone report; blocked seams stay honest `xfail`s (`LD-`/`accepted:`), never loosened.
5. **Set `projectStatus`** in `docs/build/LEDGER.md` per BM-TAIL-03 — `DONE` only if every manifest row is
   landed or consciously-skipped AND no OPEN deferral lacks a landing or a precise recorded blocker; otherwise
   the honest lesser status with the reason.

## Out of scope
- SENDING records requests (D-R7.2-SEND — operator-gated); registering the MapRoulette/OSM OE page (D-P21.7-1);
  the from-first-principles resolution re-derivation (D-R6.1-EVAL remainder — needs human ground truth).
- Any new product feature; any merge/tag/push to `main` (operator integrates after the chain).

## Acceptance criteria
- [ ] every go-live deferral closed by P30.1–P30.3 verified `DONE` with evidence (or honestly kept OPEN with the recorded state) *(live-verified)*.
- [ ] the remaining live deferrals swept honestly: **D-R7.3-BREADTH** run or precisely recorded; **D-R7.2-SEND** re-confirmed operator-gated (no send); **D-P21.7-1** re-confirmed deferred (HG-08); every remaining OPEN row names its precise blocker *(deterministic + live where run)*.
- [ ] the launch recorded in a dated report; whole-build capstone verification green (composed e2e under `SIG_REQUIRE_DB_TESTS=1` + coverage + `make check`); no loosened assertion, no flipped xfail outside its closing ticket.
- [ ] `projectStatus` set per BM-TAIL-03 with the honest justification; build-memory green; no merge/tag/push-main.

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: never fabricate green (implement-spec 5.3 / BM-DEFER-02); a deferral
not in `DEFERRALS.md` did not happen; DRAFT-never-send (SIG-TASK-018); no flip without its rights packet
(HG-03); append-only build memory (correct by a new entry, never editing an old one); no merge/tag/push-main.
