<!--
  Lane C (new code) contract — productionize Round 4. Filled by decompose-spec mode=extend over
  ~/MetaHarness/sig-golive-spec.md (2026-09-09). CITES the spec's §§ and GL-* ids.
-->
# CI.1 — CI hardening (GL-CI-01)

- **Sequence:** 82 of 87 · **Phase:** 22+ (productionize) · **Kind:** ticket
- **Tag:** golive-round4
- **base_branch:** current checkout
- **Depends on:** OBS.1 (row 81) — the previous Round-4 step
- **Run:** `implement-spec spec=docs/tickets/82_CI.1__ci-composed-hardening.md live_verification=false`
- **Gate status:** none
- **Live stage:** offline-only

## Goal
Run the composed `tests/e2e` for real in CI (Node + Docker present), closing the P20.4 gap
honestly, plus nightly composed runs and dependency/license/secret scanning, and enforce the doc
gates on PRs.

## Load (read these — do not re-read others)
- `~/MetaHarness/sig-golive-spec.md` Part II § CI.1 (GL-CI-01); `docs/build/LEDGER.md § OPEN FINDINGS`
  (CI-RED-01, DOCKER-DOWN).
- `.github/workflows/ci.yml`; `tests/e2e/`; `Makefile` (`make check`, `make docs-check`);
  `scripts/docs/check-build-memory.sh`.

## In scope — deliverables
1. A CI job that runs the composed `tests/e2e` for real (Node + Docker present, so S8/LD-V08 runs
   in CI) (GL-CI-01).
2. A nightly composed run; dependency / license / secret scanning (GL-CI-01).
3. Enforce `make docs-check` + `check-build-memory.sh` on PRs (GL-CI-01).

## Out of scope
- Observability of the *running* stack — OBS.1 (row 81). Deployment — DEPLOY.1 (row 79).
- The composed-suite code itself (it exists) — this ticket runs it in CI.

## Acceptance criteria
- [ ] CI runs `SIG_REQUIRE_DB_TESTS=1` composed suite green with the web build present *(deterministic)*
- [ ] a seeded secret/license violation fails CI *(deterministic)*
- [ ] the nightly run reports *(deterministic)*
- [ ] `make docs-check` + `check-build-memory.sh` enforced on PRs *(deterministic)*
- [ ] verification green; every new behaviour has a test that fails if it is removed; requirement ids stamped in the PR; anything not automatically verifiable is a `DEFERRALS.md` row with its compensating control; ADRs written for every deviation and owned decision; `BUILD_INDEX.md` row and `LEDGER.md` advanced. *(agentic — the universal phase-gate AC)*

## Requirement IDs to satisfy and stamp in the PR
GL-CI-01.

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`.

## Notes
- Closes the CI-RED-01 / DOCKER-DOWN open findings honestly (real composed run in CI). Re-confirm
  the `ci.yml` job structure at build time.
