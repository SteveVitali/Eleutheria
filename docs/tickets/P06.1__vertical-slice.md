# P06.1 — Vertical slice: one jurisdiction end-to-end  **[HARD SYNCHRONIZATION GATE]**

- **Sequence:** 16 of 46
- **Phase:** 6 — Vertical slice: one jurisdiction end-to-end
- **base_branch:** current checkout — the branch the previous ticket left checked out (stacked PR chain); `main` for P00.1
- **Depends on (must land earlier in the stack):** P05.2, and transitively everything above it (this slice exercises the whole stack built in Phases 0–5)
- **Run:** `implement-spec spec=docs/tickets/P06.1__vertical-slice.md`

## Goal
**Falsify the design on real data before scaling it.** Carry ONE real jurisdiction from evidence to
a rendered dossier — assets, organizations, a deployment, a contract, a portal- or records-derived
configuration, and at least one genuine contradiction — prove J-1 (the journalist's traversal)
executes end to end, and commit a written retrospective recording what the design got wrong.

## Load
- `docs/2_canonical_design_spec.md` §29.1 (camera-count reconciliation: the distinct count predicates, `PREDICATE_CONFLATION`, unresolved deltas as research tasks), §39.2 (the local dossier: section order, "what we don't know," print/PDF, expandable reconciliations) — plus, per §52 Phase 6, all prior loaded sections as the slice touches the whole stack.
- Part 0, Part I §3, Appendix E glossary; §2.2 J-1 and Appendix D (the traversal spec).

## In scope — deliverables
1. **One slice jurisdiction** carried end to end: assets, organizations, a deployment, a contract, a portal-/records-derived configuration, and **≥1 real contradiction** rendered without collapse.
2. **J-1 executes end to end** for the slice jurisdiction (SIG-CHART-009), with every material fact resolving to a document at a locator.
3. **Distinct count predicates**: `contracted / invoiced / installed / active / mapped / claimed` kept separate, with `PREDICATE_CONFLATION` firing on a deliberate conflation, and unresolved deltas emitted as research tasks — SIG-RECON-026/028/029.
4. **A slice-proof rendered dossier** exercising the §39.2 content contract (section order; "what we don't know" in summary/print/API; incompleteness banner; print/PDF path; every material figure expandable to its reconciliation) — SIG-UI-010…014. **Scope note:** this is a *proof that J-1 renders end to end*, built before the epistemic visual language (P15.1). The **production** dossier surface — the real SIG-UI-010…015 owner, with the full epistemic language, a11y/no-JS, and editorial standards — is **P15.2**, which supersedes this renderer. Keep this one minimal; do not gold-plate it.
5. **A committed written retrospective** recording what the design got wrong on real data.
6. **A declared hardness precondition**, recorded *before* the slice begins.

## Out of scope (do NOT do here)
- Scaling to further jurisdictions or national ingestion — later phases.
- New connectors, the parsing stack, or new detectors — P07.x and beyond; the slice uses only what Phases 0–5 already built.

## Acceptance criteria
- [ ] **J-1 executes end to end** for the slice jurisdiction. *(deterministic)*
- [ ] Every material fact resolves to a document at a locator. *(deterministic)*
- [ ] At least one genuine contradiction is detected and rendered without collapse. *(agentic — judgement that it is genuine; detection deterministic)*
- [ ] The count predicates are distinct; `PREDICATE_CONFLATION` fires on a deliberate conflation. *(deterministic)*
- [ ] A written retrospective is committed, recording what the design got wrong. *(agentic)*
- [ ] **The slice jurisdiction satisfies the hardness precondition, declared BEFORE the slice begins**: ≥3 independent source families; ≥2 claims on one predicate that disagree; ≥1 asset with no operator; ≥1 lifecycle transition evidenced by a dated document. A jurisdiction failing any precondition MUST NOT be used. *(deterministic — checkable property; the declaration is agentic)*
- [ ] **Phase-gate (§51.3):** CI green incl. data-quality checks; new requirements have automated tests (SIG-ENG-004); ADRs written for any deviation; traceability matrix updated; risk-register entries for this phase updated.

## Requirement IDs to satisfy and stamp in the PR (Definition of Done §0.6)
SIG-CHART-009, SIG-RECON-026, SIG-RECON-028, SIG-RECON-029, SIG-UI-010, SIG-UI-011, SIG-UI-012, SIG-UI-013, SIG-UI-014, SIG-ENG-034 — plus the §29.1/§39.2 requirement IDs the slice exercises (enumerate remaining in PR table). Each must appear in the commit/PR and be covered by an automated test.

## Cross-cutting invariants — re-check before opening the PR (apply to EVERY ticket)
- **Defining standard (§3.1):** no unexplained dots/edges; no silent overwrites; no synthetic certainty; every node has identity; every state has time; every claim has evidence; every inference is labelled; every contradiction stays visible.
- **Part VIII is binding (§0.7):** no plate/trip/per-person storage or lookup; officer-naming test gates any person-named claim; sensitivity tiers + coordinate rules enforced; licence gate + ODbL separation hold.
- **Append-only & provenance (P1–P3):** no writable "current value" columns; raw_value preserved; corrections are new assertions, never overwrites.
- **Additive/back-compat:** do not break a prior ticket's wire names, IDs, or schema contracts; new fields optional with today's-behavior defaults.

## Notes
**This ticket is a HARD SYNCHRONIZATION BARRIER (§54, SIG-ENG-034).** Per §54, **no ticket after
P06.1 may begin until this ticket's written retrospective is committed** — the critical path
`0→1→2→3→4→5→6→8` halts here until then. This ticket **owns** that retrospective artifact and the
hardness-precondition record. The precondition MUST be declared and recorded **before** the slice
begins, so a too-easy jurisdiction cannot be rationalized afterwards; it replaces the unfalsifiable
"a slice that surfaced no problems was too easy" with a checkable, pre-declared property.
