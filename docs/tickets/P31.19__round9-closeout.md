<!--
  P31.19 — Round 9 closeout (Round 9 / CLOSE.1).
  SEEDED 2026-09-25 on devin/round9-waveb-seed from the draft on devin/round9-planning (Wave B; operator answers 2026-09-24/25; re-checked against the landed Wave A code). The P30.4 pattern: implement the leak-check policy decision (D-P30.4-4), sweep
  every OPEN row, run the whole-build capstone, and set projectStatus per BM-TAIL-03. RE-SCOPED at the Wave-B seed:
  Q7 = (a); P31.17 dropped; D-R6.1-EVAL (+ the human halves of D-P30.2b-1/-2) are owned Round-10 items, not Round-9
  blockers this ticket pretends to close.
-->
# P31.19 — Round 9 closeout (P31)

- **Semantic id:** `CLOSE.1` · **Phase:** 31 · **Kind:** ticket · **Tag:** p31-followups · **Wave:** tail
- **base_branch:** `devin/p31-16-rematerialize-reexport-republish` · **Depends on:** P31.1–P31.16 landed (P31.16 may be landed with HG-11 pending → RETURN PASS). P31.17 was **dropped** (operator 2026-09-25; manifest row 158 unused) and P31.18 **moved to Round 10** (row 159 unused) — neither is a Round-9 dependency.
- **Run:** `implement-spec spec=docs/tickets/P31.19__round9-closeout.md live_verification=true`
- **Gate status:** **operator policy decision for D-P30.4-4 (design §6 Q7) — ANSWERED; no pause.**
  - **RATIFIED 2026-09-24 (operator, LEDGER § GATE DECISIONS "Round 9 ratification", Q7): option (a)** — narrow the
    check's scope to code + config, recorded in an ADR (ADR-R9-LEAKSCOPE). Option (b) (redaction) was **not**
    chosen and is not done.
  - **RE-SCOPED at the Wave-B seed 2026-09-25 (operator, LEDGER § GATE DECISIONS 2026-09-25 (2): "Drop P31.17";
    human review + P31.18 → Round 10).** Nothing in Round 9 depends on a human review campaign. This ticket treats
    **D-R6.1-EVAL** (and the human-decision halves of **D-P30.2b-1** / **D-P30.2b-2**) as **owned Round-10 items** —
    each with an explicit owner (Round 10: human review campaign → P31.18 eval re-derivation) and landing in the sweep —
    and does **not** close them, soften them, or treat them as a Round-9 blocker. The PROVISIONAL disclosure stays.

## Goal
Close Round 9 honestly. Implement the leak-check policy, verify every Round-9 deferral closure live, sweep what
remains, run the whole-build capstone, and set `projectStatus`.

## Load (cite; do not copy)
- `docs/tickets/DEFERRALS.md`: row **D-P30.4-4**, every row noted "scheduled → P31.x" (the § "Round 9 seed" and
  § "Round 9 Wave B seed (2026-09-25)" scheduling tables), **D-P31.4-1** (the 10-10 replay), D-P31.1-1 / D-P31.1-3
  (unscheduled, BL-057), and the P30.4 sweep table (the format). `docs/build/reports/round9-drafts/` (P31.17 DROPPED,
  P31.18 → Round 10: the Round-10 starting points). `docs/build/runs/P30.4.md` + `docs/build/reports/CAPSTONE_VERIFICATION_2026-09-24.md` (capstone format).
  The BM-TAIL-03 rule (build-memory layout).
- Code: `tests/connectors/test_secrets.py:95-114` (`test_gcp_project_id_is_env_resolved_not_committed`: greps every
  tracked file); `make check`, `make test-db`, `SIG_REQUIRE_DB_TESTS=1 uv run pytest tests/e2e`,
  `docs/build/tools/check_coverage_matrix.py`, `scripts/docs/check-build-memory.sh`.

## In scope — deliverables
1. **D-P30.4-4 per Q7 = (a).** Narrow the check to code + config (an explicit include list: package `src/`, `ops/`,
   `web/src`, config files; build memory under `docs/build/`, `docs/tickets/` and `docs/adr/` excluded, with the reason),
   recorded in ADR-R9-LEAKSCOPE; then arm the check in CI (it no longer fails on history). No redaction of append-only
   records (P1–P3; option (b) was not chosen). The armed check passes:
   `SIG_GCP_PROJECT=<id> uv run pytest tests/connectors/test_secrets.py` (the id comes from the environment; it is
   never written into a committed file).
2. **Verification** that every Round-9 closure is DONE with live evidence, or honestly kept OPEN.
3. **Sweep:** a new "P31.19 sweep" table (the P30.4 format) listing every remaining OPEN/PARTIAL row with owner, precise
   blocker and landing.
4. **Capstone:** composed e2e (`SIG_REQUIRE_DB_TESTS=1`), `make test-db`, the coverage matrix and `make check`, all green,
   recorded in a dated capstone report. Blocked seams stay `xfail` (`LD-`/`accepted:`), never loosened.
5. **The 2026-10-10 replay check** (**D-P31.4-1**, P31.4's RETURN PASS — the first full OSM run of
   `sig-sched-camreg-batch-05` on the rolled image `sha256:feff986c…`, projected ≈ 12–15 min vs the 36 h limit): read
   back its run rows, `ingest_run_completion` and duration, and record the outcome (duration, inserts, resume
   behaviour) against the ADR-107 / ADR-111 revisit trigger (a). If this ticket runs before 2026-10-10, keep the row
   OPEN with its exact verify command — never a pre-dated green.
6. **`projectStatus`** per BM-TAIL-03: DONE only if every row has landed or been consciously skipped and no OPEN
   engineering row lacks a landing. P31.17 counts as **consciously skipped** (dropped by the operator 2026-09-25) and
   P31.18 as **moved to Round 10** (rows 158/159 unused). D-R6.1-EVAL and the human halves of D-P30.2b-1/-2 carry the
   owner + landing "Round 10 (human review campaign → P31.18)"; the ticket states plainly whether a not-yet-seeded
   Round-10 landing satisfies BM-TAIL-03 and, if not, sets the honest lesser status with the reason (e.g. IN-PROGRESS
   pending the Round-10 seed) — it never marks these rows DONE.

## Out of scope
- Operator-only actions (send, OE page, key rotation, the PR-stack merge). New features.

## Acceptance criteria
- [ ] ADR-R9-LEAKSCOPE (with `## Revisit trigger` + Appendix F row) lands; the armed leak check passes under policy (a); D-P30.4-4 → DONE *(deterministic)*.
- [ ] D-R6.1-EVAL, D-P30.2b-1 and D-P30.2b-2 stay OPEN with owner + Round-10 landing in the sweep; no Round-9 artifact claims eval closure; the PROVISIONAL disclosure is unchanged *(deterministic)*.
- [ ] Every Round-9 closure is verified; the sweep table is complete *(live + deterministic)*.
- [ ] The capstone is green (composed e2e + DB + coverage + `make check`); no loosened assertion; no xfail flipped outside its closing ticket.
- [ ] The 10-10 replay outcome is recorded (D-P31.4-1), or the row stays OPEN if the run has not happened; `projectStatus` is set per BM-TAIL-03 with its justification; build memory green.

## Cross-cutting invariants
Cited from `docs/tickets/00_MANIFEST.md`: never fabricate green; append-only build memory (redaction, if chosen, is a
new commit, never a history rewrite); a deferral not in `DEFERRALS.md` did not happen; DRAFT-never-send; no
merge/tag/push-main.
