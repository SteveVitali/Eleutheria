<!--
  P25.4 — live data-driven & coarse-international. P25+ backlog. Standalone
  contract; registered in the manifest chain table + BACKLOG.csv (BL-042 (international backlog)) +
  SOURCE_LIVE_OPS_MATRIX.md. Depends on P25.1 (carve-out impl + live_targets).
-->
# P25.4 — Live fetch: data-driven + coarse-international sources

- **Semantic id:** `LIVE-OPS.4` · **Phase:** 25 · **Kind:** ticket · **Tag:** p25-backlog
- **base_branch:** current checkout · **Depends on:** P25.1 (ADR-083 carve-out impl + `live_targets.toml`), P21.8 (the `data_driven`/`coarse_international` connectors)
- **Run:** `implement-spec spec=docs/tickets/P25.4__live-data-driven-and-international.md live_verification=true`
- **Gate status:** RESOLVED 2026-09-15 — `eff_data_driven` flipped (CC-BY-4.0); the three coarse sources flipped on counsel approval (HG-02, `LicenseRef-DerivedFacts-Citations`, coarse granularity floor SIG-INGEST-042).
- **Live stage:** partially landed — `eff_data_driven` live (the EFF release zip → aggregate claims, committed to the spine); `carnegie_ai_gsi` + `facial_recognition_world_map` fetch their index/map pages as `evidence_artifact` provenance rows (P25.5 document-capture path); `aspi_mapping_chinas_tech_giants` fetch is a recorded WAF-403 disappearance.

## Goal
Bring the data-driven and coarse-international aggregate sources live: `eff_data_driven` (data.gov-hosted), and the three coarse-international sources (`aspi_mapping_chinas_tech_giants`, `carnegie_ai_gsi`, `facial_recognition_world_map`) — aggregate-only, Part VIII-safe, under the ADR-083 carve-out where the host is an API.

## In scope — deliverables
1. ~~`data/live_targets.toml` rows~~ **DONE 2026-09-15** — `eff_data_driven` (the EFF release zip, live) + the three coarse sources' upstream pages as document-capture targets.
2. **Curated coarse-dataset adapters** (the owed deep work — page captures are provenance only): a per-source adapter turning each upstream's real data surface (the GSI index data, the FRWM dataset, the ASPI map's underlying data where reachable) into the connector's curated `rows` payload, at country/vendor granularity only (SIG-INGEST-042). `aspi` needs an egress vantage that isn't WAF-challenged (Cloud Run probe — the MuckRock precedent) or HG-04 outreach.
3. Confirm the connectors' parse/extract handle live shapes (content-drift guard, P25.1); the aggregate-only schema gate stays enforced (RISK-P0-08 — no per-search/per-person columns); shadow diff=0 preserved.
4. Real fetch → versioned dated claims (the data-driven release model — new snapshots add, never overwrite); agency crosswalk resolves. Advance `D-P21.8-1` (PARTIAL as of 2026-09-15).

## Out of scope
- The carve-out mechanism (P25.1). Records/procurement (P25.2), OSM (P25.3), pathways/FR-BE/CCOPS (P25.5). Any per-device/per-person data (forbidden, Part VIII).

## Acceptance criteria
- [ ] each flipped source's live fetch lands aggregate-only, typed, evidenced claims; a re-fetch of a changed snapshot adds new dated claims (0 overwrites) *(deterministic, live-gated)*
- [ ] the aggregate-only schema gate rejects any per-search/per-person column from live data *(deterministic)*
- [ ] content drift raises `ContentDrift` (recorded); shadow diff=0; no token literal; `make check` green; DEFERRALS advanced *(deterministic)*

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`: Part VIII (aggregate-only, no per-person data); every live claim has evidence+time; append-only; ADR-083 conduct.
