# P23.1 — capstone gap analysis (Round 2, CAP.1)

- **Sequence:** 67 of 73 · **Phase:** 23 (capstone) · **Kind:** capstone · **Tag:** capstone
- **base_branch:** current checkout (the branch P22.3 left checked out)
- **Depends on:** P22.3 (build-memory v2 migration)
- **Run:** `implement-spec spec=docs/tickets/P23.1__capstone-gap-analysis.md gap_analysis=true live_verification=false`
- **Gate status:** none · **Live stage:** none

> **Round 2 — delta over the completed Round-1 capstone.** The round-1 capstone (PR #68,
> `devin/sig-postbuild-capstone`) already judged the composed build as a whole
> (`docs/build/reports/CAPSTONE_VERIFICATION.md`, `COVERAGE_MATRIX.csv` 671 rows, `CAPSTONE_CLOSURE.md`).
> This ticket re-judges **only what changed since PR #68** (the P22.3 migration touched docs/build-memory
> layout, not product code). Do the delta verdicts in a fresh context before reading `docs/build/runs/*`.

## Goal
Confirm that the P22.3 migration and any Round-2 changes introduced no new gap against the spec: a delta
gap analysis over the frozen Round-1 verdicts, not a re-run of the whole matrix.

## Load (read these — do not re-read others)
- `docs/build/reports/CAPSTONE_VERIFICATION.md`, `docs/build/CAPSTONE_GAP_ANALYSIS.md`, `docs/build/CAPSTONE_CLOSURE.md` (Round-1 verdicts — the baseline).
- `docs/build/COVERAGE_MATRIX.csv` (671 rows; the frozen id→verdict set).
- `docs/tickets/00_MANIFEST.md` (chain, invariants), `docs/tickets/DEFERRALS.md` (owed work).
- The composed final tree: `git diff <pinnedBaseSha>...<chainTip>`. **Do not** read `docs/build/runs/*` until the delta verdicts are recorded (anti-bias rule).

## In scope — deliverables
1. A delta section appended to `docs/build/CAPSTONE_GAP_ANALYSIS.md`: which spec ids (if any) changed verdict since PR #68, with evidence; a seam re-hunt limited to seams the Round-2 change could touch.
2. If any id's verdict changed, update its `docs/build/COVERAGE_MATRIX.csv` row (append-only rationale) and route it (fix in P23.3 / accept / defer).

## Out of scope
- Closing gaps (P23.3), composed execution (P23.2), reconciliation (P23.5–P23.7). Re-classifying unchanged Round-1 rows.

## Acceptance criteria
- [ ] every spec id whose verdict changed since PR #68 has an updated matrix row with evidence; unchanged rows are untouched *(deterministic)*
- [ ] the delta verdicts were recorded before any `docs/build/runs/*` was read (stated in the report) *(agentic)*
- [ ] **Phase-gate:** verification green; new behaviour has a test that fails if removed; requirement ids stamped; anything not automatically verifiable is a `DEFERRALS.md` row with a compensating control; ADRs for every deviation/owned decision; `BUILD_INDEX.md` row and `LEDGER.md` advanced. *(agentic)*

## Requirement IDs to satisfy and stamp in the PR
The capstone/gap-analysis ids (matching `SIG-[A-Z]+-[0-9]+`) whose verdict the Round-2 delta touches.

## Cross-cutting invariants
- Cited from `docs/tickets/00_MANIFEST.md § Cross-cutting invariants`; the delta re-checks each it touches.

## Notes
- This ticket consumes (never renames) `COVERAGE_MATRIX.csv`'s column set and verdict vocabulary owned by P19.2/SK.1.
