[project]
name = "sig-policy"
version = "0.1.0"
description = "SIG policy package: executable governance — sensitivity tiers, licence compartments, publication and ingestion gates (§42-§46, §47)."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
sig-policy = "policy.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/policy"]
